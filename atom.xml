<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>汀屿・漫记</title>
  
  
  <link href="https://flametinary.github.io/atom.xml" rel="self"/>
  
  <link href="https://flametinary.github.io/"/>
  <updated>2026-09-12T03:37:07.566Z</updated>
  <id>https://flametinary.github.io/</id>
  
  <author>
    <name>Sheldon</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>iOS项目搭建Jenkins自动化：从安装到测试、打包和发布</title>
    <link href="https://flametinary.github.io/2026/09/12/iOS%E9%A1%B9%E7%9B%AE%E6%90%AD%E5%BB%BAJenkins%E8%87%AA%E5%8A%A8%E5%8C%96%E4%BB%8E%E5%AE%89%E8%A3%85%E5%88%B0%E6%B5%8B%E8%AF%95%E6%89%93%E5%8C%85%E5%92%8C%E5%8F%91%E5%B8%83/"/>
    <id>https://flametinary.github.io/2026/09/12/iOS%E9%A1%B9%E7%9B%AE%E6%90%AD%E5%BB%BAJenkins%E8%87%AA%E5%8A%A8%E5%8C%96%E4%BB%8E%E5%AE%89%E8%A3%85%E5%88%B0%E6%B5%8B%E8%AF%95%E6%89%93%E5%8C%85%E5%92%8C%E5%8F%91%E5%B8%83/</id>
    <published>2026-09-11T16:00:00.000Z</published>
    <updated>2026-09-12T03:37:07.566Z</updated>
    
    <content type="html"><![CDATA[<h1 id="iOS-项目搭建-Jenkins-自动化：从安装到测试、打包和发布"><a href="#iOS-项目搭建-Jenkins-自动化：从安装到测试、打包和发布" class="headerlink" title="iOS 项目搭建 Jenkins 自动化：从安装到测试、打包和发布"></a>iOS 项目搭建 Jenkins 自动化：从安装到测试、打包和发布</h1><p>做 iOS 开发的人，大概都经历过这种时刻：本地测试明明全绿，提测前却要手动开构建页面、等半小时、再登录 App Store Connect 传包。更怕的是，某天负责发版的同事休假，整条发布链路只有他电脑上能跑通。</p><p>Jenkins 的价值不是“替你点几个按钮”，而是把 iOS 项目里那些重复、容易出错、又必须可追溯的动作——测试、签名、归档、上传——变成一台长期运行、可被团队共享的自动化服务器。你把规则写进 <code>Jenkinsfile</code> 和 <code>Fastfile</code>，以后无论是自己在本机运行，还是让 Jenkins 在专用 Mac 上运行，走的都是同一套流程。</p><p>本文以 ExampleApp 的真实搭建过程为例（<strong>所有项目名、仓库地址、Bundle ID、Team ID、账号、凭证 ID 均已脱敏</strong>，统一用占位符替代），完整说明如何在 macOS 上从零搭建一套 Jenkins CI&#x2F;CD：安装、配置、创建 Job，直到跑通测试、打包和发布。文章假设你已经用 Fastlane 把签名、测试、归档、上传逻辑收敛进了 <code>Fastfile</code>（参考同目录文章：<a href="/2026/09/07/iOS%E9%A1%B9%E7%9B%AE%E5%A6%82%E4%BD%95%E6%8E%A5%E5%85%A5Fastlane/">iOS项目如何接入Fastlane</a>），Jenkins 只做“编排层”。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs text">开发者 push / 提 PR<br>        ↓<br>Jenkins 接收 Webhook，按 Jenkinsfile 编排<br>        ↓<br>准备环境（Ruby / Xcode / 临时 Keychain / 凭证注入）<br>        ↓<br>fastlane ios test → 归档 → （备用）上传 TestFlight / 提交审核<br>        ↓<br>回写 GitHub 状态、归档 IPA/dSYM/xcresult、清理密钥<br></code></pre></td></tr></table></figure><h2 id="一、先用一句话理解-Jenkins-在-iOS-交付里的位置"><a href="#一、先用一句话理解-Jenkins-在-iOS-交付里的位置" class="headerlink" title="一、先用一句话理解 Jenkins 在 iOS 交付里的位置"></a>一、先用一句话理解 Jenkins 在 iOS 交付里的位置</h2><p>Jenkins 是一套可长期运行在 macOS 主机上的开源自动化服务器。它不替代 Xcode，也不替代 Fastlane 或 App Store Connect；它做的是把“什么时候跑、跑哪条流水线、用什么节点、注入哪些凭证、产物怎么归档”这些编排决策集中管理起来。</p><p>你可以把它理解为 iOS 项目的“自动化调度台”：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs text">GitHub 事件（push / PR / 手动触发）<br>        ↓<br>Jenkins 选择对应 Job / 流水线<br>        ↓<br>在指定 macOS 节点上准备环境、注入凭证<br>        ↓<br>调用 fastlane lane：测试 / 归档 / 上传 TestFlight / 提交审核<br>        ↓<br>回写状态、归档产物、清理临时密钥<br></code></pre></td></tr></table></figure><p>在 ExampleApp 中，最常用的触发长这样：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs text">向 test 分支 push 或提 PR   →  exampleapp-test（自动测试 + Debug 构建）<br>手动指定 commit/tag          →  exampleapp-build（归档，不上传）<br>手动指定不可变 commit/tag    →  exampleapp-testflight-backup（上传 TestFlight）<br>手动 + 两阶段确认            →  exampleapp-appstore-backup（提交审核）<br></code></pre></td></tr></table></figure><p>这里有一个边界值得从第一天就定清楚：<strong>Jenkins 只负责“调度与编排”，签名和商店逻辑全部下沉到 Fastfile。</strong> 本地、Jenkins、其他 CI 用同一套 lane，避免三套脚本漂移。</p><h2 id="二、Jenkins-在-iOS-CI-CD-里到底能做什么"><a href="#二、Jenkins-在-iOS-CI-CD-里到底能做什么" class="headerlink" title="二、Jenkins 在 iOS CI&#x2F;CD 里到底能做什么"></a>二、Jenkins 在 iOS CI&#x2F;CD 里到底能做什么</h2><p>Jenkins 并不是一个单独的“打包工具”，它是“Job + 节点 + 凭证 + 插件”的组合。最常见的几类能力如下。</p><table><thead><tr><th>需求</th><th>Jenkins 提供的能力</th><th>ExampleApp 中的用法</th></tr></thead><tbody><tr><td>监听代码事件自动触发</td><td>GitHub Webhook + Branch Source</td><td><code>test</code> 分支 push &#x2F; PR 自动跑测试</td></tr><tr><td>在指定机器上运行</td><td>静态 Agent 节点 + Label</td><td>专用 Mac mini，单执行器避免模拟器&#x2F;钥匙串冲突</td></tr><tr><td>安全注入密钥</td><td>Credentials Binding</td><td>match 密码、<code>.p8</code>、SSH 私钥只在运行时出现</td></tr><tr><td>串行化高风险动作</td><td>Lockable Resources</td><td><code>exampleapp-release-signing</code> 锁禁止并发签名</td></tr><tr><td>运行声明式流水线</td><td>Pipeline 插件</td><td><code>Jenkinsfile</code> 定义阶段与步骤</td></tr><tr><td>展示测试报告</td><td>JUnit 插件</td><td>读取 <code>fastlane/test_output/**/*.xml</code></td></tr><tr><td>回写仓库状态</td><td>GitHub 插件</td><td>回写 <code>jenkins/exampleapp-all</code> 检查状态</td></tr><tr><td>归档产物</td><td>Archive Artifacts</td><td>保留 IPA、dSYM、xcresult、日志</td></tr><tr><td>人工审批闸门</td><td>Pipeline <code>input</code></td><td>App Store 提交审核前由发布负责人确认</td></tr></tbody></table><p>你不必一口气把所有能力都接上。一个健康的演进顺序通常是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs text">先让 Jenkins 跑通测试 → 再接归档 Job<br>→ 再接 TestFlight 备用上传 → 最后才是 App Store 两阶段提交<br></code></pre></td></tr></table></figure><p>每往前走一步，先确认本机用同一条 <code>fastlane</code> lane 可复现，再交给 Jenkins。这样当问题出现时，你能区分是工程问题、签名问题，还是 Jenkins 节点&#x2F;凭证问题。</p><h2 id="三、搭建前先准备什么"><a href="#三、搭建前先准备什么" class="headerlink" title="三、搭建前先准备什么"></a>三、搭建前先准备什么</h2><p>Jenkins 本身安装很简单，但 iOS 发布所依赖的外部信息不少。先把它们分成三类，会清楚很多。</p><h3 id="1-macOS-构建主机"><a href="#1-macOS-构建主机" class="headerlink" title="1. macOS 构建主机"></a>1. macOS 构建主机</h3><p>建议使用一台专用 Mac mini，并使用专用 macOS 用户运行 Jenkins。不要用个人登录用户的钥匙串和桌面环境作为长期 CI 依赖。</p><p>至少准备：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs text">macOS 15 或已经验证过的更高版本<br>Xcode（同时安装可用的 iOS 17.6+ Simulator runtime）<br>Command Line Tools、Git、SSH<br>受支持的 Ruby、Bundler、Java 17（或 Jenkins 当前 LTS 支持的更高版本）<br>稳定的 GitHub、Apple Developer 和 App Store Connect 网络访问<br>至少 20% 可用磁盘空间<br></code></pre></td></tr></table></figure><p>如果巡检发现主机只有过旧的 macOS、Xcode、Ruby，且没有 Java、fastlane 或 Jenkins，这种主机不能直接作为 CI 节点，必须先完成工具链标准化。</p><h3 id="2-Apple-侧准备"><a href="#2-Apple-侧准备" class="headerlink" title="2. Apple 侧准备"></a>2. Apple 侧准备</h3><ol><li>在 Apple Developer 中创建主 App 和 Widget 两个 App ID，并启用 iCloud、App Groups 等项目实际使用的能力。</li><li>确认 App Group 与 CloudKit 容器，例如 <code>group.com.example.exampleapp</code>、<code>iCloud.com.example.exampleapp</code>。</li><li>使用 fastlane match 管理开发和 App Store 分发证书。match 仓库 deploy key 必须是该证书仓库的只读私钥，不能使用 <code>.pub</code> 公钥文件。</li><li>在 App Store Connect 创建最小权限 API Key，记录 Key ID、Issuer ID 和 <code>.p8</code> 私钥。<code>.p8</code> 不进入 Git。</li><li>App Store 发布还需要审核联系人文件。仓库提供 <code>fastlane/review_information.example.yml</code> 模板，真实文件放在 Jenkins 用户的受限路径，不要提交。</li></ol><h3 id="3-GitHub-侧准备"><a href="#3-GitHub-侧准备" class="headerlink" title="3. GitHub 侧准备"></a>3. GitHub 侧准备</h3><ul><li>应用仓库地址示例：<code>https://github.com/your-org/ExampleApp.git</code>。</li><li>你需要仓库管理员权限来创建 Webhook、配置 Branch Source 和分支保护。</li><li>Jenkins Webhook 地址必须能从 GitHub 访问。生产环境使用 HTTPS、VPN 或安全反向代理，不要把 Mac mini 的管理端口直接暴露到公网。</li></ul><h3 id="4-机密字段：哪些可以进仓库，哪些绝对不行"><a href="#4-机密字段：哪些可以进仓库，哪些绝对不行" class="headerlink" title="4. 机密字段：哪些可以进仓库，哪些绝对不行"></a>4. 机密字段：哪些可以进仓库，哪些绝对不行</h3><p>下面这张表很重要。</p><table><thead><tr><th>内容</th><th>是否可提交到业务仓库</th><th>合理位置</th></tr></thead><tbody><tr><td><code>Jenkinsfile</code>、Fastfile、Gemfile</td><td>可以</td><td>业务仓库</td></tr><tr><td>Job 名称、节点标签、非敏感配置</td><td>通常可以</td><td>仓库 &#x2F; Jenkins 配置</td></tr><tr><td>match 证书仓库地址</td><td>私有项目通常可以</td><td><code>Matchfile</code> 或 Fastfile</td></tr><tr><td>match 加密密码 <code>MATCH_PASSWORD</code></td><td>不可以</td><td>Jenkins Credentials &#x2F; 密码管理器</td></tr><tr><td><code>.p12</code>、Provisioning Profile</td><td>不可以直接放业务仓库</td><td>match 加密仓库</td></tr><tr><td>App Store Connect <code>.p8</code> 私钥</td><td>绝对不可以</td><td>Jenkins Secret File &#x2F; 本机受限路径</td></tr><tr><td>Apple ID 密码</td><td>不建议作为自动化凭据</td><td>尽量改用 App Store Connect API Key</td></tr><tr><td>App Review 联系人信息</td><td>不应进公共仓库</td><td>本机受限文件或 Jenkins 受限路径</td></tr></tbody></table><p><strong>本项目的固定事实（脱敏示例）</strong></p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs text">主 App Bundle ID：      com.example.exampleapp<br>Widget Bundle ID：       com.example.exampleapp.Widget<br>Apple Team ID：          ABCDE12345<br>match 证书仓库：          git@example.com:your-org/ios-certificates.git（分支 main）<br>App Group：              group.com.example.exampleapp<br>CloudKit 容器：          iCloud.com.example.exampleapp<br></code></pre></td></tr></table></figure><blockquote><p>示例项目的 CI 基线：Swift 6、SwiftUI、SwiftData，最低 iOS 17.6；Xcode 与 iOS Simulator runtime 需匹配；Ruby &#x2F; Bundler &#x2F; fastlane 版本由 <code>Gemfile.lock</code> 锁定。所有命令行 <code>xcodebuild</code> 或 fastlane 测试&#x2F;构建都要注入 <code>OTHER_SWIFT_FLAGS=&quot;-disable-sandbox&quot;</code>，否则 Swift 宏插件可能报 <code>swift-plugin-server produced malformed response</code>。</p></blockquote><h2 id="四、安装-Jenkins：从-Homebrew-开始"><a href="#四、安装-Jenkins：从-Homebrew-开始" class="headerlink" title="四、安装 Jenkins：从 Homebrew 开始"></a>四、安装 Jenkins：从 Homebrew 开始</h2><p>下面以 Homebrew 安装 Jenkins LTS 为主。Jenkins LTS 会随插件和 Java 支持范围更新，安装时不要固定一个过旧的版本。</p><h3 id="1-安装-Homebrew（已有可跳过）"><a href="#1-安装-Homebrew（已有可跳过）" class="headerlink" title="1. 安装 Homebrew（已有可跳过）"></a>1. 安装 Homebrew（已有可跳过）</h3><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs sh">/bin/bash -c <span class="hljs-string">&quot;<span class="hljs-subst">$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)</span>&quot;</span><br>brew update<br>brew --version<br></code></pre></td></tr></table></figure><p>安装结束后，按终端提示把 Homebrew 加入当前用户的 <code>PATH</code>。如果公司策略不允许 Homebrew，也可以从 <a href="https://www.jenkins.io/download/">Jenkins 下载页</a> 下载 macOS&#x2F;Generic Java package；后续 Jenkins 页面配置完全相同。</p><h3 id="2-安装-Java-和-Jenkins-LTS"><a href="#2-安装-Java-和-Jenkins-LTS" class="headerlink" title="2. 安装 Java 和 Jenkins LTS"></a>2. 安装 Java 和 Jenkins LTS</h3><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs sh">brew install --cask temurin@17<br>brew install jenkins-lts<br><br><span class="hljs-built_in">export</span> JAVA_HOME=<span class="hljs-string">&quot;<span class="hljs-subst">$(/usr/libexec/java_home -v 17)</span>&quot;</span><br>java -version<br>brew services start jenkins-lts<br>brew services list | grep jenkins<br></code></pre></td></tr></table></figure><p>如果 <code>temurin@17</code> 在你所在的 Homebrew 源中不可用，请按 Jenkins 当前 LTS 的 <a href="https://www.jenkins.io/doc/book/platform-information/support-policy-java/">Java 要求</a> 安装受支持的 Temurin 17&#x2F;21，然后再次执行 <code>java -version</code>。不要在 Jenkins 运行后随意升级 Java 大版本，先在测试主机验证插件兼容性。</p><p>默认监听 <code>http://localhost:8080</code>。确认端口：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs sh">curl -I http://127.0.0.1:8080/login<br></code></pre></td></tr></table></figure><h3 id="3-获取初始管理员密码"><a href="#3-获取初始管理员密码" class="headerlink" title="3. 获取初始管理员密码"></a>3. 获取初始管理员密码</h3><p>在浏览器打开 <code>http://localhost:8080</code>。页面会提示解锁 Jenkins。Homebrew 服务常见密码路径是：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs sh"><span class="hljs-built_in">cat</span> <span class="hljs-string">&quot;<span class="hljs-variable">$HOME</span>/.jenkins/secrets/initialAdminPassword&quot;</span><br></code></pre></td></tr></table></figure><p>如果该路径不存在，使用页面提示的路径；也可以只查找 Jenkins Home 下的文件：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs sh">find <span class="hljs-string">&quot;<span class="hljs-variable">$HOME</span>&quot;</span> -path <span class="hljs-string">&#x27;*/secrets/initialAdminPassword&#x27;</span> -<span class="hljs-built_in">print</span> 2&gt;/dev/null<br></code></pre></td></tr></table></figure><p>把输出粘贴到解锁页面。该密码只用于首次解锁，后续使用你创建的管理员账号。</p><h3 id="4-创建第一个管理员账号"><a href="#4-创建第一个管理员账号" class="headerlink" title="4. 创建第一个管理员账号"></a>4. 创建第一个管理员账号</h3><p>在“创建第一个管理员用户”页面填写：</p><table><thead><tr><th>字段</th><th>建议值</th></tr></thead><tbody><tr><td>用户名</td><td>团队专用英文名，例如 <code>ci-user</code></td></tr><tr><td>密码</td><td>密码管理器生成的长密码，不与 macOS 密码相同</td></tr><tr><td>确认密码</td><td>同上</td></tr><tr><td>全名</td><td>真实姓名或团队名称</td></tr><tr><td>邮箱</td><td>能接收 Jenkins 通知的团队邮箱</td></tr></tbody></table><p>点击“保存并完成”。随后设置 Jenkins URL：本机实验可先填 <code>http://localhost:8080/</code>；要接收 GitHub Webhook，必须改成外部可访问的 HTTPS 地址，例如 <code>https://jenkins.example.com/</code>。</p><h2 id="五、安装必要的插件"><a href="#五、安装必要的插件" class="headerlink" title="五、安装必要的插件"></a>五、安装必要的插件</h2><p>首次向导可以选择“安装推荐插件”，完成后再补齐下面的插件。若选择“选择插件安装”，按表格搜索名称安装。</p><h3 id="1-必装插件"><a href="#1-必装插件" class="headerlink" title="1. 必装插件"></a>1. 必装插件</h3><table><thead><tr><th>插件</th><th>用途</th></tr></thead><tbody><tr><td>Pipeline</td><td>运行 Jenkinsfile 声明式流水线</td></tr><tr><td>Git</td><td>Checkout Git 仓库</td></tr><tr><td>GitHub</td><td>GitHub 服务器配置和状态回写</td></tr><tr><td>GitHub Branch Source</td><td>Multibranch Pipeline 发现分支和 Pull Request</td></tr><tr><td>Credentials Binding</td><td>在 <code>withCredentials</code> 中绑定密钥</td></tr><tr><td>JUnit</td><td>展示 <code>fastlane/test_output</code> 的 XML 报告</td></tr><tr><td>Lockable Resources</td><td>提供 <code>exampleapp-release-signing</code> 签名锁</td></tr><tr><td>Timestamper</td><td>日志显示时间戳</td></tr><tr><td>AnsiColor</td><td>显示 fastlane 彩色日志</td></tr><tr><td>Workspace Cleanup</td><td>清理工作区</td></tr></tbody></table><p><code>Pipeline</code> 会自动带上基础依赖。如果 Jenkins 提示依赖缺失，按页面的“安装依赖”一起安装。使用经典 Pipeline 页面即可，不必为了本项目安装 Blue Ocean。</p><h3 id="2-安装步骤"><a href="#2-安装步骤" class="headerlink" title="2. 安装步骤"></a>2. 安装步骤</h3><ol><li>点击左侧“Manage Jenkins”。</li><li>点击“Plugins”（旧版本可能显示“Manage Plugins”）。</li><li>打开“Available plugins”，逐个搜索并勾选上表插件。</li><li>点击“Install”。</li><li>安装完成后勾选“Restart Jenkins when installation is complete and no jobs are running”，或手动重启。</li><li>回到“Installed plugins”，确认插件状态为 Enabled。</li></ol><p>插件升级应在维护窗口进行，并在升级后重新跑一次测试 Job。</p><h2 id="六、Manage-Jenkins-逐项配置"><a href="#六、Manage-Jenkins-逐项配置" class="headerlink" title="六、Manage Jenkins 逐项配置"></a>六、Manage Jenkins 逐项配置</h2><h3 id="1-System：Jenkins-地址、邮件和全局选项"><a href="#1-System：Jenkins-地址、邮件和全局选项" class="headerlink" title="1. System：Jenkins 地址、邮件和全局选项"></a>1. System：Jenkins 地址、邮件和全局选项</h3><p>路径：<code>Manage Jenkins</code> → <code>System</code>。</p><ol><li><strong>Jenkins URL</strong>：填写可被 GitHub 访问的 HTTPS 根地址，并保留末尾 <code>/</code>，例如 <code>https://jenkins.example.com/</code>。</li><li><strong>System Admin e-mail address</strong>：填写维护邮箱。</li><li><strong>Locale&#x2F;时区</strong>：团队在中国时可使用 <code>Asia/Shanghai</code>，统一发布记录时间。</li><li><strong>GitHub Servers</strong>：新增服务器，名称填 <code>github.com</code>，API URL 保持 <code>https://api.github.com</code>；选择下一节创建的 GitHub 凭证。</li><li><strong>Global properties</strong>：只能放非敏感变量，例如 <code>CI=true</code>。不要放 <code>MATCH_PASSWORD</code>、<code>.p8</code> 内容或 SSH 私钥。</li></ol><h3 id="2-Security：关闭匿名访问"><a href="#2-Security：关闭匿名访问" class="headerlink" title="2. Security：关闭匿名访问"></a>2. Security：关闭匿名访问</h3><p>路径：<code>Manage Jenkins</code> → <code>Security</code>。</p><ul><li>勾选 <strong>Enable security</strong>。</li><li>Authorization 选择 <strong>Matrix-based security</strong> 或团队验证过的 Role-based 策略。</li><li>只给管理员 <code>Overall/Administer</code>；普通开发者只给读取 Job、查看日志、启动测试所需的权限。</li><li>取消匿名用户的管理和读取权限。</li><li>保持 CSRF 防护（crumb issuer）开启。</li><li>API Token 只给个人账号或专用 GitHub&#x2F;Jenkins 账号，使用最小权限并定期轮换。</li><li>不允许 Job 通过参数直接执行任意 shell 片段，流水线只接受校验过的 SHA、tag 和布尔确认参数。</li></ul><p>保存后用无痕窗口确认未登录用户不能查看 Job 日志或凭证。</p><h3 id="3-Credentials：创建五个发布凭证"><a href="#3-Credentials：创建五个发布凭证" class="headerlink" title="3. Credentials：创建五个发布凭证"></a>3. Credentials：创建五个发布凭证</h3><p>路径：<code>Manage Jenkins</code> → <code>Credentials</code> → <code>System</code> → <code>Global credentials (unrestricted)</code> → <code>Add Credentials</code>。</p><p>凭证 ID 必须与仓库 Jenkinsfile 完全一致：</p><table><thead><tr><th>ID</th><th>Kind</th><th>填写方式</th><th>使用范围</th></tr></thead><tbody><tr><td><code>exampleapp-match-deploy-key</code></td><td>SSH Username with private key</td><td>Username 通常填 <code>git</code>；Private Key 粘贴 match 仓库只读私钥</td><td>两个备用发布 Job</td></tr><tr><td><code>exampleapp-match-password</code></td><td>Secret text</td><td>match 加密密码</td><td>两个备用发布 Job</td></tr><tr><td><code>exampleapp-asc-key-id</code></td><td>Secret text</td><td>App Store Connect Key ID</td><td>两个备用发布 Job</td></tr><tr><td><code>exampleapp-asc-issuer-id</code></td><td>Secret text</td><td>App Store Connect Issuer ID（UUID）</td><td>两个备用发布 Job</td></tr><tr><td><code>exampleapp-asc-p8</code></td><td>Secret file</td><td>上传 <code>.p8</code> 私钥文件</td><td>两个备用发布 Job</td></tr></tbody></table><p>逐个创建：选择 Kind → Scope 选 <code>Global</code> → 手动填精确 ID → 填 Description → 点击“Create”。Description 只写用途，不写私钥内容。</p><p>测试 Job（例如 <code>ci/jenkins/test.Jenkinsfile</code>）<strong>严禁绑定这五个凭证</strong>。它只做测试和 Debug 构建，即使测试失败也不应访问 App Store Connect。</p><p>另外准备一个用于 GitHub 状态回写的细粒度 GitHub Token，只授予仓库状态&#x2F;Checks 所需权限。不要把 Token 写入 Jenkinsfile。</p><h3 id="4-Lockable-Resources：创建签名锁"><a href="#4-Lockable-Resources：创建签名锁" class="headerlink" title="4. Lockable Resources：创建签名锁"></a>4. Lockable Resources：创建签名锁</h3><p>路径：<code>Manage Jenkins</code> → <code>System</code> → <strong>Lockable Resources Manager</strong>。</p><p>新增：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs text">Name: exampleapp-release-signing<br>Description: ExampleApp 发布签名和 match 资源，禁止并发<br>Labels: exampleapp-release<br></code></pre></td></tr></table></figure><p>两个备用发布流水线会在签名阶段执行 <code>lock(resource: &#39;exampleapp-release-signing&#39;)</code>；普通测试 Job 不需要此锁。</p><h3 id="5-Nodes：配置-Mac-节点和标签"><a href="#5-Nodes：配置-Mac-节点和标签" class="headerlink" title="5. Nodes：配置 Mac 节点和标签"></a>5. Nodes：配置 Mac 节点和标签</h3><p>路径：<code>Manage Jenkins</code> → <code>Nodes</code> → <code>New Node</code>。</p><p>推荐建立静态节点：</p><ol><li>Name：<code>exampleapp-mac-mini</code>。</li><li>Type：<code>Permanent Agent</code>。</li><li>Number of executors：<code>1</code>，避免模拟器和钥匙串并发冲突。</li><li>Remote root directory：例如 <code>/Users/ci-user/jenkins-agent</code>，必须属于 Jenkins 用户。</li><li>Labels：<code>exampleapp macos ios xcode-26.3</code>。</li><li>Usage：选择“Only build jobs with label expressions matching this node”。</li><li>Launch method：按网络选择 SSH 或 inbound agent，连接后确认节点 Online。</li></ol><p>仓库中的 label 有差异：</p><ul><li>测试 Jenkinsfile：<code>exampleapp &amp;&amp; macos &amp;&amp; ios</code>。</li><li>根 <code>Jenkinsfile</code>、两个备用发布 Jenkinsfile：<code>exampleapp &amp;&amp; xcode-26.3</code>。</li></ul><p>因此同一节点设置四个标签最简单。测试流水线会运行选择模拟器的脚本动态选择设备，不要把设备名称写死在节点配置里。</p><h3 id="6-GitHub-服务器和状态回写"><a href="#6-GitHub-服务器和状态回写" class="headerlink" title="6. GitHub 服务器和状态回写"></a>6. GitHub 服务器和状态回写</h3><p>在 <code>Manage Jenkins</code> → <code>System</code> → <code>GitHub Servers</code> 中配置：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs text">Name: github.com<br>API URL: https://api.github.com<br>Credentials: 前面创建的细粒度 GitHub Token 或 GitHub App 凭证<br></code></pre></td></tr></table></figure><p>点击“Test connection”成功后保存。根 <code>Jenkinsfile</code> 会回写 <code>jenkins/exampleapp-all</code>：</p><table><thead><tr><th>状态</th><th>含义</th></tr></thead><tbody><tr><td><code>PENDING</code></td><td>Jenkins 已接收，正在排队或运行</td></tr><tr><td><code>SUCCESS</code></td><td>必需阶段通过</td></tr><tr><td><code>FAILURE</code></td><td>测试、完整性检查或构建失败</td></tr><tr><td><code>ERROR</code></td><td>Jenkins、凭证或节点基础设施异常</td></tr></tbody></table><p>如果插件不支持状态回写，日志中的警告不代表测试成功；先修复 GitHub 插件和凭证。</p><h3 id="7-Tools：固定-Jenkins-使用的工具"><a href="#7-Tools：固定-Jenkins-使用的工具" class="headerlink" title="7. Tools：固定 Jenkins 使用的工具"></a>7. Tools：固定 Jenkins 使用的工具</h3><p>路径：<code>Manage Jenkins</code> → <code>Tools</code>（旧版本显示 <code>Global Tool Configuration</code>）。</p><ol><li><strong>JDK installations</strong>：新增一个 JDK，名称填 <code>temurin-17</code>。如果 Jenkins 节点已通过 Homebrew&#x2F;安装包提供 Java，取消“自动安装”，填写实际路径，并用 <code>echo &quot;$JAVA_HOME&quot;</code> 确认。</li><li><strong>Git installations</strong>：新增 <code>system-git</code>，路径填 <code>/usr/bin/git</code> 或 <code>which git</code> 输出的路径。不要让 Job 运行时随机下载 Git。</li><li><strong>Ruby、Xcode、Simulator</strong>：不在这里使用自动安装器。Ruby 由节点固定的 Homebrew&#x2F;RVM 环境提供；Xcode 由 macOS 节点管理员安装到 <code>/Applications/Xcode.app</code>，流水线用 <code>xcode-select -p</code> 和 <code>xcodebuild -version</code> 校验。</li><li>如果 Jenkins 服务启动时没有读取用户的 shell 配置，在节点配置的 Environment variables 中设置非敏感的 <code>PATH</code>、<code>JAVA_HOME</code> 和 <code>DEVELOPER_DIR</code>；不要设置凭证变量。</li></ol><p>保存后在节点执行一次“运行命令”或临时测试 Job，确认 <code>java -version</code>、<code>git --version</code>、<code>ruby --version</code>、<code>bundle --version</code> 和 <code>xcodebuild -version</code> 均来自预期路径。工具版本漂移时先修节点，不要在 Jenkinsfile 里用 <code>sudo</code> 临时切换系统工具。</p><h2 id="七、准备-Xcode、Ruby-和项目依赖"><a href="#七、准备-Xcode、Ruby-和项目依赖" class="headerlink" title="七、准备 Xcode、Ruby 和项目依赖"></a>七、准备 Xcode、Ruby 和项目依赖</h2><h3 id="1-Xcode-和-Simulator"><a href="#1-Xcode-和-Simulator" class="headerlink" title="1. Xcode 和 Simulator"></a>1. Xcode 和 Simulator</h3><p>在 Jenkins 使用的 macOS 用户下执行：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs sh"><span class="hljs-built_in">sudo</span> xcode-select -s /Applications/Xcode.app/Contents/Developer<br><span class="hljs-built_in">sudo</span> xcodebuild -license accept<br>xcode-select -p<br>xcodebuild -version<br>xcrun simctl list runtimes<br>xcrun simctl list devices available<br></code></pre></td></tr></table></figure><p>确认 Xcode 与 iOS runtime 匹配，并且有 iOS 17.6 或更高的可用 iPhone Simulator。不要只用“iPhone 17 Pro”名称判断设备是否存在。常见根因是 Xcode&#x2F;runtime 不匹配，导致 <code>Runtime build &#39;xxx&#39; not found</code> 或 <code>No device found with name &#39;iPhone 17 Pro&#39;</code>。</p><h3 id="2-Ruby、Bundler-和-fastlane"><a href="#2-Ruby、Bundler-和-fastlane" class="headerlink" title="2. Ruby、Bundler 和 fastlane"></a>2. Ruby、Bundler 和 fastlane</h3><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs sh">brew install ruby@3.3<br><span class="hljs-built_in">echo</span> <span class="hljs-string">&#x27;export PATH=&quot;$(brew --prefix ruby@3.3)/bin:$PATH&quot;&#x27;</span> &gt;&gt; <span class="hljs-string">&quot;<span class="hljs-variable">$HOME</span>/.zprofile&quot;</span><br><span class="hljs-built_in">export</span> PATH=<span class="hljs-string">&quot;<span class="hljs-subst">$(brew --prefix ruby@3.3)</span>/bin:<span class="hljs-variable">$PATH</span>&quot;</span><br>ruby --version<br>gem install bundler -v 4.0.16<br>bundle --version<br></code></pre></td></tr></table></figure><p>项目的 <code>Gemfile.lock</code> 已锁定 Ruby、Bundler 和 fastlane 版本。仓库根目录执行：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs sh">bundle install --<span class="hljs-built_in">jobs</span> 4 --retry 3<br>bundle <span class="hljs-built_in">exec</span> fastlane --version<br></code></pre></td></tr></table></figure><p>所有 CI 命令都使用 <code>bundle exec</code>，避免调用系统里的另一套 fastlane。</p><h3 id="3-第一次本地复现"><a href="#3-第一次本地复现" class="headerlink" title="3. 第一次本地复现"></a>3. 第一次本地复现</h3><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs sh"><span class="hljs-built_in">cd</span> /path/to/ExampleApp<br><span class="hljs-built_in">export</span> OTHER_SWIFT_FLAGS=<span class="hljs-string">&#x27;-disable-sandbox&#x27;</span><br>bundle install --<span class="hljs-built_in">jobs</span> 4 --retry 3<br>ruby -c fastlane/Fastfile<br>xcodebuild -list -project ExampleApp.xcodeproj OTHER_SWIFT_FLAGS=<span class="hljs-string">&quot;<span class="hljs-variable">$OTHER_SWIFT_FLAGS</span>&quot;</span><br>bundle <span class="hljs-built_in">exec</span> fastlane ios metadata_validate<br>bundle <span class="hljs-built_in">exec</span> fastlane ios <span class="hljs-built_in">test</span><br></code></pre></td></tr></table></figure><p>预期产物：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs text">fastlane/test_output/ExampleAppTests.xml<br>fastlane/test_output/ExampleAppTests.xcresult<br>fastlane/test_output/logs/<br>build/Build/Products/Debug-iphonesimulator/ExampleApp.app<br></code></pre></td></tr></table></figure><p>这一步失败时先修复主机或项目环境，不要在 Jenkins 页面反复点击构建。</p><h2 id="八、测试、构建、上传脚本怎么写"><a href="#八、测试、构建、上传脚本怎么写" class="headerlink" title="八、测试、构建、上传脚本怎么写"></a>八、测试、构建、上传脚本怎么写</h2><p>项目把签名和商店逻辑集中在 Fastfile，把 Jenkins 保留为编排层。本地、Jenkins 和 GitHub Actions 使用同一套 lane，避免三套脚本漂移。</p><h3 id="1-测试脚本"><a href="#1-测试脚本" class="headerlink" title="1. 测试脚本"></a>1. 测试脚本</h3><p><code>ci/jenkins/test.Jenkinsfile</code> 的核心步骤如下：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-meta">#!/usr/bin/env bash</span><br><span class="hljs-built_in">set</span> -euo pipefail<br><br><span class="hljs-built_in">export</span> CI=<span class="hljs-literal">true</span><br><span class="hljs-built_in">export</span> OTHER_SWIFT_FLAGS=<span class="hljs-string">&#x27;-disable-sandbox&#x27;</span><br>bundle install --<span class="hljs-built_in">jobs</span> 4 --retry 3<br>ruby -c fastlane/Fastfile<br>xcodebuild -list -project ExampleApp.xcodeproj<br>bundle <span class="hljs-built_in">exec</span> fastlane ios metadata_validate<br>bundle <span class="hljs-built_in">exec</span> fastlane ios <span class="hljs-built_in">test</span><br><br><span class="hljs-built_in">mkdir</span> -p <span class="hljs-string">&quot;<span class="hljs-variable">$WORKSPACE</span>/build/jenkins/DerivedData&quot;</span><br>xcodebuild \<br>  -project ExampleApp.xcodeproj \<br>  -scheme ExampleApp \<br>  -configuration Debug \<br>  -derivedDataPath <span class="hljs-string">&quot;<span class="hljs-variable">$WORKSPACE</span>/build/jenkins/DerivedData&quot;</span> \<br>  -destination <span class="hljs-string">&quot;<span class="hljs-variable">$SIMULATOR_DESTINATION</span>&quot;</span> \<br>  OTHER_SWIFT_FLAGS=<span class="hljs-string">&quot;<span class="hljs-variable">$OTHER_SWIFT_FLAGS</span>&quot;</span> \<br>  build<br></code></pre></td></tr></table></figure><p><code>SIMULATOR_DESTINATION</code> 由选择模拟器的脚本产生，例如 <code>platform=iOS Simulator,id=&lt;UDID&gt;</code>。fastlane <code>test</code> 会生成 JUnit XML、日志和 <code>.xcresult</code>，Jenkins 用 JUnit 插件读取 XML。</p><h3 id="2-开发包和-App-Store-归档脚本"><a href="#2-开发包和-App-Store-归档脚本" class="headerlink" title="2. 开发包和 App Store 归档脚本"></a>2. 开发包和 App Store 归档脚本</h3><p>开发包：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">export</span> OTHER_SWIFT_FLAGS=<span class="hljs-string">&#x27;-disable-sandbox&#x27;</span><br>bundle <span class="hljs-built_in">exec</span> fastlane ios exampleapp_build_dev<br></code></pre></td></tr></table></figure><p>App Store 归档（只生成本地 IPA）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">export</span> OTHER_SWIFT_FLAGS=<span class="hljs-string">&#x27;-disable-sandbox&#x27;</span><br><span class="hljs-built_in">export</span> BUILD_NUMBER=<span class="hljs-string">&#x27;123&#x27;</span>   <span class="hljs-comment"># 仅发布构建设置，必须唯一</span><br>bundle <span class="hljs-built_in">exec</span> fastlane ios exampleapp_archive_appstore<br></code></pre></td></tr></table></figure><p>产物目录：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs text">fastlane/build_output/ios/exampleapp/dev/<br>fastlane/build_output/ios/exampleapp/appstore/<br></code></pre></td></tr></table></figure><p><code>BUILD_NUMBER</code> 会写入工程 build setting，所以必须在干净 checkout 中运行，并记录版本号、build number 和 commit SHA。普通测试不要占用 App Store Connect 的正式 build number。</p><h3 id="3-安全的签名脚本"><a href="#3-安全的签名脚本" class="headerlink" title="3. 安全的签名脚本"></a>3. 安全的签名脚本</h3><p>签名任务必须使用临时钥匙串。凭证由 Jenkins <code>withCredentials</code> 注入，绝不能写死：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">set</span> -euo pipefail<br><span class="hljs-built_in">export</span> MATCH_KEYCHAIN_NAME=<span class="hljs-string">&quot;exampleapp-ci-<span class="hljs-variable">$&#123;BUILD_TAG&#125;</span>&quot;</span><br><span class="hljs-built_in">export</span> MATCH_KEYCHAIN_PASSWORD=<span class="hljs-string">&quot;<span class="hljs-subst">$(uuidgen)</span>&quot;</span><br>security create-keychain -p <span class="hljs-string">&quot;<span class="hljs-variable">$MATCH_KEYCHAIN_PASSWORD</span>&quot;</span> <span class="hljs-string">&quot;<span class="hljs-variable">$MATCH_KEYCHAIN_NAME</span>&quot;</span><br>security set-keychain-settings -lut 21600 <span class="hljs-string">&quot;<span class="hljs-variable">$MATCH_KEYCHAIN_NAME</span>&quot;</span><br>security unlock-keychain -p <span class="hljs-string">&quot;<span class="hljs-variable">$MATCH_KEYCHAIN_PASSWORD</span>&quot;</span> <span class="hljs-string">&quot;<span class="hljs-variable">$MATCH_KEYCHAIN_NAME</span>&quot;</span><br>security list-keychains -d user -s <span class="hljs-string">&quot;<span class="hljs-variable">$MATCH_KEYCHAIN_NAME</span>&quot;</span> <span class="hljs-string">&quot;<span class="hljs-variable">$HOME</span>/Library/Keychains/login.keychain-db&quot;</span><br>security default-keychain -d user -s <span class="hljs-string">&quot;<span class="hljs-variable">$MATCH_KEYCHAIN_NAME</span>&quot;</span><br><br>install -d -m 700 <span class="hljs-string">&quot;<span class="hljs-variable">$HOME</span>/.ssh&quot;</span><br><span class="hljs-built_in">cp</span> <span class="hljs-string">&quot;<span class="hljs-variable">$MATCH_KEY_FILE</span>&quot;</span> <span class="hljs-string">&quot;<span class="hljs-variable">$HOME</span>/.ssh/exampleapp_match_key&quot;</span><br><span class="hljs-built_in">chmod</span> 600 <span class="hljs-string">&quot;<span class="hljs-variable">$HOME</span>/.ssh/exampleapp_match_key&quot;</span><br>ssh-keyscan github.com &gt;&gt; <span class="hljs-string">&quot;<span class="hljs-variable">$HOME</span>/.ssh/known_hosts&quot;</span><br><span class="hljs-built_in">export</span> GIT_SSH_COMMAND=<span class="hljs-string">&quot;ssh -i <span class="hljs-variable">$HOME</span>/.ssh/exampleapp_match_key -o IdentitiesOnly=yes&quot;</span><br><br>install -d -m 700 <span class="hljs-string">&quot;<span class="hljs-variable">$WORKSPACE</span>/.ci-secrets&quot;</span><br><span class="hljs-built_in">cp</span> <span class="hljs-string">&quot;<span class="hljs-variable">$ASC_KEY_FILEPATH</span>&quot;</span> <span class="hljs-string">&quot;<span class="hljs-variable">$WORKSPACE</span>/.ci-secrets/AuthKey.p8&quot;</span><br><span class="hljs-built_in">chmod</span> 600 <span class="hljs-string">&quot;<span class="hljs-variable">$WORKSPACE</span>/.ci-secrets/AuthKey.p8&quot;</span><br><span class="hljs-built_in">export</span> ASC_KEY_FILEPATH=<span class="hljs-string">&quot;<span class="hljs-variable">$WORKSPACE</span>/.ci-secrets/AuthKey.p8&quot;</span><br><span class="hljs-built_in">export</span> FASTLANE_WWDR_USE_HTTP1_AND_RETRIES=<span class="hljs-literal">true</span><br>bundle <span class="hljs-built_in">exec</span> fastlane ios beta<br></code></pre></td></tr></table></figure><p>Jenkins <code>post { always { ... } }</code> 必须恢复 login keychain 并删除 <code>exampleapp_match_key</code>、<code>AuthKey.p8</code>、临时钥匙串和工作区密钥。清理脚本只允许清理白名单路径；测试流水线最终还会用 <code>deleteDir()</code> 清空专用工作区。</p><h3 id="4-TestFlight-和-App-Store-上传"><a href="#4-TestFlight-和-App-Store-上传" class="headerlink" title="4. TestFlight 和 App Store 上传"></a>4. TestFlight 和 App Store 上传</h3><p>TestFlight 只上传、不自动分发外部测试组：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">bundle <span class="hljs-built_in">exec</span> fastlane ios beta<br></code></pre></td></tr></table></figure><p>正式提交审核：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">CONFIRM_RELEASE=<span class="hljs-literal">true</span> AUTOMATIC_RELEASE=<span class="hljs-literal">false</span> \<br>  bundle <span class="hljs-built_in">exec</span> fastlane ios release<br></code></pre></td></tr></table></figure><p><code>release</code> 确认模式要求当前提交精确匹配 <code>v&lt;marketing-version&gt;-build.&lt;N&gt;</code>，审核通过后仍由 App Store Connect 人工发布。不要提供“自动公开发布”参数。</p><p>发布前检查：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">bundle <span class="hljs-built_in">exec</span> fastlane ios asc_verify<br>bundle <span class="hljs-built_in">exec</span> fastlane ios metadata_validate<br>bundle <span class="hljs-built_in">exec</span> fastlane ios precheck<br></code></pre></td></tr></table></figure><h3 id="5-Fastlane-lane-对照"><a href="#5-Fastlane-lane-对照" class="headerlink" title="5. Fastlane lane 对照"></a>5. Fastlane lane 对照</h3><table><thead><tr><th>Lane</th><th>作用</th><th>外部状态</th></tr></thead><tbody><tr><td><code>exampleapp_dev</code></td><td>match 拉取主 App + Widget 开发证书&#x2F;profile</td><td>无</td></tr><tr><td><code>exampleapp_appstore</code></td><td>match 拉取 App Store 分发证书&#x2F;profile</td><td>无</td></tr><tr><td><code>test</code></td><td>动态选择 Simulator，运行测试并输出 JUnit&#x2F;xcresult</td><td>无</td></tr><tr><td><code>exampleapp_build_dev</code></td><td>Development IPA、archive、dSYM</td><td>无</td></tr><tr><td><code>exampleapp_archive_appstore</code></td><td>App Store IPA、archive、dSYM</td><td>无</td></tr><tr><td><code>asc_verify</code></td><td>验证 App Store Connect API 只读权限</td><td>无</td></tr><tr><td><code>next_build_number</code></td><td>读取本地和 ASC 的下一 build number</td><td>无</td></tr><tr><td><code>release_notes</code></td><td>从 feat&#x2F;fix&#x2F;perf 提交生成中文发布说明</td><td>无</td></tr><tr><td><code>release_prepare</code></td><td>默认 dry-run；确认后递增 build、提交并创建 tag</td><td>修改 Git，需确认</td></tr><tr><td><code>beta</code></td><td>上传 TestFlight，默认不分发</td><td>上传</td></tr><tr><td><code>release</code></td><td>测试、归档、precheck，确认后上传并提交审核</td><td>提交审核</td></tr><tr><td><code>metadata_validate</code></td><td>校验多语言商店元数据和 HTTPS 链接</td><td>无</td></tr><tr><td><code>precheck</code></td><td>App Store Connect 元数据预检查</td><td>无</td></tr><tr><td><code>metadata_upload</code></td><td>上传元数据和配置的截图，不上传 IPA&#x2F;不提交审核</td><td>元数据变化</td></tr><tr><td><code>screenshots</code></td><td>生成多语言 iPhone&#x2F;iPad 截图</td><td>无</td></tr></tbody></table><p>版本发布顺序必须是：确定唯一 build number → 提交 build number 变更 → 创建匹配 tag → 推送 commit 和 tag → 以该 tag 测试、归档和发布。不能先创建 tag 再修改 build number。</p><h2 id="九、创建自动化测试-Job"><a href="#九、创建自动化测试-Job" class="headerlink" title="九、创建自动化测试 Job"></a>九、创建自动化测试 Job</h2><p>测试 Job 使用 Multibranch Pipeline，让 Jenkins 自动发现 <code>test</code> 分支和 Pull Request。</p><h3 id="1-创建-Multibranch-Pipeline"><a href="#1-创建-Multibranch-Pipeline" class="headerlink" title="1. 创建 Multibranch Pipeline"></a>1. 创建 Multibranch Pipeline</h3><ol><li>首页点击“New Item”。</li><li>名称填 <code>exampleapp-test</code>，选择“Multibranch Pipeline”，点击“OK”。</li><li>“Branch Sources” → “Add source” → “GitHub”。</li><li>GitHub server 选择 <code>github.com</code>，Owner 填 <code>your-org</code>，Repository 填 <code>ExampleApp</code>。</li><li>Credentials 选择仓库读取凭证；公开仓库可留空，但推荐使用细粒度 GitHub App&#x2F;PAT。</li><li>Behaviors 选择发现 <code>test</code> 分支和 Pull Request。PR 策略选择构建 head revision 时，测试的是 PR 当前 commit；若团队需要验证合并结果，再改为 merge revision。</li><li>“Build Configuration” → Mode 选“by Jenkinsfile”，Script Path 填：</li></ol><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">ci/jenkins/test.Jenkinsfile<br></code></pre></td></tr></table></figure><ol start="8"><li>Orphaned Item Strategy 保留最近 20 个分支任务。</li><li>Scan trigger 可设 5 分钟作为 Webhook 故障兜底，正常触发仍依赖 Webhook。</li><li>保存后点击“Scan Multibranch Pipeline Now”。</li></ol><h3 id="2-测试-Pipeline-阶段"><a href="#2-测试-Pipeline-阶段" class="headerlink" title="2. 测试 Pipeline 阶段"></a>2. 测试 Pipeline 阶段</h3><p><code>ci/jenkins/test.Jenkinsfile</code> 会：清理工作区并 checkout 精确 commit；动态选择 Simulator；执行依赖、Fastfile 语法、工程列表和三语元数据校验；执行 <code>fastlane ios test</code>；执行 Debug build；发布 JUnit、日志、xcresult 和环境信息；最后清理 DerivedData、测试输出并 <code>deleteDir()</code>。</p><p>它不会调用 <code>match</code>、<code>beta</code> 或 <code>release</code>，也不绑定 App Store Connect 凭证。</p><h3 id="3-配置-GitHub-Webhook"><a href="#3-配置-GitHub-Webhook" class="headerlink" title="3. 配置 GitHub Webhook"></a>3. 配置 GitHub Webhook</h3><p>GitHub 仓库 <code>Settings</code> → <code>Webhooks</code> → <code>Add webhook</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs text">Payload URL: https://jenkins.example.com/github-webhook/<br>Content type: application/json<br>Secret: 与 Jenkins webhook 校验配置相同的随机字符串<br>Events: Pushes、Pull requests、Ping<br>Active: 勾选<br></code></pre></td></tr></table></figure><p>保存后在 Recent Deliveries 发送 <code>ping</code>，确认 Jenkins 返回 200。再向 <code>test</code> push 一个安全提交，检查是否创建构建。</p><p>不同版本的 GitHub Branch Source&#x2F;GitHub 插件对 webhook secret 的入口名称不同：如果 <code>Manage Jenkins → System → GitHub Servers</code> 提供 secret 校验字段，就填入与 GitHub Webhook 相同的随机值；如果页面没有该字段，使用 GitHub App 的托管 Webhook，或在 HTTPS 反向代理层校验 GitHub 的 HMAC 签名。不要仅仅在 GitHub 页面填写 Secret，却没有任何一层实际校验它。</p><h2 id="十、创建打包-Job"><a href="#十、创建打包-Job" class="headerlink" title="十、创建打包 Job"></a>十、创建打包 Job</h2><p>如果团队希望把“测试”和“签名归档”显示成两个独立的 Job，可以创建一个只执行归档阶段 A 的手动 Job。它复用 <code>ci/jenkins/appstore-backup.Jenkinsfile</code>，不复制签名逻辑，也不会上传商店。</p><h3 id="1-创建方式"><a href="#1-创建方式" class="headerlink" title="1. 创建方式"></a>1. 创建方式</h3><ol><li>首页 → “New Item” → 名称 <code>exampleapp-build</code>。</li><li>选择“Pipeline” → “OK”。</li><li>Build Triggers 全部不勾选。</li><li>Pipeline → Definition 选择“Pipeline script from SCM”。</li><li>SCM 选择 Git，Repository URL 填应用仓库地址，Credentials 选择仓库读取凭证。</li><li>Branches to build 填 <code>*/main</code> 或要打包的受信任分支。</li><li>Script Path 填 <code>ci/jenkins/appstore-backup.Jenkinsfile</code>。</li><li>保存后点击一次“Build Now”，让 Jenkins 解析参数。</li></ol><h3 id="2-打包参数"><a href="#2-打包参数" class="headerlink" title="2. 打包参数"></a>2. 打包参数</h3><p>点击“Build with Parameters”：</p><table><thead><tr><th>参数</th><th>示例</th><th>填写方式</th></tr></thead><tbody><tr><td><code>RELEASE_REF</code></td><td><code>a1b2c3d</code> 或 <code>v1.0.0-build.7</code></td><td>必填，固定要打包的 commit&#x2F;tag</td></tr><tr><td><code>BUILD_NUMBER</code></td><td><code>8</code></td><td>必填，唯一正整数</td></tr><tr><td><code>CONFIRM_PRECHECK</code></td><td>勾选</td><td>允许执行测试、签名归档和 precheck</td></tr><tr><td><code>CONFIRM_RELEASE</code></td><td>不勾选</td><td>保持阶段 B 关闭，绝不上传或提交审核</td></tr></tbody></table><p>阶段 A 成功后，在 Job 的 Artifacts 中下载：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs text">fastlane/build_output/ios/exampleapp/appstore/ExampleApp.ipa<br>fastlane/build_output/ios/exampleapp/appstore/ExampleApp.xcarchive/<br>fastlane/test_output/<br>commit.txt<br>environment.txt<br></code></pre></td></tr></table></figure><p>这个 Job 仍然需要 App Store 分发凭证，因为归档要验证主 App 和 Widget 的签名；它不应被当作无凭证的普通测试 Job。若只需要快速编译检查，使用测试 Job 的 Debug build。</p><h2 id="十一、创建-TestFlight-备用发布-Job"><a href="#十一、创建-TestFlight-备用发布-Job" class="headerlink" title="十一、创建 TestFlight 备用发布 Job"></a>十一、创建 TestFlight 备用发布 Job</h2><p>该 Job 只在默认 CI 或其 macOS runner 故障、需要恢复 TestFlight 上传时使用，必须手动触发。</p><h3 id="1-创建-Pipeline-Job"><a href="#1-创建-Pipeline-Job" class="headerlink" title="1. 创建 Pipeline Job"></a>1. 创建 Pipeline Job</h3><ol><li>首页 → “New Item” → 名称 <code>exampleapp-testflight-backup</code>。</li><li>选择“Pipeline” → “OK”。</li><li>Build Triggers 全部不勾选。</li><li>Pipeline → Definition 选择“Pipeline script from SCM”。</li><li>SCM 选择 Git；Repository URL 填 <code>git@example.com:your-org/ExampleApp.git</code> 或 HTTPS；Credentials 选择应用仓库读取凭证。</li><li>Branches to build 填 <code>*/main</code> 或团队指定的受信任发布分支。</li><li>Script Path 填 <code>ci/jenkins/testflight-backup.Jenkinsfile</code>。</li><li>保存后先点击一次“Build Now”让 Jenkins 解析 Jenkinsfile；参数通常从第二次运行开始显示。</li></ol><h3 id="2-运行参数"><a href="#2-运行参数" class="headerlink" title="2. 运行参数"></a>2. 运行参数</h3><table><thead><tr><th>参数</th><th>示例</th><th>要求</th></tr></thead><tbody><tr><td><code>RELEASE_REF</code></td><td><code>a1b2c3d</code>、完整 SHA 或 <code>v1.0.0-build.7</code></td><td>必填，只接受 SHA 或 <code>v*</code> tag</td></tr><tr><td><code>CONFIRM_UPLOAD</code></td><td>勾选</td><td>不勾选会在参数校验阶段失败</td></tr></tbody></table><p>流水线会 checkout 指定 ref，运行完整测试，然后在 <code>exampleapp-release-signing</code> 锁内创建临时钥匙串、读取 match 和 ASC 凭证，最后调用 <code>bundle exec fastlane ios beta</code>。它会校验 App 与 Widget dSYM，上传后默认不分发外部测试组。</p><p>运行前把 Jenkins 构建 URL、commit SHA、版本号和 build number 记录到受控发布记录。完成后下载 <code>commit.txt</code>、<code>environment.txt</code>、JUnit、xcresult、IPA、archive 和 dSYM artifact。</p><h2 id="十二、创建-App-Store-备用发布-Job（两阶段闸门）"><a href="#十二、创建-App-Store-备用发布-Job（两阶段闸门）" class="headerlink" title="十二、创建 App Store 备用发布 Job（两阶段闸门）"></a>十二、创建 App Store 备用发布 Job（两阶段闸门）</h2><p>App Store 备用发布采用两阶段：阶段 A 只测试、归档和预检查；阶段 B 由发布负责人批准后提交审核。</p><h3 id="1-配置审核联系人文件"><a href="#1-配置审核联系人文件" class="headerlink" title="1. 配置审核联系人文件"></a>1. 配置审核联系人文件</h3><p>以 Jenkins 运行用户执行：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs sh"><span class="hljs-built_in">mkdir</span> -p <span class="hljs-string">&quot;<span class="hljs-variable">$HOME</span>/.config/exampleapp/appstoreconnect&quot;</span><br><span class="hljs-built_in">cp</span> /path/to/ExampleApp/fastlane/review_information.example.yml \<br>  <span class="hljs-string">&quot;<span class="hljs-variable">$HOME</span>/.config/exampleapp/appstoreconnect/review_information.yml&quot;</span><br><span class="hljs-built_in">chmod</span> 600 <span class="hljs-string">&quot;<span class="hljs-variable">$HOME</span>/.config/exampleapp/appstoreconnect/review_information.yml&quot;</span><br></code></pre></td></tr></table></figure><p>编辑真实姓名、电话、邮箱和审核说明。独立 <code>appstore-backup.Jenkinsfile</code> 没有绑定审核信息 Secret，因此必须使用默认路径，或在节点环境提供 <code>ASC_REVIEW_INFO_FILEPATH</code>。内容不得打印或归档。</p><h3 id="2-创建-Job"><a href="#2-创建-Job" class="headerlink" title="2. 创建 Job"></a>2. 创建 Job</h3><p>按 TestFlight Job 的步骤创建 Pipeline，改为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs text">Job name: exampleapp-appstore-backup<br>Script Path: ci/jenkins/appstore-backup.Jenkinsfile<br>Build Triggers: 全部关闭<br></code></pre></td></tr></table></figure><p>确保 Jenkins 用户属于可通过 <code>input</code> 审批的 <code>exampleapp-release-managers</code> 用户组。若没有组管理能力，应在代码评审后把 Jenkinsfile 的 <code>submitter</code> 改为实际用户 ID，不能删除人工审批。</p><h3 id="3-参数和两个阶段"><a href="#3-参数和两个阶段" class="headerlink" title="3. 参数和两个阶段"></a>3. 参数和两个阶段</h3><table><thead><tr><th>参数</th><th>示例</th><th>要求</th></tr></thead><tbody><tr><td><code>RELEASE_REF</code></td><td><code>v1.0.0-build.7</code> 或完整 SHA</td><td>必填，必须可由完整 Git clone 获取</td></tr><tr><td><code>BUILD_NUMBER</code></td><td><code>8</code></td><td>必填，唯一正整数</td></tr><tr><td><code>CONFIRM_PRECHECK</code></td><td>阶段 A 勾选</td><td>允许预演，否则失败</td></tr><tr><td><code>CONFIRM_RELEASE</code></td><td>阶段 A 不勾选；正式提交时勾选</td><td>进入审批和阶段 B</td></tr></tbody></table><p>阶段 A：checkout 指定 ref → 安装依赖 → 元数据校验 → 完整测试 → 在签名锁内归档 → <code>precheck</code>。它只产生 IPA、archive、dSYM 和报告，不上传、不提交审核。</p><p>阶段 B：只有 <code>CONFIRM_RELEASE=true</code> 才显示 Jenkins <code>input</code>；审批人限制为 <code>exampleapp-release-managers</code>；再次比较当前 checkout SHA 与 <code>commit.txt</code>；调用 <code>CONFIRM_RELEASE=true bundle exec fastlane ios release</code>；上传 IPA 和三语元数据并提交审核，固定 <code>automatic_release=false</code>。审核通过后仍由 App Store Connect 人工公开发布。</p><p>一个容易被忽略的 Jenkins 细节：每个 <code>sh</code> 步骤都是新的 shell，阶段 A 中 <code>export</code> 的 <code>GIT_SSH_COMMAND</code>、<code>MATCH_KEYCHAIN_NAME</code> 和 <code>ASC_KEY_FILEPATH</code> 不会自动传到阶段 B。首次启用真实发布前，必须在 Jenkins 上做一次低风险预演，并确认阶段 B 也能访问 match 仓库和临时钥匙串；若日志显示 SSH 或签名失败，应把阶段 A 的 SSH&#x2F;钥匙串初始化片段复制到阶段 B，或把两步合并到同一个 <code>sh</code>&#x2F;共享脚本中。不能假设前一个阶段的 <code>export</code> 会跨步骤生效。</p><h2 id="十三、Artifact、报告和清理"><a href="#十三、Artifact、报告和清理" class="headerlink" title="十三、Artifact、报告和清理"></a>十三、Artifact、报告和清理</h2><p>每个 Job 至少归档：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs text">commit.txt<br>environment.txt<br>simulator.env<br>fastlane/test_output/**<br>fastlane/build_output/ios/exampleapp/**<br>build/jenkins/**<br>build/*.log<br>**/*.xcresult/**<br>**/*.ipa<br>**/*.dSYM/**<br></code></pre></td></tr></table></figure><p>Jenkins JUnit pattern：<code>fastlane/test_output/**/*.xml</code>。</p><p>建议保留：普通测试 14 天、<code>test</code> 候选归档 30 天、备用发布至少 180 天，正式发布按团队审计策略长期保留。每次构建至少能追溯 repository、branch&#x2F;tag、commit SHA、commit message、版本号、build number、Xcode、macOS、Job 和 Jenkins build number。</p><p><code>post { always { ... } }</code> 必须恢复 login keychain，删除临时钥匙串、match deploy key、<code>AuthKey.p8</code>、审核信息临时文件、DerivedData 和输出目录。禁止对 <code>$HOME</code>、<code>/Users</code>、工作区根目录或系统根目录做递归删除。</p><h2 id="十四、GitHub-分支与-CI-CD-规则"><a href="#十四、GitHub-分支与-CI-CD-规则" class="headerlink" title="十四、GitHub 分支与 CI&#x2F;CD 规则"></a>十四、GitHub 分支与 CI&#x2F;CD 规则</h2><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs text">feature/* / fix/* / refactor/* / hotfix/*<br>        ↓ Pull Request<br>test    ← Jenkins 自动测试和 Debug 构建<br>        ↓ 人工审核<br>main    ← 默认 CI 归档和正式发布<br></code></pre></td></tr></table></figure><p>完成 Webhook、状态回写和误报演练后，再在 GitHub <code>Settings</code> → <code>Branches</code> 中启用 Pull Request、审批、状态检查 required check、限制直接 push 和合并后自动删除分支。</p><p>不要只检查分支名。测试、归档和上传必须使用同一个不可变 commit；测试完成后如果分支又有新提交，旧状态不能用于合并或发布。</p><h2 id="十五、这次搭建中遇到的真实问题与解决方案"><a href="#十五、这次搭建中遇到的真实问题与解决方案" class="headerlink" title="十五、这次搭建中遇到的真实问题与解决方案"></a>十五、这次搭建中遇到的真实问题与解决方案</h2><p>下面这些不是“理论上可能发生”的问题，而是 ExampleApp 在搭建 Jenkins 与后续 CI 时实际验证过或遇到过的情况。</p><h3 id="问题-1：命令行构建时出现-Swift-宏找不到"><a href="#问题-1：命令行构建时出现-Swift-宏找不到" class="headerlink" title="问题 1：命令行构建时出现 Swift 宏找不到"></a>问题 1：命令行构建时出现 Swift 宏找不到</h3><p><strong>现象</strong></p><p>命令行 <code>xcodebuild</code> 或 Fastlane 测试时出现大量类似错误：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs text">could not be found for macro &#x27;Model()&#x27;<br>swift-plugin-server produced malformed response<br></code></pre></td></tr></table></figure><p><strong>原因</strong></p><p>项目使用 SwiftData 的宏。当前命令行环境中，Swift 宏插件沙箱导致插件服务无法正常启动；Xcode IDE 内构建却可能正常，所以问题很容易被误判成代码问题。</p><p><strong>解决方案</strong></p><p>在所有命令行构建入口统一传递：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">export</span> OTHER_SWIFT_FLAGS=<span class="hljs-string">&#x27;-disable-sandbox&#x27;</span><br></code></pre></td></tr></table></figure><p>无论是 <code>xcodebuild</code>、<code>fastlane ios test</code>、归档还是截图，都带上它。关键不是这一个参数本身，而是不要只在某一条命令里临时补上；否则本机测试通过、Jenkins 或 release 又失败，问题会反复出现。</p><h3 id="问题-2：Simulator-runtime-不匹配"><a href="#问题-2：Simulator-runtime-不匹配" class="headerlink" title="问题 2：Simulator &#x2F; runtime 不匹配"></a>问题 2：Simulator &#x2F; runtime 不匹配</h3><p><strong>现象</strong></p><p><code>xcrun simctl list devices available</code> 找不到目标设备，或 <code>xcodebuild</code> 报 <code>Runtime build &#39;xxx&#39; not found</code>、<code>No device found with name &#39;iPhone 17 Pro&#39;</code>。</p><p><strong>原因</strong></p><p>Xcode 版本与 iOS Simulator runtime 不匹配，或只按“iPhone 17 Pro”名称判断设备是否存在。</p><p><strong>解决方案</strong></p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs sh">xcode-select -p<br>xcodebuild -version<br>xcrun simctl list runtimes<br>xcrun simctl list devices available<br>xcodebuild -showdestinations -project ExampleApp.xcodeproj -scheme ExampleApp<br></code></pre></td></tr></table></figure><p>不要只按设备名称判断。让选择模拟器的脚本挑选满足最低 iOS 版本的可用 iPhone UDID，避免把设备名写死在节点配置里。</p><h3 id="问题-3：“没有测试报告”"><a href="#问题-3：“没有测试报告”" class="headerlink" title="问题 3：“没有测试报告”"></a>问题 3：“没有测试报告”</h3><p><strong>现象</strong></p><p>Jenkins JUnit 报告为空，构建看似“成功”却没有任何测试结论。</p><p><strong>原因</strong></p><p>多数时候测试根本没有启动，真正原因是 Xcode、runtime 或宏插件失败；只有测试成功后才检查 XML 报告。</p><p><strong>解决方案</strong></p><p>先看 <code>fastlane/test_output/logs</code> 和 xcodebuild 输出，确认测试真正执行并生成 <code>*.xcresult</code>。不要只看“构建绿了”就认为测试通过了——确保 JUnit 插件确实读到了 <code>fastlane/test_output/**/*.xml</code>。</p><h3 id="问题-4：WWDR-签名失败"><a href="#问题-4：WWDR-签名失败" class="headerlink" title="问题 4：WWDR &#x2F; 签名失败"></a>问题 4：WWDR &#x2F; 签名失败</h3><p><strong>现象</strong></p><p>归档或上传时报证书&#x2F;签名相关错误，临时钥匙串似乎没生效。</p><p><strong>原因</strong></p><p>临时钥匙串未解锁、未进入搜索列表、未设为默认，或 WWDR 证书拉取被网络限制。</p><p><strong>解决方案</strong></p><p>检查临时钥匙串已解锁、<code>security set-keychain-settings -lut 21600</code> 已设置、临时钥匙串位于搜索列表并是默认钥匙串，同时设置：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">export</span> FASTLANE_WWDR_USE_HTTP1_AND_RETRIES=<span class="hljs-literal">true</span><br></code></pre></td></tr></table></figure><p>构建结束恢复 login keychain，否则下一次构建可能继续使用已删除的钥匙串。</p><h3 id="问题-5：match-clone-失败"><a href="#问题-5：match-clone-失败" class="headerlink" title="问题 5：match clone 失败"></a>问题 5：match clone 失败</h3><p><strong>现象</strong></p><p><code>match</code> 无法 clone 私有证书仓库，或报 SSH permission denied、502、超时。</p><p><strong>原因</strong></p><p>SSH 私钥没有读取权限、Deploy Key 没加到正确仓库、<code>known_hosts</code> 缺 GitHub 指纹，或代理配置错误。</p><p><strong>解决方案</strong></p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs sh">ssh-keygen -y -f /path/to/private-key &gt; /tmp/match-key.pub<br>git ls-remote git@example.com:your-org/ios-certificates.git<br></code></pre></td></tr></table></figure><p>确认凭证是证书仓库的只读私钥，不是 <code>.pub</code> 文件，也不是个人源仓库密钥。CI 里显式设置 <code>GIT_SSH_COMMAND</code> 指定唯一 Deploy Key；本机代理异常时，用已验证可用的代理做一次性 Git 覆盖，不要永久污染全局配置。</p><h3 id="问题-6：ASC-p8-路径无效"><a href="#问题-6：ASC-p8-路径无效" class="headerlink" title="问题 6：ASC .p8 路径无效"></a>问题 6：ASC <code>.p8</code> 路径无效</h3><p><strong>现象</strong></p><p>Fastfile 报 <code>.p8</code> 后缀检查失败或找不到私钥。</p><p><strong>原因</strong></p><p>Jenkins Secret File 路径可能没有 <code>.p8</code> 后缀，而 Fastfile 会检查后缀。</p><p><strong>解决方案</strong></p><p>复制到工作区 <code>.ci-secrets/AuthKey.p8</code>、设置权限 <code>600</code>，导出 <code>ASC_KEY_FILEPATH</code>，结束后删除。不要直接把原始 Secret File 路径传给只认 <code>.p8</code> 后缀的检查。</p><h3 id="问题-7：Widget-不刷新"><a href="#问题-7：Widget-不刷新" class="headerlink" title="问题 7：Widget 不刷新"></a>问题 7：Widget 不刷新</h3><p><strong>现象</strong></p><p>主 App 数据已更新，但 Widget 不刷新。</p><p><strong>原因</strong></p><p>Widget 不直接读 SwiftData。若 provisioning profile 没有 App Group 能力，真机 <code>containerURL</code> 可能为 nil。</p><p><strong>解决方案</strong></p><p>确认主 App 写入 App Group 的 <code>widget_snapshot.json</code> 并调用 <code>WidgetCenter.shared.reloadAllTimelines()</code>。Jenkins 不应尝试让 Widget 直接读 SwiftData，也不能把数据库搬到 App Group。Apple Developer 后台、match、Fastfile、CI 签名检查四处都要包含 Widget 的 Bundle ID 与 App Group 能力。</p><h3 id="问题-8：tag-build-number-不一致"><a href="#问题-8：tag-build-number-不一致" class="headerlink" title="问题 8：tag &#x2F; build number 不一致"></a>问题 8：tag &#x2F; build number 不一致</h3><p><strong>现象</strong></p><p>正式 <code>release</code> 报当前提交不匹配 <code>v&lt;version&gt;-build.&lt;N&gt;</code>，或上传后被拒“build number 已存在”。</p><p><strong>原因</strong></p><p>发布要求精确匹配 tag；或并行构建重复使用同一个 build number。</p><p><strong>解决方案</strong></p><p>正确顺序是提交 build number、创建 tag、推送 commit 和 tag，再以该 tag 发布。上传失败后不要复用同一 ASC build number；频繁并行发布时应串行化或用外部构建号策略协调。</p><h3 id="问题-9：截图尺寸不符合要求"><a href="#问题-9：截图尺寸不符合要求" class="headerlink" title="问题 9：截图尺寸不符合要求"></a>问题 9：截图尺寸不符合要求</h3><p><strong>现象</strong></p><p>生成的截图被 App Store Connect 拒收，提示尺寸不符合槽位。</p><p><strong>原因</strong></p><p>不同设备型号输出的像素尺寸不同，不能把某一型号的截图直接当作某尺寸槽位。</p><p><strong>解决方案</strong></p><p>生成后用 <code>sips -g pixelWidth -g pixelHeight file.png</code> 检查实际像素，确认与 App Store Connect 要求的槽位尺寸一致后再上传。</p><h3 id="问题-10：发布-Job-跨阶段环境变量丢失"><a href="#问题-10：发布-Job-跨阶段环境变量丢失" class="headerlink" title="问题 10：发布 Job 跨阶段环境变量丢失"></a>问题 10：发布 Job 跨阶段环境变量丢失</h3><p><strong>现象</strong></p><p>阶段 A 已配置好 <code>GIT_SSH_COMMAND</code>、临时钥匙串，阶段 B 却报 SSH 或签名失败。</p><p><strong>原因</strong></p><p>Jenkins 每个 <code>sh</code> 步骤都是新的 shell，阶段 A 的 <code>export</code> 不会自动传到阶段 B。</p><p><strong>解决方案</strong></p><p>把 SSH&#x2F;钥匙串初始化片段复制到阶段 B，或把两步合并到同一个 <code>sh</code>&#x2F;共享脚本中。不能假设前一个阶段的 <code>export</code> 会跨步骤生效；首次启用真实发布前必须在 Jenkins 上做一次低风险预演。</p><h2 id="十六、一份可照着执行的日常清单"><a href="#十六、一份可照着执行的日常清单" class="headerlink" title="十六、一份可照着执行的日常清单"></a>十六、一份可照着执行的日常清单</h2><h3 id="日常开发"><a href="#日常开发" class="headerlink" title="日常开发"></a>日常开发</h3><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs sh">bundle install<br>bundle <span class="hljs-built_in">exec</span> fastlane ios <span class="hljs-built_in">test</span><br></code></pre></td></tr></table></figure><p>测试通过后再提交代码。不要等到发布日才第一次运行自动化。</p><h3 id="需要给测试人员一个包"><a href="#需要给测试人员一个包" class="headerlink" title="需要给测试人员一个包"></a>需要给测试人员一个包</h3><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs sh"><span class="hljs-comment"># 本地归档，先检查产物</span><br>bundle <span class="hljs-built_in">exec</span> fastlane ios exampleapp_archive_appstore<br><br><span class="hljs-comment"># 需要上传 TestFlight 时（Jenkins 备用 Job 或手动）</span><br>bundle <span class="hljs-built_in">exec</span> fastlane ios beta<br></code></pre></td></tr></table></figure><h3 id="需要修改商店文案"><a href="#需要修改商店文案" class="headerlink" title="需要修改商店文案"></a>需要修改商店文案</h3><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs sh"><span class="hljs-comment"># 先做本地格式校验</span><br>bundle <span class="hljs-built_in">exec</span> fastlane ios metadata_validate<br><br><span class="hljs-comment"># 再做 App Store Connect 预检</span><br>bundle <span class="hljs-built_in">exec</span> fastlane ios precheck<br><br><span class="hljs-comment"># 确认后只上传文案</span><br>bundle <span class="hljs-built_in">exec</span> fastlane ios metadata_upload<br></code></pre></td></tr></table></figure><h3 id="准备正式审核（Jenkins-备用-Job）"><a href="#准备正式审核（Jenkins-备用-Job）" class="headerlink" title="准备正式审核（Jenkins 备用 Job）"></a>准备正式审核（Jenkins 备用 Job）</h3><ol><li>手动触发 <code>exampleapp-build</code>，填 <code>RELEASE_REF</code> 与唯一 <code>BUILD_NUMBER</code>，勾选 <code>CONFIRM_PRECHECK</code>，下载归档产物自查。</li><li>手动触发 <code>exampleapp-testflight-backup</code>，必须勾选 <code>CONFIRM_UPLOAD</code>，确认临时钥匙串和 secret 已清理。</li><li>手动触发 <code>exampleapp-appstore-backup</code> 阶段 A（勾选 <code>CONFIRM_PRECHECK</code>），确认只生成预演产物，不上传、不提交审核。</li><li>阶段 B 由 <code>exampleapp-release-managers</code> 审批，确认 SHA 未变化、<code>CONFIRM_RELEASE=true</code> 生效、<code>automatic_release=false</code>。</li><li>检查 artifact 中的 commit、版本、build number、IPA、dSYM、JUnit、xcresult 和日志，确认没有 <code>.p8</code>、SSH 私钥或审核联系人文件。</li></ol><p>正式提交前，还应确认：当前工作树干净、当前提交具有正确 tag、TestFlight 已验证、审核联系信息已准备好、隐私链接和商店元数据正确。</p><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>Jenkins 最好的使用方式，不是追求“一台服务器把所有事情都做掉”，而是把 iOS 交付里的每个高风险环节做成边界明确、可单独验证的流水线：</p><ul><li>测试 Job 只负责可靠测试和报告，不碰发布凭证；</li><li>打包 Job 只负责签名归档导出 IPA，不自动上传；</li><li>TestFlight &#x2F; App Store 备用 Job 在签名锁与人工闸门之后才允许外部动作；</li><li>Fastlane 只负责把受控签名材料安全地带到当前机器并产出制品；</li><li>Jenkins 只负责在合适的时机、用合适的节点、注入合适的凭证来调用这些能力。</li></ul><p>当这些规则被写进仓库、被本机和 CI 共同执行后，发版就不再依赖某位同事记得住一套手工步骤。它变成了一条可阅读、可复现、可审计的工程流程——这才是 Jenkins 在 iOS 项目里真正长期有价值的地方。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;iOS-项目搭建-Jenkins-自动化：从安装到测试、打包和发布&quot;&gt;&lt;a href=&quot;#iOS-项目搭建-Jenkins-自动化：从安装到测试、打包和发布&quot; class=&quot;headerlink&quot; title=&quot;iOS 项目搭建 Jenkins 自动化：从安装到测</summary>
      
    
    
    
    
    <category term="macOS" scheme="https://flametinary.github.io/tags/macOS/"/>
    
    <category term="iOS" scheme="https://flametinary.github.io/tags/iOS/"/>
    
    <category term="Fastlane" scheme="https://flametinary.github.io/tags/Fastlane/"/>
    
    <category term="Jenkins" scheme="https://flametinary.github.io/tags/Jenkins/"/>
    
    <category term="CI/CD" scheme="https://flametinary.github.io/tags/CI-CD/"/>
    
    <category term="自动化测试" scheme="https://flametinary.github.io/tags/%E8%87%AA%E5%8A%A8%E5%8C%96%E6%B5%8B%E8%AF%95/"/>
    
    <category term="打包发布" scheme="https://flametinary.github.io/tags/%E6%89%93%E5%8C%85%E5%8F%91%E5%B8%83/"/>
    
  </entry>
  
  <entry>
    <title>iOS开发证书与Provisioning Profile创建指南：从CSR到真机签名</title>
    <link href="https://flametinary.github.io/2026/09/08/iOS%E5%BC%80%E5%8F%91%E8%AF%81%E4%B9%A6%E4%B8%8EProvisioning%20Profile%E5%88%9B%E5%BB%BA%E6%8C%87%E5%8D%97%EF%BC%9A%E4%BB%8ECSR%E5%88%B0%E7%9C%9F%E6%9C%BA%E7%AD%BE%E5%90%8D/"/>
    <id>https://flametinary.github.io/2026/09/08/iOS%E5%BC%80%E5%8F%91%E8%AF%81%E4%B9%A6%E4%B8%8EProvisioning%20Profile%E5%88%9B%E5%BB%BA%E6%8C%87%E5%8D%97%EF%BC%9A%E4%BB%8ECSR%E5%88%B0%E7%9C%9F%E6%9C%BA%E7%AD%BE%E5%90%8D/</id>
    <published>2026-09-07T16:00:00.000Z</published>
    <updated>2026-09-12T03:37:07.566Z</updated>
    
    <content type="html"><![CDATA[<p>第一次给 iOS App 配真机签名时，Apple Developer 后台里会同时出现 Certificate、Identifier、Device、Profile、Capability 等好几个入口，很容易让人不知道先做什么、后做什么。</p><p>其实这件事可以拆成一条清晰的链路：先让 Apple 认可你的 App 身份，再创建可用于签名的证书，最后用 Provisioning Profile 把“App、证书、能力和设备”组合在一起。本文以<strong>手动签名</strong>为主，带你从创建 CSR（Certificate Signing Request，证书签名请求）开始，完成 Development Certificate 和 Development Provisioning Profile 的创建，并说明它和 Xcode 自动签名、Fastlane match 的关系。</p><p>本文讨论的是 Apple Developer 的代码签名体系，不涉及 App Store Connect API Key。后者用于上传 TestFlight、管理商店元数据和提交审核，和“能不能把 App 签名安装到设备”是两套不同的权限系统。</p><h2 id="一、先建立正确的概念"><a href="#一、先建立正确的概念" class="headerlink" title="一、先建立正确的概念"></a>一、先建立正确的概念</h2><p>在动手前，先把几个名词分清。很多签名问题并不是操作错了，而是把它们当成同一种文件。</p><table><thead><tr><th>名称</th><th>解决的问题</th><th>可以简单理解为</th></tr></thead><tbody><tr><td>Identifier &#x2F; App ID</td><td>这个 App 是谁</td><td>App 的身份证，通常对应 Bundle ID</td></tr><tr><td>Certificate</td><td>谁可以代表团队签名</td><td>团队认可的签名身份</td></tr><tr><td>Private Key</td><td>你是否真的拥有签名能力</td><td>证书背后的“钥匙”，必须严格保护</td></tr><tr><td>Device</td><td>哪些真机可安装开发包</td><td>开发测试的设备白名单</td></tr><tr><td>Provisioning Profile</td><td>这个 App 用哪张证书、允许哪些能力和设备</td><td>把前面几项装订成一份通行证</td></tr><tr><td>Entitlements</td><td>App 实际请求的能力</td><td>如推送、App Groups、iCloud、Sign in with Apple</td></tr></tbody></table><p>可以把 Development 签名过程想成下面这样：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs text">明确 Bundle ID 和能力<br>        ↓<br>创建 CSR → Apple 签发开发证书 → 本机保留私钥<br>        ↓<br>登记测试设备（真机开发时）<br>        ↓<br>创建 Development Profile<br>        ↓<br>Profile 绑定 App ID + 开发证书 + 设备 + 能力<br>        ↓<br>Xcode 或 Fastlane 使用它签名并安装 App<br></code></pre></td></tr></table></figure><p>其中最重要的一点是：<strong>证书文件本身不等于签名能力。</strong> 如果某台 Mac 只导入了 <code>.cer</code>，却没有创建 CSR 时生成的私钥，codesign 仍然无法用它签名。</p><h2 id="二、开始前需要准备什么"><a href="#二、开始前需要准备什么" class="headerlink" title="二、开始前需要准备什么"></a>二、开始前需要准备什么</h2><p>开始之前，请确认你具备以下条件：</p><ul><li>已加入 Apple Developer Program，并在团队中拥有创建证书&#x2F;Profile 的权限；</li><li>项目已经确定主 App 的 Bundle ID，例如 <code>com.example.myapp</code>；</li><li>如果项目有 Widget、Share Extension、Notification Service Extension 等 Target，已经列出它们各自的 Bundle ID；</li><li>使用一台自己的 Mac 创建 CSR，且能访问“钥匙串访问”；</li><li>如果要真机开发，准备好测试 iPhone&#x2F;iPad 的 UDID；</li><li>知道项目需要哪些 Capability，例如 Push Notifications、App Groups、iCloud 等。</li></ul><p>Apple 当前的手动 Development Profile 页面要求 Account Holder 或 Admin 权限。如果你只是普通团队成员，通常应让管理员创建 Profile，或者直接使用 Xcode 的 Automatically manage signing，由 Xcode 代为管理开发证书、设备和 Profile。</p><p>如果项目暂时只跑 Simulator，严格来说不需要 Development Profile；Simulator App 不使用 Apple Developer 的真机签名链路。不过，只要要连接真机、导出 Development 包，或之后准备接入 Fastlane，就建议尽早把签名资产整理好。</p><h3 id="主-App-和-Extension-要单独看待"><a href="#主-App-和-Extension-要单独看待" class="headerlink" title="主 App 和 Extension 要单独看待"></a>主 App 和 Extension 要单独看待</h3><p>一个常见误区是“我的 App 已经有 Bundle ID，所以 Widget 也应该自动可以签”。实际上，Widget 等 Extension 是独立的签名 Target，通常拥有不同 Bundle ID，例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs text">主 App：com.example.myapp<br>Widget：com.example.myapp.Widget<br></code></pre></td></tr></table></figure><p>主 App 和 Widget 可能共享某些能力，比如 App Groups；但在 Apple Developer 后台中，它们仍要分别有对应的 Identifier，并在 Profile 和自动化签名配置中被完整覆盖。</p><h2 id="三、第一步：创建-App-ID-和配置能力"><a href="#三、第一步：创建-App-ID-和配置能力" class="headerlink" title="三、第一步：创建 App ID 和配置能力"></a>三、第一步：创建 App ID 和配置能力</h2><p>创建证书前，先确认 Apple Developer 后台已有正确的 Identifier。</p><p>进入 <a href="https://developer.apple.com/account/">Apple Developer</a> 后，依次打开：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs text">Certificates, Identifiers &amp; Profiles<br>→ Identifiers<br>→ 点击 +<br>→ App IDs<br></code></pre></td></tr></table></figure><p>选择 App 类型后，填写描述名称和 Bundle ID。</p><h3 id="Explicit-App-ID-和-Wildcard-App-ID-怎么选"><a href="#Explicit-App-ID-和-Wildcard-App-ID-怎么选" class="headerlink" title="Explicit App ID 和 Wildcard App ID 怎么选"></a>Explicit App ID 和 Wildcard App ID 怎么选</h3><p>通常应选择 <strong>Explicit App ID（明确 App ID）</strong>，也就是完整写出 Bundle ID：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">com.example.myapp<br></code></pre></td></tr></table></figure><p>Wildcard App ID 形如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">com.example.*<br></code></pre></td></tr></table></figure><p>它在非常简单的老项目中可能有用，但对现代 iOS 项目通常不够。只要使用推送通知、App Groups、iCloud、Sign in with Apple、Apple Pay 等能力，就需要明确 App ID。实际开发中，为了降低后续迁移成本，即使暂时没开这些能力，也更推荐使用 Explicit App ID。</p><h3 id="Capability-必须和工程保持一致"><a href="#Capability-必须和工程保持一致" class="headerlink" title="Capability 必须和工程保持一致"></a>Capability 必须和工程保持一致</h3><p>如果 Xcode 的 Signing &amp; Capabilities 中启用了 App Groups、iCloud 或 Push Notifications，Apple Developer 后台对应 Identifier 也必须开启这些能力。否则经常会出现：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">Provisioning profile doesn&#x27;t include the ... entitlement<br></code></pre></td></tr></table></figure><p>或者：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">Profile doesn&#x27;t match the entitlements file&#x27;s values<br></code></pre></td></tr></table></figure><p>遇到这类错误时，不要急着反复删除 Profile。应先对比三处是否一致：</p><ol><li>Xcode Target 的 Signing &amp; Capabilities；</li><li><code>.entitlements</code> 文件中的值；</li><li>Apple Developer 中对应 Identifier 已启用的 Capability。</li></ol><p>能力发生变化后，旧 Profile 未必包含新 entitlement，通常需要重新生成或更新 Profile。</p><h2 id="四、第二步：创建-CSR"><a href="#四、第二步：创建-CSR" class="headerlink" title="四、第二步：创建 CSR"></a>四、第二步：创建 CSR</h2><p>CSR 是发给 Apple 的“证书申请”。它包含公钥和申请信息；与之配对的私钥会保留在创建 CSR 的 Mac 的 Keychain 中。CSR 本身不是私钥，通常不属于最高敏感级别，但也没有必要提交到业务 Git 仓库。</p><h3 id="1-打开钥匙串访问"><a href="#1-打开钥匙串访问" class="headerlink" title="1. 打开钥匙串访问"></a>1. 打开钥匙串访问</h3><p>在 macOS 中打开：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">应用程序 → 实用工具 → 钥匙串访问<br></code></pre></td></tr></table></figure><p>或者直接用 Spotlight 搜索“钥匙串访问”。</p><h3 id="2-发起证书请求"><a href="#2-发起证书请求" class="headerlink" title="2. 发起证书请求"></a>2. 发起证书请求</h3><p>在顶部菜单选择：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">钥匙串访问 → 证书助手 → 向证书颁发机构请求证书<br></code></pre></td></tr></table></figure><blockquote><p>界面示意：在 Certificate Assistant 中填写开发者邮箱和一个便于识别的通用名称；本文不展示含邮箱或姓名的原始截图。</p></blockquote><p>在弹出的窗口中：</p><ol><li>在“用户电子邮件地址”填写你的开发者邮箱；</li><li>在“通用名称”填写方便识别的名称，例如 <code>MyApp Development Key 2026</code>；</li><li>“CA 电子邮件地址”保持为空；</li><li>选择“保存到磁盘”；</li><li>点击“继续”，选择一个安全的位置保存。</li></ol><p>生成的文件通常名为：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">CertificateSigningRequest.certSigningRequest<br></code></pre></td></tr></table></figure><p>![[CertificateSigningRequest.png|368]]</p><p>这个 CSR 可以上传到 Apple Developer 后台，但它本身不能用于签名，也不需要提交进 Git。</p><h3 id="3-为什么一定要在自己的-Mac-上生成-CSR"><a href="#3-为什么一定要在自己的-Mac-上生成-CSR" class="headerlink" title="3. 为什么一定要在自己的 Mac 上生成 CSR"></a>3. 为什么一定要在自己的 Mac 上生成 CSR</h3><p>创建 CSR 的同时，钥匙串访问会生成一对密钥：公钥被放进 CSR，私钥保留在当前 Mac。Apple 后台下载的证书会与这个私钥匹配。</p><p>因此：</p><ul><li>证书应由负责签名的人或受控的签名机器创建；</li><li>CSR 可以上传给 Apple 或证书颁发机构，但不要把真正的私钥、<code>.p12</code> 或 Keychain 导出文件一并发出去；</li><li>更不要只备份 <code>.cer</code> 而忘了备份含私钥的签名身份；</li><li>如果换电脑，需要从旧机器导出带私钥的 <code>.p12</code>，或用 Fastlane match 统一恢复签名资产。</li></ul><h2 id="五、第三步：在-Apple-Developer-创建-Development-Certificate"><a href="#五、第三步：在-Apple-Developer-创建-Development-Certificate" class="headerlink" title="五、第三步：在 Apple Developer 创建 Development Certificate"></a>五、第三步：在 Apple Developer 创建 Development Certificate</h2><p>回到 Apple Developer 后台，依次进入：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs text">Certificates, Identifiers &amp; Profiles<br>→ Certificates<br>→ 点击 +<br></code></pre></td></tr></table></figure><p>在证书类型中选择适合开发用途的开发证书。Apple 后台的命名会随时间调整，但原则不变：<strong>真机调试用 Development；TestFlight&#x2F;App Store 用 Distribution。</strong> 如果你使用 Xcode 自动签名，Xcode 可能会代为创建或管理开发证书，这时不必重复手动创建。</p><p>接下来上传刚才创建的 <code>.certSigningRequest</code> 文件。</p><p>![[choose_file.png|492]]</p><p>点击继续后，Apple 会生成证书。下载生成的 <code>.cer</code> 文件：</p><blockquote><p>生成成功后，页面会显示证书类型、创建者和到期时间。为避免泄露个人邮箱和姓名，本文不展示原始结果页截图。</p></blockquote><p>下载后的文件类似：</p><p>![[download_development_cer.png|511]]</p><p>双击 <code>.cer</code> 文件，系统会将证书导入“钥匙串访问”。正常情况下，你应在“我的证书”分类中看到证书，并能展开看到下面的私钥。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs text">Apple Development: Your Name (TEAMID)<br>  └── private key<br></code></pre></td></tr></table></figure><p>如果能看到证书，却不能展开出 private key，说明当前 Mac 没有对应私钥。最常见的原因是 CSR 在另一台 Mac 上生成，或曾经清理过 Keychain。此时不要新建一堆重复证书；应回到生成 CSR 的机器导出带私钥的身份，或按团队既定的 match 流程恢复。</p><h3 id="Development-与-Distribution-证书的区别"><a href="#Development-与-Distribution-证书的区别" class="headerlink" title="Development 与 Distribution 证书的区别"></a>Development 与 Distribution 证书的区别</h3><table><thead><tr><th>类型</th><th>用途</th><th>能否上传 TestFlight&#x2F;App Store</th></tr></thead><tbody><tr><td>Development Certificate</td><td>Xcode 真机运行、Development 包</td><td>不可以</td></tr><tr><td>Distribution Certificate</td><td>Ad Hoc、TestFlight、App Store Archive</td><td>可以，具体分发方式由 Profile&#x2F;导出方式决定</td></tr></tbody></table><p>开发证书和发布证书可以由同一个团队管理，但不是同一种证书。不要拿 Development Certificate 去导出 App Store IPA，也不要因为“Xcode 能跑”就认为发布签名已经准备好。</p><h2 id="六、第四步：登记测试设备"><a href="#六、第四步：登记测试设备" class="headerlink" title="六、第四步：登记测试设备"></a>六、第四步：登记测试设备</h2><p>如果你要创建传统的 Development Profile 或 Ad Hoc Profile，需要把测试设备登记到 Apple Developer 后台。</p><p>进入：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs text">Certificates, Identifiers &amp; Profiles<br>→ Devices<br>→ 点击 +<br></code></pre></td></tr></table></figure><p>填写设备名称和 UDID。获取 UDID 的常见方式：</p><ul><li>在 Finder 中连接 iPhone&#x2F;iPad，点击设备信息区的序列号字段，直到显示 UDID，然后复制；</li><li>在 Xcode 的 Devices and Simulators 窗口查看；</li><li>使用受信任的设备管理流程导出设备标识。</li></ul><p>UDID 是设备唯一标识，应像普通设备资产信息一样管理。设备更换、退出测试范围或团队成员变化时，及时从测试清单中移除或停止使用对应 Profile。</p><p>设备登记完成后，已经下载过的旧 Profile 不会自动获得新设备。新增设备后，需要重新生成并重新下载 Development Profile，或者让 Xcode 自动签名重新同步。</p><h2 id="七、第五步：创建-Development-Provisioning-Profile"><a href="#七、第五步：创建-Development-Provisioning-Profile" class="headerlink" title="七、第五步：创建 Development Provisioning Profile"></a>七、第五步：创建 Development Provisioning Profile</h2><p>现在可以创建 Profile。进入：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs text">Certificates, Identifiers &amp; Profiles<br>→ Profiles<br>→ 点击 +<br></code></pre></td></tr></table></figure><p>选择开发用途的 Profile 类型，然后按页面步骤完成：</p><ol><li>选择主 App 对应的 Explicit App ID；</li><li>选择刚创建的 Development Certificate；</li><li>选择允许安装的测试设备；</li><li>为 Profile 起一个清晰名称，例如 <code>MyApp Development 2026</code>；</li><li>生成并下载 <code>.mobileprovision</code> 文件；</li><li>双击该文件导入 Xcode，或让 Xcode&#x2F;fastlane 自动安装。</li></ol><blockquote><p>在“Select an App ID”步骤中，选择当前 Target 对应的 Explicit App ID。请勿在公开文章中展示真实 Team ID 或 Bundle ID。</p></blockquote><p>对于含 Widget 的项目，主 App 和 Widget 通常都需要对应的 Profile。创建时要反复确认当前 App ID 属于哪个 Target，不要把主 App 的 Profile 误配给 Extension。</p><h3 id="Profile-里的内容到底是什么"><a href="#Profile-里的内容到底是什么" class="headerlink" title="Profile 里的内容到底是什么"></a>Profile 里的内容到底是什么</h3><p>一个 Development Profile 大致可以理解为以下集合：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs text">明确的 App ID<br>  + 可用的 Capability / Entitlements<br>  + 一张或多张开发证书<br>  + 一组已登记的测试设备<br>  + 有效期<br></code></pre></td></tr></table></figure><p>任意一项不匹配都可能导致安装或签名失败。例如：新增测试机但 Profile 没重新生成、新增 App Group 但 Profile 还是旧版本、选错了证书、Widget 没有对应 Profile。</p><h2 id="八、Offline-support-7-day-validity-是什么"><a href="#八、Offline-support-7-day-validity-是什么" class="headerlink" title="八、Offline support 7 day validity 是什么"></a>八、Offline support 7 day validity 是什么</h2><p>创建某些开发或 Ad Hoc Profile 时，Apple Developer 后台可能会显示 <strong>Offline support (7 day validity)</strong> 选项。</p><blockquote><p>该页面会显示 Offline support 选项；本文省略包含真实证书名称的原始截图。</p></blockquote><p>它的核心取舍可以这样理解：</p><table><thead><tr><th>选项</th><th>适用情况</th><th>主要代价</th></tr></thead><tbody><tr><td>No（通常默认）</td><td>日常开发、普通真机测试、Fastlane match</td><td>设备需按 Apple 的常规验证机制使用网络环境</td></tr><tr><td>Yes</td><td>设备长期处于隔离网络、无法访问 Apple 验证服务的特殊测试</td><td>Profile 有效期只有 7 天，必须频繁重新生成和安装</td></tr></tbody></table><h3 id="日常开发为什么建议选-No"><a href="#日常开发为什么建议选-No" class="headerlink" title="日常开发为什么建议选 No"></a>日常开发为什么建议选 No</h3><p>对绝大多数项目，选择默认的 <strong>No</strong> 更合适：</p><ul><li>Profile 有效期按常规规则管理，通常不需要每周重建；</li><li>更适合持续开发、多人协作和自动化打包；</li><li>Fastlane match 和 Xcode 的常规签名流程也更容易维护；</li><li>测试设备只要能在正常网络条件下完成 Apple 的验证即可。</li></ul><h3 id="什么时候才考虑-Yes"><a href="#什么时候才考虑-Yes" class="headerlink" title="什么时候才考虑 Yes"></a>什么时候才考虑 Yes</h3><p>只有当测试设备确实处于隔离网络，长期无法访问 Apple 的验证服务，并且你能接受维护成本时，才考虑启用。典型场景包括特殊内网、离线现场演示或受严格网络限制的测试环境。</p><p>代价是明确的：Profile 只有 7 天有效，失效后 App 可能无法继续启动；你需要重新生成 Profile、重新签名、重新安装。这不是“更高级的模式”，而是为了离线场景做的有期限交换。</p><blockquote><p>Profile 生成页通常会显示 Profile 名称、App ID 和到期时间。公开记录时应使用占位示例，不展示真实项目标识。</p></blockquote><p>如果你正在使用 Fastlane match，优先保持默认的常规 Profile。把频繁过期的 7 天离线 Profile 纳入自动化资产，通常只会增加维护成本和故障概率。</p><h2 id="九、把证书和-Profile-安装到-Xcode"><a href="#九、把证书和-Profile-安装到-Xcode" class="headerlink" title="九、把证书和 Profile 安装到 Xcode"></a>九、把证书和 Profile 安装到 Xcode</h2><p>手动流程中：</p><ul><li>双击 <code>.cer</code>，导入钥匙串；</li><li>双击 <code>.mobileprovision</code>，让 Xcode 识别并安装；</li><li>在 Xcode 选择主 App Target → Signing &amp; Capabilities；</li><li>设置正确 Team；</li><li>根据团队策略选择 Automatically manage signing 或手动指定 Profile；</li><li>对每一个 Extension Target 重复检查 Team、Bundle ID 与 Capability。</li></ul><h3 id="自动签名和手动签名怎么选"><a href="#自动签名和手动签名怎么选" class="headerlink" title="自动签名和手动签名怎么选"></a>自动签名和手动签名怎么选</h3><table><thead><tr><th>方式</th><th>适合什么情况</th><th>注意事项</th></tr></thead><tbody><tr><td>Automatically manage signing</td><td>个人开发、早期项目、简单真机调试</td><td>Xcode 会尝试创建&#x2F;更新资产；多人和 CI 下可控性较弱</td></tr><tr><td>Manual signing</td><td>已接入 Fastlane match、多个 Target、CI、正式发布</td><td>需要明确管理证书&#x2F;Profile，但行为可复现</td></tr></tbody></table><p>两种方式没有绝对好坏。项目刚起步时自动签名能减少操作；一旦需要多人协作、Widget、多环境或 CI，建议逐步迁移到受控的签名管理方案。最怕的不是选哪一种，而是本机自动签名、CI 手动签名、团队成员各自一套证书混着用。</p><h3 id="当前项目的实际对应关系"><a href="#当前项目的实际对应关系" class="headerlink" title="当前项目的实际对应关系"></a>当前项目的实际对应关系</h3><p>当前工程采用 Manual signing，并按 SDK 和构建配置区分签名身份：真机 Debug 使用 Apple Development，Release 使用 Apple Distribution；主 App 和 Widget 分别指定对应的 <code>match Development ...</code> 或 <code>match AppStore ...</code> Profile。这里的省略号代表脱敏后的完整 Bundle ID。</p><p>这说明手动 Profile 不是“下载一次就结束”：工程的 Target、Bundle ID、签名身份、Profile specifier 和 entitlements 必须长期保持一致。只要新增 Widget、修改 App Groups 或切换分发方式，就要一起检查这些字段。</p><h2 id="十、检查-Profile-是否真的包含预期内容"><a href="#十、检查-Profile-是否真的包含预期内容" class="headerlink" title="十、检查 Profile 是否真的包含预期内容"></a>十、检查 Profile 是否真的包含预期内容</h2><p><code>.mobileprovision</code> 是签名配置文件，可以在本机查看它的有效期、UUID、App ID、Team 标识、设备列表和 Entitlements。不要直接用文本编辑器打开，而是让 macOS 的 <code>security</code> 工具解码：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs sh">security cms -D -i MyApp_Development.mobileprovision &gt; /tmp/profile.plist<br><br><span class="hljs-comment"># 查看关键字段</span><br>/usr/libexec/PlistBuddy -c <span class="hljs-string">&#x27;Print :Name&#x27;</span> /tmp/profile.plist<br>/usr/libexec/PlistBuddy -c <span class="hljs-string">&#x27;Print :ExpirationDate&#x27;</span> /tmp/profile.plist<br>/usr/libexec/PlistBuddy -c <span class="hljs-string">&#x27;Print :Entitlements:application-identifier&#x27;</span> /tmp/profile.plist<br>/usr/libexec/PlistBuddy -c <span class="hljs-string">&#x27;Print :ProvisionedDevices&#x27;</span> /tmp/profile.plist<br></code></pre></td></tr></table></figure><p>重点核对：</p><ul><li><code>application-identifier</code> 是否对应当前 Team 和 Bundle ID；</li><li><code>com.apple.developer.team-identifier</code> 是否属于预期团队；</li><li><code>Entitlements</code> 是否包含项目实际使用的能力；</li><li><code>ProvisionedDevices</code> 是否包含本次测试设备；</li><li><code>ExpirationDate</code> 是否已经过期。</li></ul><p>临时解码文件只用于排查，检查完成后可以删除。不要把包含团队标识、设备 UDID 或能力信息的完整 Profile 上传到公开 issue。</p><h2 id="十一、如何安全备份和迁移签名身份"><a href="#十一、如何安全备份和迁移签名身份" class="headerlink" title="十一、如何安全备份和迁移签名身份"></a>十一、如何安全备份和迁移签名身份</h2><p>如果暂时不用 Fastlane match，至少要知道如何从旧 Mac 迁移签名能力。</p><p>在“钥匙串访问 → 我的证书”中，找到能展开显示 private key 的开发或发布证书，导出为 <code>.p12</code>，并设置强密码。这个 <code>.p12</code> 同时携带证书和私钥，才可以在新 Mac 上继续签名。</p><p>安全规则：</p><ul><li><code>.p12</code>、私钥、<code>.mobileprovision</code> 不要提交到业务 Git 仓库；</li><li>不要用聊天工具、邮件或公开网盘随意传播私钥；</li><li>为 <code>.p12</code> 设置独立强密码；</li><li>只在受信任设备上导入；</li><li>团队协作时优先使用 Fastlane match 等统一机制，避免每个人手里散落一份证书；</li><li>证书疑似泄露时应立即在 Apple Developer 后台撤销并重新签发，而不是只删除本地文件。</li></ul><h2 id="十二、如何与-Fastlane-match-衔接"><a href="#十二、如何与-Fastlane-match-衔接" class="headerlink" title="十二、如何与 Fastlane match 衔接"></a>十二、如何与 Fastlane match 衔接</h2><p>当项目从“一个人一台电脑”发展到多人协作、需要换电脑、需要 CI 时，手动导入证书和 Profile 会越来越脆弱。这时可以使用 Fastlane match。</p><p>match 的做法是：把证书、私钥和 Profile 存放在<strong>独立的私有、加密仓库</strong>中，项目仓库只保存规则。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs text">业务仓库<br>  └── Fastfile：声明需要的 Bundle ID 和分发类型<br><br>私有 match 仓库<br>  └── 加密保存证书、私钥、Profile<br><br>开发机 / CI Runner<br>  └── match 下载、解密、导入 Keychain，再执行签名<br></code></pre></td></tr></table></figure><p>脱敏示例：</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs ruby"><span class="hljs-variable constant_">APP_IDENTIFIERS</span> = [<br>  <span class="hljs-string">&quot;com.example.myapp&quot;</span>,<br>  <span class="hljs-string">&quot;com.example.myapp.Widget&quot;</span><br>].freeze<br><br>lane <span class="hljs-symbol">:install_development_signing</span> <span class="hljs-keyword">do</span><br>  match(<br>    <span class="hljs-symbol">type:</span> <span class="hljs-string">&quot;development&quot;</span>,<br>    <span class="hljs-symbol">app_identifier:</span> <span class="hljs-variable constant_">APP_IDENTIFIERS</span>,<br>    <span class="hljs-symbol">git_url:</span> <span class="hljs-string">&quot;git@github.com:example/ios-certificates.git&quot;</span>,<br>    <span class="hljs-symbol">readonly:</span> <span class="hljs-literal">true</span><br>  )<br><span class="hljs-keyword">end</span><br></code></pre></td></tr></table></figure><p>日常开发和 CI 建议用：</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs ruby"><span class="hljs-symbol">readonly:</span> <span class="hljs-literal">true</span><br></code></pre></td></tr></table></figure><p>这表示当前机器只消费已有签名材料，不会擅自创建、修改或吊销团队证书。证书资产的创建、迁移、更新应在受控的本机环境中完成，再同步到 match 仓库。</p><p>如果想进一步了解 match、Archive、TestFlight、版本号与发布流程，可以阅读同目录文章：<a href="iOS%E9%A1%B9%E7%9B%AE%E5%A6%82%E4%BD%95%E6%8E%A5%E5%85%A5Fastlane.md">iOS项目如何接入Fastlane</a>。</p><h2 id="十三、常见问题与排查顺序"><a href="#十三、常见问题与排查顺序" class="headerlink" title="十三、常见问题与排查顺序"></a>十三、常见问题与排查顺序</h2><h3 id="1-找不到可用的签名身份"><a href="#1-找不到可用的签名身份" class="headerlink" title="1. 找不到可用的签名身份"></a>1. 找不到可用的签名身份</h3><p><strong>现象</strong>：Xcode 或 <code>codesign</code> 报找不到 Development&#x2F;Distribution certificate。</p><p><strong>优先检查</strong>：</p><ol><li>钥匙串访问“我的证书”中是否有证书；</li><li>证书下面能否展开看到 private key；</li><li>证书是否属于当前选择的 Team；</li><li>证书是否过期或已被撤销；</li><li>CI 是否创建、解锁并使用了正确 Keychain。</li></ol><p>只有 <code>.cer</code> 没有私钥，不能解决签名问题。</p><h3 id="2-Profile-不包含所需-entitlement"><a href="#2-Profile-不包含所需-entitlement" class="headerlink" title="2. Profile 不包含所需 entitlement"></a>2. Profile 不包含所需 entitlement</h3><p><strong>现象</strong>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">Provisioning profile doesn&#x27;t include the ... entitlement<br></code></pre></td></tr></table></figure><p><strong>处理顺序</strong>：检查 Identifier Capability → 检查 Xcode 的 Signing &amp; Capabilities → 检查 <code>.entitlements</code> → 更新 Profile。不要只在 Xcode 里反复切换 Team。</p><h3 id="3-真机无法安装或提示设备不在-Profile-内"><a href="#3-真机无法安装或提示设备不在-Profile-内" class="headerlink" title="3. 真机无法安装或提示设备不在 Profile 内"></a>3. 真机无法安装或提示设备不在 Profile 内</h3><p><strong>原因</strong>：设备 UDID 没登记，或者当前 Development&#x2F;Ad Hoc Profile 创建时没有勾选这台设备。</p><p><strong>解决</strong>：登记设备后重新生成 Profile，并在 Xcode 或 match 中更新安装的 Profile。</p><h3 id="4-Widget-能编译，Archive-时却签名失败"><a href="#4-Widget-能编译，Archive-时却签名失败" class="headerlink" title="4. Widget 能编译，Archive 时却签名失败"></a>4. Widget 能编译，Archive 时却签名失败</h3><p><strong>原因</strong>：Widget 的 Identifier 或 Profile 漏配；也可能 App Group 等共享能力在主 App 和 Widget 中不一致。</p><p><strong>解决</strong>：逐个检查 Target 的 Bundle ID、Team、Capability 和 Provisioning Profile。自动化中也要让 <code>app_identifier</code> 包含所有 Target。</p><h3 id="5-App-一周后无法打开"><a href="#5-App-一周后无法打开" class="headerlink" title="5. App 一周后无法打开"></a>5. App 一周后无法打开</h3><p><strong>原因</strong>：可能使用了 7 天 Offline support Profile；也可能使用了个人免费开发者账户或其他有期限的签名方式。不能只凭“7 天失效”就断言原因，先检查 Profile 类型和签名身份。</p><p><strong>解决</strong>：在 Apple Developer 后台查看该 Profile 的配置与有效期；日常开发改用常规 Development Profile，并重新签名安装。</p><h3 id="6-新机器导入证书后还是不能签名"><a href="#6-新机器导入证书后还是不能签名" class="headerlink" title="6. 新机器导入证书后还是不能签名"></a>6. 新机器导入证书后还是不能签名</h3><p><strong>原因</strong>：只导入了 <code>.cer</code>，没有私钥；或者导入的私钥与证书不匹配。</p><p><strong>解决</strong>：从旧机器导出带私钥的 <code>.p12</code>，或通过 match 恢复完整签名身份；不要在新机器上盲目多次创建证书。</p><h3 id="7-Fastlane-或-CI-里-match-拉取失败"><a href="#7-Fastlane-或-CI-里-match-拉取失败" class="headerlink" title="7. Fastlane 或 CI 里 match 拉取失败"></a>7. Fastlane 或 CI 里 match 拉取失败</h3><p><strong>原因</strong>：证书仓库 SSH 权限、Deploy Key、<code>known_hosts</code>、<code>MATCH_PASSWORD</code> 或临时 Keychain 配置有问题。</p><p><strong>解决</strong>：先检查仓库读取权限和密钥文件 <code>600</code> 权限；CI 中明确设置 <code>GIT_SSH_COMMAND</code>，并使用 <code>match readonly</code>。不要把证书仓库改公开来“解决”权限问题。</p><h2 id="十四、最后的检查清单"><a href="#十四、最后的检查清单" class="headerlink" title="十四、最后的检查清单"></a>十四、最后的检查清单</h2><p>在点击 Build 或 Archive 前，可以快速过一遍：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs text">[ ] 主 App 与所有 Extension 都有正确的 Explicit App ID<br>[ ] Capability 与 Xcode entitlements 一致<br>[ ] 本机证书在“我的证书”中能展开看到 private key<br>[ ] 已登记本次测试的真机 UDID（Development/Ad Hoc 场景）<br>[ ] Profile 的类型符合目标：Development / Ad Hoc / App Store<br>[ ] Profile 覆盖了正确的 App ID、证书、设备与能力<br>[ ] 未误用 7 天 Offline support Profile<br>[ ] 私钥、.p12、.p8 和 Profile 没有提交进业务仓库<br>[ ] 有 Widget/Extension 时，所有 Target 的签名配置均已检查<br>[ ] 需要 CI 时，签名资产已通过 match 或等价的受控方案管理<br></code></pre></td></tr></table></figure><h2 id="十五、与-Apple-官方流程的对应关系"><a href="#十五、与-Apple-官方流程的对应关系" class="headerlink" title="十五、与 Apple 官方流程的对应关系"></a>十五、与 Apple 官方流程的对应关系</h2><p>本文的手动步骤与 Apple 官方 Development Profile 流程一一对应：先准备 App ID、开发证书和已登记设备，再在 Profiles 中选择 Development 类型、App ID、证书和设备，填写 Profile 名称后 Generate、Download。使用自动签名时，Xcode 会管理开发 Profile，不必重复执行这些手动步骤。</p><p>建议在 Apple 后台改版后，以官方页面的当前字段名称为准：</p><ul><li><a href="https://developer.apple.com/help/account/provisioning-profiles/create-a-development-provisioning-profile/">Create a development provisioning profile</a></li><li><a href="https://developer.apple.com/help/account/identifiers/register-an-app-id/">Register an App ID</a></li><li><a href="https://developer.apple.com/help/account/devices/register-a-single-device/">Register a device</a></li></ul><p>Apple 后台界面和证书名称会随 Xcode、平台政策调整，但“App ID + 证书 + 设备 + 能力 + Profile”这条关系不会变。</p><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>iOS 签名看起来复杂，是因为它同时要回答四个问题：这个 App 是谁、谁可以签名、哪些能力被允许、包可以安装到哪里。把 App ID、证书与私钥、设备、Profile 这几层关系理清后，绝大多数问题都有固定的排查路径。</p><p>对个人项目来说，手动创建一次 Development Certificate 和 Profile，是理解 Apple 签名体系最好的起点；对多人协作和持续集成项目来说，下一步则是把这些资产收敛到 Fastlane match 等可控流程中。无论采用哪种方式，最重要的原则都一样：签名资产必须可追溯、私钥必须安全、每个 Target 都必须被完整覆盖。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;第一次给 iOS App 配真机签名时，Apple Developer 后台里会同时出现 Certificate、Identifier、Device、Profile、Capability 等好几个入口，很容易让人不知道先做什么、后做什么。&lt;/p&gt;
&lt;p&gt;其实这件事可以拆成一</summary>
      
    
    
    
    
    <category term="iOS" scheme="https://flametinary.github.io/tags/iOS/"/>
    
    <category term="代码签名" scheme="https://flametinary.github.io/tags/%E4%BB%A3%E7%A0%81%E7%AD%BE%E5%90%8D/"/>
    
    <category term="Provisioning-Profile" scheme="https://flametinary.github.io/tags/Provisioning-Profile/"/>
    
    <category term="Apple-Developer" scheme="https://flametinary.github.io/tags/Apple-Developer/"/>
    
    <category term="Fastlane" scheme="https://flametinary.github.io/tags/Fastlane/"/>
    
  </entry>
  
  <entry>
    <title>用 SwiftData + CloudKit 给习惯 App 加上「无感」多端同步</title>
    <link href="https://flametinary.github.io/2026/09/08/%E7%94%A8%20SwiftData%20+%20CloudKit%20%E7%BB%99%E4%B9%A0%E6%83%AF%20App%20%E5%8A%A0%E4%B8%8A%E3%80%8C%E6%97%A0%E6%84%9F%E3%80%8D%E5%A4%9A%E7%AB%AF%E5%90%8C%E6%AD%A5%20%E2%80%94%E2%80%94%20%E4%B8%80%E6%AC%A1%E7%9C%9F%E5%AE%9E%E8%90%BD%E5%9C%B0%E7%9A%84%E8%B8%A9%E5%9D%91%E8%AE%B0%E5%BD%95/"/>
    <id>https://flametinary.github.io/2026/09/08/%E7%94%A8%20SwiftData%20+%20CloudKit%20%E7%BB%99%E4%B9%A0%E6%83%AF%20App%20%E5%8A%A0%E4%B8%8A%E3%80%8C%E6%97%A0%E6%84%9F%E3%80%8D%E5%A4%9A%E7%AB%AF%E5%90%8C%E6%AD%A5%20%E2%80%94%E2%80%94%20%E4%B8%80%E6%AC%A1%E7%9C%9F%E5%AE%9E%E8%90%BD%E5%9C%B0%E7%9A%84%E8%B8%A9%E5%9D%91%E8%AE%B0%E5%BD%95/</id>
    <published>2026-09-07T16:00:00.000Z</published>
    <updated>2026-09-12T03:37:07.567Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>本文基于 HabitPulse（一个用 SwiftUI + SwiftData 构建的习惯追踪 App）的真实落地过程，分享如何用 Apple 原生方案零后端实现 iCloud 云同步，以及我们在开发、调试、测试中长期踩过的坑与解法。</p></blockquote><hr><h2 id="引言"><a href="#引言" class="headerlink" title="引言"></a>引言</h2><p>HabitPulse 让用户每天记录习惯打卡。一个很自然的诉求是：<strong>在 iPhone 上打了卡，iPad 上、换了一台手机后，数据都还在。</strong></p><p>最省事的方案不是自己搭服务器，而是直接用 Apple 提供的 <strong>iCloud + CloudKit</strong>：用户用自己的 Apple ID 当账号，App 之间自动同步，我们一行后端代码都不用写。听起来很美好，但真做起来，从「能编译」到「真机可靠同步」，中间有几个不踩不知道的坑。</p><hr><h2 id="一、这个功能是什么"><a href="#一、这个功能是什么" class="headerlink" title="一、这个功能是什么"></a>一、这个功能是什么</h2><p><strong>业务用途</strong>：把「习惯（Habit）」和「打卡记录（HabitCompletion）」两类数据，在用户同一 Apple ID 下的多台设备之间实时同步；离线时照常本地打卡，联网后自动补齐；换机、重装 App 也不丢数据。</p><p><strong>在项目里的集成方式</strong>：HabitPulse 用 <strong>SwiftData</strong> 做本地持久层，用 <strong>CloudKit</strong> 做云端镜像后端。两者不需要我们手写同步逻辑——只要让 SwiftData 的容器「跑在 CloudKit 上」，系统自带的 <code>NSPersistentCloudKitContainer</code> 就会在后台把本地变更自动镜像到 iCloud 私有库，包括离线队列、冲突合并、重试全部由框架完成。</p><p>关键落点有四个文件：</p><ul><li><code>HabitPulseApp.swift</code>：App 启动时构建 SwiftData 容器，并指定它使用 CloudKit 私有库。</li><li><code>HabitPulse.entitlements</code>：声明 App 具备 iCloud 能力（容器标识、CloudKit 服务、环境分区）。</li><li><code>AppEnvironment.swift</code>：一个编译期判定当前是开发还是生产的小枚举，集中管理容器标识。</li><li><code>SyncMonitor.swift</code>：一个轻量监测器，向 UI 提供真实的同步&#x2F;账户状态（这是后文「坑」部分的重点）。</li></ul><p>其中启动容器的核心只有一行思路性的代码：</p><figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs swift"><span class="hljs-keyword">let</span> cloudConfig <span class="hljs-operator">=</span> <span class="hljs-type">ModelConfiguration</span>(<br>    schema: schema,<br>    isStoredInMemoryOnly: <span class="hljs-literal">false</span>,<br>    cloudKitDatabase: .private(env.cloudKitContainerID) <span class="hljs-comment">// 关键：让 SwiftData 跑在 CloudKit 上</span><br>)<br></code></pre></td></tr></table></figure><p>而 entitlements 里声明容器（注意第三项是「环境分区」，值是一个变量占位符，后文会解释）：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">key</span>&gt;</span>com.apple.developer.icloud-container-identifiers<span class="hljs-tag">&lt;/<span class="hljs-name">key</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">array</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">string</span>&gt;</span>iCloud.com.sheldon.HabitPulse<span class="hljs-tag">&lt;/<span class="hljs-name">string</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">array</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">key</span>&gt;</span>com.apple.developer.icloud-services<span class="hljs-tag">&lt;/<span class="hljs-name">key</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">array</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">string</span>&gt;</span>CloudKit<span class="hljs-tag">&lt;/<span class="hljs-name">string</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">array</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">key</span>&gt;</span>com.apple.developer.icloud-container-environment<span class="hljs-tag">&lt;/<span class="hljs-name">key</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">string</span>&gt;</span>$(ICLOUD_CONTAINER_ENV)<span class="hljs-tag">&lt;/<span class="hljs-name">string</span>&gt;</span><br></code></pre></td></tr></table></figure><hr><h2 id="二、为什么使用"><a href="#二、为什么使用" class="headerlink" title="二、为什么使用"></a>二、为什么使用</h2><p><strong>痛点 1：自建后端太重。</strong> 一个「记习惯」的小工具，如果为了同步去搭服务器、做账号体系、处理隐私合规和运维，成本与收益完全不匹配。</p><p><strong>痛点 2：用户不想再注册一个账号。</strong> 习惯类 App 的使用门槛越低越好。用 Apple ID 即账号、零注册，转化率远高于「请先注册登录」。</p><p><strong>价值 1：端到端加密与隐私合规由 Apple 兜底。</strong> CloudKit 私有库默认端到端加密，且数据不经过我们服务器，隐私合规压力小很多。</p><p><strong>价值 2：冲突合并自动完成。</strong> 同一习惯在两台设备各改一笔，框架按「最后写入获胜（LWW）+ 字段级合并」自动合并，我们无需写任何合并代码。</p><p><strong>价值 3：本地优先、离线可用。</strong> 同步是「增强」而非「前提」：没网也能打卡，联网后自动补传。即便 iCloud 完全不可用，App 仍可纯本地使用（这点我们在代码里明确做了降级保护）。</p><p>一句话总结：<strong>用 CloudKit，我们把「多端同步」从「一个需要养的后端项目」降级成了「几行配置 + 一个容器选项」。</strong></p><hr><h2 id="三、怎么做（完整步骤与关键思路）"><a href="#三、怎么做（完整步骤与关键思路）" class="headerlink" title="三、怎么做（完整步骤与关键思路）"></a>三、怎么做（完整步骤与关键思路）</h2><h3 id="步骤-1：开启能力，配好-entitlements"><a href="#步骤-1：开启能力，配好-entitlements" class="headerlink" title="步骤 1：开启能力，配好 entitlements"></a>步骤 1：开启能力，配好 entitlements</h3><p>在 Xcode 的 Signing &amp; Capabilities 里勾选 iCloud → CloudKit，并声明容器标识。本质就是生成上面那份 <code>HabitPulse.entitlements</code>。这里有个极易忽略的点：**<code>icloud-container-environment</code> 不写默认是 <code>Production</code>**，而开发期我们希望数据落在 <code>Development</code> 分区（避免污染生产 schema），所以才单独声明它并用变量占位。</p><h3 id="步骤-2：让-SwiftData-容器跑在-CloudKit-上"><a href="#步骤-2：让-SwiftData-容器跑在-CloudKit-上" class="headerlink" title="步骤 2：让 SwiftData 容器跑在 CloudKit 上"></a>步骤 2：让 SwiftData 容器跑在 CloudKit 上</h3><p>如第一节代码所示，构建 <code>ModelConfiguration</code> 时把 <code>cloudKitDatabase</code> 设为 <code>.private(容器标识)</code>。SwiftData 会自动在底层创建 <code>NSPersistentCloudKitContainer</code> 并接管镜像。至此，「数据能同步」的底层已经通了。</p><h3 id="步骤-3：区分开发-生产环境（关键工程实践）"><a href="#步骤-3：区分开发-生产环境（关键工程实践）" class="headerlink" title="步骤 3：区分开发 &#x2F; 生产环境（关键工程实践）"></a>步骤 3：区分开发 &#x2F; 生产环境（关键工程实践）</h3><p>我们没有把环境值写死在 <code>project.pbxproj</code>，而是抽到「纯 xcconfig」体系：</p><ul><li><code>HabitPulse.base.xcconfig</code>：Debug &#x2F; Release 共享的「底座」（签名、版本、部署目标等）。</li><li><code>HabitPulse.debug.xcconfig</code>：<code>#include</code> base 后，追加 <code>ICLOUD_CONTAINER_ENV = Development</code> 和 <code>SWIFT_ACTIVE_COMPILATION_CONDITIONS = DEBUG</code>。</li><li><code>HabitPulse.release.xcconfig</code>：同理追加 <code>ICLOUD_CONTAINER_ENV = Production</code>。</li></ul><p>构建时，<code>ICLOUD_CONTAINER_ENV</code> 注入到 entitlements 的 <code>$(ICLOUD_CONTAINER_ENV)</code>，决定容器落在哪个分区。Swift 侧则用一个编译宏判断当前环境：</p><figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs swift"><span class="hljs-keyword">static</span> <span class="hljs-keyword">var</span> current: <span class="hljs-type">AppEnvironment</span> &#123;<br>    <span class="hljs-keyword">#if</span> <span class="hljs-type">DEBUG</span><br>    <span class="hljs-keyword">return</span> .development   <span class="hljs-comment">// Debug 包</span><br>    <span class="hljs-keyword">#else</span><br>    <span class="hljs-keyword">return</span> .production    <span class="hljs-comment">// Release / 上架包</span><br>    <span class="hljs-keyword">#endif</span><br>&#125;<br></code></pre></td></tr></table></figure><p>这两路「同一来源、同时决定」，保证 Swift 判断和 CloudKit 分区永远一致。</p><h3 id="步骤-4：把「真实同步状态」暴露给-UI"><a href="#步骤-4：把「真实同步状态」暴露给-UI" class="headerlink" title="步骤 4：把「真实同步状态」暴露给 UI"></a>步骤 4：把「真实同步状态」暴露给 UI</h3><p>这是最容易被省略、却最影响体验的一步（也是坑最多的一步，见第四节）。我们新增了 <code>SyncMonitor</code>：启动时查一次 <code>CKContainer.accountStatus</code>，订阅 <code>CKAccountChanged</code> 通知（用户登出&#x2F;切换 Apple ID 时实时感知），并订阅 CloudKit 镜像事件通知来更新「最近同步成功时间」和「失败错误」。UI 拿它渲染「未登录 &#x2F; 同步中 &#x2F; 已同步于 xx &#x2F; 失败可重试」三态，取代硬编码的「已开启」。</p><hr><h2 id="四、开发过程中遇到的坑"><a href="#四、开发过程中遇到的坑" class="headerlink" title="四、开发过程中遇到的坑"></a>四、开发过程中遇到的坑</h2><h3 id="坑-1：模拟器上看似「同步没生效」，其实是假象"><a href="#坑-1：模拟器上看似「同步没生效」，其实是假象" class="headerlink" title="坑 1：模拟器上看似「同步没生效」，其实是假象"></a>坑 1：模拟器上看似「同步没生效」，其实是假象</h3><p>在模拟器里查容器分区，永远显示 <code>Production</code>，我们一度以为 Development 分区没配成功。实际上<strong>模拟器运行时会剥离 iCloud entitlement</strong>，根本不会真正连 CloudKit——变量替换本身是对的，只是模拟器无法验证 Development 分区，必须真机才能看到效果。</p><h3 id="坑-2：「iCloud-同步已开启」是个谎言"><a href="#坑-2：「iCloud-同步已开启」是个谎言" class="headerlink" title="坑 2：「iCloud 同步已开启」是个谎言"></a>坑 2：「iCloud 同步已开启」是个谎言</h3><p>最初 <code>MeView</code> 里写死了一行 <code>Text(&quot;已开启&quot;)</code>，配个绿勾图标。无论用户有没有登录 iCloud、容器有没有真连上，都显示「已开启」。这既误导用户，也违反产品对「状态三态」的要求——用户退出 Apple ID 后 App 毫无感知。</p><h3 id="坑-3：工程改坏了，Xcode-一打开就崩溃"><a href="#坑-3：工程改坏了，Xcode-一打开就崩溃" class="headerlink" title="坑 3：工程改坏了，Xcode 一打开就崩溃"></a>坑 3：工程改坏了，Xcode 一打开就崩溃</h3><p>把 <code>XCBuildConfiguration</code> 的配置内联值清空、只留 xcconfig 引用时，Xcode（旧项目模型）断言失败直接崩溃，报错大意是 <code>buildSettings should be an instance inheriting from ... but it is nil</code>。原因是这个版本的 Xcode <strong>要求 <code>buildSettings</code> 这个键至少存在</strong>（哪怕是个空字典）。</p><h3 id="坑-4：xcconfig-路径被「双写」"><a href="#坑-4：xcconfig-路径被「双写」" class="headerlink" title="坑 4：xcconfig 路径被「双写」"></a>坑 4：xcconfig 路径被「双写」</h3><p>xcconfig 的 <code>PBXFileReference.path</code> 是相对它所在「组」的 <code>path</code> 的。Config 组的 <code>path</code> 已经是 <code>Config</code>，我又在文件引用里写了 <code>Config/HabitPulse.debug.xcconfig</code>，结果拼接成 <code>Config/Config/...</code> 找不到文件。</p><h3 id="坑-5：单元测试一跑就整个崩溃"><a href="#坑-5：单元测试一跑就整个崩溃" class="headerlink" title="坑 5：单元测试一跑就整个崩溃"></a>坑 5：单元测试一跑就整个崩溃</h3><p><code>SyncMonitor</code> 需要从 SwiftData 容器里取出底层 <code>NSPersistentCloudKitContainer</code> 来订阅镜像事件。最初用 KVC <code>value(forKey: &quot;backingContainer&quot;)</code>。但单元测试用的是<strong>内存容器</strong>，这个私有 key 不存在，KVC 会抛 Objective-C 的 <code>NSException</code>——而 Swift 的 <code>try/catch</code> 抓不到 OC 异常，于是测试 host 进程直接崩，连一个用例都没跑起来。</p><h3 id="坑-6：测试环境判定在新-Xcode-下失效"><a href="#坑-6：测试环境判定在新-Xcode-下失效" class="headerlink" title="坑 6：测试环境判定在新 Xcode 下失效"></a>坑 6：测试环境判定在新 Xcode 下失效</h3><p>我们靠「测试运行器是否注入 <code>XCTestConfigurationFilePath</code> 环境变量」来识别测试环境，从而跳过 CloudKit 初始化。但升级 Xcode 26 后这个信号不再可靠，导致测试仍去初始化 CloudKit，产生噪音甚至失败。</p><h3 id="坑-7：上架后跨设备「静默不同步」"><a href="#坑-7：上架后跨设备「静默不同步」" class="headerlink" title="坑 7：上架后跨设备「静默不同步」"></a>坑 7：上架后跨设备「静默不同步」</h3><p>CloudKit 的 <strong>Development 分区会自动推断 schema，但 Production 分区不会</strong>——必须手动在 CloudKit Dashboard 点「Deploy to Production」。如果忘了这一步，App 上架&#x2F;TestFlight 后多设备同步会全部静默失败，而又因为没有状态监测（坑 2 同期存在），谁都发现不了。</p><hr><h2 id="五、对应的解决方案"><a href="#五、对应的解决方案" class="headerlink" title="五、对应的解决方案"></a>五、对应的解决方案</h2><p><strong>坑 1（模拟器假象）</strong>：接受平台限制，<strong>用真机验证 Development 分区</strong>；工程侧用 <code>xcodebuild -showBuildSettings</code> 在构建期确认 <code>ICLOUD_CONTAINER_ENV</code> 在 Debug&#x3D;Development、Release&#x3D;Production 正确解析即可，不必纠结模拟器运行时值。</p><p><strong>坑 2（假状态）</strong>：落地 <code>SyncMonitor</code>。启动时用 <code>await container.accountStatus()</code> 查询真实账户状态，并订阅 <code>Notification.Name.CKAccountChanged</code> 监听登出&#x2F;切换；UI 改为按聚合态渲染真实三态，失败时提供「重试」入口。顺便把 <code>isTesting</code> 判定改成组合式（见坑 6），避免测试环境触发真实查询。</p><p><strong>坑 3（工程崩溃）</strong>：给 <code>XCBuildConfiguration</code> 加回 <code>buildSettings = {};</code> 空字典，同时保留 <code>baseConfigurationReference</code> 引用 xcconfig——既满足 Xcode 加载要求，又把值外置。</p><p><strong>坑 4（路径双写）</strong>：Config 组 <code>path=Config</code> 时，文件引用只写文件名 <code>HabitPulse.debug.xcconfig</code>，去掉前缀，让其相对所在组解析。</p><p><strong>坑 5（测试崩溃）</strong>：把 KVC 换成 <strong>Swift <code>Mirror</code> 反射</strong>取出底层容器：</p><figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs swift"><span class="hljs-keyword">let</span> mirror <span class="hljs-operator">=</span> <span class="hljs-type">Mirror</span>(reflecting: modelContainer)<br><span class="hljs-keyword">for</span> child <span class="hljs-keyword">in</span> mirror.children &#123;<br>    <span class="hljs-keyword">if</span> child.label <span class="hljs-operator">==</span> <span class="hljs-string">&quot;backingContainer&quot;</span>,<br>       <span class="hljs-keyword">let</span> ck <span class="hljs-operator">=</span> child.value <span class="hljs-keyword">as?</span> <span class="hljs-type">NSPersistentCloudKitContainer</span> &#123;<br>        <span class="hljs-keyword">return</span> ck   <span class="hljs-comment">// 取不到返回 nil，绝不抛异常</span><br>    &#125;<br>&#125;<br><span class="hljs-keyword">return</span> <span class="hljs-literal">nil</span><br></code></pre></td></tr></table></figure><p>反射对不存在的属性安全返回 <code>nil</code>，内存容器场景不再崩溃；取不到时 <code>SyncMonitor</code> 自动降级为「仅账户状态监测」，不影响真实同步能力。同时把桥接调用移到 <code>guard startMonitoring</code> 之后，测试环境根本不触达这条路径。</p><p><strong>坑 6（判定失效）</strong>：<code>isTesting</code> 改为三个信号取或：<code>NSClassFromString(&quot;XCTestCase&quot;) != nil</code>、环境变量 <code>XCTestConfigurationFilePath</code>、<code>XCTestSessionIdentifier</code>。单一信号在某 Xcode 版本失效时，其余信号仍能兜住。</p><p><strong>坑 7（生产 schema）</strong>：把「Deploy to Production」写进发布清单 &#x2F; fastlane 流程，作为上架前的必查项；同时配合坑 2 的状态监测，即使漏部署也能在 UI 暴露「同步失败」而非黑盒。</p><hr><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>CloudKit 让「多端同步」这件传统上很重的事，变成「一个容器选项 + 一份 entitlements」。但「能同步」和「可靠、可观测、可排查」之间，还隔着环境分区、状态监测、工程配置、测试隔离、生产部署这些细节。</p><p>如果只带三条经验走人，会是：</p><ol><li><strong>开发&#x2F;生产分区一定要显式区分</strong>，且 Production schema 要手动部署；</li><li><strong>同步状态必须真实可观测</strong>，别用硬编码「已开启」骗自己和用户；</li><li><strong>任何对私有 API &#x2F; 内部容器的桥接，优先用 <code>Mirror</code> 反射而非 KVC</strong>，尤其在测试内存容器场景下能救命。</li></ol><p>希望这篇踩坑记录，能帮你在自己的 App 里少走几步弯路。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;blockquote&gt;
&lt;p&gt;本文基于 HabitPulse（一个用 SwiftUI + SwiftData 构建的习惯追踪 App）的真实落地过程，分享如何用 Apple 原生方案零后端实现 iCloud 云同步，以及我们在开发、调试、测试中长期踩过的坑与解法。&lt;/p&gt;
&lt;/</summary>
      
    
    
    
    
    <category term="iOS" scheme="https://flametinary.github.io/tags/iOS/"/>
    
    <category term="SwiftData" scheme="https://flametinary.github.io/tags/SwiftData/"/>
    
    <category term="CloudKit" scheme="https://flametinary.github.io/tags/CloudKit/"/>
    
    <category term="踩坑" scheme="https://flametinary.github.io/tags/%E8%B8%A9%E5%9D%91/"/>
    
    <category term="SwiftUI" scheme="https://flametinary.github.io/tags/SwiftUI/"/>
    
  </entry>
  
  <entry>
    <title>iOS项目如何接入Fastlane</title>
    <link href="https://flametinary.github.io/2026/09/07/iOS%E9%A1%B9%E7%9B%AE%E5%A6%82%E4%BD%95%E6%8E%A5%E5%85%A5Fastlane/"/>
    <id>https://flametinary.github.io/2026/09/07/iOS%E9%A1%B9%E7%9B%AE%E5%A6%82%E4%BD%95%E6%8E%A5%E5%85%A5Fastlane/</id>
    <published>2026-09-06T16:00:00.000Z</published>
    <updated>2026-09-12T03:37:07.566Z</updated>
    
    <content type="html"><![CDATA[<h1 id="iOS项目如何接入Fastlane"><a href="#iOS项目如何接入Fastlane" class="headerlink" title="iOS项目如何接入Fastlane"></a>iOS项目如何接入Fastlane</h1><p>做 iOS 开发的人，大概都经历过这种时刻：本地调试明明很顺，临近提测才开始手动改版本号、找证书、切 Scheme、Archive、导 IPA、登录 App Store Connect、传 TestFlight。中间只要漏一步，或者用错一张 Profile，就得从头再来。</p><p>Fastlane 的价值不是“替你点几个按钮”，而是把这些重复、容易出错、又必须可追溯的动作变成代码。你把规则写进 <code>Fastfile</code>，以后无论是自己在终端运行，还是让 GitHub Actions 的 macOS Runner 运行，走的都是同一套流程。</p><p>本文以 HabitPulse 的真实接入过程为例，完整说明一个 SwiftUI iOS 项目如何接入 Fastlane：它能做什么、项目里常用哪些功能、证书怎么管理、前置字段如何准备，以及接入过程中实际遇到的问题和解决办法。项目包含主 App 和 Widget Extension，所有账号、仓库地址、Bundle ID、Team ID 与密钥内容均已脱敏。</p><h2 id="一、先用一句话理解-Fastlane"><a href="#一、先用一句话理解-Fastlane" class="headerlink" title="一、先用一句话理解 Fastlane"></a>一、先用一句话理解 Fastlane</h2><p>Fastlane 是一套用 Ruby 配置的移动端自动化工具集。它不替代 Xcode，也不替代 Apple Developer 或 App Store Connect；它做的是把这些平台和命令行工具串成稳定的流水线。</p><p>你可以把它理解为 iOS 项目的“自动化总控台”：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs text">开发者输入一条 Fastlane 命令<br>        ↓<br>Fastlane 选择正确的证书/Profile、调用 xcodebuild<br>        ↓<br>测试 / 归档 / 导出 IPA / 生成截图 / 上传 TestFlight / 提交审核<br>        ↓<br>输出 IPA、dSYM、xcresult、日志、发布说明等可追溯产物<br></code></pre></td></tr></table></figure><p>在 HabitPulse 中，最常用的命令长这样：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs sh"><span class="hljs-comment"># 用固定模拟器运行测试，并生成报告</span><br>bundle <span class="hljs-built_in">exec</span> fastlane ios <span class="hljs-built_in">test</span><br><br><span class="hljs-comment"># 安装开发签名材料（主 App + Widget）</span><br>bundle <span class="hljs-built_in">exec</span> fastlane ios habitpulse_dev<br><br><span class="hljs-comment"># 生成开发 IPA、archive、dSYM、xcresult</span><br>bundle <span class="hljs-built_in">exec</span> fastlane ios habitpulse_build_dev<br><br><span class="hljs-comment"># 生成 App Store IPA、archive、dSYM、xcresult，但不上传</span><br>bundle <span class="hljs-built_in">exec</span> fastlane ios habitpulse_archive_appstore<br><br><span class="hljs-comment"># 上传 TestFlight</span><br>bundle <span class="hljs-built_in">exec</span> fastlane ios beta<br></code></pre></td></tr></table></figure><p>这里有一个习惯值得从第一天就建立：<strong>始终使用 <code>bundle exec</code>。</strong></p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs sh">bundle <span class="hljs-built_in">exec</span> fastlane ios <span class="hljs-built_in">test</span><br></code></pre></td></tr></table></figure><p>不要随手运行系统全局的：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs sh">fastlane ios <span class="hljs-built_in">test</span><br></code></pre></td></tr></table></figure><p>因为前者一定使用项目 <code>Gemfile.lock</code> 锁定的 Fastlane 版本，团队成员和 CI 才会保持一致；后者可能悄悄调用你电脑里另一个版本的 Fastlane，今天能跑、明天升级后就变了。</p><h2 id="二、Fastlane-到底能做什么"><a href="#二、Fastlane-到底能做什么" class="headerlink" title="二、Fastlane 到底能做什么"></a>二、Fastlane 到底能做什么</h2><p>Fastlane 并不是一个单独的“打包工具”，它是许多 action 的集合。最常见的几类能力如下。</p><table><thead><tr><th>需求</th><th>常见 Fastlane action</th><th>HabitPulse 中的用法</th></tr></thead><tbody><tr><td>执行单元&#x2F;UI 测试</td><td><code>run_tests</code> &#x2F; <code>scan</code></td><td>固定模拟器运行测试，保存 JUnit、<code>.xcresult</code> 和日志</td></tr><tr><td>管理证书与 Profile</td><td><code>match</code></td><td>主 App 与 Widget 使用同一份受加密保护的签名仓库</td></tr><tr><td>Archive 与导出 IPA</td><td><code>build_app</code> &#x2F; <code>gym</code></td><td>导出开发版或 App Store 版 IPA、archive 与 dSYM</td></tr><tr><td>读取和更新版本号</td><td><code>get_version_number</code>、<code>get_build_number</code>、<code>increment_build_number</code></td><td>生成唯一 TestFlight build number，准备发布 tag</td></tr><tr><td>调用 App Store Connect API</td><td><code>app_store_connect_api_key</code></td><td>无需 Apple ID 会话即可读取构建号、上传或预检</td></tr><tr><td>上传 TestFlight</td><td><code>upload_to_testflight</code> &#x2F; <code>pilot</code></td><td>上传构建并附上中文更新说明，默认不外部分发</td></tr><tr><td>管理商店元数据</td><td><code>deliver</code>、<code>check_app_store_metadata</code> &#x2F; <code>precheck</code></td><td>校验多语言元数据、上传元数据、提交审核</td></tr><tr><td>生成商店截图</td><td><code>snapshot</code></td><td>通过稳定的 UI Test 批量生成多语言、多设备截图</td></tr><tr><td>读写 Git 信息</td><td><code>ensure_git_status_clean</code>、<code>add_git_tag</code></td><td>发布时检查工作树与 tag，避免“代码和包不是同一份”</td></tr></tbody></table><p>你不必一口气把所有功能都接入。一个健康的演进顺序通常是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs text">先测试 → 再归档 → 再管理签名 → 再上传 TestFlight<br>→ 再管理元数据和截图 → 最后才是审核提交与 CI<br></code></pre></td></tr></table></figure><p>每往前走一步，先确保本机可复现，再交给 CI。这样当问题出现时，你能知道是工程问题、签名问题，还是 CI 环境问题。</p><h2 id="三、接入前先准备什么"><a href="#三、接入前先准备什么" class="headerlink" title="三、接入前先准备什么"></a>三、接入前先准备什么</h2><p>Fastlane 本身安装很简单，但 iOS 发布所依赖的外部信息不少。先把它们分成三类，会清楚很多。</p><h3 id="1-工程内必须明确的信息"><a href="#1-工程内必须明确的信息" class="headerlink" title="1. 工程内必须明确的信息"></a>1. 工程内必须明确的信息</h3><p>至少要确认下面这些值，而不是在 Fastfile 里“猜”。</p><table><thead><tr><th>字段</th><th>例子</th><th>用途</th></tr></thead><tbody><tr><td>Xcode 工程或 Workspace</td><td><code>HabitPulse.xcodeproj</code></td><td>测试、Archive 的入口</td></tr><tr><td>Scheme</td><td><code>HabitPulse</code></td><td>告诉 xcodebuild 构建哪个方案</td></tr><tr><td>主 App Bundle ID</td><td><code>com.example.habitpulse</code></td><td>签名和 App Store Connect 的应用标识</td></tr><tr><td>所有 Extension Bundle ID</td><td><code>com.example.habitpulse.Widget</code></td><td>Widget、Share Extension 等也要签名</td></tr><tr><td>Apple Team ID</td><td><code>ABCDE12345</code></td><td>导出选项、签名校验</td></tr><tr><td>版本号与构建号策略</td><td><code>1.0</code> &#x2F; <code>42</code></td><td>App Store 与 TestFlight 要求 build number 唯一</td></tr><tr><td>测试设备与 Runtime</td><td><code>iPhone 17 Pro (26.3)</code></td><td>使本机和 CI 的测试一致</td></tr></tbody></table><p>最容易漏掉的是 Extension。只要工程里有 Widget、Share Extension、Notification Service Extension 等 Target，签名就不是“主 App 一个 Bundle ID”这么简单。HabitPulse 的 <code>APP_IDENTIFIERS</code> 显式包括主 App 和 Widget：</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs ruby"><span class="hljs-variable constant_">APP_IDENTIFIERS</span> = [<br>  <span class="hljs-string">&quot;com.example.habitpulse&quot;</span>,<br>  <span class="hljs-string">&quot;com.example.habitpulse.Widget&quot;</span><br>].freeze<br></code></pre></td></tr></table></figure><p>在 Apple Developer 后台、match、Fastfile、CI 签名校验中，都要以这份完整列表为准。</p><h3 id="2-本地工具链"><a href="#2-本地工具链" class="headerlink" title="2. 本地工具链"></a>2. 本地工具链</h3><p>HabitPulse 把 Ruby、Fastlane 和 Bundler 版本锁进 <code>Gemfile</code>：</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs ruby">ruby <span class="hljs-string">&quot;~&gt; 3.3.0&quot;</span><br><br>source <span class="hljs-string">&quot;https://rubygems.org&quot;</span><br><br>gem <span class="hljs-string">&quot;fastlane&quot;</span>, <span class="hljs-string">&quot;2.238.0&quot;</span><br>gem <span class="hljs-string">&quot;bundler&quot;</span>, <span class="hljs-string">&quot;4.0.16&quot;</span><br></code></pre></td></tr></table></figure><p>第一次接入时，在项目根目录运行：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs sh">bundle install<br>bundle <span class="hljs-built_in">exec</span> fastlane --version<br></code></pre></td></tr></table></figure><p>此外你还需要：</p><ul><li>macOS 和可用的 Xcode；</li><li>项目最低系统版本所需的 iOS Simulator Runtime；</li><li>已登录并有相应权限的 Apple Developer 账号（首次准备签名材料时需要）；</li><li>Git 可访问私有证书仓库；</li><li>如果计划上传 TestFlight 或提交审核，还要有 App Store Connect API Key。</li></ul><h3 id="3-机密字段：哪些可以进仓库，哪些绝对不行"><a href="#3-机密字段：哪些可以进仓库，哪些绝对不行" class="headerlink" title="3. 机密字段：哪些可以进仓库，哪些绝对不行"></a>3. 机密字段：哪些可以进仓库，哪些绝对不行</h3><p>下面这张表很重要。</p><table><thead><tr><th>内容</th><th>是否可提交到业务仓库</th><th>合理位置</th></tr></thead><tbody><tr><td><code>Fastfile</code>、<code>Gemfile</code>、元数据模板</td><td>可以</td><td>业务仓库</td></tr><tr><td>Bundle ID、Scheme、非敏感 Team ID</td><td>通常可以</td><td>Fastfile&#x2F;xcconfig</td></tr><tr><td>match 证书仓库地址</td><td>可视仓库权限而定，私有项目通常可以</td><td><code>Matchfile</code> 或 Fastfile</td></tr><tr><td>match 加密密码</td><td>不可以</td><td>本机安全环境变量、CI Secret</td></tr><tr><td><code>.p12</code>、Provisioning Profile</td><td>不可以直接放业务仓库</td><td>match 加密仓库</td></tr><tr><td>App Store Connect <code>.p8</code> 私钥</td><td>绝对不可以</td><td>密码管理器、本机安全路径、CI Secret</td></tr><tr><td>Apple ID 密码</td><td>不建议作为自动化凭据</td><td>尽量改用 App Store Connect API Key</td></tr><tr><td>App Review 联系人信息</td><td>不应进公共仓库</td><td>本机受限文件或 CI Secret</td></tr></tbody></table><p>HabitPulse 提供了被 <code>.gitignore</code> 忽略的本地环境变量模板：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs dotenv"># fastlane/.env（仅本机使用，不提交）<br>ASC_KEY_ID=<br>ASC_ISSUER_ID=<br>ASC_KEY_FILEPATH=<br>ASC_REVIEW_INFO_FILEPATH=<br></code></pre></td></tr></table></figure><p>其中 <code>ASC_KEY_FILEPATH</code> 指向本机的 <code>.p8</code> 文件；文件权限也应该收紧：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs sh"><span class="hljs-built_in">chmod</span> 600 /安全路径/AuthKey_XXXXXXXXXX.p8<br></code></pre></td></tr></table></figure><h2 id="四、开始接入：初始化和目录结构"><a href="#四、开始接入：初始化和目录结构" class="headerlink" title="四、开始接入：初始化和目录结构"></a>四、开始接入：初始化和目录结构</h2><p>在一个已有 iOS 项目的根目录执行：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs sh">bundle <span class="hljs-built_in">exec</span> fastlane init<br></code></pre></td></tr></table></figure><p>Fastlane 会引导创建 <code>fastlane/</code> 目录。实际项目里，建议把目录逐步整理成这样：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs text">fastlane/<br>├── Fastfile                         # 各条自动化 lane 的核心定义<br>├── Appfile                          # App 标识等基础配置（按需）<br>├── Matchfile                        # match 的默认签名仓库配置（按需）<br>├── .env.example                     # 环境变量模板，不含真实密钥<br>├── review_information.example.yml   # 审核联系信息模板，不含真实个人资料<br>├── metadata/                        # App Store 多语言文案<br>│   ├── zh-Hans/<br>│   ├── en-US/<br>│   └── fr-FR/<br>├── screenshots/                     # snapshot 生成的截图<br>├── test_output/                     # JUnit、xcresult、测试日志<br>├── build_output/                    # IPA、archive、dSYM、构建 xcresult<br>└── release_notes/                   # 自动生成的发布说明<br></code></pre></td></tr></table></figure><p>其中 <code>Fastfile</code> 是核心。Fastlane 里的可执行单元叫 <strong>lane</strong>，你可以把它理解为一条命名的自动化任务。</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs ruby">default_platform(<span class="hljs-symbol">:ios</span>)<br><br>platform <span class="hljs-symbol">:ios</span> <span class="hljs-keyword">do</span><br>  desc <span class="hljs-string">&quot;运行 iOS 测试&quot;</span><br>  lane <span class="hljs-symbol">:test</span> <span class="hljs-keyword">do</span><br>    run_tests(<br>      <span class="hljs-symbol">project:</span> <span class="hljs-string">&quot;HabitPulse.xcodeproj&quot;</span>,<br>      <span class="hljs-symbol">scheme:</span> <span class="hljs-string">&quot;HabitPulse&quot;</span><br>    )<br>  <span class="hljs-keyword">end</span><br><span class="hljs-keyword">end</span><br></code></pre></td></tr></table></figure><p>上面定义之后，运行方式是：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs sh">bundle <span class="hljs-built_in">exec</span> fastlane ios <span class="hljs-built_in">test</span><br></code></pre></td></tr></table></figure><p><code>ios</code> 对应 <code>platform :ios</code>，<code>test</code> 对应 <code>lane :test</code>。lane 不一定要复杂；把一个动作做清楚、可单独验证，比写一条无所不包的“全自动超级 lane”更容易维护。</p><h2 id="五、先做好公共配置和前置校验"><a href="#五、先做好公共配置和前置校验" class="headerlink" title="五、先做好公共配置和前置校验"></a>五、先做好公共配置和前置校验</h2><p>真实项目中，最开始的 Fastfile 很容易充满散落的字符串：工程路径写一遍、Scheme 写一遍、Bundle ID 写一遍、输出路径又写一遍。HabitPulse 在接入的第一阶段把它们抽成常量：</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs ruby"><span class="hljs-variable constant_">REPOSITORY_ROOT</span> = <span class="hljs-title class_">File</span>.expand_path(<span class="hljs-string">&quot;..&quot;</span>, __dir__)<br><span class="hljs-variable constant_">PROJECT</span> = <span class="hljs-title class_">File</span>.join(<span class="hljs-variable constant_">REPOSITORY_ROOT</span>, <span class="hljs-string">&quot;HabitPulse.xcodeproj&quot;</span>)<br><span class="hljs-variable constant_">SCHEME</span> = <span class="hljs-string">&quot;HabitPulse&quot;</span><br><span class="hljs-variable constant_">DEFAULT_TEAM_ID</span> = <span class="hljs-string">&quot;ABCDE12345&quot;</span><br><span class="hljs-variable constant_">APP_IDENTIFIERS</span> = [<br>  <span class="hljs-string">&quot;com.example.habitpulse&quot;</span>,<br>  <span class="hljs-string">&quot;com.example.habitpulse.Widget&quot;</span><br>].freeze<br><span class="hljs-variable constant_">APP_IDENTIFIER</span> = <span class="hljs-variable constant_">APP_IDENTIFIERS</span>.first<br><span class="hljs-variable constant_">OUTPUT_ROOT</span> = <span class="hljs-title class_">File</span>.join(<span class="hljs-variable constant_">REPOSITORY_ROOT</span>, <span class="hljs-string">&quot;fastlane/build_output/ios/habitpulse&quot;</span>)<br><span class="hljs-variable constant_">TEST_OUTPUT_ROOT</span> = <span class="hljs-title class_">File</span>.join(<span class="hljs-variable constant_">REPOSITORY_ROOT</span>, <span class="hljs-string">&quot;fastlane/test_output&quot;</span>)<br><span class="hljs-variable constant_">TEST_DEVICE</span> = <span class="hljs-string">&quot;iPhone 17 Pro (26.3)&quot;</span><br></code></pre></td></tr></table></figure><p>然后给各 lane 一个统一的入口校验：</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs ruby">private_lane <span class="hljs-symbol">:verify_fastlane_environment</span> <span class="hljs-keyword">do</span> |<span class="hljs-params">options</span>|<br>  ensure_bundle_exec<br>  <span class="hljs-variable constant_">UI</span>.user_error!(<span class="hljs-string">&quot;找不到 Xcode 工程：<span class="hljs-subst">#&#123;<span class="hljs-variable constant_">PROJECT</span>&#125;</span>&quot;</span>) <span class="hljs-keyword">unless</span> <span class="hljs-title class_">File</span>.exist?(<span class="hljs-variable constant_">PROJECT</span>)<br><br>  <span class="hljs-keyword">if</span> options[<span class="hljs-symbol">:requires_match_password</span>] &amp;&amp; <span class="hljs-variable constant_">ENV</span>[<span class="hljs-string">&quot;MATCH_PASSWORD&quot;</span>].to_s.empty?<br>    <span class="hljs-variable constant_">UI</span>.user_error!(<span class="hljs-string">&quot;缺少 MATCH_PASSWORD，无法解密 match 证书仓库&quot;</span>)<br>  <span class="hljs-keyword">end</span><br><br>  team_id = <span class="hljs-variable constant_">ENV</span>[<span class="hljs-string">&quot;FASTLANE_TEAM_ID&quot;</span>] |<span class="hljs-params"></span>| <span class="hljs-variable constant_">DEFAULT_TEAM_ID</span><br>  <span class="hljs-variable constant_">UI</span>.user_error!(<span class="hljs-string">&quot;FASTLANE_TEAM_ID 必须是 10 位 Apple Team ID&quot;</span>) <span class="hljs-keyword">unless</span> team_id.match?(<span class="hljs-regexp">/\A[A-Z0-9]&#123;10&#125;\z/</span>)<br><span class="hljs-keyword">end</span><br></code></pre></td></tr></table></figure><p>这类“先失败”的设计看似啰嗦，其实能省很多时间。比如 <code>.p8</code> 路径填错、忘记设置 <code>MATCH_PASSWORD</code>、用错 Ruby 环境时，应该在一分钟内给出清晰错误，而不是等五分钟 Archive 后才报一个难懂的签名失败。</p><h2 id="六、测试自动化：从能跑到可排查"><a href="#六、测试自动化：从能跑到可排查" class="headerlink" title="六、测试自动化：从能跑到可排查"></a>六、测试自动化：从能跑到可排查</h2><p>Fastlane 的测试 action 通常是 <code>run_tests</code>，也常被称为 <code>scan</code>。HabitPulse 的测试 lane 做了几件比“直接跑测试”更完整的事：清理上次报告、固定设备、输出日志、输出 JUnit、输出 <code>.xcresult</code>。</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><code class="hljs ruby">lane <span class="hljs-symbol">:test</span> <span class="hljs-keyword">do</span><br>  verify_fastlane_environment<br><br>  <span class="hljs-title class_">FileUtils</span>.rm_rf(<span class="hljs-variable constant_">TEST_OUTPUT_ROOT</span>)<br>  <span class="hljs-title class_">FileUtils</span>.mkdir_p(<span class="hljs-title class_">File</span>.join(<span class="hljs-variable constant_">TEST_OUTPUT_ROOT</span>, <span class="hljs-string">&quot;logs&quot;</span>))<br><br>  run_tests(<br>    <span class="hljs-symbol">project:</span> <span class="hljs-variable constant_">PROJECT</span>,<br>    <span class="hljs-symbol">scheme:</span> <span class="hljs-variable constant_">SCHEME</span>,<br>    <span class="hljs-symbol">devices:</span> [<span class="hljs-variable constant_">TEST_DEVICE</span>],<br>    <span class="hljs-symbol">ensure_devices_found:</span> <span class="hljs-literal">true</span>,<br>    <span class="hljs-symbol">clean:</span> <span class="hljs-literal">true</span>,<br>    <span class="hljs-symbol">output_directory:</span> <span class="hljs-variable constant_">TEST_OUTPUT_ROOT</span>,<br>    <span class="hljs-symbol">output_types:</span> <span class="hljs-string">&quot;junit&quot;</span>,<br>    <span class="hljs-symbol">output_files:</span> <span class="hljs-string">&quot;HabitPulseTests.junit&quot;</span>,<br>    <span class="hljs-symbol">buildlog_path:</span> <span class="hljs-title class_">File</span>.join(<span class="hljs-variable constant_">TEST_OUTPUT_ROOT</span>, <span class="hljs-string">&quot;logs&quot;</span>),<br>    <span class="hljs-symbol">result_bundle:</span> <span class="hljs-literal">true</span>,<br>    <span class="hljs-symbol">result_bundle_path:</span> <span class="hljs-title class_">File</span>.join(<span class="hljs-variable constant_">TEST_OUTPUT_ROOT</span>, <span class="hljs-string">&quot;HabitPulseTests.xcresult&quot;</span>),<br>    <span class="hljs-symbol">xcargs:</span> <span class="hljs-variable constant_">SWIFT_SANDBOX_XCARGS</span><br>  )<br><span class="hljs-keyword">end</span><br></code></pre></td></tr></table></figure><p>运行后重点看这些产物：</p><table><thead><tr><th>产物</th><th>作用</th></tr></thead><tbody><tr><td><code>HabitPulseTests.xcresult</code></td><td>用 Xcode 打开后可查看失败用例、附件与测试层级</td></tr><tr><td><code>HabitPulseTests.junit</code></td><td>适合 CI 系统解析和统计</td></tr><tr><td><code>logs/</code></td><td>最接近原始 <code>xcodebuild</code> 输出，签名或编译失败时最有用</td></tr></tbody></table><h3 id="为什么要固定模拟器"><a href="#为什么要固定模拟器" class="headerlink" title="为什么要固定模拟器"></a>为什么要固定模拟器</h3><p>“在我的电脑上能跑”不等于 CI 上能跑。设备型号、iOS Runtime、Xcode 版本不同，都可能让 UI 测试出现布局、权限弹窗或系统行为差异。HabitPulse 固定到 <code>iPhone 17 Pro (26.3)</code>，并在 CI 安装相同 Xcode 版本。</p><p>不要盲目照搬这个型号和版本；你应该选择你本机与 CI 都可获得的一组组合。关键原则是：<strong>把它当作工程配置维护，而不是临时参数。</strong></p><h2 id="七、iOS-签名到底在管理什么"><a href="#七、iOS-签名到底在管理什么" class="headerlink" title="七、iOS 签名到底在管理什么"></a>七、iOS 签名到底在管理什么</h2><p>这是 Fastlane 接入最容易卡住、也最值得理解的部分。</p><h3 id="1-四个容易混淆的东西"><a href="#1-四个容易混淆的东西" class="headerlink" title="1. 四个容易混淆的东西"></a>1. 四个容易混淆的东西</h3><table><thead><tr><th>名称</th><th>它证明什么</th><th>通常在哪里</th></tr></thead><tbody><tr><td>App ID &#x2F; Identifier</td><td>这个 Bundle ID 属于你的开发团队</td><td>Apple Developer 后台</td></tr><tr><td>Certificate（证书）</td><td>谁可以为团队签名</td><td>Apple Developer 后台与本机 Keychain</td></tr><tr><td>Private Key（私钥）</td><td>证书真正可用于签名的关键</td><td>创建证书的 Keychain；必须严格保护</td></tr><tr><td>Provisioning Profile</td><td>某个 App ID、证书、能力、设备或分发方式的组合许可</td><td>Apple 后台和本机</td></tr></tbody></table><p>可以这样粗略理解：Bundle ID 是“这是谁的 App”，证书和私钥是“谁有权签名”，Profile 是“这份签名允许以什么方式安装或分发”。</p><p>因此，一个包含 Widget 的 App Store Archive 至少要确保：主 App 和 Widget 都有正确的 App ID、所需 Capability、相应的签名证书和正确类型的 Profile。</p><h3 id="2-为什么不建议手工到处导入-p12-和-Profile"><a href="#2-为什么不建议手工到处导入-p12-和-Profile" class="headerlink" title="2. 为什么不建议手工到处导入 .p12 和 Profile"></a>2. 为什么不建议手工到处导入 <code>.p12</code> 和 Profile</h3><p>小项目刚开始时，手动导入也能用。但一旦需要换电脑、多人协作或者接入 CI，你会遇到：</p><ul><li>私钥只在某一台老电脑里，其他机器虽然有证书却无法签名；</li><li>Profile 到期后，大家不知道该谁更新；</li><li>新增 Widget 后，主 App 能编译、Extension 没有 Profile；</li><li>CI 缺证书、缺 Keychain、缺 Profile，错误信息还不直观；</li><li>有人误点了“重新生成”，导致其他人的签名材料失效。</li></ul><p>这正是 <code>match</code> 的用武之地。</p><h2 id="八、使用-match-管理证书和-Profile"><a href="#八、使用-match-管理证书和-Profile" class="headerlink" title="八、使用 match 管理证书和 Profile"></a>八、使用 match 管理证书和 Profile</h2><p>Fastlane match 的核心思路是：把证书、私钥和 Profile 放进<strong>独立的私有 Git 仓库</strong>，并且加密保存。业务项目不保存敏感签名文件，只保存“去哪里拿、拿哪些 Bundle ID”的规则。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs text">业务仓库<br>  └── Fastfile：声明需要哪些 Bundle ID、什么分发类型<br><br>私有 match 仓库（加密）<br>  └── Certificates / Profiles / 加密材料<br><br>本机或 CI<br>  └── match 下载并解密 → 导入临时 Keychain → xcodebuild 签名<br></code></pre></td></tr></table></figure><h3 id="1-初始化-match"><a href="#1-初始化-match" class="headerlink" title="1. 初始化 match"></a>1. 初始化 match</h3><p>初次使用可执行：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs sh">bundle <span class="hljs-built_in">exec</span> fastlane match init<br></code></pre></td></tr></table></figure><p>之后配置 <code>Matchfile</code> 或在 Fastfile 中传入仓库地址。脱敏示例：</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs ruby"><span class="hljs-variable constant_">MATCH_GIT_URL</span> = <span class="hljs-string">&quot;git@github.com:example/ios-certificates.git&quot;</span><br><span class="hljs-variable constant_">MATCH_GIT_BRANCH</span> = <span class="hljs-string">&quot;main&quot;</span><br></code></pre></td></tr></table></figure><p>首次创建或迁移证书，需要在受控的开发机上执行相应 match 命令，并登录有 Apple Developer 权限的账号。完成后，日常开发机和 CI 应优先使用只读模式。</p><h3 id="2-为开发版和-App-Store-版分别定义-lane"><a href="#2-为开发版和-App-Store-版分别定义-lane" class="headerlink" title="2. 为开发版和 App Store 版分别定义 lane"></a>2. 为开发版和 App Store 版分别定义 lane</h3><p>HabitPulse 这样定义：</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><code class="hljs ruby">lane <span class="hljs-symbol">:habitpulse_dev</span> <span class="hljs-keyword">do</span><br>  verify_fastlane_environment(<span class="hljs-symbol">requires_match_password:</span> <span class="hljs-literal">true</span>)<br>  match(<br>    <span class="hljs-symbol">type:</span> <span class="hljs-string">&quot;development&quot;</span>,<br>    <span class="hljs-symbol">app_identifier:</span> <span class="hljs-variable constant_">APP_IDENTIFIERS</span>,<br>    <span class="hljs-symbol">git_url:</span> <span class="hljs-variable constant_">MATCH_GIT_URL</span>,<br>    <span class="hljs-symbol">git_branch:</span> <span class="hljs-variable constant_">MATCH_GIT_BRANCH</span>,<br>    <span class="hljs-symbol">readonly:</span> <span class="hljs-literal">true</span><br>  )<br><span class="hljs-keyword">end</span><br><br>lane <span class="hljs-symbol">:habitpulse_appstore</span> <span class="hljs-keyword">do</span><br>  verify_fastlane_environment(<span class="hljs-symbol">requires_match_password:</span> <span class="hljs-literal">true</span>)<br>  match(<br>    <span class="hljs-symbol">type:</span> <span class="hljs-string">&quot;appstore&quot;</span>,<br>    <span class="hljs-symbol">app_identifier:</span> <span class="hljs-variable constant_">APP_IDENTIFIERS</span>,<br>    <span class="hljs-symbol">git_url:</span> <span class="hljs-variable constant_">MATCH_GIT_URL</span>,<br>    <span class="hljs-symbol">git_branch:</span> <span class="hljs-variable constant_">MATCH_GIT_BRANCH</span>,<br>    <span class="hljs-symbol">readonly:</span> <span class="hljs-literal">true</span><br>  )<br><span class="hljs-keyword">end</span><br></code></pre></td></tr></table></figure><p>分别运行：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs sh">bundle <span class="hljs-built_in">exec</span> fastlane ios habitpulse_dev<br>bundle <span class="hljs-built_in">exec</span> fastlane ios habitpulse_appstore<br></code></pre></td></tr></table></figure><p><code>development</code> 用于开发安装，<code>appstore</code> 用于 TestFlight 和 App Store 分发。不要拿开发 Profile 去导出 App Store IPA，也不要为了“让 CI 过”随意切成 automatic signing；这样虽然一时可能绕过去，后续会更难追踪。</p><h3 id="3-为什么-readonly-true-是-CI-的正确默认值"><a href="#3-为什么-readonly-true-是-CI-的正确默认值" class="headerlink" title="3. 为什么 readonly: true 是 CI 的正确默认值"></a>3. 为什么 <code>readonly: true</code> 是 CI 的正确默认值</h3><p>CI 应该是一个消费者，而不是证书的管理员。</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs ruby"><span class="hljs-symbol">readonly:</span> <span class="hljs-literal">true</span><br></code></pre></td></tr></table></figure><p>意味着 CI 只会下载、解密、安装已有材料。如果材料缺失或过期，它会明确失败，而不会擅自新建、吊销或替换团队证书。需要修复签名资产时，应该由有权限的人在受控的本机环境中处理，再把更新后的加密材料提交进 match 仓库。</p><p>特别不要在日常 CI 里用 <code>match --force</code>。它可能导致重新创建甚至影响既有材料；只有明确诊断到证书损坏、换新机等场景，才应由负责人谨慎执行。</p><h3 id="4-match-需要哪些秘密"><a href="#4-match-需要哪些秘密" class="headerlink" title="4. match 需要哪些秘密"></a>4. match 需要哪些秘密</h3><p>通常至少有两个：</p><table><thead><tr><th>名称</th><th>含义</th></tr></thead><tbody><tr><td><code>MATCH_PASSWORD</code></td><td>解密 match 仓库中材料的密码</td></tr><tr><td>证书仓库读取凭据</td><td>例如只读 Deploy Key 对应的 SSH 私钥</td></tr></tbody></table><p><code>MATCH_PASSWORD</code> 不是 Apple ID 密码。它是你为 match 加密仓库设置的密码，应该存放在密码管理器、本机安全环境变量或 CI Secret 中。</p><h2 id="九、归档和导出-IPA：build-app-的正确用法"><a href="#九、归档和导出-IPA：build-app-的正确用法" class="headerlink" title="九、归档和导出 IPA：build_app 的正确用法"></a>九、归档和导出 IPA：build_app 的正确用法</h2><p>Fastlane 中的 <code>build_app</code>（别名 <code>gym</code>）负责把 <code>xcodebuild archive</code> 和导出 IPA 的步骤封装起来。HabitPulse 将开发版和 App Store 版拆开，输出目录也拆开，避免产物互相覆盖。</p><h3 id="1-开发版打包"><a href="#1-开发版打包" class="headerlink" title="1. 开发版打包"></a>1. 开发版打包</h3><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><code class="hljs ruby">lane <span class="hljs-symbol">:habitpulse_build_dev</span> <span class="hljs-keyword">do</span><br>  habitpulse_dev<br><br>  build_app(<br>    <span class="hljs-symbol">project:</span> <span class="hljs-variable constant_">PROJECT</span>,<br>    <span class="hljs-symbol">scheme:</span> <span class="hljs-variable constant_">SCHEME</span>,<br>    <span class="hljs-symbol">configuration:</span> <span class="hljs-string">&quot;Debug&quot;</span>,<br>    <span class="hljs-symbol">output_directory:</span> <span class="hljs-title class_">File</span>.join(<span class="hljs-variable constant_">OUTPUT_ROOT</span>, <span class="hljs-string">&quot;dev&quot;</span>),<br>    <span class="hljs-symbol">output_name:</span> <span class="hljs-string">&quot;HabitPulse_dev&quot;</span>,<br>    <span class="hljs-symbol">export_method:</span> <span class="hljs-string">&quot;development&quot;</span>,<br>    <span class="hljs-symbol">export_options:</span> &#123;<br>      <span class="hljs-symbol">teamID:</span> <span class="hljs-variable constant_">ENV</span>[<span class="hljs-string">&quot;FASTLANE_TEAM_ID&quot;</span>] |<span class="hljs-params"></span>| <span class="hljs-variable constant_">DEFAULT_TEAM_ID</span>,<br>      <span class="hljs-symbol">compileBitcode:</span> <span class="hljs-literal">false</span><br>    &#125;,<br>    <span class="hljs-symbol">clean:</span> <span class="hljs-literal">true</span>,<br>    <span class="hljs-symbol">archive_path:</span> <span class="hljs-title class_">File</span>.join(<span class="hljs-variable constant_">OUTPUT_ROOT</span>, <span class="hljs-string">&quot;dev&quot;</span>, <span class="hljs-string">&quot;HabitPulse.xcarchive&quot;</span>),<br>    <span class="hljs-symbol">result_bundle:</span> <span class="hljs-literal">true</span>,<br>    <span class="hljs-symbol">result_bundle_path:</span> <span class="hljs-title class_">File</span>.join(<span class="hljs-variable constant_">OUTPUT_ROOT</span>, <span class="hljs-string">&quot;dev&quot;</span>, <span class="hljs-string">&quot;HabitPulse.xcresult&quot;</span>),<br>    <span class="hljs-symbol">xcargs:</span> <span class="hljs-variable constant_">SWIFT_SANDBOX_XCARGS</span><br>  )<br><span class="hljs-keyword">end</span><br></code></pre></td></tr></table></figure><p>运行：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs sh">bundle <span class="hljs-built_in">exec</span> fastlane ios habitpulse_build_dev<br></code></pre></td></tr></table></figure><p>它输出一个 development IPA，适合受 Profile 限制的开发安装场景。</p><h3 id="2-App-Store-归档"><a href="#2-App-Store-归档" class="headerlink" title="2. App Store 归档"></a>2. App Store 归档</h3><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><code class="hljs ruby">lane <span class="hljs-symbol">:habitpulse_archive_appstore</span> <span class="hljs-keyword">do</span><br>  habitpulse_appstore<br><br>  version = get_version_number(<span class="hljs-symbol">xcodeproj:</span> <span class="hljs-variable constant_">PROJECT</span>, <span class="hljs-symbol">target:</span> <span class="hljs-variable constant_">SCHEME</span>)<br>  build_number = <span class="hljs-variable constant_">ENV</span>[<span class="hljs-string">&quot;BUILD_NUMBER&quot;</span>] |<span class="hljs-params"></span>| get_build_number(<span class="hljs-symbol">xcodeproj:</span> <span class="hljs-variable constant_">PROJECT</span>)<br>  increment_build_number(<span class="hljs-symbol">build_number:</span> build_number, <span class="hljs-symbol">xcodeproj:</span> <span class="hljs-variable constant_">PROJECT</span>) <span class="hljs-keyword">if</span> <span class="hljs-variable constant_">ENV</span>[<span class="hljs-string">&quot;BUILD_NUMBER&quot;</span>]<br><br>  build_app(<br>    <span class="hljs-symbol">project:</span> <span class="hljs-variable constant_">PROJECT</span>,<br>    <span class="hljs-symbol">scheme:</span> <span class="hljs-variable constant_">SCHEME</span>,<br>    <span class="hljs-symbol">configuration:</span> <span class="hljs-string">&quot;Release&quot;</span>,<br>    <span class="hljs-symbol">output_directory:</span> <span class="hljs-title class_">File</span>.join(<span class="hljs-variable constant_">OUTPUT_ROOT</span>, <span class="hljs-string">&quot;appstore&quot;</span>),<br>    <span class="hljs-symbol">output_name:</span> <span class="hljs-string">&quot;HabitPulse&quot;</span>,<br>    <span class="hljs-symbol">export_method:</span> <span class="hljs-string">&quot;app-store&quot;</span>,<br>    <span class="hljs-symbol">export_options:</span> &#123;<br>      <span class="hljs-symbol">teamID:</span> <span class="hljs-variable constant_">ENV</span>[<span class="hljs-string">&quot;FASTLANE_TEAM_ID&quot;</span>] |<span class="hljs-params"></span>| <span class="hljs-variable constant_">DEFAULT_TEAM_ID</span>,<br>      <span class="hljs-symbol">uploadSymbols:</span> <span class="hljs-literal">true</span>,<br>      <span class="hljs-symbol">uploadBitcode:</span> <span class="hljs-literal">false</span><br>    &#125;,<br>    <span class="hljs-symbol">clean:</span> <span class="hljs-literal">true</span>,<br>    <span class="hljs-symbol">archive_path:</span> <span class="hljs-title class_">File</span>.join(<span class="hljs-variable constant_">OUTPUT_ROOT</span>, <span class="hljs-string">&quot;appstore&quot;</span>, <span class="hljs-string">&quot;HabitPulse.xcarchive&quot;</span>),<br>    <span class="hljs-symbol">result_bundle:</span> <span class="hljs-literal">true</span>,<br>    <span class="hljs-symbol">result_bundle_path:</span> <span class="hljs-title class_">File</span>.join(<span class="hljs-variable constant_">OUTPUT_ROOT</span>, <span class="hljs-string">&quot;appstore&quot;</span>, <span class="hljs-string">&quot;HabitPulse.xcresult&quot;</span>),<br>    <span class="hljs-symbol">xcargs:</span> <span class="hljs-variable constant_">SWIFT_SANDBOX_XCARGS</span><br>  )<br><span class="hljs-keyword">end</span><br></code></pre></td></tr></table></figure><p>执行时可以临时传入构建号：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs sh">BUILD_NUMBER=42 bundle <span class="hljs-built_in">exec</span> fastlane ios habitpulse_archive_appstore<br></code></pre></td></tr></table></figure><p>这条 lane 的一个重要边界是：<strong>它只归档和导出 IPA，不上传。</strong>把“构建一个可发布包”和“把包发到外部平台”拆开后，日常验证更安全，CI 也能稳定保留一份主干可发布的制品。</p><h3 id="3-为什么要同时保留-IPA、archive、dSYM-和-xcresult"><a href="#3-为什么要同时保留-IPA、archive、dSYM-和-xcresult" class="headerlink" title="3. 为什么要同时保留 IPA、archive、dSYM 和 xcresult"></a>3. 为什么要同时保留 IPA、archive、dSYM 和 xcresult</h3><table><thead><tr><th>产物</th><th>为什么要留</th></tr></thead><tbody><tr><td><code>.ipa</code></td><td>上传 TestFlight 或给分发渠道使用</td></tr><tr><td><code>.xcarchive</code></td><td>复查归档内容、签名和 dSYM 的来源</td></tr><tr><td><code>.dSYM</code></td><td>日后崩溃日志符号化需要</td></tr><tr><td><code>.xcresult</code></td><td>构建或测试失败时排查 Xcode 级别细节</td></tr></tbody></table><p>HabitPulse 统一输出到：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs text">fastlane/build_output/ios/habitpulse/dev/<br>fastlane/build_output/ios/habitpulse/appstore/<br>fastlane/test_output/<br></code></pre></td></tr></table></figure><p>在 CI 中，这些目录还会作为 Artifact 上传。不要只保留一份 IPA；真遇到问题时，archive 与完整日志往往更有价值。</p><h2 id="十、版本号、构建号和发布说明"><a href="#十、版本号、构建号和发布说明" class="headerlink" title="十、版本号、构建号和发布说明"></a>十、版本号、构建号和发布说明</h2><h3 id="1-先分清两个版本字段"><a href="#1-先分清两个版本字段" class="headerlink" title="1. 先分清两个版本字段"></a>1. 先分清两个版本字段</h3><table><thead><tr><th>字段</th><th>Xcode 中常见键</th><th>例子</th><th>规则</th></tr></thead><tbody><tr><td>面向用户的版本</td><td><code>MARKETING_VERSION</code> &#x2F; <code>CFBundleShortVersionString</code></td><td><code>1.0</code></td><td>只有产品版本变化时才增加</td></tr><tr><td>构建号</td><td><code>CURRENT_PROJECT_VERSION</code> &#x2F; <code>CFBundleVersion</code></td><td><code>42</code></td><td>每次上传到 TestFlight 必须唯一且递增</td></tr></tbody></table><p>很多“上传被拒绝”的问题都来自 build number 重复。HabitPulse 的思路是先读取本地构建号和 App Store Connect 上已经存在的最大值，再取更大的下一个值：</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs ruby">private_lane <span class="hljs-symbol">:next_testflight_build_number</span> <span class="hljs-keyword">do</span><br>  local_build = get_build_number(<span class="hljs-symbol">xcodeproj:</span> <span class="hljs-variable constant_">PROJECT</span>).to_i<br>  uploaded_build = latest_testflight_build_number(<br>    <span class="hljs-symbol">api_key:</span> lane_context[<span class="hljs-title class_">SharedValues</span><span class="hljs-symbol">:</span><span class="hljs-symbol">:APP_STORE_CONNECT_API_KEY</span>],<br>    <span class="hljs-symbol">app_identifier:</span> <span class="hljs-variable constant_">APP_IDENTIFIER</span>,<br>    <span class="hljs-symbol">initial_build_number:</span> <span class="hljs-number">0</span><br>  ).to_i<br>  [local_build + <span class="hljs-number">1</span>, uploaded_build + <span class="hljs-number">1</span>].max<br><span class="hljs-keyword">end</span><br></code></pre></td></tr></table></figure><p>使用：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs sh">bundle <span class="hljs-built_in">exec</span> fastlane ios next_build_number<br></code></pre></td></tr></table></figure><h3 id="2-从-Conventional-Commits-生成发布说明"><a href="#2-从-Conventional-Commits-生成发布说明" class="headerlink" title="2. 从 Conventional Commits 生成发布说明"></a>2. 从 Conventional Commits 生成发布说明</h3><p>把提交信息规范化后，发布说明不必每次手写。HabitPulse 只提取 <code>feat</code>、<code>fix</code>、<code>perf</code>，过滤 <code>docs</code>、<code>chore</code> 等不面向用户的提交：</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs ruby">subjects = sh(<span class="hljs-string">&quot;git log --no-merges --pretty=format:%s <span class="hljs-subst">#&#123;previous_tag&#125;</span>..HEAD&quot;</span>).lines.map(&amp;<span class="hljs-symbol">:strip</span>)<br>notes = subjects.filter_map <span class="hljs-keyword">do</span> |<span class="hljs-params">subject</span>|<br>  <span class="hljs-keyword">next</span> <span class="hljs-keyword">unless</span> subject.match?(<span class="hljs-regexp">/^(feat|fix|perf)(\([^)]*\))?:/</span>)<br>  <span class="hljs-string">&quot;- <span class="hljs-subst">#&#123;subject.sub(<span class="hljs-regexp">/^(feat|fix|perf)(\([^)]*\))?:\s*/</span>, <span class="hljs-string">&quot;&quot;</span>)&#125;</span>&quot;</span><br><span class="hljs-keyword">end</span><br></code></pre></td></tr></table></figure><p>例如提交：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs text">feat(reminder): 新增提醒管理页面<br>fix(widget): 修复快照刷新延迟<br>docs(readme): 补充安装说明<br></code></pre></td></tr></table></figure><p>生成的面向用户说明是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs text">- 新增提醒管理页面<br>- 修复快照刷新延迟<br></code></pre></td></tr></table></figure><p>这不是让开发者偷懒，而是让提交信息成为可复用的工程记录。前提是提交标题真的描述了用户能感知的变化。</p><h3 id="3-用-tag-约束正式发布"><a href="#3-用-tag-约束正式发布" class="headerlink" title="3. 用 tag 约束正式发布"></a>3. 用 tag 约束正式发布</h3><p>HabitPulse 的发布 tag 采用：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">v&lt;版本号&gt;-build.&lt;构建号&gt;<br></code></pre></td></tr></table></figure><p>例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">v1.0-build.42<br></code></pre></td></tr></table></figure><p>发布 lane 会确认当前 <code>HEAD</code> 精确对应这个 tag。这样你不会出现“本来准备发布 A 提交，实际打出来的是后来又混进几个提交的 B 包”。</p><p><code>release_prepare</code> 默认是 dry-run：会计算下一构建号和生成发布说明，但不会修改工程，也不会创建 tag。只有明确设置确认变量才会写入：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs sh"><span class="hljs-comment"># 默认只预览</span><br>bundle <span class="hljs-built_in">exec</span> fastlane ios release_prepare<br><br><span class="hljs-comment"># 确认后才会修改 build number 并创建 tag</span><br>CONFIRM_RELEASE_PREPARE=<span class="hljs-literal">true</span> bundle <span class="hljs-built_in">exec</span> fastlane ios release_prepare<br></code></pre></td></tr></table></figure><h2 id="十一、接入-App-Store-Connect-API-Key"><a href="#十一、接入-App-Store-Connect-API-Key" class="headerlink" title="十一、接入 App Store Connect API Key"></a>十一、接入 App Store Connect API Key</h2><p>过去 Fastlane 经常依赖 Apple ID 登录、双重验证和会话 Cookie。现在更推荐 App Store Connect API Key：它适合本机与 CI，也更容易收紧权限和撤销。</p><h3 id="1-需要准备的三个字段"><a href="#1-需要准备的三个字段" class="headerlink" title="1. 需要准备的三个字段"></a>1. 需要准备的三个字段</h3><table><thead><tr><th>环境变量</th><th>含义</th></tr></thead><tbody><tr><td><code>ASC_KEY_ID</code></td><td>App Store Connect 创建 API Key 后的 Key ID，通常是 10 位字符</td></tr><tr><td><code>ASC_ISSUER_ID</code></td><td>App Store Connect 页面提供的 Issuer ID，UUID 格式</td></tr><tr><td><code>ASC_KEY_FILEPATH</code></td><td>本机 <code>.p8</code> 私钥文件的绝对路径</td></tr></tbody></table><p><code>.p8</code> 文件创建后通常只有一次下载机会。下载后立即保存到密码管理器或受保护路径，绝对不要提交到 Git。</p><p>Fastfile 可以做格式和权限检查：</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><code class="hljs ruby">private_lane <span class="hljs-symbol">:app_store_connect_api_key_from_environment</span> <span class="hljs-keyword">do</span><br>  key_id = <span class="hljs-variable constant_">ENV</span>[<span class="hljs-string">&quot;ASC_KEY_ID&quot;</span>]<br>  issuer_id = <span class="hljs-variable constant_">ENV</span>[<span class="hljs-string">&quot;ASC_ISSUER_ID&quot;</span>]<br>  key_filepath = <span class="hljs-variable constant_">ENV</span>[<span class="hljs-string">&quot;ASC_KEY_FILEPATH&quot;</span>]<br><br>  <span class="hljs-variable constant_">UI</span>.user_error!(<span class="hljs-string">&quot;ASC_KEY_ID 格式错误&quot;</span>) <span class="hljs-keyword">unless</span> key_id&amp;.match?(<span class="hljs-regexp">/\A[A-Z0-9]&#123;10&#125;\z/</span>)<br>  <span class="hljs-variable constant_">UI</span>.user_error!(<span class="hljs-string">&quot;ASC_ISSUER_ID 格式错误&quot;</span>) <span class="hljs-keyword">unless</span> issuer_id&amp;.match?(<span class="hljs-regexp">/\A[0-9a-fA-F-]&#123;36&#125;\z/</span>)<br>  <span class="hljs-variable constant_">UI</span>.user_error!(<span class="hljs-string">&quot;缺少 .p8 私钥&quot;</span>) <span class="hljs-keyword">unless</span> key_filepath&amp;.end_with?(<span class="hljs-string">&quot;.p8&quot;</span>) &amp;&amp; <span class="hljs-title class_">File</span>.file?(key_filepath)<br>  <span class="hljs-variable constant_">UI</span>.user_error!(<span class="hljs-string">&quot;私钥权限过宽&quot;</span>) <span class="hljs-keyword">unless</span> (<span class="hljs-title class_">File</span>.stat(key_filepath).mode &amp; <span class="hljs-number">0o077</span>).zero?<br><br>  app_store_connect_api_key(<br>    <span class="hljs-symbol">key_id:</span> key_id,<br>    <span class="hljs-symbol">issuer_id:</span> issuer_id,<br>    <span class="hljs-symbol">key_filepath:</span> key_filepath,<br>    <span class="hljs-symbol">duration:</span> <span class="hljs-number">1_200</span>,<br>    <span class="hljs-symbol">in_house:</span> <span class="hljs-literal">false</span><br>  )<br><span class="hljs-keyword">end</span><br></code></pre></td></tr></table></figure><p>先做一个只读验证，比直接上传更稳：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs sh">bundle <span class="hljs-built_in">exec</span> fastlane ios asc_verify<br></code></pre></td></tr></table></figure><p>它能先确认 Key 是否有效、权限是否足够、网络和配置是否正确。</p><h3 id="2-API-Key-与签名证书不是同一回事"><a href="#2-API-Key-与签名证书不是同一回事" class="headerlink" title="2. API Key 与签名证书不是同一回事"></a>2. API Key 与签名证书不是同一回事</h3><p>这是新手常混淆的点：</p><ul><li><code>match</code> 管理 <strong>Apple Developer 签名证书和 Provisioning Profile</strong>；</li><li>App Store Connect API Key 管理 <strong>读取 TestFlight、上传构建、上传元数据、提交审核</strong> 的权限。</li></ul><p>一个解决“我能不能签出这个 IPA”，另一个解决“我能不能把 IPA 传到 App Store Connect”。两者都需要，但职责不能互相替代。</p><h2 id="十二、上传-TestFlight：先上传，不自动外发"><a href="#十二、上传-TestFlight：先上传，不自动外发" class="headerlink" title="十二、上传 TestFlight：先上传，不自动外发"></a>十二、上传 TestFlight：先上传，不自动外发</h2><p>HabitPulse 的 <code>beta</code> lane 做的顺序是：计算下一个构建号 → 生成发布说明 → 安装 App Store 签名材料 → Release Archive → 验证 IPA&#x2F;dSYM&#x2F;Bundle ID → 上传 TestFlight。</p><p>核心上传部分如下：</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs ruby">upload_to_testflight(<br>  <span class="hljs-symbol">ipa:</span> ipa_path,<br>  <span class="hljs-symbol">api_key:</span> lane_context[<span class="hljs-title class_">SharedValues</span><span class="hljs-symbol">:</span><span class="hljs-symbol">:APP_STORE_CONNECT_API_KEY</span>],<br>  <span class="hljs-symbol">app_identifier:</span> <span class="hljs-variable constant_">APP_IDENTIFIER</span>,<br>  <span class="hljs-symbol">changelog:</span> <span class="hljs-title class_">File</span>.read(notes_path),<br>  <span class="hljs-symbol">skip_submission:</span> <span class="hljs-literal">true</span>,<br>  <span class="hljs-symbol">distribute_external:</span> <span class="hljs-literal">false</span>,<br>  <span class="hljs-symbol">notify_external_testers:</span> <span class="hljs-literal">false</span>,<br>  <span class="hljs-symbol">skip_waiting_for_build_processing:</span> <span class="hljs-literal">false</span>,<br>  <span class="hljs-symbol">wait_processing_timeout_duration:</span> <span class="hljs-number">600</span><br>)<br></code></pre></td></tr></table></figure><p>执行：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs sh">bundle <span class="hljs-built_in">exec</span> fastlane ios beta<br></code></pre></td></tr></table></figure><p>几个参数的含义：</p><ul><li><code>skip_submission: true</code>：不自动把构建提交给 Beta Review；</li><li><code>distribute_external: false</code>：默认不外部分发；</li><li><code>notify_external_testers: false</code>：不自动向外部测试者发通知；</li><li><code>wait_processing_timeout_duration: 600</code>：最多等待十分钟处理状态，避免无限挂起。</li></ul><p>这种默认值更保守。第一次跑通 Fastlane 或新换 API Key 时，最适合先上传到 TestFlight、确认构建存在，再决定是否分发给外部测试人员。</p><h3 id="上传前为什么要验证产物"><a href="#上传前为什么要验证产物" class="headerlink" title="上传前为什么要验证产物"></a>上传前为什么要验证产物</h3><p>归档成功不等于导出的东西完全正确。HabitPulse 在上传前会检查：</p><ul><li>IPA 文件是否存在；</li><li>archive 是否存在；</li><li>archive 中主 App 与 Widget 的 dSYM 是否齐全；</li><li>导出的 IPA 是否为 distribution 签名；</li><li>Team ID 和 Bundle ID 是否符合预期。</li></ul><p>这个额外步骤能把“上传后才发现签错包”的问题提前到本机解决。</p><h2 id="十三、商店元数据、预检和截图"><a href="#十三、商店元数据、预检和截图" class="headerlink" title="十三、商店元数据、预检和截图"></a>十三、商店元数据、预检和截图</h2><p>Fastlane 的发布能力不只有传 IPA。App Store 名称、副标题、描述、关键词、支持链接、隐私链接、版权、年龄分级、截图等也可以作为版本化文件管理。</p><h3 id="1-多语言元数据"><a href="#1-多语言元数据" class="headerlink" title="1. 多语言元数据"></a>1. 多语言元数据</h3><p>HabitPulse 将三种语言放在：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs text">fastlane/metadata/zh-Hans/<br>fastlane/metadata/en-US/<br>fastlane/metadata/fr-FR/<br></code></pre></td></tr></table></figure><p>每个目录包含类似：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs text">name.txt<br>subtitle.txt<br>promotional_text.txt<br>description.txt<br>keywords.txt<br>support_url.txt<br>marketing_url.txt<br>privacy_url.txt<br>copyright.txt<br></code></pre></td></tr></table></figure><p>本地校验 lane 检查文件是否完整、内容是否为空、URL 是否为 HTTPS、字符数是否超出 App Store 限制：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs sh">bundle <span class="hljs-built_in">exec</span> fastlane ios metadata_validate<br></code></pre></td></tr></table></figure><p>这个命令不需要上传，也不需要读取私钥，适合放进 PR 检查。</p><h3 id="2-precheck-与-metadata-upload"><a href="#2-precheck-与-metadata-upload" class="headerlink" title="2. precheck 与 metadata_upload"></a>2. precheck 与 metadata_upload</h3><p>两者职责不同：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs sh"><span class="hljs-comment"># 与 App Store Connect 做预检查：不上传、不提交审核</span><br>bundle <span class="hljs-built_in">exec</span> fastlane ios precheck<br><br><span class="hljs-comment"># 只上传商店文案：不上传二进制、不传截图、不提交审核</span><br>bundle <span class="hljs-built_in">exec</span> fastlane ios metadata_upload<br></code></pre></td></tr></table></figure><p>这样可以把“修改文案”和“上传一个新的 App 二进制”分开。真实实践中，App Store Connect 的首个版本审核详情接口偶尔可能返回空数据，导致元数据上传后出现非关键的清理异常。HabitPulse 的 lane 对已知、可确认的 <code>No data</code> 情况做了窄范围处理：确认元数据已经上传后跳过非必要的审核附件清理；其他错误仍然抛出，不能一概吞掉。</p><h3 id="3-snapshot-自动截图"><a href="#3-snapshot-自动截图" class="headerlink" title="3. snapshot 自动截图"></a>3. snapshot 自动截图</h3><p>商店截图最适合由 UI Test 自动化：稳定、可重复、多语言和多尺寸不会漏。HabitPulse 的 <code>screenshots</code> lane：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs sh"><span class="hljs-comment"># 默认生成简体中文、英语、法语截图</span><br>bundle <span class="hljs-built_in">exec</span> fastlane ios screenshots<br><br><span class="hljs-comment"># 只生成英文和法语，便于调试</span><br>SCREENSHOT_LANGUAGES=en-US,fr-FR bundle <span class="hljs-built_in">exec</span> fastlane ios screenshots<br></code></pre></td></tr></table></figure><p>它指定了 UI Test Target、设备、语言和启动参数：</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs ruby">snapshot(<br>  <span class="hljs-symbol">scheme:</span> <span class="hljs-variable constant_">SCHEME</span>,<br>  <span class="hljs-symbol">test_target_name:</span> <span class="hljs-string">&quot;HabitPulseUITests&quot;</span>,<br>  <span class="hljs-symbol">devices:</span> [<span class="hljs-string">&quot;iPhone 17 Pro&quot;</span>, <span class="hljs-string">&quot;iPad Pro 13-inch (M5)&quot;</span>],<br>  <span class="hljs-symbol">languages:</span> (<span class="hljs-variable constant_">ENV</span>[<span class="hljs-string">&quot;SCREENSHOT_LANGUAGES&quot;</span>] |<span class="hljs-params"></span>| <span class="hljs-string">&quot;zh-Hans,en-US,fr-FR&quot;</span>).split(<span class="hljs-string">&quot;,&quot;</span>),<br>  <span class="hljs-symbol">launch_arguments:</span> [<span class="hljs-string">&quot;--ui-testing&quot;</span>, <span class="hljs-string">&quot;--skip-onboarding&quot;</span>],<br>  <span class="hljs-symbol">headless:</span> <span class="hljs-literal">true</span>,<br>  <span class="hljs-symbol">concurrent_simulators:</span> <span class="hljs-literal">false</span><br>)<br></code></pre></td></tr></table></figure><p>截图自动化的重点不在 Fastlane 本身，而在你的 App 必须有可预测的 UI Test 状态：不要读取真实 iCloud 数据，不要在截图过程中弹不可控权限框，不要让首次引导随机出现。必要时给 App 增加仅限 UI Test 的启动参数和种子数据。</p><h2 id="十四、正式审核提交：必须有安全闸门"><a href="#十四、正式审核提交：必须有安全闸门" class="headerlink" title="十四、正式审核提交：必须有安全闸门"></a>十四、正式审核提交：必须有安全闸门</h2><p>正式提交审核是外部状态变更，不能和普通 Archive 混在一起。HabitPulse 的 <code>release</code> lane 采取了两层保护：</p><ol><li>当前提交必须精确对应 <code>v&lt;version&gt;-build.&lt;build&gt;</code> tag；</li><li>只有 <code>CONFIRM_RELEASE=true</code> 才真的上传 IPA、上传元数据、提交审核。</li></ol><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs sh"><span class="hljs-comment"># 默认 dry-run：测试、归档、precheck，但不上传、不提交</span><br>bundle <span class="hljs-built_in">exec</span> fastlane ios release<br><br><span class="hljs-comment"># 显式确认后才允许外部动作</span><br>CONFIRM_RELEASE=<span class="hljs-literal">true</span> bundle <span class="hljs-built_in">exec</span> fastlane ios release<br></code></pre></td></tr></table></figure><p>dry-run 不是摆设。HabitPulse 实测过：在没有正确发布 tag 的情况下，即使设置了 <code>CONFIRM_RELEASE=true</code>，lane 会在任何测试、归档、网络上传之前以退出码 1 阻断。</p><p>真正调用 <code>deliver</code> 时还会保持：</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs ruby"><span class="hljs-symbol">submit_for_review:</span> <span class="hljs-literal">true</span>,<br><span class="hljs-symbol">automatic_release:</span> <span class="hljs-literal">false</span><br></code></pre></td></tr></table></figure><p>这表示提交审核可以自动化，但审核通过后仍需你在 App Store Connect 手动选择公开发布。对于大多数独立开发者和小团队，这个默认值既高效又稳妥。</p><h2 id="十五、这次接入中遇到的真实问题与解决方案"><a href="#十五、这次接入中遇到的真实问题与解决方案" class="headerlink" title="十五、这次接入中遇到的真实问题与解决方案"></a>十五、这次接入中遇到的真实问题与解决方案</h2><p>下面这些不是“理论上可能发生”的问题，而是 HabitPulse 在接入 Fastlane 与后续 CI 时实际验证过或遇到过的情况。</p><h3 id="问题-1：命令行构建时出现-Swift-宏找不到"><a href="#问题-1：命令行构建时出现-Swift-宏找不到" class="headerlink" title="问题 1：命令行构建时出现 Swift 宏找不到"></a>问题 1：命令行构建时出现 Swift 宏找不到</h3><p><strong>现象</strong></p><p>命令行 <code>xcodebuild</code> 或 Fastlane 测试时出现大量类似错误：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs text">could not be found for macro &#x27;Model()&#x27;<br>swift-plugin-server produced malformed response<br></code></pre></td></tr></table></figure><p><strong>原因</strong></p><p>项目使用 SwiftData 的宏。当前命令行环境中，Swift 宏插件沙箱导致插件服务无法正常启动；Xcode IDE 内构建却可能正常，所以问题很容易被误判成代码问题。</p><p><strong>解决方案</strong></p><p>在所有命令行构建入口统一传递：</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs ruby"><span class="hljs-variable constant_">SWIFT_SANDBOX_XCARGS</span> = <span class="hljs-string">&#x27;OTHER_SWIFT_FLAGS=&quot;-disable-sandbox&quot;&#x27;</span><br></code></pre></td></tr></table></figure><p>然后将它放进 <code>run_tests</code>、开发 Archive、App Store Archive、截图等所有需要 xcodebuild 的 lane。关键不是这一个参数本身，而是不要只在某一条命令里临时补上；否则本机测试通过、CI 或 release 又失败，问题会反复出现。</p><h3 id="问题-2：构建工具链版本漂移"><a href="#问题-2：构建工具链版本漂移" class="headerlink" title="问题 2：构建工具链版本漂移"></a>问题 2：构建工具链版本漂移</h3><p><strong>现象</strong></p><p>本地能跑，GitHub Actions 上找不到模拟器、Ruby 依赖不兼容，或者 Xcode 版本不满足项目要求。</p><p><strong>原因</strong></p><p>没有锁定 Xcode、Ruby、Fastlane 以及测试 Runtime；GitHub-hosted Runner 会更新，系统全局 Ruby&#x2F;Gem 也会变化。</p><p><strong>解决方案</strong></p><ul><li>用 <code>Gemfile</code> 与 <code>Gemfile.lock</code> 固定 Ruby&#x2F;Fastlane&#x2F;Bundler；</li><li>本机和 CI 都使用 <code>bundle exec</code>；</li><li>Fastlane 固定测试设备和 Runtime；</li><li>GitHub Actions 使用 <code>setup-xcode</code> 显式选择 Xcode 版本；</li><li>升级 Xcode 前先在分支或 PR 流程验证。</li></ul><h3 id="问题-3：Widget-导致签名失败"><a href="#问题-3：Widget-导致签名失败" class="headerlink" title="问题 3：Widget 导致签名失败"></a>问题 3：Widget 导致签名失败</h3><p><strong>现象</strong></p><p>主 App 看起来已经有证书和 Profile，但 Archive 报 Extension 没有合适的 Provisioning Profile，或导出时签名失败。</p><p><strong>原因</strong></p><p>只把主 App Bundle ID 配给了 match，漏了 Widget 的 Bundle ID；或者 Apple Developer 后台没有为 Extension 正确配置 App ID&#x2F;Capability。</p><p><strong>解决方案</strong></p><p>把所有需要签名的 Target 列成常量：</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs ruby"><span class="hljs-variable constant_">APP_IDENTIFIERS</span> = [main_app_bundle_id, widget_bundle_id].freeze<br></code></pre></td></tr></table></figure><p>并让 development&#x2F;appstore 两条 match lane 都使用同一列表。新增任何 Extension 时，把 Apple Developer、match、Fastfile、CI 签名检查这四处一起更新。</p><h3 id="问题-4：TestFlight-提示构建号重复"><a href="#问题-4：TestFlight-提示构建号重复" class="headerlink" title="问题 4：TestFlight 提示构建号重复"></a>问题 4：TestFlight 提示构建号重复</h3><p><strong>现象</strong></p><p>IPA 签名正确，但上传被 App Store Connect 拒绝，提示 build number 已存在。</p><p><strong>原因</strong></p><p>只从工程本地读取 <code>CURRENT_PROJECT_VERSION</code>，忽略了已经上传到 TestFlight 的构建号；或者并行构建时重复使用同一个号。</p><p><strong>解决方案</strong></p><p>在上传前查询 App Store Connect 的最新 build number，并与本地号比较取更大值的下一个数。对于频繁并行发布的团队，还应在发布流程中串行化或用外部构建号策略协调，避免两个 Job 同时算到相同结果。</p><h3 id="问题-5：把-Fastlane-CLI-的-dry-run-当作业务-dry-run"><a href="#问题-5：把-Fastlane-CLI-的-dry-run-当作业务-dry-run" class="headerlink" title="问题 5：把 Fastlane CLI 的 --dry-run 当作业务 dry-run"></a>问题 5：把 Fastlane CLI 的 <code>--dry-run</code> 当作业务 dry-run</h3><p><strong>现象</strong></p><p>尝试运行：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs sh">bundle <span class="hljs-built_in">exec</span> fastlane ios release --dry-run<br></code></pre></td></tr></table></figure><p>结果失败或没有达到预期。</p><p><strong>原因</strong></p><p>“是否产生上传&#x2F;提交审核等外部副作用”是你的 release lane 的业务规则，不是 Fastlane CLI 自动知道的概念。</p><p><strong>解决方案</strong></p><p>在 Fastfile 内设计明确的确认变量，例如：</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs ruby"><span class="hljs-keyword">unless</span> <span class="hljs-variable constant_">ENV</span>[<span class="hljs-string">&quot;CONFIRM_RELEASE&quot;</span>] == <span class="hljs-string">&quot;true&quot;</span><br>  <span class="hljs-variable constant_">UI</span>.important(<span class="hljs-string">&quot;dry-run：未上传 IPA、未上传元数据、未提交审核&quot;</span>)<br>  <span class="hljs-keyword">next</span><br><span class="hljs-keyword">end</span><br></code></pre></td></tr></table></figure><p>默认执行 <code>bundle exec fastlane ios release</code> 时只做测试、归档与预检；只有显式设置 <code>CONFIRM_RELEASE=true</code> 才进入 <code>deliver</code>。这才是可审计、可测试的 dry-run。</p><h3 id="问题-6：Fastlane-把-README-自动重写"><a href="#问题-6：Fastlane-把-README-自动重写" class="headerlink" title="问题 6：Fastlane 把 README 自动重写"></a>问题 6：Fastlane 把 README 自动重写</h3><p><strong>现象</strong></p><p>运行某些 Fastlane 命令或文档生成动作后，已跟踪的 <code>fastlane/README.md</code> 被生成内容覆盖，导致工作树出现不属于当前任务的改动。</p><p><strong>原因</strong></p><p>Fastlane 的默认文档生成行为会产出 lane 列表；如果你把 README 当成手写操作手册，两者会冲突。</p><p><strong>解决方案</strong></p><p>明确 README 的所有权：要么接受自动生成、不要手写；要么将手写使用说明放到独立文件并避免生成覆盖。HabitPulse 的实践是先检查 diff，确认它只是生成副作用后恢复到提交版本，避免把无关改动混进功能提交。</p><h3 id="问题-7：商店预检出现版权字段警告"><a href="#问题-7：商店预检出现版权字段警告" class="headerlink" title="问题 7：商店预检出现版权字段警告"></a>问题 7：商店预检出现版权字段警告</h3><p><strong>现象</strong></p><p>本地三语 <code>copyright.txt</code> 已包含年份，App Store Connect 的 <code>precheck</code> 仍给出版权相关警告。</p><p><strong>原因</strong></p><p>预检有时检查的是远端当前商店元数据或特定 App Store Connect 规则，未必等同于本地文本文件格式。</p><p><strong>解决方案</strong></p><p>先区分 warning 与 error：本地元数据校验是否已经通过？远端 warning 是否阻断提交？如果不阻断，不要为了消除一条远端提示盲目来回修改已经正确的本地文本。将它记录为非阻断项，等真正上传并同步远端元数据后再复查。</p><h3 id="问题-8：审核联系信息在本机有，CI-却没有"><a href="#问题-8：审核联系信息在本机有，CI-却没有" class="headerlink" title="问题 8：审核联系信息在本机有，CI 却没有"></a>问题 8：审核联系信息在本机有，CI 却没有</h3><p><strong>现象</strong></p><p>本机 <code>release</code> 能找到 <code>review_information.yml</code>，GitHub Actions 的正式 release 因缺少审核联系人、电话或审核备注失败。</p><p><strong>原因</strong></p><p>审核信息应该不提交到仓库，所以 CI Runner 天然没有这个文件。</p><p><strong>解决方案</strong></p><p>将审核信息作为受保护 Environment Secret 注入到 Runner 临时文件，或为 CI 单独配置受保护的安全文件路径；并让 lane 在缺失时明确报错。不要把真实电话、邮箱、登录说明直接提交进 <code>fastlane/</code>。</p><h3 id="问题-9：Fastlane-的-lane-名与内置工具重名"><a href="#问题-9：Fastlane-的-lane-名与内置工具重名" class="headerlink" title="问题 9：Fastlane 的 lane 名与内置工具重名"></a>问题 9：Fastlane 的 lane 名与内置工具重名</h3><p><strong>现象</strong></p><p>运行时出现类似提示：<code>Lane name &#39;precheck&#39; should not be used because it is the name of a fastlane tool</code>。</p><p><strong>原因</strong></p><p>lane 叫 <code>precheck</code>，同时 Fastlane 也有同名 action&#x2F;tool。多数情况下仍可运行，但可读性和未来兼容性都不好。</p><p><strong>解决方案</strong></p><p>更推荐使用语义明确且不冲突的命名，例如：</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs ruby">lane <span class="hljs-symbol">:store_precheck</span> <span class="hljs-keyword">do</span><br>  check_app_store_metadata(...)<br><span class="hljs-keyword">end</span><br></code></pre></td></tr></table></figure><p>然后同步更新 README、CI 和团队命令。若历史项目暂时保留旧 lane，至少把这条 warning 记录下来，避免误认为是构建失败。</p><h3 id="问题-10：证书仓库拉取失败或-GitHub-走错代理"><a href="#问题-10：证书仓库拉取失败或-GitHub-走错代理" class="headerlink" title="问题 10：证书仓库拉取失败或 GitHub 走错代理"></a>问题 10：证书仓库拉取失败或 GitHub 走错代理</h3><p><strong>现象</strong></p><p><code>match</code> 无法 clone 私有证书仓库，或 GitHub 请求报 502、超时、SSH permission denied。</p><p><strong>原因</strong></p><p>可能是 SSH 私钥没有读取权限、Deploy Key 没加到正确仓库、<code>known_hosts</code> 缺 GitHub 指纹，或者本机透明代理配置错误。</p><p><strong>解决方案</strong></p><ul><li>先用 <code>ssh -T git@github.com</code> 或对目标仓库做只读连接诊断；</li><li>确保私钥权限是 <code>600</code>；</li><li>CI 里显式设置 <code>GIT_SSH_COMMAND</code>，指定唯一的 Deploy Key；</li><li>本机代理异常时，使用已验证可用的代理做一次性 Git 覆盖，不要永久污染全局配置；</li><li>不要为了跳过问题把证书仓库改为公开。</li></ul><h2 id="十六、把-Fastlane-接进-GitHub-Actions-后，职责如何划分"><a href="#十六、把-Fastlane-接进-GitHub-Actions-后，职责如何划分" class="headerlink" title="十六、把 Fastlane 接进 GitHub Actions 后，职责如何划分"></a>十六、把 Fastlane 接进 GitHub Actions 后，职责如何划分</h2><p>Fastlane 接入完成后，GitHub Actions 不必重复实现构建逻辑，只需调用 lane。HabitPulse 最终采用三段式：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs text">PR workflow<br>  └── bundle exec fastlane ios metadata_validate<br>  └── bundle exec fastlane ios test<br><br>main archive workflow<br>  └── 设置临时 SSH / Keychain / match 密码<br>  └── bundle exec fastlane ios habitpulse_archive_appstore<br><br>manual release workflow<br>  └── 设置临时 SSH / Keychain / App Store Connect API Key<br>  └── bundle exec fastlane ios beta 或 release<br></code></pre></td></tr></table></figure><p>这样 Fastlane 是能力层，GitHub Actions 是调度层。你仍然可以在本机执行同一条 lane；CI 的差别只是它从 Secret 注入凭据，并把产物上传成 Artifact。</p><p>关于 GitHub Actions 的完整配置、Environment Secret 和临时 Keychain，可参阅同目录文章：<a href="%E5%A6%82%E4%BD%95%E5%B0%86iOS%E9%A1%B9%E7%9B%AE%E6%8E%A5%E5%85%A5GitHub%20Action.md">如何将 iOS 项目接入 GitHub Action</a>。</p><h2 id="十七、一份可照着执行的日常清单"><a href="#十七、一份可照着执行的日常清单" class="headerlink" title="十七、一份可照着执行的日常清单"></a>十七、一份可照着执行的日常清单</h2><h3 id="日常开发"><a href="#日常开发" class="headerlink" title="日常开发"></a>日常开发</h3><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs sh">bundle install<br>bundle <span class="hljs-built_in">exec</span> fastlane ios <span class="hljs-built_in">test</span><br></code></pre></td></tr></table></figure><p>测试通过后再提交代码。不要等到发布日才第一次运行 Fastlane。</p><h3 id="需要给测试人员一个包"><a href="#需要给测试人员一个包" class="headerlink" title="需要给测试人员一个包"></a>需要给测试人员一个包</h3><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs sh"><span class="hljs-comment"># 仅本地归档，先检查产物</span><br>bundle <span class="hljs-built_in">exec</span> fastlane ios habitpulse_archive_appstore<br><br><span class="hljs-comment"># 需要上传 TestFlight 时</span><br>bundle <span class="hljs-built_in">exec</span> fastlane ios beta<br></code></pre></td></tr></table></figure><h3 id="需要修改商店文案"><a href="#需要修改商店文案" class="headerlink" title="需要修改商店文案"></a>需要修改商店文案</h3><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs sh"><span class="hljs-comment"># 先做本地格式校验</span><br>bundle <span class="hljs-built_in">exec</span> fastlane ios metadata_validate<br><br><span class="hljs-comment"># 再做 App Store Connect 预检</span><br>bundle <span class="hljs-built_in">exec</span> fastlane ios precheck<br><br><span class="hljs-comment"># 确认后只上传文案</span><br>bundle <span class="hljs-built_in">exec</span> fastlane ios metadata_upload<br></code></pre></td></tr></table></figure><h3 id="准备正式审核"><a href="#准备正式审核" class="headerlink" title="准备正式审核"></a>准备正式审核</h3><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs sh"><span class="hljs-comment"># 预览下一个构建号与发布说明</span><br>bundle <span class="hljs-built_in">exec</span> fastlane ios release_prepare<br><br><span class="hljs-comment"># 显式准备版本和 tag（会修改工程与 Git）</span><br>CONFIRM_RELEASE_PREPARE=<span class="hljs-literal">true</span> bundle <span class="hljs-built_in">exec</span> fastlane ios release_prepare<br><br><span class="hljs-comment"># 默认 dry-run：不上传、不提交审核</span><br>bundle <span class="hljs-built_in">exec</span> fastlane ios release<br><br><span class="hljs-comment"># 最后确认后才真正提交审核</span><br>CONFIRM_RELEASE=<span class="hljs-literal">true</span> bundle <span class="hljs-built_in">exec</span> fastlane ios release<br></code></pre></td></tr></table></figure><p>正式提交前，还应确认：当前工作树干净、当前提交具有正确 tag、TestFlight 已验证、审核联系信息已准备好、隐私链接和商店元数据正确。</p><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>Fastlane 最好的使用方式，不是追求“一条命令把所有事情都做掉”，而是把 iOS 交付里的每个高风险环节做成边界明确、可单独验证的 lane：</p><ul><li><code>test</code> 只负责可靠测试和报告；</li><li><code>match</code> 只负责把受控签名材料安全地带到当前机器；</li><li><code>build_app</code> 只负责稳定归档和导出；</li><li><code>beta</code> 只负责 TestFlight；</li><li><code>release</code> 在 tag 与显式确认之后才允许提交审核；</li><li>GitHub Actions 只负责合适的时机调用这些能力，并不重写业务规则。</li></ul><p>当这些规则被写进仓库、被本机和 CI 共同执行后，发版就不再依赖某位同事记得住一套手工步骤。它变成了一条可阅读、可复现、可审计的工程流程——这才是 Fastlane 在 iOS 项目里真正长期有价值的地方。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;iOS项目如何接入Fastlane&quot;&gt;&lt;a href=&quot;#iOS项目如何接入Fastlane&quot; class=&quot;headerlink&quot; title=&quot;iOS项目如何接入Fastlane&quot;&gt;&lt;/a&gt;iOS项目如何接入Fastlane&lt;/h1&gt;&lt;p&gt;做 iOS 开发的人</summary>
      
    
    
    
    
    <category term="iOS" scheme="https://flametinary.github.io/tags/iOS/"/>
    
    <category term="Fastlane" scheme="https://flametinary.github.io/tags/Fastlane/"/>
    
    <category term="自动化" scheme="https://flametinary.github.io/tags/%E8%87%AA%E5%8A%A8%E5%8C%96/"/>
    
    <category term="证书签名" scheme="https://flametinary.github.io/tags/%E8%AF%81%E4%B9%A6%E7%AD%BE%E5%90%8D/"/>
    
    <category term="TestFlight" scheme="https://flametinary.github.io/tags/TestFlight/"/>
    
  </entry>
  
  <entry>
    <title>如何将 iOS 项目接入 GitHub Action</title>
    <link href="https://flametinary.github.io/2026/09/07/%E5%A6%82%E4%BD%95%E5%B0%86iOS%E9%A1%B9%E7%9B%AE%E6%8E%A5%E5%85%A5GitHub%20Action/"/>
    <id>https://flametinary.github.io/2026/09/07/%E5%A6%82%E4%BD%95%E5%B0%86iOS%E9%A1%B9%E7%9B%AE%E6%8E%A5%E5%85%A5GitHub%20Action/</id>
    <published>2026-09-06T16:00:00.000Z</published>
    <updated>2026-09-12T03:37:07.567Z</updated>
    
    <content type="html"><![CDATA[<h1 id="如何将-iOS-项目接入-GitHub-Action"><a href="#如何将-iOS-项目接入-GitHub-Action" class="headerlink" title="如何将 iOS 项目接入 GitHub Action"></a>如何将 iOS 项目接入 GitHub Action</h1><p>很多人第一次给 iOS 项目接 GitHub Actions，会以为就是“写一个 YAML，跑一下 <code>xcodebuild test</code>”。真开始做才会发现，真正麻烦的不是 YAML，而是签名证书、Provisioning Profile、App Store Connect API Key、TestFlight 上传权限，以及“怎样确保一次误操作不会直接把包提交审核”。</p><p>这篇文章以 HabitPulse 的真实落地过程为主线，讲清楚一个可发布 iOS 项目是怎样接入 GitHub Actions 的。它是一个 SwiftUI + SwiftData 项目，最低支持 iOS 17.6，包含主 App 和 Widget 扩展；构建和发布能力由 Fastlane 统一封装。文中所有仓库地址、Bundle ID、Team ID 和密钥均已脱敏。</p><p>最终得到的不是一个大而全、什么都自动跑的工作流，而是三条职责明确的流水线：PR 做质量检查，<code>main</code> 做受保护归档，发布只能人工发起。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs text">创建或更新 PR<br>    ↓<br>PR 检查：工程校验 + 单元测试 + 测试报告<br>    ↓<br>合并到 main<br>    ↓<br>主干归档：获取签名材料 + Release Archive + 保存构建产物<br>    ↓<br>在 Actions 页面手动选择 beta / release<br>    ↓<br>上传 TestFlight 或提交 App Store 审核（不会自动公开发布）<br></code></pre></td></tr></table></figure><h2 id="一、先定边界：GitHub-Actions-不该替代-Fastlane"><a href="#一、先定边界：GitHub-Actions-不该替代-Fastlane" class="headerlink" title="一、先定边界：GitHub Actions 不该替代 Fastlane"></a>一、先定边界：GitHub Actions 不该替代 Fastlane</h2><p>一个非常关键的分层是：<strong>Fastlane 负责“如何构建和发布”，GitHub Actions 负责“什么时候、在哪台机器上调用 Fastlane”。</strong></p><p>如果把复杂的 <code>xcodebuild</code> 参数、签名逻辑、TestFlight 上传逻辑都塞进 YAML，后面你想在本机复现 CI 失败，往往会很痛苦。反过来，把这些动作都写成 Fastlane lane 后，本地和 CI 只需要执行同一条命令。</p><p>例如 HabitPulse 里的核心命令是：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs sh"><span class="hljs-comment"># 固定模拟器运行测试，并输出 JUnit、xcresult 与日志</span><br>bundle <span class="hljs-built_in">exec</span> fastlane ios <span class="hljs-built_in">test</span><br><br><span class="hljs-comment"># 使用 App Store 签名归档并导出 IPA、dSYM、archive；不上传</span><br>bundle <span class="hljs-built_in">exec</span> fastlane ios habitpulse_archive_appstore<br><br><span class="hljs-comment"># 上传 TestFlight</span><br>bundle <span class="hljs-built_in">exec</span> fastlane ios beta<br><br><span class="hljs-comment"># 正式提交审核；需要额外安全确认</span><br>CONFIRM_RELEASE=<span class="hljs-literal">true</span> bundle <span class="hljs-built_in">exec</span> fastlane ios release<br></code></pre></td></tr></table></figure><p>这样做有三个直接好处：</p><ul><li>本地先跑通的命令，放到 CI 中通常也能跑通；</li><li>workflow 文件更短，重点落在运行环境、权限和产物保存；</li><li>发版规则不会只藏在 GitHub YAML 中，开发者在本机也能看见、验证和使用。</li></ul><p>本文后面的 YAML 都假设你已经有一个可在本机运行的 Fastlane 基础。至少应当锁定 Ruby 与 Bundler 版本，并将构建参数收进 <code>Fastfile</code>。</p><h2 id="二、先把本地发布链路跑通"><a href="#二、先把本地发布链路跑通" class="headerlink" title="二、先把本地发布链路跑通"></a>二、先把本地发布链路跑通</h2><p>不要一上来就把问题交给 GitHub Actions。先在自己电脑上解决“能测试”“能归档”“能签名”这三件事，之后才值得把它搬到云端 Runner。</p><h3 id="1-锁定工具版本"><a href="#1-锁定工具版本" class="headerlink" title="1. 锁定工具版本"></a>1. 锁定工具版本</h3><p>GitHub 的 macOS Runner 会更新，Xcode 也会更新。没有版本约束时，今天能过的构建下周可能因为 SDK、Swift 或 Ruby 变化突然失败。</p><p>HabitPulse 实际固定了：</p><table><thead><tr><th>工具</th><th align="right">CI 使用的版本</th><th>用途</th></tr></thead><tbody><tr><td>Runner</td><td align="right"><code>macos-15</code></td><td>提供 macOS 构建环境</td></tr><tr><td>Xcode</td><td align="right"><code>26.3</code></td><td>编译、测试、归档</td></tr><tr><td>Ruby</td><td align="right"><code>3.3</code></td><td>运行 Bundler 和 Fastlane</td></tr><tr><td>Bundler</td><td align="right">由 <code>Gemfile.lock</code> 锁定</td><td>保证 Fastlane 依赖一致</td></tr></tbody></table><p>请按自己的工程调整 Xcode 与模拟器型号。尤其要注意：Xcode 版本必须包含你的 deployment target 所需的 SDK，以及 Fastlane 测试 lane 中指定的 iOS Simulator Runtime。</p><h3 id="2-把测试、构建和归档变成-lane"><a href="#2-把测试、构建和归档变成-lane" class="headerlink" title="2. 把测试、构建和归档变成 lane"></a>2. 把测试、构建和归档变成 lane</h3><p>下面是简化后的思路。真实项目的 lane 通常还会包含日志、产物目录、参数校验和错误提示，但职责应该保持单一。</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs ruby">platform <span class="hljs-symbol">:ios</span> <span class="hljs-keyword">do</span><br>  lane <span class="hljs-symbol">:test</span> <span class="hljs-keyword">do</span><br>    run_tests(<br>      <span class="hljs-symbol">project:</span> <span class="hljs-string">&quot;HabitPulse.xcodeproj&quot;</span>,<br>      <span class="hljs-symbol">scheme:</span> <span class="hljs-string">&quot;HabitPulse&quot;</span>,<br>      <span class="hljs-symbol">device:</span> <span class="hljs-string">&quot;iPhone 17 Pro (26.3)&quot;</span>,<br>      <span class="hljs-symbol">result_bundle:</span> <span class="hljs-literal">true</span>,<br>      <span class="hljs-symbol">result_bundle_path:</span> <span class="hljs-string">&quot;fastlane/test_output/HabitPulseTests.xcresult&quot;</span>,<br>      <span class="hljs-symbol">output_types:</span> <span class="hljs-string">&quot;junit&quot;</span>,<br>      <span class="hljs-symbol">output_files:</span> <span class="hljs-string">&quot;HabitPulseTests.junit&quot;</span><br>    )<br>  <span class="hljs-keyword">end</span><br><span class="hljs-keyword">end</span><br></code></pre></td></tr></table></figure><p>HabitPulse 还有一个环境相关的细节：命令行执行 <code>xcodebuild</code> 时，Swift 宏插件在沙箱里可能启动失败，表现为 <code>could not be found for macro &#39;Model()&#39;</code> 或 <code>swift-plugin-server produced malformed response</code>。项目将下面的参数统一收进 Fastlane，避免本机和 CI 行为不一致：</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs ruby"><span class="hljs-variable constant_">SWIFT_SANDBOX_XCARGS</span> = <span class="hljs-string">&#x27;OTHER_SWIFT_FLAGS=&quot;-disable-sandbox&quot;&#x27;</span><br></code></pre></td></tr></table></figure><p>这不是所有项目都需要照抄的配置。正确做法是：先确认自己的命令行构建问题和原因，再把必要参数固定在构建入口，而不是散落在各个 workflow 里。</p><h3 id="3-使用-match-管理签名材料"><a href="#3-使用-match-管理签名材料" class="headerlink" title="3. 使用 match 管理签名材料"></a>3. 使用 match 管理签名材料</h3><p>iOS CI 最大的门槛通常是签名。开发证书、发布证书和 Profile 不能凭空出现在 GitHub Runner 上；而直接把 <code>.p12</code>、profile 文件放进业务仓库，既不安全也难维护。</p><p>HabitPulse 使用 Fastlane match：</p><ul><li>签名证书和 Profile 存在一个独立、加密的私有 Git 仓库；</li><li>业务项目只记录 match 仓库地址和两个 Bundle ID；</li><li>CI 使用 <code>readonly: true</code>，只下载、解密和安装，绝不在云端新建或吊销证书；</li><li>主 App 和 Widget 都必须写进 <code>app_identifier</code>，否则 Archive 时很容易出现“扩展没有 Profile”的错误。</li></ul><p>示例：</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs ruby"><span class="hljs-variable constant_">APP_IDENTIFIERS</span> = [<br>  <span class="hljs-string">&quot;com.example.HabitPulse&quot;</span>,<br>  <span class="hljs-string">&quot;com.example.HabitPulse.Widget&quot;</span><br>].freeze<br><br>lane <span class="hljs-symbol">:appstore_signing</span> <span class="hljs-keyword">do</span><br>  match(<br>    <span class="hljs-symbol">type:</span> <span class="hljs-string">&quot;appstore&quot;</span>,<br>    <span class="hljs-symbol">app_identifier:</span> <span class="hljs-variable constant_">APP_IDENTIFIERS</span>,<br>    <span class="hljs-symbol">git_url:</span> <span class="hljs-variable constant_">ENV</span>.fetch(<span class="hljs-string">&quot;MATCH_GIT_URL&quot;</span>),<br>    <span class="hljs-symbol">readonly:</span> <span class="hljs-literal">true</span><br>  )<br><span class="hljs-keyword">end</span><br></code></pre></td></tr></table></figure><p>这里的 <code>readonly: true</code> 很重要。它让 CI 失败时只会失败，不会因为一次环境异常去重建或撤销团队的证书。</p><h2 id="三、为什么要拆成三个-workflow"><a href="#三、为什么要拆成三个-workflow" class="headerlink" title="三、为什么要拆成三个 workflow"></a>三、为什么要拆成三个 workflow</h2><p>HabitPulse 没有用一个“万能 workflow”。它在 <code>.github/workflows/</code> 下有三个文件：</p><table><thead><tr><th>文件</th><th>触发条件</th><th>做什么</th><th>不做什么</th></tr></thead><tbody><tr><td><code>pr.yml</code></td><td>PR 的目标分支是 <code>main</code></td><td>工程完整性检查、元数据校验、测试、上传报告</td><td>不读取发布密钥，不签名，不上传</td></tr><tr><td><code>main-archive.yml</code></td><td>推送到 <code>main</code></td><td>取回签名材料，生成 App Store archive，保存 IPA&#x2F;dSYM</td><td>不上传 TestFlight，不提交审核</td></tr><tr><td><code>manual-release.yml</code></td><td>在 Actions 页面手动运行</td><td>选择 TestFlight 或正式审核动作</td><td>不会在普通 push 时自动运行</td></tr></tbody></table><p>这个划分不是形式主义，而是权限分层。</p><p>PR 来自开发分支，最需要的是快速反馈和低权限；<code>main</code> 代表已经合并的代码，可以生成一份可追溯的正式归档；真正会改变外部状态的动作，例如上传 TestFlight、提交审核，必须由人明确点击触发。</p><h3 id="命令行直接合并会触发什么？"><a href="#命令行直接合并会触发什么？" class="headerlink" title="命令行直接合并会触发什么？"></a>命令行直接合并会触发什么？</h3><p>这是一个很常见的误会。下面这种操作：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs sh">git checkout main<br>git merge --ff-only feature/my-change<br>git push origin main<br></code></pre></td></tr></table></figure><p>触发的是 <code>push</code> 事件，因此会执行 <code>main-archive.yml</code>，<strong>不会</strong>执行只监听 <code>pull_request</code> 的 <code>pr.yml</code>。</p><p>如果你希望每次合并前一定经过 PR 检查，应在 GitHub 仓库里给 <code>main</code> 设置 Branch protection rule，把对应的 PR check 设为必需检查。否则，拥有直接推送权限的人仍然可以绕过 PR 流程。</p><h2 id="四、PR-工作流：只做验证，不碰发布凭据"><a href="#四、PR-工作流：只做验证，不碰发布凭据" class="headerlink" title="四、PR 工作流：只做验证，不碰发布凭据"></a>四、PR 工作流：只做验证，不碰发布凭据</h2><p>PR workflow 最好足够“干净”：即使来自 fork 的 PR，或者有人误改了分支，也不应接触发布证书、match 密码、App Store Connect 私钥。</p><p>HabitPulse 的核心版本如下：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">name:</span> <span class="hljs-string">iOS</span> <span class="hljs-string">PR</span><br><br><span class="hljs-attr">on:</span><br>  <span class="hljs-attr">pull_request:</span><br>    <span class="hljs-attr">branches:</span> [<span class="hljs-string">main</span>]<br><br><span class="hljs-attr">permissions:</span><br>  <span class="hljs-attr">contents:</span> <span class="hljs-string">read</span><br><br><span class="hljs-attr">jobs:</span><br>  <span class="hljs-attr">test:</span><br>    <span class="hljs-attr">runs-on:</span> <span class="hljs-string">macos-15</span><br>    <span class="hljs-attr">timeout-minutes:</span> <span class="hljs-number">30</span><br>    <span class="hljs-attr">steps:</span><br>      <span class="hljs-bullet">-</span> <span class="hljs-attr">uses:</span> <span class="hljs-string">actions/checkout@v4</span><br><br>      <span class="hljs-bullet">-</span> <span class="hljs-attr">uses:</span> <span class="hljs-string">maxim-lobanov/setup-xcode@v1</span><br>        <span class="hljs-attr">with:</span><br>          <span class="hljs-attr">xcode-version:</span> <span class="hljs-string">&#x27;26.3&#x27;</span><br><br>      <span class="hljs-bullet">-</span> <span class="hljs-attr">uses:</span> <span class="hljs-string">ruby/setup-ruby@v1</span><br>        <span class="hljs-attr">with:</span><br>          <span class="hljs-attr">ruby-version:</span> <span class="hljs-string">&#x27;3.3&#x27;</span><br>          <span class="hljs-attr">bundler-cache:</span> <span class="hljs-literal">true</span><br><br>      <span class="hljs-bullet">-</span> <span class="hljs-attr">name:</span> <span class="hljs-string">Check</span> <span class="hljs-string">project</span> <span class="hljs-string">integrity</span><br>        <span class="hljs-attr">run:</span> <span class="hljs-string">|</span><br><span class="hljs-string">          ruby -c fastlane/Fastfile</span><br><span class="hljs-string">          xcodebuild -list -project HabitPulse.xcodeproj</span><br><span class="hljs-string">          bundle exec fastlane ios metadata_validate</span><br><span class="hljs-string"></span><br>      <span class="hljs-bullet">-</span> <span class="hljs-attr">name:</span> <span class="hljs-string">Run</span> <span class="hljs-string">tests</span><br>        <span class="hljs-attr">run:</span> <span class="hljs-string">bundle</span> <span class="hljs-string">exec</span> <span class="hljs-string">fastlane</span> <span class="hljs-string">ios</span> <span class="hljs-string">test</span><br><br>      <span class="hljs-bullet">-</span> <span class="hljs-attr">name:</span> <span class="hljs-string">Upload</span> <span class="hljs-string">test</span> <span class="hljs-string">reports</span><br>        <span class="hljs-attr">if:</span> <span class="hljs-string">always()</span><br>        <span class="hljs-attr">uses:</span> <span class="hljs-string">actions/upload-artifact@v4</span><br>        <span class="hljs-attr">with:</span><br>          <span class="hljs-attr">name:</span> <span class="hljs-string">ios-pr-test-reports</span><br>          <span class="hljs-attr">if-no-files-found:</span> <span class="hljs-string">warn</span><br>          <span class="hljs-attr">path:</span> <span class="hljs-string">fastlane/test_output/</span><br></code></pre></td></tr></table></figure><p>有几个小点很值得保留：</p><ul><li><code>permissions: contents: read</code> 是最小权限。没有写入仓库、创建 Release 或读取 Secret 的权限；</li><li><code>timeout-minutes</code> 防止模拟器或依赖安装卡死，持续消耗 macOS Runner 时间；</li><li><code>if: always()</code> 保证测试失败后仍上传 <code>.xcresult</code>、JUnit 和日志。否则最需要排查材料的时候，反而什么都拿不到；</li><li><code>bundler-cache: true</code> 会缓存 Ruby gems，能明显减少重复安装 Fastlane 的时间。</li></ul><p>除了测试，HabitPulse 还执行 <code>ruby -c fastlane/Fastfile</code>、<code>xcodebuild -list</code> 和商店元数据校验。它们都很轻量，但能及早发现 Fastfile 写错、Xcode Scheme 丢失、上架文案文件缺失这些“测试未必能覆盖”的问题。</p><h2 id="五、主干归档：有签名，但不发布"><a href="#五、主干归档：有签名，但不发布" class="headerlink" title="五、主干归档：有签名，但不发布"></a>五、主干归档：有签名，但不发布</h2><p>一旦代码进入 <code>main</code>，HabitPulse 会自动生成 App Store 归档。这里的目标不是马上发布，而是确保主干在一个干净的 macOS 环境中<strong>真的能被签名并导出</strong>。</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">name:</span> <span class="hljs-string">iOS</span> <span class="hljs-string">Main</span> <span class="hljs-string">Archive</span><br><br><span class="hljs-attr">on:</span><br>  <span class="hljs-attr">push:</span><br>    <span class="hljs-attr">branches:</span> [<span class="hljs-string">main</span>]<br><br><span class="hljs-attr">permissions:</span><br>  <span class="hljs-attr">contents:</span> <span class="hljs-string">read</span><br><br><span class="hljs-attr">jobs:</span><br>  <span class="hljs-attr">archive:</span><br>    <span class="hljs-attr">runs-on:</span> <span class="hljs-string">macos-15</span><br>    <span class="hljs-attr">timeout-minutes:</span> <span class="hljs-number">45</span><br>    <span class="hljs-attr">environment:</span> <span class="hljs-string">appstore</span><br>    <span class="hljs-attr">steps:</span><br>      <span class="hljs-bullet">-</span> <span class="hljs-attr">uses:</span> <span class="hljs-string">actions/checkout@v4</span><br>      <span class="hljs-comment"># 此处省略 Xcode、Ruby 安装步骤</span><br>      <span class="hljs-bullet">-</span> <span class="hljs-attr">name:</span> <span class="hljs-string">Configure</span> <span class="hljs-string">SSH</span> <span class="hljs-string">for</span> <span class="hljs-string">match</span><br>        <span class="hljs-comment"># 将 MATCH_DEPLOY_KEY 写入 Runner 的临时文件</span><br>      <span class="hljs-bullet">-</span> <span class="hljs-attr">name:</span> <span class="hljs-string">Configure</span> <span class="hljs-string">temporary</span> <span class="hljs-string">keychain</span><br>        <span class="hljs-comment"># 创建、解锁临时钥匙串，并写入 MATCH_PASSWORD</span><br>      <span class="hljs-bullet">-</span> <span class="hljs-attr">name:</span> <span class="hljs-string">Archive</span> <span class="hljs-string">App</span> <span class="hljs-string">Store</span> <span class="hljs-string">build</span><br>        <span class="hljs-attr">run:</span> <span class="hljs-string">bundle</span> <span class="hljs-string">exec</span> <span class="hljs-string">fastlane</span> <span class="hljs-string">ios</span> <span class="hljs-string">habitpulse_archive_appstore</span><br>      <span class="hljs-bullet">-</span> <span class="hljs-attr">name:</span> <span class="hljs-string">Upload</span> <span class="hljs-string">archive</span> <span class="hljs-string">artifacts</span><br>        <span class="hljs-attr">if:</span> <span class="hljs-string">always()</span><br>        <span class="hljs-attr">uses:</span> <span class="hljs-string">actions/upload-artifact@v4</span><br></code></pre></td></tr></table></figure><h3 id="1-为什么要用-GitHub-Environment"><a href="#1-为什么要用-GitHub-Environment" class="headerlink" title="1. 为什么要用 GitHub Environment"></a>1. 为什么要用 GitHub Environment</h3><p>workflow 中的：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">environment:</span> <span class="hljs-string">appstore</span><br></code></pre></td></tr></table></figure><p>表示 Job 使用 GitHub 仓库的 <code>appstore</code> Environment。建议把发布相关 Secret 放在这里，而不是放在仓库级 Secrets：</p><ul><li>能把发布凭据和普通 CI 配置隔离开；</li><li>可为 Environment 配置 required reviewers，使发布 Job 必须经过审批；</li><li>后续有 staging、production 等不同环境时，扩展更自然；</li><li>GitHub Actions 页面能清楚看到某次 Job 使用了哪个环境。</li></ul><p>在 GitHub 仓库中依次进入：<code>Settings → Environments → New environment</code>，创建 <code>appstore</code>；再进入这个 Environment 的 Secrets 页面添加后文的五个 Secret。</p><h3 id="2-让-match-仓库可被-Runner-读取"><a href="#2-让-match-仓库可被-Runner-读取" class="headerlink" title="2. 让 match 仓库可被 Runner 读取"></a>2. 让 match 仓库可被 Runner 读取</h3><p>match 的证书仓库通常是私有仓库，Runner 需要一把专用 SSH Key 才能拉取它。workflow 会把密钥仅写入当前 Runner：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-bullet">-</span> <span class="hljs-attr">name:</span> <span class="hljs-string">Configure</span> <span class="hljs-string">SSH</span> <span class="hljs-string">for</span> <span class="hljs-string">match</span><br>  <span class="hljs-attr">env:</span><br>    <span class="hljs-attr">MATCH_DEPLOY_KEY:</span> <span class="hljs-string">$&#123;&#123;</span> <span class="hljs-string">secrets.MATCH_DEPLOY_KEY</span> <span class="hljs-string">&#125;&#125;</span><br>  <span class="hljs-attr">run:</span> <span class="hljs-string">|</span><br><span class="hljs-string">    install -d -m 700 ~/.ssh</span><br><span class="hljs-string">    printf &#x27;%s\n&#x27; &quot;$MATCH_DEPLOY_KEY&quot; &gt; ~/.ssh/match_deploy_key</span><br><span class="hljs-string">    chmod 600 ~/.ssh/match_deploy_key</span><br><span class="hljs-string">    ssh-keyscan github.com &gt;&gt; ~/.ssh/known_hosts</span><br><span class="hljs-string">    echo &#x27;GIT_SSH_COMMAND=ssh -i ~/.ssh/match_deploy_key -o IdentitiesOnly=yes&#x27; &gt;&gt; &quot;$GITHUB_ENV&quot;</span><br></code></pre></td></tr></table></figure><p>其中 <code>IdentitiesOnly=yes</code> 的作用是让 SSH 只尝试这一把部署密钥，避免 Runner 上其他身份造成不确定性。<code>ssh-keyscan</code> 是为了预先写入 GitHub 主机指纹，避免非交互式执行卡在“是否信任主机”。</p><h3 id="3-为什么还要临时-Keychain"><a href="#3-为什么还要临时-Keychain" class="headerlink" title="3. 为什么还要临时 Keychain"></a>3. 为什么还要临时 Keychain</h3><p>下载到 match 仓库的证书不是放到文件夹里就能用于 codesign。macOS 的签名工具需要从 Keychain 中找到证书和私钥。因此每个 Runner Job 建一个临时 Keychain：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-bullet">-</span> <span class="hljs-attr">name:</span> <span class="hljs-string">Configure</span> <span class="hljs-string">temporary</span> <span class="hljs-string">keychain</span><br>  <span class="hljs-attr">env:</span><br>    <span class="hljs-attr">MATCH_PASSWORD:</span> <span class="hljs-string">$&#123;&#123;</span> <span class="hljs-string">secrets.MATCH_PASSWORD</span> <span class="hljs-string">&#125;&#125;</span><br>  <span class="hljs-attr">run:</span> <span class="hljs-string">|</span><br><span class="hljs-string">    security create-keychain -p &quot;$MATCH_PASSWORD&quot; ci.keychain-db</span><br><span class="hljs-string">    security set-keychain-settings -lut 21600 ci.keychain-db</span><br><span class="hljs-string">    security unlock-keychain -p &quot;$MATCH_PASSWORD&quot; ci.keychain-db</span><br><span class="hljs-string">    security list-keychains -d user -s ci.keychain-db login.keychain-db</span><br><span class="hljs-string">    echo &quot;MATCH_PASSWORD=$MATCH_PASSWORD&quot; &gt;&gt; &quot;$GITHUB_ENV&quot;</span><br></code></pre></td></tr></table></figure><p>这里把 Keychain 密码直接复用为 <code>MATCH_PASSWORD</code> 只是实践中方便的一种做法，不是硬性要求。更重要的原则是：它只在当前临时 Runner 存在，结束时删除。</p><p>最后无论构建成功还是失败，都应清理：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-bullet">-</span> <span class="hljs-attr">name:</span> <span class="hljs-string">Remove</span> <span class="hljs-string">temporary</span> <span class="hljs-string">credentials</span><br>  <span class="hljs-attr">if:</span> <span class="hljs-string">always()</span><br>  <span class="hljs-attr">run:</span> <span class="hljs-string">|</span><br><span class="hljs-string">    security delete-keychain ci.keychain-db || true</span><br><span class="hljs-string">    rm -f ~/.ssh/match_deploy_key</span><br></code></pre></td></tr></table></figure><p>GitHub-hosted Runner 本身是临时的，但显式清理仍然值得保留：代码的意图更清楚，也便于未来迁移到 self-hosted Runner。</p><h3 id="4-最隐蔽的-Keychain-坑：创建了临时钥匙串，不代表-match-会用它"><a href="#4-最隐蔽的-Keychain-坑：创建了临时钥匙串，不代表-match-会用它" class="headerlink" title="4. 最隐蔽的 Keychain 坑：创建了临时钥匙串，不代表 match 会用它"></a>4. 最隐蔽的 Keychain 坑：创建了临时钥匙串，不代表 match 会用它</h3><p>这是一次真实踩坑：workflow 已经创建并解锁了 <code>ci.keychain-db</code>，但 Fastlane <code>match</code> 没有被显式告知该钥匙串名称和密码。于是 match 退回使用 <code>login.keychain</code>。证书和 Profile 看起来都“安装成功”，但 match 无法为登录钥匙串中的私钥设置 partition list，日志会出现类似内容：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs text">Keychain password for .../login.keychain-db was not specified<br>Could not configure imported keychain item ... to prevent UI permission popup<br></code></pre></td></tr></table></figure><p>随后 <code>codesign</code> 在签名 App 或 Extension（本例是 Widget）时等待钥匙串权限弹窗；GitHub-hosted Runner 没有桌面可点击，进程会一直挂起，最后只留下误导性的 <code>The operation was canceled</code>，实际是 job 达到超时后被 Actions 终止。</p><p>修复的关键是把 Keychain 从 workflow 明确传递给 Fastlane，并由 <code>match</code> 显式使用它：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-bullet">-</span> <span class="hljs-attr">name:</span> <span class="hljs-string">Configure</span> <span class="hljs-string">temporary</span> <span class="hljs-string">keychain</span><br>  <span class="hljs-attr">run:</span> <span class="hljs-string">|</span><br><span class="hljs-string">    security create-keychain -p &quot;$MATCH_PASSWORD&quot; ci.keychain-db</span><br><span class="hljs-string">    security unlock-keychain -p &quot;$MATCH_PASSWORD&quot; ci.keychain-db</span><br><span class="hljs-string">    security list-keychains -d user -s ci.keychain-db login.keychain-db</span><br><span class="hljs-string">    echo &quot;MATCH_KEYCHAIN_NAME=ci.keychain-db&quot; &gt;&gt; &quot;$GITHUB_ENV&quot;</span><br><span class="hljs-string">    echo &quot;MATCH_KEYCHAIN_PASSWORD=$MATCH_PASSWORD&quot; &gt;&gt; &quot;$GITHUB_ENV&quot;</span><br></code></pre></td></tr></table></figure><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs ruby">match(<br>  <span class="hljs-symbol">type:</span> <span class="hljs-string">&quot;appstore&quot;</span>,<br>  <span class="hljs-symbol">app_identifier:</span> <span class="hljs-variable constant_">APP_IDENTIFIERS</span>,<br>  <span class="hljs-symbol">readonly:</span> <span class="hljs-literal">true</span>,<br>  <span class="hljs-symbol">keychain_name:</span> <span class="hljs-variable constant_">ENV</span>.fetch(<span class="hljs-string">&quot;MATCH_KEYCHAIN_NAME&quot;</span>),<br>  <span class="hljs-symbol">keychain_password:</span> <span class="hljs-variable constant_">ENV</span>.fetch(<span class="hljs-string">&quot;MATCH_KEYCHAIN_PASSWORD&quot;</span>)<br>)<br></code></pre></td></tr></table></figure><p>本机运行时不必强行复用 CI 临时钥匙串。一个实用做法是：只有环境变量存在时才传这两个参数；本机保留 match 默认的登录钥匙串，CI 则固定使用 <code>ci.keychain-db</code>。排查签名卡死时，优先看 match 最终摘要里的 <code>keychain_name</code>，并确认它与 workflow 创建的名称完全一致。</p><h2 id="六、手动发布：把“可能产生外部影响”的动作锁起来"><a href="#六、手动发布：把“可能产生外部影响”的动作锁起来" class="headerlink" title="六、手动发布：把“可能产生外部影响”的动作锁起来"></a>六、手动发布：把“可能产生外部影响”的动作锁起来</h2><p>HabitPulse 的手动工作流只接受两个输入：选择 <code>beta</code> 或 <code>release</code>，再决定是否确认正式审核。</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">on:</span><br>  <span class="hljs-attr">workflow_dispatch:</span><br>    <span class="hljs-attr">inputs:</span><br>      <span class="hljs-attr">lane:</span><br>        <span class="hljs-attr">description:</span> <span class="hljs-string">&#x27;选择发布动作&#x27;</span><br>        <span class="hljs-attr">required:</span> <span class="hljs-literal">true</span><br>        <span class="hljs-attr">type:</span> <span class="hljs-string">choice</span><br>        <span class="hljs-attr">options:</span> [<span class="hljs-string">beta</span>, <span class="hljs-string">release</span>]<br>      <span class="hljs-attr">confirm_release:</span><br>        <span class="hljs-attr">description:</span> <span class="hljs-string">&#x27;仅正式审核选择 true；会上传并提交审核，不会自动公开发布&#x27;</span><br>        <span class="hljs-attr">required:</span> <span class="hljs-literal">true</span><br>        <span class="hljs-attr">default:</span> <span class="hljs-literal">false</span><br>        <span class="hljs-attr">type:</span> <span class="hljs-string">boolean</span><br></code></pre></td></tr></table></figure><p>这里分两层保护：</p><ol><li>GitHub Actions UI 要求你明确选择；</li><li>Fastlane 的 <code>release</code> lane 还会检查 <code>CONFIRM_RELEASE=true</code>。</li></ol><p>也就是说，不能因为某次提交推到了 <code>main</code>，就自动把版本提交给 App Review。对于面向用户的应用，这种显式确认非常有价值。</p><h3 id="beta-与-release-的区别"><a href="#beta-与-release-的区别" class="headerlink" title="beta 与 release 的区别"></a>beta 与 release 的区别</h3><table><thead><tr><th>选择</th><th>最终动作</th><th>使用时机</th></tr></thead><tbody><tr><td><code>beta</code></td><td>Archive、验证、上传到 TestFlight；默认不分发外部测试</td><td>给内部人员或 TestFlight 测试</td></tr><tr><td><code>release</code> + <code>confirm_release=true</code></td><td>校验 tag、测试、Archive、precheck、上传二进制和元数据、提交审核</td><td>TestFlight 验证完成后</td></tr></tbody></table><p>HabitPulse 的正式 release 还固定了 <code>automatic_release: false</code>。这意味着审核通过也不会自动公开到 App Store，仍由开发者在 App Store Connect 中手动发布。这是我很推荐的默认值：少一次意外，就少一次深夜救火。</p><h3 id="把-App-Store-Connect-的-p8-写到临时目录"><a href="#把-App-Store-Connect-的-p8-写到临时目录" class="headerlink" title="把 App Store Connect 的 .p8 写到临时目录"></a>把 App Store Connect 的 <code>.p8</code> 写到临时目录</h3><p>上传 TestFlight 与提交审核需要 App Store Connect API Key。<code>.p8</code> 是私钥，不能提交到仓库，也不应该放在固定工作路径。正确方式是从 GitHub Secret 恢复到 <code>$RUNNER_TEMP</code>：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-bullet">-</span> <span class="hljs-attr">name:</span> <span class="hljs-string">Configure</span> <span class="hljs-string">App</span> <span class="hljs-string">Store</span> <span class="hljs-string">Connect</span> <span class="hljs-string">credentials</span><br>  <span class="hljs-attr">env:</span><br>    <span class="hljs-attr">ASC_KEY:</span> <span class="hljs-string">$&#123;&#123;</span> <span class="hljs-string">secrets.ASC_KEY_P8</span> <span class="hljs-string">&#125;&#125;</span><br>    <span class="hljs-attr">ASC_KEY_ID:</span> <span class="hljs-string">$&#123;&#123;</span> <span class="hljs-string">secrets.ASC_KEY_ID</span> <span class="hljs-string">&#125;&#125;</span><br>    <span class="hljs-attr">ASC_ISSUER_ID:</span> <span class="hljs-string">$&#123;&#123;</span> <span class="hljs-string">secrets.ASC_ISSUER_ID</span> <span class="hljs-string">&#125;&#125;</span><br>  <span class="hljs-attr">run:</span> <span class="hljs-string">|</span><br><span class="hljs-string">    install -d -m 700 &quot;$RUNNER_TEMP/appstore&quot;</span><br><span class="hljs-string">    printf &#x27;%s\n&#x27; &quot;$ASC_KEY&quot; &gt; &quot;$RUNNER_TEMP/appstore/AuthKey_$&#123;ASC_KEY_ID&#125;.p8&quot;</span><br><span class="hljs-string">    chmod 600 &quot;$RUNNER_TEMP/appstore/AuthKey_$&#123;ASC_KEY_ID&#125;.p8&quot;</span><br><span class="hljs-string">    echo &quot;ASC_KEY_FILEPATH=$RUNNER_TEMP/appstore/AuthKey_$&#123;ASC_KEY_ID&#125;.p8&quot; &gt;&gt; &quot;$GITHUB_ENV&quot;</span><br><span class="hljs-string">    echo &quot;ASC_KEY_ID=$ASC_KEY_ID&quot; &gt;&gt; &quot;$GITHUB_ENV&quot;</span><br><span class="hljs-string">    echo &quot;ASC_ISSUER_ID=$ASC_ISSUER_ID&quot; &gt;&gt; &quot;$GITHUB_ENV&quot;</span><br></code></pre></td></tr></table></figure><p>Fastlane 则只从环境变量读取这三个值，并检查 Key ID 格式、Issuer ID 格式、文件是否存在以及文件权限是否过宽。换句话说：YAML 负责“把 Secret 安全地交给当前 Job”，Fastlane 负责“确认这些凭据看起来可用”。</p><p>真实项目里临时目录叫 <code>dayvu</code> 还是 <code>appstore</code> 并不重要；重要的是它在 <code>$RUNNER_TEMP</code> 下，不被提交、上传或复用。</p><h2 id="七、五个-Secret-到底从哪里来"><a href="#七、五个-Secret-到底从哪里来" class="headerlink" title="七、五个 Secret 到底从哪里来"></a>七、五个 Secret 到底从哪里来</h2><p>HabitPulse 的 <code>appstore</code> Environment 使用下面五个 Secret：</p><table><thead><tr><th>Secret</th><th>内容</th><th>获取方式</th></tr></thead><tbody><tr><td><code>MATCH_DEPLOY_KEY</code></td><td>读取 match 证书仓库的 SSH 私钥全文</td><td>为 CI 单独生成 SSH key；把公钥作为证书仓库的 Deploy key（只读）或机器账户的 SSH key</td></tr><tr><td><code>MATCH_PASSWORD</code></td><td>match 加密密码</td><td>初始化 match 时设定的密码；不是 Apple ID 密码</td></tr><tr><td><code>ASC_KEY_ID</code></td><td>App Store Connect API Key 的 Key ID</td><td>App Store Connect → Users and Access → Integrations → Keys</td></tr><tr><td><code>ASC_ISSUER_ID</code></td><td>App Store Connect API 的 Issuer ID</td><td>同一 Keys 页面顶部</td></tr><tr><td><code>ASC_KEY_P8</code></td><td><code>.p8</code> 私钥完整文本</td><td>创建 API Key 时下载；通常只能下载一次，务必安全保存</td></tr></tbody></table><h3 id="MATCH-DEPLOY-KEY-的推荐创建方式"><a href="#MATCH-DEPLOY-KEY-的推荐创建方式" class="headerlink" title="MATCH_DEPLOY_KEY 的推荐创建方式"></a><code>MATCH_DEPLOY_KEY</code> 的推荐创建方式</h3><p>不要把自己的个人 SSH 私钥直接塞进 CI。单独生成一把只给 CI 使用的 key：</p><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs sh">ssh-keygen -t ed25519 -C <span class="hljs-string">&quot;github-actions-match&quot;</span> -f ./github-actions-match<br></code></pre></td></tr></table></figure><p>随后：</p><ol><li>将 <code>github-actions-match.pub</code> 添加到<strong>证书仓库</strong>的 Deploy keys；勾选只读即可；</li><li>将没有 <code>.pub</code> 后缀的私钥文件完整内容添加为 <code>MATCH_DEPLOY_KEY</code>；</li><li>不要把这两个文件提交到任何仓库；用完从当前目录删除或移到密码管理器。</li></ol><p>私钥内容应包含开头和结尾，例如：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs text">-----BEGIN OPENSSH PRIVATE KEY-----<br>...多行密钥内容...<br>-----END OPENSSH PRIVATE KEY-----<br></code></pre></td></tr></table></figure><h3 id="App-Store-Connect-API-Key-的权限"><a href="#App-Store-Connect-API-Key-的权限" class="headerlink" title="App Store Connect API Key 的权限"></a>App Store Connect API Key 的权限</h3><p>在 App Store Connect 创建 API Key 时，按实际操作授予最小权限。若 CI 需要上传 TestFlight 和提交审核，通常需要 App Manager 或能覆盖发布流程的适当角色；具体权限名称会随 Apple 后台演进。创建后立刻记录 Key ID、Issuer ID，并把 <code>.p8</code> 放进密码管理器。</p><p>有两个坑特别常见：</p><ul><li><code>.p8</code> 一般只能下载一次。丢了不能重新下载同一把，需要撤销并新建 Key；</li><li>GitHub Secret 必须保存 <code>.p8</code> 的<strong>完整多行内容</strong>，不是文件路径，也不是 Base64（除非你同时修改 workflow 进行解码）。</li></ul><h3 id="不要把-Secret-放错位置"><a href="#不要把-Secret-放错位置" class="headerlink" title="不要把 Secret 放错位置"></a>不要把 Secret 放错位置</h3><p>如果 workflow 写的是：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">environment:</span> <span class="hljs-string">appstore</span><br></code></pre></td></tr></table></figure><p>那么最直观、也最符合本项目权限模型的做法，是把 Secret 放在 <code>Settings → Environments → appstore → Environment secrets</code>。如果你只放在仓库级 Secrets，可能也能通过 <code>secrets.xxx</code> 读取，但会失去 Environment 审批和隔离的意义。</p><h2 id="八、Artifact-是-CI-的“事故现场”"><a href="#八、Artifact-是-CI-的“事故现场”" class="headerlink" title="八、Artifact 是 CI 的“事故现场”"></a>八、Artifact 是 CI 的“事故现场”</h2><p>CI 构建不应该只给你一个绿色或红色的小图标。发生失败时，最重要的是能拿到足够材料定位问题。</p><p>HabitPulse 会上传：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">path:</span> <span class="hljs-string">|</span><br><span class="hljs-string">  fastlane/build_output/ios/habitpulse/appstore/</span><br><span class="hljs-string">  fastlane/test_output/</span><br><span class="hljs-string">  build/*.log</span><br></code></pre></td></tr></table></figure><p>其中通常包含：</p><ul><li><code>.ipa</code>：可安装或提交的打包产物；</li><li><code>.xcarchive</code>：用于检查签名、dSYM 和归档结构；</li><li><code>.dSYM</code>：将来符号化崩溃日志需要；</li><li><code>.xcresult</code>：Xcode 测试报告，可在 Xcode 中打开；</li><li>JUnit 与 xcodebuild 日志：适合快速查 CI 报错。</li></ul><p>注意，Artifact 不等于长期制品仓库。它适合排错和短期下载；如果你有保留周期、访问控制或合规要求，应在仓库设置中明确配置 retention，或使用专门的制品存储方案。</p><h2 id="九、从零配置到第一次成功的建议顺序"><a href="#九、从零配置到第一次成功的建议顺序" class="headerlink" title="九、从零配置到第一次成功的建议顺序"></a>九、从零配置到第一次成功的建议顺序</h2><p>下面是一个比较稳的实施顺序。每一步只增加一点复杂度，出问题时也更容易定位。</p><ol><li>本机执行 <code>bundle exec fastlane ios test</code>，直到稳定通过；</li><li>本机执行 <code>bundle exec fastlane ios habitpulse_archive_appstore</code>，确认主 App 与所有 Extension 都能签名；</li><li>提交 <code>pr.yml</code>，创建一个指向 <code>main</code> 的 PR，确认无 Secret 的测试工作流通过；</li><li>创建 GitHub <code>appstore</code> Environment，添加 <code>MATCH_DEPLOY_KEY</code> 和 <code>MATCH_PASSWORD</code>；</li><li>合并一个小改动到 <code>main</code>，确认 <code>main-archive.yml</code> 能完成 archive，并能下载 Artifact；</li><li>再添加 <code>ASC_KEY_ID</code>、<code>ASC_ISSUER_ID</code>、<code>ASC_KEY_P8</code>；</li><li>在 Actions 中手动运行 <code>iOS Manual Release</code>，选择 <code>beta</code>，确认 TestFlight 出现新构建；</li><li>TestFlight 验证完成后，准备正确的版本 tag，再人工运行 <code>release</code> 并勾选确认；</li><li>到 App Store Connect 检查构建、元数据、审核信息，审核通过后按自己的发布节奏手动公开。</li></ol><p>如果你刚开始搭建，强烈建议先跑 <code>beta</code>。不要把“第一次验证 API Key、match 和 CI 环境”的风险，和“提交 App Review”绑在同一次执行中。</p><h2 id="十、几个真实踩过的坑"><a href="#十、几个真实踩过的坑" class="headerlink" title="十、几个真实踩过的坑"></a>十、几个真实踩过的坑</h2><h3 id="1-以为-push-到-main-会跑-PR-流程"><a href="#1-以为-push-到-main-会跑-PR-流程" class="headerlink" title="1. 以为 push 到 main 会跑 PR 流程"></a>1. 以为 push 到 main 会跑 PR 流程</h3><p>不会。<code>pull_request</code> 与 <code>push</code> 是不同事件。直接命令行合并并推送到 <code>main</code>，只会触发监听 <code>push</code> 的主干归档 workflow。要保证 PR 检查不被绕过，使用 GitHub 分支保护规则。</p><h3 id="2-把所有动作塞进-main-的自动工作流"><a href="#2-把所有动作塞进-main-的自动工作流" class="headerlink" title="2. 把所有动作塞进 main 的自动工作流"></a>2. 把所有动作塞进 main 的自动工作流</h3><p>这是最危险的设计。主干自动归档很合理，自动上传 TestFlight 已经需要更谨慎，自动提交审核通常不值得。把发布留给 <code>workflow_dispatch</code>，并额外要求 <code>confirm_release</code>，能避免许多可预防的问题。</p><h3 id="3-CI-里运行了-match，却没有临时-Keychain"><a href="#3-CI-里运行了-match，却没有临时-Keychain" class="headerlink" title="3. CI 里运行了 match，却没有临时 Keychain"></a>3. CI 里运行了 <code>match</code>，却没有临时 Keychain</h3><p>这样可能下载证书成功，但 <code>codesign</code> 找不到私钥或无法访问证书。match、Keychain 和 Profile 是一条链，缺一个都可能在 Archive 阶段失败。</p><h3 id="4-忘记-Extension-的-Bundle-ID"><a href="#4-忘记-Extension-的-Bundle-ID" class="headerlink" title="4. 忘记 Extension 的 Bundle ID"></a>4. 忘记 Extension 的 Bundle ID</h3><p>iOS App 只要有 Widget、Share Extension、Notification Service Extension 等目标，就不能只配置主 App 的 Bundle ID。match、Apple Developer 后台的 App ID 和 Profile 都要覆盖所有需要签名的 Target。</p><h3 id="5-让-CI-以写模式运行-match"><a href="#5-让-CI-以写模式运行-match" class="headerlink" title="5. 让 CI 以写模式运行 match"></a>5. 让 CI 以写模式运行 match</h3><p>CI 不该承担创建证书或 Profile 的责任。应使用 <code>readonly: true</code>。如果签名材料过期或缺失，让构建明确失败，然后由有权限的开发者在受控的本机环境处理。</p><h3 id="6-测试失败后没有报告"><a href="#6-测试失败后没有报告" class="headerlink" title="6. 测试失败后没有报告"></a>6. 测试失败后没有报告</h3><p>没有 <code>if: always()</code> 时，失败步骤会跳过 Artifact 上传；最终你只能看到一句“Process completed with exit code 65”。保留 xcresult 和完整日志，会让排查效率有非常明显的差别。</p><h3 id="7-把-p8、-p12-或-SSH-私钥提交进仓库"><a href="#7-把-p8、-p12-或-SSH-私钥提交进仓库" class="headerlink" title="7. 把 .p8、.p12 或 SSH 私钥提交进仓库"></a>7. 把 <code>.p8</code>、<code>.p12</code> 或 SSH 私钥提交进仓库</h3><p>这不是“之后再删掉就好”的问题。Git 历史里出现过的私钥应视为泄露，即使后来删除也应立即撤销并换新。正确位置是 GitHub Secret、密码管理器或专门的密钥管理系统。</p><h3 id="8-没有检查-Runner-的-Xcode-与模拟器-Runtime"><a href="#8-没有检查-Runner-的-Xcode-与模拟器-Runtime" class="headerlink" title="8. 没有检查 Runner 的 Xcode 与模拟器 Runtime"></a>8. 没有检查 Runner 的 Xcode 与模拟器 Runtime</h3><p><code>macos-15</code> 不代表一定存在你随手写下的每个 Xcode 或 iOS Runtime。把 Xcode 版本、测试设备和 Runtime 当作一个整体维护；如果升级 Xcode，先在 PR workflow 验证。</p><h2 id="十一、可以直接复用的最小工作流组合"><a href="#十一、可以直接复用的最小工作流组合" class="headerlink" title="十一、可以直接复用的最小工作流组合"></a>十一、可以直接复用的最小工作流组合</h2><p>如果你暂时不做 App Store 发布，可以只从 PR workflow 开始；如果需要正式签名归档和 TestFlight，则按本文的三段式结构扩展。</p><p>最小但完整的职责图是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs text">Fastlane<br>├── test                       ← 模拟器测试、报告<br>├── archive_appstore           ← match readonly、Archive、导出 IPA<br>├── beta                       ← 调用 archive 后上传 TestFlight<br>└── release                    ← 额外检查与人工确认后提交审核<br><br>GitHub Actions<br>├── pr.yml                     ← 只调用 test<br>├── main-archive.yml           ← 只调用 archive_appstore<br>└── manual-release.yml         ← 只允许人工调用 beta / release<br></code></pre></td></tr></table></figure><p>它的核心不是“自动化得越多越好”，而是把自动化放到合适的位置：高频、低风险的检查自动做；低频、会对外产生影响的发布动作由人明确确认。</p><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>给 iOS 项目接 GitHub Actions，真正的完成标准不是“看到一次绿色勾”，而是下面几件事都成立：</p><ul><li>PR 能在不接触发布凭据的前提下稳定验证代码；</li><li><code>main</code> 能在干净环境中稳定签名、归档并留下可下载的证据；</li><li>发布凭据被隔离在 Environment Secret 中，且只在需要它们的 Job 出现；</li><li>TestFlight 和 App Store 审核不会因为一次普通 push 被误触发；</li><li>开发者能在本地用同一条 Fastlane 命令复现 CI 的关键动作。</li></ul><p>做到这些之后，GitHub Actions 才不只是“替你点了构建按钮”，而是成为一条可靠、可审计、能让团队放心交付的 iOS 工程流水线。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;如何将-iOS-项目接入-GitHub-Action&quot;&gt;&lt;a href=&quot;#如何将-iOS-项目接入-GitHub-Action&quot; class=&quot;headerlink&quot; title=&quot;如何将 iOS 项目接入 GitHub Action&quot;&gt;&lt;/a&gt;如何将 iOS </summary>
      
    
    
    
    
    <category term="iOS" scheme="https://flametinary.github.io/tags/iOS/"/>
    
    <category term="Fastlane" scheme="https://flametinary.github.io/tags/Fastlane/"/>
    
    <category term="TestFlight" scheme="https://flametinary.github.io/tags/TestFlight/"/>
    
    <category term="GitHub-Actions" scheme="https://flametinary.github.io/tags/GitHub-Actions/"/>
    
    <category term="CI-CD" scheme="https://flametinary.github.io/tags/CI-CD/"/>
    
  </entry>
  
  <entry>
    <title>桌面组件 Widget 开发实战：HabitPulse 从选型到落地</title>
    <link href="https://flametinary.github.io/2026/09/01/%E6%A1%8C%E9%9D%A2%E7%BB%84%E4%BB%B6Widget%E5%BC%80%E5%8F%91%E5%AE%9E%E6%88%98%EF%BC%9AHabitPulse%E4%BB%8E%E9%80%89%E5%9E%8B%E5%88%B0%E8%90%BD%E5%9C%B0/"/>
    <id>https://flametinary.github.io/2026/09/01/%E6%A1%8C%E9%9D%A2%E7%BB%84%E4%BB%B6Widget%E5%BC%80%E5%8F%91%E5%AE%9E%E6%88%98%EF%BC%9AHabitPulse%E4%BB%8E%E9%80%89%E5%9E%8B%E5%88%B0%E8%90%BD%E5%9C%B0/</id>
    <published>2026-08-31T16:00:00.000Z</published>
    <updated>2026-09-12T03:37:07.567Z</updated>
    
    <content type="html"><![CDATA[<h1 id="桌面组件-Widget-开发实战：HabitPulse-从选型到落地"><a href="#桌面组件-Widget-开发实战：HabitPulse-从选型到落地" class="headerlink" title="桌面组件 Widget 开发实战：HabitPulse 从选型到落地"></a>桌面组件 Widget 开发实战：HabitPulse 从选型到落地</h1><blockquote><p>以习惯打卡 App <strong>HabitPulse</strong>（Swift 6 + SwiftUI + SwiftData + CloudKit，手动 <code>.xcodeproj</code>）为例，系统复盘「今日打卡进度」桌面组件从<strong>方案选型 → 工程接入 → 开发者中心配置 → 真机踩坑</strong>的完整过程。</p><p>如果你只想看结论：本项目最终采用 <strong>「App Group + JSON 快照 + 深链」</strong> 方案，主 app 写文件、Widget 读文件、点击 Widget 深链回主 app，全程不碰 Widget 内的写入与 SwiftData 直连。</p></blockquote><hr><h2 id="0-背景与目标"><a href="#0-背景与目标" class="headerlink" title="0. 背景与目标"></a>0. 背景与目标</h2><p>HabitPulse 是一个习惯打卡应用，核心数据模型：</p><ul><li><code>Habit</code>：习惯（名称、图标、颜色、频率规则、是否归档）</li><li><code>HabitCompletion</code>：打卡记录（日期、状态 done&#x2F;skip、关联 habitID）</li></ul><p>我们想做一个<strong>桌面组件（Widget）</strong>，在手机主屏直接展示「今日待打卡习惯 + 完成进度」，并且点一下能跳回 App 的「今日」页面去打卡。</p><p>技术栈有一个关键约束：<strong>数据层是 SwiftData + CloudKit 同步</strong>。</p><ul><li>SwiftData 底层是 SQLite；开启 CloudKit 后，本地还有一个 <strong>CloudKit 镜像库（<code>.sqlite</code> + <code>.sqlite-wal</code>）</strong>。</li><li>Widget 是<strong>独立进程（App Extension）</strong>，与主 app 不在同一个进程空间。</li></ul><p>这个约束直接决定了 Widget 能不能「直连数据库」——下面第一节展开。</p><hr><h2 id="一、Widget-的几种接入方式对比"><a href="#一、Widget-的几种接入方式对比" class="headerlink" title="一、Widget 的几种接入方式对比"></a>一、Widget 的几种接入方式对比</h2><p>WidgetKit 扩展要拿到主 app 的数据，业界主流有三条路：</p><h3 id="方案-A：App-Group-共享-SwiftData-直连"><a href="#方案-A：App-Group-共享-SwiftData-直连" class="headerlink" title="方案 A：App Group + 共享 SwiftData 直连"></a>方案 A：App Group + 共享 SwiftData 直连</h3><p>让 Widget Extension 直接 <code>ModelContainer(for: schema, ...)</code> 打开<strong>主 app 同一个 SwiftData store</strong>（store 放到 App Group 容器里，两个进程共享同一份 <code>.sqlite</code>）。</p><figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs swift"><span class="hljs-comment">// Widget 侧（设想）</span><br><span class="hljs-keyword">let</span> config <span class="hljs-operator">=</span> <span class="hljs-type">ModelConfiguration</span>(<br>    schema: schema,<br>    url: <span class="hljs-type">AppGroupContainer</span>.appGroupSQLITEURL   <span class="hljs-comment">// 与主 app 同一路径</span><br>)<br><span class="hljs-keyword">let</span> container <span class="hljs-operator">=</span> <span class="hljs-keyword">try</span> <span class="hljs-type">ModelContainer</span>(for: schema, configurations: [config])<br></code></pre></td></tr></table></figure><h3 id="方案-B：App-Group-JSON-快照-深链（本项目采用）"><a href="#方案-B：App-Group-JSON-快照-深链（本项目采用）" class="headerlink" title="方案 B：App Group + JSON 快照 + 深链（本项目采用）"></a>方案 B：App Group + JSON 快照 + 深链（本项目采用）</h3><p>主 app 是唯一写入者：在合适的时机把「今日进度」序列化成 <code>widget_snapshot.json</code> 写到 App Group 容器。Widget 只<strong>读</strong>这个 JSON 渲染，并通过 <code>widgetURL</code> 深链把点击转给主 app。</p><figure class="highlight brainfuck"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs brainfuck"><span class="hljs-comment">主 app (进程1)</span>  <span class="hljs-literal">--</span><span class="hljs-comment">写</span><span class="hljs-literal">--</span>&gt;  <span class="hljs-comment">App Group/widget_snapshot</span><span class="hljs-string">.</span><span class="hljs-comment">json</span>  &lt;<span class="hljs-literal">--</span><span class="hljs-comment">读</span><span class="hljs-literal">--</span>  <span class="hljs-comment">Widget (进程2)</span><br>                                  <span class="hljs-comment">│</span><br>                          <span class="hljs-comment">WidgetCenter</span><span class="hljs-string">.</span><span class="hljs-comment">reloadAllTimelines()</span><br>                                  <span class="hljs-comment">│</span><br>                    <span class="hljs-comment">widgetURL(habitpulse://today) 点击 ──</span>&gt; <span class="hljs-comment">主 app 今日 Tab</span><br></code></pre></td></tr></table></figure><h3 id="方案-C：App-Intents-可交互-Widget（Widget-内直接打卡）"><a href="#方案-C：App-Intents-可交互-Widget（Widget-内直接打卡）" class="headerlink" title="方案 C：App Intents + 可交互 Widget（Widget 内直接打卡）"></a>方案 C：App Intents + 可交互 Widget（Widget 内直接打卡）</h3><p>Widget 上放一个按钮，用户点了直接在 Widget 进程里完成打卡（通过 <code>AppIntent</code>&#x2F;<code>TimelineProvider</code> 的 <code>intent</code> 配置）。写入仍需落到共享存储（方案 A 或 B 的存储之上再叠一套写回机制）。</p><h3 id="三者对比"><a href="#三者对比" class="headerlink" title="三者对比"></a>三者对比</h3><table><thead><tr><th>维度</th><th>A. 共享 SwiftData 直连</th><th>B. JSON 快照 + 深链（采用）</th><th>C. Widget 内可交互打卡</th></tr></thead><tbody><tr><td><strong>多进程并发安全</strong></td><td>⚠️ 差：两进程同时开同一 SQLite，CloudKit 镜像库易锁&#x2F;崩溃</td><td>✅ 好：单一写入者，Widget 只读文件</td><td>⚠️ 中：Widget 也变写入者，需额外同步</td></tr><tr><td><strong>是否需要迁移 store</strong></td><td>⚠️ 需把默认 store 迁到 App Group 容器，可能清空&#x2F;复杂</td><td>✅ 不需动现有 SwiftData</td><td>⚠️ 同 A 的存储问题</td></tr><tr><td><strong>数据实时性</strong></td><td>✅ 最高（直接读库）</td><td>✅ 高（写入即 <code>reloadAllTimelines</code>）</td><td>✅ 高</td></tr><tr><td><strong>CloudKit 冲突风险</strong></td><td>❌ 高：扩展误改镜像库会污染同步</td><td>✅ 无：扩展从不碰 SwiftData</td><td>❌ 高</td></tr><tr><td><strong>实现复杂度</strong></td><td>中（但后期坑多）</td><td>中（契约要双方对齐）</td><td>高（Intent + 写回 + 冲突）</td></tr><tr><td><strong>打卡交互位置</strong></td><td>取决于是否在 Widget 内加按钮</td><td>点击深链回主 app 打卡</td><td>Widget 内直接打卡</td></tr><tr><td><strong>适合本项目的程度</strong></td><td>不适合（CloudKit 多进程锁）</td><td>✅ 最适合</td><td>过度设计（v1 不需要 Widget 内打卡）</td></tr></tbody></table><hr><h2 id="二、为什么选方案-B（JSON-快照-深链）"><a href="#二、为什么选方案-B（JSON-快照-深链）" class="headerlink" title="二、为什么选方案 B（JSON 快照 + 深链）"></a>二、为什么选方案 B（JSON 快照 + 深链）</h2><p>一句话：<strong>在「SwiftData + CloudKit」项目里，让 Widget 直连数据库是高风险动作；让主 app 独占写入、Widget 只读快照，把并发冲突面降到最低。</strong></p><p>具体原因：</p><h3 id="1-规避多进程-SQLite-锁（最核心）"><a href="#1-规避多进程-SQLite-锁（最核心）" class="headerlink" title="1. 规避多进程 SQLite 锁（最核心）"></a>1. 规避多进程 SQLite 锁（最核心）</h3><p>CloudKit + SwiftData 的本地镜像库，本质是多个 SQLite 文件。主 app 运行时会持续持有并写入它（本地改动 → 触发 CloudKit 上传）。如果 Widget 进程也用同一份 store 打开：</p><ul><li>两个进程各自持锁，轻则 <code>database is locked</code>，重则镜像库损坏、CloudKit 同步异常。</li><li>Widget 进程生命周期由系统管理（主屏 daemon 拉起&#x2F;挂起），无法与主 app 协调事务。</li></ul><blockquote><p>业界竞品（如 Kado 等习惯类 App）的成熟做法也是：<strong>扩展不直开 SwiftData，改走 JSON 快照</strong>。我们用同样的思路。</p></blockquote><h3 id="2-不迁移-store，避免清空用户数据"><a href="#2-不迁移-store，避免清空用户数据" class="headerlink" title="2. 不迁移 store，避免清空用户数据"></a>2. 不迁移 store，避免清空用户数据</h3><p>方案 A 要把 SwiftData 默认 store 迁到 App Group 容器才能共享。迁移过程一旦出错（路径、版本、CloudKit 重连），<strong>可能清空本地数据</strong>。对已经上线、有真实用户数据的 App 这是不可接受的。方案 B 完全不碰 store。</p><h3 id="3-单一写入者原则"><a href="#3-单一写入者原则" class="headerlink" title="3. 单一写入者原则"></a>3. 单一写入者原则</h3><p>只有主 app 持有 <code>ModelContext</code> 并负责写入。Widget 是纯消费者，读一份 JSON 即可。并发模型从「双写者竞争」降级为「单写者 + 多读者」，简单且可靠。</p><h3 id="4-打卡放在主-app，而非-Widget-内"><a href="#4-打卡放在主-app，而非-Widget-内" class="headerlink" title="4. 打卡放在主 app，而非 Widget 内"></a>4. 打卡放在主 app，而非 Widget 内</h3><p>打卡是<strong>写操作</strong>。如果放 Widget 里（方案 C），Widget 既要读又要写，又回到多进程写入的坑；而且「今日进度」这种聚合数据在 Widget 里重算也很重。本项目 v1 的策略是：<strong>Widget 只展示 + 引导</strong>，点击 → 深链回主 app 今日 Tab 完成打卡。职责清晰。</p><h3 id="5-JSON-契约解耦"><a href="#5-JSON-契约解耦" class="headerlink" title="5. JSON 契约解耦"></a>5. JSON 契约解耦</h3><p>主 app 内部模型随便演进，只要持续输出一份稳定结构的 <code>widget_snapshot.json</code>，Widget 就不需要跟着改。两侧通过 JSON 字段契约解耦。</p><hr><h2 id="三、方案-B-的具体接入步骤"><a href="#三、方案-B-的具体接入步骤" class="headerlink" title="三、方案 B 的具体接入步骤"></a>三、方案 B 的具体接入步骤</h2><h3 id="3-1-主-app-侧：快照写入服务-WidgetSnapshotService"><a href="#3-1-主-app-侧：快照写入服务-WidgetSnapshotService" class="headerlink" title="3.1 主 app 侧：快照写入服务 WidgetSnapshotService"></a>3.1 主 app 侧：快照写入服务 <code>WidgetSnapshotService</code></h3><p>核心职责：取今日数据 → 算进度 → 写 App Group JSON → <strong>通知 WidgetKit 刷新</strong>。</p><figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br></pre></td><td class="code"><pre><code class="hljs swift"><span class="hljs-keyword">import</span> Foundation<br><span class="hljs-keyword">import</span> SwiftData<br><span class="hljs-keyword">import</span> os<br><span class="hljs-keyword">import</span> WidgetKit<br><br><span class="hljs-keyword">enum</span> <span class="hljs-title class_">WidgetSnapshotService</span> &#123;<br>    <span class="hljs-keyword">static</span> <span class="hljs-keyword">let</span> appGroupID <span class="hljs-operator">=</span> <span class="hljs-string">&quot;group.com.sheldon.HabitPulse&quot;</span><br>    <span class="hljs-keyword">static</span> <span class="hljs-keyword">let</span> fileName <span class="hljs-operator">=</span> <span class="hljs-string">&quot;widget_snapshot.json&quot;</span><br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">let</span> logger <span class="hljs-operator">=</span> <span class="hljs-type">Logger</span>(<br>        subsystem: <span class="hljs-string">&quot;com.sheldon.HabitPulse&quot;</span>,<br>        category: <span class="hljs-string">&quot;widget-snapshot&quot;</span><br>    )<br><br>    <span class="hljs-comment">/// 刷新并写入今日快照。须在持有主 app ModelContext 的主线程调用。</span><br>    <span class="hljs-meta">@MainActor</span><br>    <span class="hljs-keyword">static</span> <span class="hljs-keyword">func</span> <span class="hljs-title function_">refresh</span>(<span class="hljs-params">using</span> <span class="hljs-params">context</span>: <span class="hljs-type">ModelContext</span>) &#123;<br>        <span class="hljs-keyword">let</span> snapshot <span class="hljs-operator">=</span> buildSnapshot(using: context)<br>        write(snapshot)<br>        <span class="hljs-comment">// ⚠️ 关键：跨进程同步完成后必须显式 reload，</span><br>        <span class="hljs-comment">// 否则 WidgetKit 不会主动重读 App Group 文件。</span><br>        <span class="hljs-type">WidgetCenter</span>.shared.reloadAllTimelines()<br>    &#125;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">func</span> <span class="hljs-title function_">buildSnapshot</span>(<span class="hljs-params">using</span> <span class="hljs-params">context</span>: <span class="hljs-type">ModelContext</span>) -&gt; <span class="hljs-type">WidgetSnapshot</span> &#123;<br>        <span class="hljs-keyword">let</span> habits <span class="hljs-operator">=</span> (<span class="hljs-keyword">try?</span> context.fetch(<span class="hljs-type">FetchDescriptor</span>&lt;<span class="hljs-type">Habit</span>&gt;(<br>            predicate: #<span class="hljs-type">Predicate</span> &#123; <span class="hljs-operator">!</span><span class="hljs-variable">$0</span>.isArchived &#125;<br>        ))) <span class="hljs-operator">??</span> []<br>        <span class="hljs-keyword">let</span> completions <span class="hljs-operator">=</span> (<span class="hljs-keyword">try?</span> context.fetch(<span class="hljs-type">FetchDescriptor</span>&lt;<span class="hljs-type">HabitCompletion</span>&gt;())) <span class="hljs-operator">??</span> []<br>        <span class="hljs-keyword">let</span> today <span class="hljs-operator">=</span> <span class="hljs-type">Calendar</span>.current.startOfDay(for: <span class="hljs-type">Date</span>())<br>        <span class="hljs-keyword">let</span> todayDone <span class="hljs-operator">=</span> completions.filter &#123;<br>            <span class="hljs-variable">$0</span>.status <span class="hljs-operator">==</span> .done <span class="hljs-operator">&amp;&amp;</span> <span class="hljs-type">Calendar</span>.current.isDate(<span class="hljs-variable">$0</span>.date, inSameDayAs: today)<br>        &#125;<br><br>        <span class="hljs-keyword">var</span> overallDone <span class="hljs-operator">=</span> <span class="hljs-number">0</span>, overallTarget <span class="hljs-operator">=</span> <span class="hljs-number">0</span><br>        <span class="hljs-keyword">var</span> items: [<span class="hljs-type">WidgetHabitItem</span>] <span class="hljs-operator">=</span> []<br><br>        <span class="hljs-keyword">for</span> habit <span class="hljs-keyword">in</span> habits &#123;<br>            <span class="hljs-keyword">guard</span> <span class="hljs-type">FrequencyEvaluator</span>.isDue(on: <span class="hljs-type">Date</span>(), habit: habit) <span class="hljs-keyword">else</span> &#123; <span class="hljs-keyword">continue</span> &#125;<br>            <span class="hljs-keyword">let</span> target <span class="hljs-operator">=</span> <span class="hljs-type">FrequencyEvaluator</span>.targetPerDay(habit: habit)<br>            <span class="hljs-keyword">let</span> done <span class="hljs-operator">=</span> todayDone.filter &#123; <span class="hljs-variable">$0</span>.habitID <span class="hljs-operator">==</span> habit.id &#125;.count<br>            <span class="hljs-keyword">let</span> fully <span class="hljs-operator">=</span> done <span class="hljs-operator">&gt;=</span> target<br>            <span class="hljs-keyword">if</span> fully &#123; overallDone <span class="hljs-operator">+=</span> <span class="hljs-number">1</span> &#125;<br>            overallTarget <span class="hljs-operator">+=</span> <span class="hljs-number">1</span><br>            items.append(<span class="hljs-type">WidgetHabitItem</span>(<br>                id: habit.id.uuidString, name: habit.name, icon: habit.icon,<br>                colorHex: habit.colorHex, targetPerDay: target,<br>                doneCount: done, isFullyDone: fully<br>            ))<br>        &#125;<br>        <span class="hljs-comment">// 未完成排前、已完成排后，名称字典序稳定排序。</span><br>        items.sort &#123;<br>            <span class="hljs-keyword">if</span> <span class="hljs-variable">$0</span>.isFullyDone <span class="hljs-operator">==</span> <span class="hljs-variable">$1</span>.isFullyDone &#123; <span class="hljs-keyword">return</span> <span class="hljs-variable">$0</span>.name <span class="hljs-operator">&lt;</span> <span class="hljs-variable">$1</span>.name &#125;<br>            <span class="hljs-keyword">return</span> <span class="hljs-operator">!</span><span class="hljs-variable">$0</span>.isFullyDone<br>        &#125;<br>        <span class="hljs-keyword">return</span> <span class="hljs-type">WidgetSnapshot</span>(<br>            date: <span class="hljs-type">ISO8601DateFormatter</span>().string(from: <span class="hljs-type">Date</span>()),<br>            overallDone: overallDone, overallTarget: overallTarget, habits: items<br>        )<br>    &#125;<br><br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">func</span> <span class="hljs-title function_">write</span>(<span class="hljs-keyword">_</span> <span class="hljs-params">snapshot</span>: <span class="hljs-type">WidgetSnapshot</span>) &#123;<br>        <span class="hljs-keyword">guard</span> <span class="hljs-keyword">let</span> container <span class="hljs-operator">=</span> <span class="hljs-type">FileManager</span>.default<br>            .containerURL(forSecurityApplicationGroupIdentifier: appGroupID) <span class="hljs-keyword">else</span> &#123;<br>            <span class="hljs-comment">// 真机 containerURL 为 nil = entitlements 声明了 App Group</span><br>            <span class="hljs-comment">// 但描述文件(profile)没该能力——模拟器不校验所以测不出。</span><br>            logger.error(<span class="hljs-string">&quot;App Group 容器不可用：profile 缺能力或 ID 不一致&quot;</span>)<br>            <span class="hljs-keyword">return</span><br>        &#125;<br>        <span class="hljs-keyword">let</span> fileURL <span class="hljs-operator">=</span> container.appendingPathComponent(fileName)<br>        <span class="hljs-keyword">do</span> &#123;<br>            <span class="hljs-keyword">let</span> data <span class="hljs-operator">=</span> <span class="hljs-keyword">try</span> <span class="hljs-type">JSONEncoder</span>().encode(snapshot)<br>            <span class="hljs-keyword">try</span> data.write(to: fileURL, options: [.atomic])<br>            logger.debug(<span class="hljs-string">&quot;快照写入成功：habits=<span class="hljs-subst">\(snapshot.habits.count)</span>&quot;</span>)<br>        &#125; <span class="hljs-keyword">catch</span> &#123;<br>            logger.error(<span class="hljs-string">&quot;快照写入失败：<span class="hljs-subst">\(String(describing: error))</span>&quot;</span>)<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-comment">/// 供 Widget 复用的 JSON 契约（主 app 编码、Widget 解码，字段须完全一致）。</span><br>    <span class="hljs-keyword">struct</span> <span class="hljs-title class_">WidgetSnapshot</span>: <span class="hljs-title class_ inherited__">Codable</span> &#123;<br>        <span class="hljs-keyword">let</span> date: <span class="hljs-type">String</span><br>        <span class="hljs-keyword">let</span> overallDone: <span class="hljs-type">Int</span><br>        <span class="hljs-keyword">let</span> overallTarget: <span class="hljs-type">Int</span><br>        <span class="hljs-keyword">let</span> habits: [<span class="hljs-type">WidgetHabitItem</span>]<br>    &#125;<br>    <span class="hljs-keyword">struct</span> <span class="hljs-title class_">WidgetHabitItem</span>: <span class="hljs-title class_ inherited__">Codable</span> &#123;<br>        <span class="hljs-keyword">let</span> id: <span class="hljs-type">String</span><br>        <span class="hljs-keyword">let</span> name: <span class="hljs-type">String</span><br>        <span class="hljs-keyword">let</span> icon: <span class="hljs-type">String</span><br>        <span class="hljs-keyword">let</span> colorHex: <span class="hljs-type">String</span><br>        <span class="hljs-keyword">let</span> targetPerDay: <span class="hljs-type">Int</span><br>        <span class="hljs-keyword">let</span> doneCount: <span class="hljs-type">Int</span><br>        <span class="hljs-keyword">let</span> isFullyDone: <span class="hljs-type">Bool</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><blockquote><p>注意：<code>WidgetSnapshot</code> &#x2F; <code>WidgetHabitItem</code> 在**主 app 和 Widget 两侧各定义一份相同字段的 <code>Codable</code>**（Widget 侧文件 <code>WidgetSnapshot.swift</code> 里也有一份）。这是 JSON 契约的两端，改动任一侧都要同步另一侧。</p></blockquote><h3 id="3-2-主-app-触发刷新的时机"><a href="#3-2-主-app-触发刷新的时机" class="headerlink" title="3.2 主 app 触发刷新的时机"></a>3.2 主 app 触发刷新的时机</h3><p>只在「写完文件」还不够，必须在用户会看到变化的地方触发 <code>refresh</code>：</p><ul><li><strong>App 启动时</strong>写首屏数据（否则首次添加 Widget 永远空）：</li></ul><figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs swift"><span class="hljs-comment">// HabitPulseApp.swift</span><br>.task &#123;<br>    <span class="hljs-type">WidgetSnapshotService</span>.refresh(using: modelContainer.mainContext)<br>&#125;<br>.onChange(of: scenePhase) &#123; <span class="hljs-keyword">_</span> <span class="hljs-keyword">in</span><br>    <span class="hljs-comment">// 回到前台：跨天 / 后台数据变更后，Widget 重新可见即更新。</span><br>    <span class="hljs-type">WidgetSnapshotService</span>.refresh(using: modelContainer.mainContext)<br>&#125;<br></code></pre></td></tr></table></figure><ul><li><strong>打卡 &#x2F; 跳过 &#x2F; 新建习惯后</strong>立即刷新（保证「动一下就同步」）：</li></ul><figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs swift"><span class="hljs-comment">// TodayViewModel.swift 的 persist() 末尾</span><br><span class="hljs-type">WidgetSnapshotService</span>.refresh(using: modelContext)<br></code></pre></td></tr></table></figure><h3 id="3-3-深链：点击-Widget-回「今日」Tab"><a href="#3-3-深链：点击-Widget-回「今日」Tab" class="headerlink" title="3.3 深链：点击 Widget 回「今日」Tab"></a>3.3 深链：点击 Widget 回「今日」Tab</h3><p>Widget 本身不打卡，点击 → <code>habitpulse://today</code> → 主 app 切到今日 Tab。</p><p><strong>(1) 主 app <code>Info.plist</code> 注册 URL Scheme：</strong></p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">key</span>&gt;</span>CFBundleURLTypes<span class="hljs-tag">&lt;/<span class="hljs-name">key</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">array</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">dict</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">key</span>&gt;</span>CFBundleURLSchemes<span class="hljs-tag">&lt;/<span class="hljs-name">key</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">array</span>&gt;</span><br>      <span class="hljs-tag">&lt;<span class="hljs-name">string</span>&gt;</span>habitpulse<span class="hljs-tag">&lt;/<span class="hljs-name">string</span>&gt;</span><br>    <span class="hljs-tag">&lt;/<span class="hljs-name">array</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">key</span>&gt;</span>CFBundleURLName<span class="hljs-tag">&lt;/<span class="hljs-name">key</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">string</span>&gt;</span>com.sheldon.HabitPulse<span class="hljs-tag">&lt;/<span class="hljs-name">string</span>&gt;</span><br>  <span class="hljs-tag">&lt;/<span class="hljs-name">dict</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">array</span>&gt;</span><br></code></pre></td></tr></table></figure><p><strong>(2) <code>HabitPulseApp</code> 处理 URL：</strong></p><figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs swift">.onOpenURL &#123; handleDeepLink(<span class="hljs-variable">$0</span>) &#125;<br><br><span class="hljs-keyword">private</span> <span class="hljs-keyword">func</span> <span class="hljs-title function_">handleDeepLink</span>(<span class="hljs-keyword">_</span> <span class="hljs-params">url</span>: <span class="hljs-type">URL</span>) &#123;<br>    <span class="hljs-keyword">guard</span> url.scheme <span class="hljs-operator">==</span> <span class="hljs-string">&quot;habitpulse&quot;</span> <span class="hljs-keyword">else</span> &#123; <span class="hljs-keyword">return</span> &#125;<br>    <span class="hljs-type">NotificationCenter</span>.default.post(name: .widgetDeepLinkToday, object: <span class="hljs-literal">nil</span>)<br>&#125;<br><br><span class="hljs-comment">// 通知名</span><br><span class="hljs-keyword">extension</span> <span class="hljs-title class_">Notification</span>.<span class="hljs-type">Name</span> &#123;<br>    <span class="hljs-keyword">static</span> <span class="hljs-keyword">let</span> widgetDeepLinkToday <span class="hljs-operator">=</span><br>        <span class="hljs-type">Notification</span>.<span class="hljs-type">Name</span>(<span class="hljs-string">&quot;com.sheldon.HabitPulse.deepLink.today&quot;</span>)<br>&#125;<br></code></pre></td></tr></table></figure><p><strong>(3) <code>RootTabView</code> 用 <code>selection</code> 接收并切 Tab：</strong></p><figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs swift"><span class="hljs-keyword">struct</span> <span class="hljs-title class_">RootTabView</span>: <span class="hljs-title class_ inherited__">View</span> &#123;<br>    <span class="hljs-meta">@State</span> <span class="hljs-keyword">private</span> <span class="hljs-keyword">var</span> selectedTab: <span class="hljs-type">MainTab</span> <span class="hljs-operator">=</span> .today<br>    <span class="hljs-keyword">var</span> body: <span class="hljs-keyword">some</span> <span class="hljs-type">View</span> &#123;<br>        <span class="hljs-type">TabView</span>(selection: <span class="hljs-variable">$selectedTab</span>) &#123;<br>            <span class="hljs-type">TodayView</span>().tag(<span class="hljs-type">MainTab</span>.today)<br>                .tabItem &#123; <span class="hljs-type">Label</span> &#123; <span class="hljs-type">LText</span>(<span class="hljs-string">&quot;tab.today&quot;</span>) &#125; icon: &#123; <span class="hljs-type">Image</span>(systemName: <span class="hljs-string">&quot;sun.max.fill&quot;</span>) &#125; &#125;<br>            <span class="hljs-comment">// ... 其余 3 个 Tab</span><br>        &#125;<br>        .onReceive(<span class="hljs-type">NotificationCenter</span>.default.publisher(for: .widgetDeepLinkToday)) &#123; <span class="hljs-keyword">_</span> <span class="hljs-keyword">in</span><br>            selectedTab <span class="hljs-operator">=</span> .today<br>        &#125;<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">enum</span> <span class="hljs-title class_">MainTab</span>: <span class="hljs-title class_ inherited__">Hashable</span> &#123; <span class="hljs-keyword">case</span> today, calendar, stats, me &#125;<br></code></pre></td></tr></table></figure><h3 id="3-4-Widget-Extension-Target-的工程接入"><a href="#3-4-Widget-Extension-Target-的工程接入" class="headerlink" title="3.4 Widget Extension Target 的工程接入"></a>3.4 Widget Extension Target 的工程接入</h3><p>本项目是<strong>手动维护的 <code>.xcodeproj</code><strong>，没有用 Xcode 的「Editor → Add Target → Widget Extension」向导，所以 target 是</strong>手写进 <code>project.pbxproj</code></strong> 的（UUID 前缀统一用 <code>2B</code> 区隔主 app 的 <code>1A</code>）。需要补齐以下段落：</p><ul><li><code>PBXBuildFile</code>：每个源文件一条 <code>in Sources</code>，外加一条 <code>Embed App Extensions</code> 的 copy</li><li><code>PBXFileReference</code>：5 个 swift 源 + <code>Info.plist</code> + <code>.entitlements</code> + 最终的 <code>.appex</code></li><li><code>PBXGroup</code>：新建 <code>HabitPulseWidget</code> 分组，挂到主 <code>mainGroup</code></li><li><code>PBXNativeTarget</code>：<code>productType = com.apple.product-type.app-extension</code></li><li><code>PBXSourcesBuildPhase</code> &#x2F; <code>PBXFrameworksBuildPhase</code> &#x2F; <code>PBXResourcesBuildPhase</code></li><li><code>PBXCopyFilesBuildPhase</code>：Embed App Extensions（<code>dstSubfolderSpec = 13</code>），把 <code>.appex</code> 拷进主 app 的 <code>PlugIns/</code></li><li><code>PBXTargetDependency</code> + <code>PBXContainerItemProxy</code>：主 app 依赖 Widget</li><li><code>XCConfigurationList</code> + 两个 <code>XCBuildConfiguration</code>（Debug&#x2F;Release）</li></ul><p>关键构建设置（<code>Debug</code> 为例）：</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs ruby"><span class="hljs-variable constant_">CODE_SIGN_STYLE</span> = <span class="hljs-title class_">Manual</span><br><span class="hljs-variable constant_">CODE_SIGN_IDENTITY</span> = <span class="hljs-title class_">Apple</span> <span class="hljs-title class_">Development</span><br><span class="hljs-variable constant_">DEVELOPMENT_TEAM</span> = <span class="hljs-variable constant_">CJ5L2XW65B</span><br><span class="hljs-variable constant_">PRODUCT_BUNDLE_IDENTIFIER</span> = com.sheldon.<span class="hljs-title class_">HabitPulse</span>.<span class="hljs-title class_">Widget</span><br><span class="hljs-variable constant_">INFOPLIST_FILE</span> = <span class="hljs-title class_">HabitPulseWidget</span>/<span class="hljs-title class_">Info</span>.plist<br><span class="hljs-variable constant_">CODE_SIGN_ENTITLEMENTS</span> = <span class="hljs-title class_">HabitPulseWidget</span>/<span class="hljs-title class_">HabitPulseWidget</span>.entitlements<br><span class="hljs-variable constant_">SWIFT_VERSION</span> = <span class="hljs-number">6.0</span><br><span class="hljs-variable constant_">SWIFT_STRICT_CONCURRENCY</span> = complete<br><span class="hljs-variable constant_">IPHONEOS_DEPLOYMENT_TARGET</span> = <span class="hljs-number">17.6</span><br><span class="hljs-variable constant_">GENERATE_INFOPLIST_FILE</span> = <span class="hljs-variable constant_">NO</span><br></code></pre></td></tr></table></figure><p>主 app 的 <code>PBXCopyFilesBuildPhase</code> 里加入 <code>Embed App Extensions</code>，<code>dependencies</code> 加入对 Widget 的 <code>PBXTargetDependency</code>——这样 <code>.appex</code> 才会被打进主 app 包。</p><blockquote><p>⚠️ 本项目用 Swift 6 严格并发（<code>SWIFT_STRICT_CONCURRENCY = complete</code>），Widget 源码也要继承同样的并发约束，否则主 app 能过、Widget target 单独编不过。</p></blockquote><h3 id="3-5-Widget-侧代码（5-个文件-Bundle）"><a href="#3-5-Widget-侧代码（5-个文件-Bundle）" class="headerlink" title="3.5 Widget 侧代码（5 个文件 + Bundle）"></a>3.5 Widget 侧代码（5 个文件 + Bundle）</h3><ul><li>**<code>WidgetSnapshot.swift</code>**：与主 app 同字段的 <code>WidgetSnapshot</code>&#x2F;<code>WidgetHabitItem</code> <code>Codable</code> + <code>WidgetSnapshotStore</code>（读 App Group JSON 解码，失败回退 <code>.placeholder</code>）。</li><li>**<code>HabitWidgetProvider.swift</code>**：<code>TimelineProvider</code><ul><li><code>placeholder</code> &#x2F; <code>getSnapshot</code>：直接读 JSON（预览&#x2F;首屏）</li><li><code>getTimeline</code>：每小时生成一个刷新点作为<strong>兜底</strong>（即使主 app 没主动 reload，最多 1 小时也会自己刷新一次）</li></ul></li><li>**<code>WidgetViews.swift</code>**：独立 <code>Color(widgetHex:)</code> + 14 色调色板（复制自主 app）、<code>WidgetProgressRing</code> 进度环、<code>HabitWidgetEntryView</code>（small&#x2F;medium&#x2F;large 三尺寸；medium 显示前 3 条、large 全量）</li><li>**<code>HabitPulseWidget.swift</code>**：</li></ul><figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs swift"><span class="hljs-keyword">@main</span>   <span class="hljs-comment">// 注意：Widget 的 @main 在 Bundle 文件，这里不要重复</span><br><span class="hljs-keyword">struct</span> <span class="hljs-title class_">HabitPulseWidget</span>: <span class="hljs-title class_ inherited__">Widget</span> &#123;<br>    <span class="hljs-keyword">let</span> kind <span class="hljs-operator">=</span> <span class="hljs-string">&quot;HabitPulseWidget&quot;</span><br>    <span class="hljs-keyword">var</span> body: <span class="hljs-keyword">some</span> <span class="hljs-type">WidgetConfiguration</span> &#123;<br>        <span class="hljs-type">StaticConfiguration</span>(kind: kind, provider: <span class="hljs-type">HabitWidgetProvider</span>()) &#123; entry <span class="hljs-keyword">in</span><br>            <span class="hljs-type">HabitWidgetEntryView</span>(entry: entry)<br>        &#125;<br>        .configurationDisplayName(<span class="hljs-string">&quot;今日打卡&quot;</span>)<br>        .description(<span class="hljs-string">&quot;查看今日习惯完成进度&quot;</span>)<br>        .widgetURL(<span class="hljs-type">URL</span>(string: <span class="hljs-string">&quot;habitpulse://today&quot;</span>))   <span class="hljs-comment">// 点击深链</span><br>        .supportedFamilies([.systemSmall, .systemMedium, .systemLarge])<br>        .containerBackground(<span class="hljs-type">Color</span>(.systemBackground), for: .widget)<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><ul><li>**<code>HabitPulseWidgetBundle.swift</code>**：</li></ul><figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs swift"><span class="hljs-keyword">@main</span><br><span class="hljs-keyword">struct</span> <span class="hljs-title class_">HabitPulseWidgetBundle</span>: <span class="hljs-title class_ inherited__">WidgetBundle</span> &#123;<br>    <span class="hljs-keyword">var</span> body: <span class="hljs-keyword">some</span> <span class="hljs-type">Widget</span> &#123; <span class="hljs-type">HabitPulseWidget</span>() &#125;<br>&#125;<br></code></pre></td></tr></table></figure><ul><li>**<code>Info.plist</code>**：必须含 <code>NSExtensionPointIdentifier = com.apple.widgetkit-extension</code></li><li>**<code>HabitPulseWidget.entitlements</code>**：声明 <code>com.apple.security.application-groups</code> 含 <code>group.com.sheldon.HabitPulse</code></li></ul><h3 id="3-6-App-Group-容器共享"><a href="#3-6-App-Group-容器共享" class="headerlink" title="3.6 App Group 容器共享"></a>3.6 App Group 容器共享</h3><p>主 app 与 Widget 的 entitlements 都声明同一个 App Group：</p><figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs stylus">group<span class="hljs-selector-class">.com</span><span class="hljs-selector-class">.sheldon</span>.HabitPulse<br></code></pre></td></tr></table></figure><ul><li>主 app 写：<code>FileManager.default.containerURL(forSecurityApplicationGroupIdentifier: &quot;group.com.sheldon.HabitPulse&quot;)</code></li><li>Widget 读：同一句，拼 <code>widget_snapshot.json</code></li></ul><hr><h2 id="四、开发者中心（Apple-Developer-Portal）配置要点"><a href="#四、开发者中心（Apple-Developer-Portal）配置要点" class="headerlink" title="四、开发者中心（Apple Developer Portal）配置要点"></a>四、开发者中心（Apple Developer Portal）配置要点</h2><p>这一块是真机才能暴露的坑——<strong>模拟器不强制校验描述文件 &#x2F; entitlements</strong>，所以本地编译全过、模拟器能跑，但真机装完「找不到 Widget」十有八九是这里没配。</p><h3 id="4-1-注册-Widget-的-App-ID"><a href="#4-1-注册-Widget-的-App-ID" class="headerlink" title="4.1 注册 Widget 的 App ID"></a>4.1 注册 Widget 的 App ID</h3><ol><li>开发者中心 → Identifiers → <strong>新建 App ID</strong></li><li><strong>类型选「App」（不是 App Clip）</strong>：Widget Extension 复用宿主 App 的签名证书与 App Group，本质是普通 App ID。</li><li><strong>Bundle ID 类型选「Explicit」（显式）</strong>，填 <code>com.sheldon.HabitPulse.Widget</code>。<strong>不要用 Wildcard（<code>*</code>）</strong>——通配 App ID 不能启用 App Groups，而 Widget 强依赖它。</li><li><strong>Capabilities 勾选 App Groups</strong>，并在其配置里关联 <code>group.com.sheldon.HabitPulse</code>（主 app 已注册过的那个 group）。</li></ol><h3 id="4-2-App-Groups-注册与关联（若未注册）"><a href="#4-2-App-Groups-注册与关联（若未注册）" class="headerlink" title="4.2 App Groups 注册与关联（若未注册）"></a>4.2 App Groups 注册与关联（若未注册）</h3><p>左侧 <strong>App Groups</strong> 里确认 <code>group.com.sheldon.HabitPulse</code> 存在，并把它关联到<strong>主 app 和 Widget 两个 App ID</strong>。</p><h3 id="4-3-Fastfile-改造（纳入-Widget-identifier）"><a href="#4-3-Fastfile-改造（纳入-Widget-identifier）" class="headerlink" title="4.3 Fastfile 改造（纳入 Widget identifier）"></a>4.3 Fastfile 改造（纳入 Widget identifier）</h3><p>本项目的签名走 <code>fastlane match</code>。原 lane 的 <code>app_identifier</code> 只写了主 app，必须把 Widget 一起纳进来，否则 <code>match</code> 不会生成 Widget 的 profile：</p><figure class="highlight ruby"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><code class="hljs ruby">desc <span class="hljs-string">&quot;habitpulse 开发证书（含 Widget 扩展）&quot;</span><br>lane <span class="hljs-symbol">:habitpulse_dev</span> <span class="hljs-keyword">do</span><br>  match(<br>    <span class="hljs-symbol">type:</span> <span class="hljs-string">&quot;development&quot;</span>,<br>    <span class="hljs-symbol">app_identifier:</span> [<span class="hljs-string">&quot;com.sheldon.HabitPulse&quot;</span>, <span class="hljs-string">&quot;com.sheldon.HabitPulse.Widget&quot;</span>],<br>    <span class="hljs-symbol">git_url:</span> <span class="hljs-variable constant_">MATCH_GIT_URL</span>,<br>    <span class="hljs-symbol">git_branch:</span> <span class="hljs-variable constant_">MATCH_GIT_BRANCH</span>,<br>  )<br><span class="hljs-keyword">end</span><br><br>desc <span class="hljs-string">&quot;habitpulse AppStore证书（含 Widget 扩展）&quot;</span><br>lane <span class="hljs-symbol">:habitpulse_appstore</span> <span class="hljs-keyword">do</span><br>  match(<br>    <span class="hljs-symbol">type:</span> <span class="hljs-string">&quot;appstore&quot;</span>,<br>    <span class="hljs-symbol">app_identifier:</span> [<span class="hljs-string">&quot;com.sheldon.HabitPulse&quot;</span>, <span class="hljs-string">&quot;com.sheldon.HabitPulse.Widget&quot;</span>],<br>    <span class="hljs-symbol">git_url:</span> <span class="hljs-variable constant_">MATCH_GIT_URL</span>,<br>    <span class="hljs-symbol">git_branch:</span> <span class="hljs-variable constant_">MATCH_GIT_BRANCH</span>,<br>  )<br><span class="hljs-keyword">end</span><br></code></pre></td></tr></table></figure><h3 id="4-4-match-重签（不需要-force）"><a href="#4-4-match-重签（不需要-force）" class="headerlink" title="4.4 match 重签（不需要 --force）"></a>4.4 match 重签（不需要 <code>--force</code>）</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">fastlane habitpulse_dev        <span class="hljs-comment"># 生成 match Development com.sheldon.HabitPulse.Widget</span><br>fastlane habitpulse_appstore   <span class="hljs-comment"># 仅打 Release/IPA 时需要</span><br></code></pre></td></tr></table></figure><blockquote><p><strong>为什么不用 <code>--force</code><strong>：你这次是给</strong>全新 App ID</strong> 出 profile，第一次创建就是干净的，会直接把刚在门户启用的 App Groups 能力打进描述文件。只有「<strong>已存在 App ID 后来才加能力</strong>」时才需 <code>--force</code> 强制重签。先配能力再出 profile，省掉 <code>--force</code>。</p></blockquote><h3 id="4-5-Xcode-签名对齐"><a href="#4-5-Xcode-签名对齐" class="headerlink" title="4.5 Xcode 签名对齐"></a>4.5 Xcode 签名对齐</h3><p>Widget target → Build Settings → Signing：</p><ul><li><code>Code Signing Style</code> → <strong>Manual</strong></li><li>Debug → <code>Provisioning Profile</code> &#x3D; <code>match Development com.sheldon.HabitPulse.Widget</code></li><li>Release → <code>Provisioning Profile</code> &#x3D; <code>match AppStore com.sheldon.HabitPulse.Widget</code></li></ul><p><strong>关键</strong>：主 app 与 Widget 的签名方式必须一致（本项目都用 <code>match</code> Manual）。如果 Widget 是 Automatic、主 app 是 Manual，真机上 Widget 的 App Group 能力可能没正确预置，扩展在启动访问 <code>containerURL</code> 时被系统拒绝，从而被<strong>悄悄排除在组件库之外</strong>（表现就是「app 能跑、Widget 找不到」）。</p><h3 id="4-6-真机安装后"><a href="#4-6-真机安装后" class="headerlink" title="4.6 真机安装后"></a>4.6 真机安装后</h3><p>装完<strong>重启一次手机</strong>——Widget daemon 偶发不刷新，重启能强制重新索引组件。</p><hr><h2 id="五、开发过程中遇到的实际问题-解决方案"><a href="#五、开发过程中遇到的实际问题-解决方案" class="headerlink" title="五、开发过程中遇到的实际问题 &amp; 解决方案"></a>五、开发过程中遇到的实际问题 &amp; 解决方案</h2><h3 id="问题-1：Cannot-find-WidgetSnapshotService-in-scope"><a href="#问题-1：Cannot-find-WidgetSnapshotService-in-scope" class="headerlink" title="问题 1：Cannot find &#39;WidgetSnapshotService&#39; in scope"></a>问题 1：<code>Cannot find &#39;WidgetSnapshotService&#39; in scope</code></h3><p><strong>现象</strong>：编译失败，报错找不到主 app 新建的快照服务。<br><strong>根因</strong>：<code>WidgetSnapshotService.swift</code> 文件建了，却<strong>漏加进主 app target</strong>（没在 <code>project.pbxproj</code> 里登记 <code>PBXBuildFile</code>&#x2F;<code>PBXFileReference</code>&#x2F;<code>Sources build phase</code>）。<br><strong>解决</strong>：补齐 4 处 pbxproj 引用（<code>PBXBuildFile</code> + <code>PBXFileReference</code> + <code>Core</code> group + 主 app <code>Sources</code> build phase），用新 UUID（<code>1A…00C2</code>&#x2F;<code>00C3</code>）避免冲突。</p><h3 id="问题-2：元组-Bool-String-不能用"><a href="#问题-2：元组-Bool-String-不能用" class="headerlink" title="问题 2：元组 (Bool, String) 不能用 &lt; 比较"></a>问题 2：元组 <code>(Bool, String)</code> 不能用 <code>&lt;</code> 比较</h3><p><strong>现象</strong>：原排序 <code>items.sort { (!$0.isFullyDone, $0.name) &lt; (!$1.isFullyDone, $1.name) }</code> 编译报错。<br><strong>根因</strong>：Swift 不允许对 <code>(Bool, String)</code> 这类异质元组用 <code>&lt;</code> 直接比较。<br><strong>解决</strong>：改成显式比较器：</p><figure class="highlight swift"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs swift">items.sort &#123;<br>    <span class="hljs-keyword">if</span> <span class="hljs-variable">$0</span>.isFullyDone <span class="hljs-operator">==</span> <span class="hljs-variable">$1</span>.isFullyDone &#123; <span class="hljs-keyword">return</span> <span class="hljs-variable">$0</span>.name <span class="hljs-operator">&lt;</span> <span class="hljs-variable">$1</span>.name &#125;<br>    <span class="hljs-keyword">return</span> <span class="hljs-operator">!</span><span class="hljs-variable">$0</span>.isFullyDone   <span class="hljs-comment">// 未完成排前</span><br>&#125;<br></code></pre></td></tr></table></figure><h3 id="问题-3（最隐蔽）：Widget-真机显示「今日-0-0-暂无打卡计划」，但-app-里明明有数据"><a href="#问题-3（最隐蔽）：Widget-真机显示「今日-0-0-暂无打卡计划」，但-app-里明明有数据" class="headerlink" title="问题 3（最隐蔽）：Widget 真机显示「今日 0&#x2F;0 暂无打卡计划」，但 app 里明明有数据"></a>问题 3（最隐蔽）：Widget 真机显示「今日 0&#x2F;0 暂无打卡计划」，但 app 里明明有数据</h3><p><strong>现象</strong>：主屏 Widget 一直空，app 内新建&#x2F;打卡后 Widget 也不更新。<br><strong>根因</strong>：主 app 把 JSON 写到 App Group 后，**没有调用 <code>WidgetCenter.shared.reloadAllTimelines()</code>**。WidgetKit 不会主动监测 App Group 文件变化，唯一的跨进程刷新手段就是 <code>reloadAllTimelines</code>。Widget 里的「每小时兜底刷新」对「刚加完 Widget」的用户完全无感。<br><strong>为什么模拟器没暴露</strong>：模拟器测试时我直接去 App Group 容器里读 <code>widget_snapshot.json</code> 验证写入成功，根本没验证 Widget 进程实际有没有拿到——写入成功 ≠ Widget 收到了。<br><strong>解决</strong>：</p><ul><li><code>WidgetSnapshotService.refresh</code> 末尾加 <code>WidgetCenter.shared.reloadAllTimelines()</code>（<code>@MainActor</code>，写在已是 MainActor 的方法末尾无并发问题）。</li><li>顺手给 <code>write</code> 加 <code>os.Logger</code>（subsystem <code>com.sheldon.HabitPulse</code>，category <code>widget-snapshot</code>），把「containerURL 为 nil &#x2F; 写入失败」从<strong>静默吞掉</strong>改成可观测——以后在 Mac 的 <strong>Console.app</strong> 过滤该 subsystem 就能定位。</li></ul><h3 id="问题-4：签名不一致（Widget-Automatic-vs-主-app-match-Manual）"><a href="#问题-4：签名不一致（Widget-Automatic-vs-主-app-match-Manual）" class="headerlink" title="问题 4：签名不一致（Widget Automatic vs 主 app match Manual）"></a>问题 4：签名不一致（Widget Automatic vs 主 app match Manual）</h3><p><strong>现象</strong>：真机装了 app 却找不到 Widget。<br><strong>根因</strong>：主 app 用 <code>match</code> 手动描述文件，Widget 一开始是 Automatic 签名，两者签名链不同；Widget 的 App Group 能力在真机可能没正确预置 → 扩展加载被拒 → 不进组件库。<br><strong>解决</strong>：Widget target 切到 <code>match</code> Manual，Debug&#x2F;Release 对齐主 app（见第四节 4.5）。本机确认两份 profile 已生成：</p><ul><li><code>match Development com.sheldon.HabitPulse.Widget</code></li><li><code>match AppStore com.sheldon.HabitPulse.Widget</code></li></ul><h3 id="问题-5：模拟器「能编能嵌」≠-真机「能显示」"><a href="#问题-5：模拟器「能编能嵌」≠-真机「能显示」" class="headerlink" title="问题 5：模拟器「能编能嵌」≠ 真机「能显示」"></a>问题 5：模拟器「能编能嵌」≠ 真机「能显示」</h3><p><strong>教训</strong>：模拟器<strong>不强制校验描述文件 &#x2F; entitlements &#x2F; App Group 能力</strong>。任何签名或能力缺失在模拟器上都「看起来正常」，只有真机才暴露。所以 <strong>Widget 类功能一定要上真机验证</strong>，不能只信模拟器编译通过。</p><h3 id="问题-6（附带）：git-push-一直-502-CONNECT-tunnel-failed"><a href="#问题-6（附带）：git-push-一直-502-CONNECT-tunnel-failed" class="headerlink" title="问题 6（附带）：git push 一直 502 CONNECT tunnel failed"></a>问题 6（附带）：<code>git push</code> 一直 <code>502 CONNECT tunnel failed</code></h3><p><strong>现象</strong>：push 到 GitHub 反复失败，报代理 502；直连又 <code>Couldn&#39;t connect to github.com:443</code>。<br><strong>根因排查</strong>：</p><ul><li>用户网络<strong>必须走代理</strong>才能上 GitHub（直连 443 不通）。</li><li>但 shell 环境被注入了全局代理 <code>HTTP_PROXY/HTTPS_PROXY/http_proxy/https_proxy = http://127.0.0.1:52383</code>（WorkBuddy&#x2F;CodeBuddy 的透明代理），它当前对 GitHub 上游返回 502。</li><li>第一次我显式用 <code>HTTP_PROXY=http://127.0.0.1:7897</code> 覆盖了<strong>大写</strong>变量，但<strong>小写</strong> <code>http_proxy/https_proxy</code> 仍指向 52383，git 实际走了那个坏的代理。</li><li>实测 <code>127.0.0.1:7897</code> 才是可用的本地代理（curl 经它访问 GitHub 返回 200）。<br><strong>解决</strong>：把大小写四个变量<strong>全部统一</strong>指向 7897 再 push：</li></ul><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">HTTP_PROXY=http://127.0.0.1:7897 HTTPS_PROXY=http://127.0.0.1:7897 \<br>http_proxy=http://127.0.0.1:7897 https_proxy=http://127.0.0.1:7897 \<br>git push origin main<br></code></pre></td></tr></table></figure><blockquote><p>经验：<strong>覆盖代理环境变量时一定要大小写一起覆盖</strong>，否则 git&#x2F;curl 可能仍读到你没清掉的那个。</p></blockquote><hr><h2 id="六、验证清单（交付前自检）"><a href="#六、验证清单（交付前自检）" class="headerlink" title="六、验证清单（交付前自检）"></a>六、验证清单（交付前自检）</h2><table><thead><tr><th>项</th><th>命令 &#x2F; 操作</th><th>期望</th></tr></thead><tbody><tr><td>双 target 编译</td><td><code>xcodebuild -scheme HabitPulse build</code>（加 <code>OTHER_SWIFT_FLAGS=&quot;-Xfrontend -disable-sandbox&quot;</code> 绕过宏沙盒）</td><td><code>BUILD SUCCEEDED</code></td></tr><tr><td>快照写入</td><td>模拟器 <code>simctl install/launch</code> 后查 App Group 容器</td><td><code>widget_snapshot.json</code> 存在且 <code>habits</code> 数组正确</td></tr><tr><td>深链</td><td><code>xcrun simctl openurl &lt;UDID&gt; &quot;habitpulse://today&quot;</code></td><td>app 不崩、切到今日 Tab</td></tr><tr><td>真机显示</td><td>主屏长按空白 → 「+」→ 搜 HabitPulse</td><td>Widget 出现在组件库</td></tr><tr><td>真机同步</td><td>app 内打卡&#x2F;新建 → 回主屏</td><td>Widget 数据立即更新</td></tr><tr><td>真机点击</td><td>点 Widget</td><td>深链回 app 今日 Tab</td></tr></tbody></table><hr><h2 id="七、总结-最佳实践"><a href="#七、总结-最佳实践" class="headerlink" title="七、总结 &amp; 最佳实践"></a>七、总结 &amp; 最佳实践</h2><ol><li><strong>CloudKit + Widget &#x3D; 别直连数据库</strong>。用「App Group JSON 快照 + 主 app 独占写入」把并发冲突面降到最低。</li><li><strong>写完文件 ≠ Widget 刷新</strong>。必须 <code>WidgetCenter.shared.reloadAllTimelines()</code>，否则用户看到的是旧数据。</li><li><strong>Widget 内只做展示与引导</strong>，写操作（打卡）交给主 app，用深链回跳。</li><li><strong>真机才是照妖镜</strong>：签名、App Group 能力、描述文件这些问题模拟器全不校验，务必上真机。</li><li><strong>主 app 与 Extension 签名方式要一致</strong>（本项目统一 <code>match</code> Manual），否则扩展会被悄悄排除。</li><li><strong>日志别静默吞</strong>：<code>containerURL</code> 为 nil &#x2F; 写入失败这类，用 <code>os.Logger</code> 留线索，真机排查全靠 Console.app。</li><li><strong>代理环境变量大小写一起管</strong>：CI &#x2F; 自动化里覆盖代理时，四个变量（HTTP&#x2F;HTTPS × 大写&#x2F;小写）一起设，否则容易踩到没清掉的坏代理。</li></ol><hr><p><em>相关代码位置（HabitPulse 工程）：</em></p><ul><li>主 app：<code>HabitPulse/Core/WidgetSnapshotService.swift</code>、<code>HabitPulse/HabitPulseApp.swift</code>、<code>HabitPulse/Views/RootTabView.swift</code>、<code>HabitPulse/ViewModels/TodayViewModel.swift</code>、<code>HabitPulse/Info.plist</code></li><li>Widget：<code>HabitPulseWidget/</code> 整个目录（5 swift + Info.plist + entitlements）</li><li>工程配置：<code>HabitPulse.xcodeproj/project.pbxproj</code>（Widget target，UUID 前缀 <code>2B</code>）</li><li>签名：<code>fastlane/Fastfile</code>（<code>habitpulse_dev</code> &#x2F; <code>habitpulse_appstore</code> 含 Widget identifier）</li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;桌面组件-Widget-开发实战：HabitPulse-从选型到落地&quot;&gt;&lt;a href=&quot;#桌面组件-Widget-开发实战：HabitPulse-从选型到落地&quot; class=&quot;headerlink&quot; title=&quot;桌面组件 Widget 开发实战：HabitPu</summary>
      
    
    
    
    
    <category term="iOS" scheme="https://flametinary.github.io/tags/iOS/"/>
    
    <category term="WidgetKit" scheme="https://flametinary.github.io/tags/WidgetKit/"/>
    
    <category term="SwiftData" scheme="https://flametinary.github.io/tags/SwiftData/"/>
    
    <category term="CloudKit" scheme="https://flametinary.github.io/tags/CloudKit/"/>
    
    <category term="App Group" scheme="https://flametinary.github.io/tags/App-Group/"/>
    
    <category term="fastlane" scheme="https://flametinary.github.io/tags/fastlane/"/>
    
    <category term="match" scheme="https://flametinary.github.io/tags/match/"/>
    
    <category term="踩坑" scheme="https://flametinary.github.io/tags/%E8%B8%A9%E5%9D%91/"/>
    
  </entry>
  
  <entry>
    <title>AI协作时代的文档驱动开发</title>
    <link href="https://flametinary.github.io/2026/06/19/AI%E5%8D%8F%E4%BD%9C%E6%97%B6%E4%BB%A3%E7%9A%84%E6%96%87%E6%A1%A3%E9%A9%B1%E5%8A%A8%E5%BC%80%E5%8F%91/"/>
    <id>https://flametinary.github.io/2026/06/19/AI%E5%8D%8F%E4%BD%9C%E6%97%B6%E4%BB%A3%E7%9A%84%E6%96%87%E6%A1%A3%E9%A9%B1%E5%8A%A8%E5%BC%80%E5%8F%91/</id>
    <published>2026-06-19T02:55:43.000Z</published>
    <updated>2026-09-12T03:37:07.556Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>最近做了一个 macOS 截图工具，抛弃古法编程，从头到尾没查看 xcode 中的代码，AI 贯穿全程：立项时帮我梳理思路，开发时补代码、查问题，写文档时参与整理。做完之后我最明显的感受不是“AI 真会写代码”，而是：有 AI 以后，文档反而更重要了。</p></blockquote><span id="more"></span><h2 id="代码变快之后，问题前移了"><a href="#代码变快之后，问题前移了" class="headerlink" title="代码变快之后，问题前移了"></a>代码变快之后，问题前移了</h2><p>这个截图工具从最基础的截图编辑开始，一路做到标注、复制保存、OCR、长截图、Pin、AI 分析和结果预览。Codex 参与以后，开发速度确实快了不少。有时候在 Codex 运行过程中还能做一组 HIIT，有点 work life balance 的感觉了。</p><p>但项目不会因为代码生成得快，就自动变得更稳。</p><p>AI 很像一个精力充沛的同事：理解快，动手快，也特别容易顺着一个想法继续往下扩。你让它改一个按钮，它可能顺手建议重构一层架构；你让它优化 AI 分析，它可能开始考虑对话、历史记录和设置页。它不是故意跑偏，只是太擅长“再多想一步”。</p><p>所以我后来意识到，AI 介入以后，开发里的很多问题其实前移了。过去不少细节是在写代码时慢慢补齐的，现在最好提前写进文档：需求是什么，边界在哪里，哪些文件能改，哪些事情暂时不做，怎么验证，什么情况才算完成。</p><p>文档写得清楚，AI 就像一个靠谱搭档；文档写得含糊，它也能产出很多东西，只是你不一定想要。</p><h2 id="我说的文档驱动，不是写一堆文档"><a href="#我说的文档驱动，不是写一堆文档" class="headerlink" title="我说的文档驱动，不是写一堆文档"></a>我说的文档驱动，不是写一堆文档</h2><p>“文档驱动开发”听起来有点重，容易让人想到很长的 PRD、几轮评审会和打开后就想关掉的共享文档。</p><p>我这里说的不是这个。</p><p>对我来说，文档驱动的重点不是“多写”，而是让文档真正参与开发。它要能回答几个问题：项目现在是什么状态，下一步做什么，AI 可以做什么，不能做什么，做完以后怎么验证，以及这次踩过的坑以后怎么避免。</p><p>后来我的流程基本变成这样：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs text">想法 / 需求<br>-&gt; AI 帮忙整理成文档<br>-&gt; 人审边界<br>-&gt; 写入 ROADMAP / Sprint / TASK<br>-&gt; AI 输出 Implementation Plan<br>-&gt; AI 做 Plan Review<br>-&gt; 按文档实施<br>-&gt; 构建和人工验证<br>-&gt; 结果写回文档<br></code></pre></td></tr></table></figure><p>看上去绕了一圈，但实际更省时间。前面多花十分钟写清楚边界，后面经常少修两个小时奇怪问题。AI 的效率越高，越需要轨道。否则它确实跑得快，但不一定朝你想去的地方跑。</p><h2 id="给文档分工，也是在给-AI-指路"><a href="#给文档分工，也是在给-AI-指路" class="headerlink" title="给文档分工，也是在给 AI 指路"></a>给文档分工，也是在给 AI 指路</h2><p>一开始我也容易把内容都塞进 README：项目介绍、开发计划、注意事项、TODO、历史记录，什么都来一点。人看起来已经不轻松，AI 看起来也不会更轻松。</p><p>后来我把文档按职责拆开：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs text">README.md<br>AGENTS.md<br>TASK.md<br>docs/ROADMAP.md<br>docs/SPRINTS/Sprint-xx.md<br>docs/DEVLOG.md<br>docs/GIT_WORKFLOW.md<br></code></pre></td></tr></table></figure><p><code>README.md</code> 只负责让人知道项目是什么、怎么运行。</p><p><code>AGENTS.md</code> 是给 AI 的入职手册，告诉 Codex 进来后先读什么、遵守什么规则、哪些事情不能自作主张。</p><p><code>TASK.md</code> 是当前唯一任务源，只回答一个问题：现在到底在做什么。</p><p><code>ROADMAP.md</code> 管长期方向，让需求排队。独立开发不缺想法，缺的是让想法一个一个来。</p><p><code>Sprint-xx.md</code> 管单次迭代，写清楚目标、范围、排除项、验收方式和风险。</p><p><code>DEVLOG.md</code> 记录开发记忆。某个问题为什么出现，最后怎么修，哪些边界要注意，都可以写进去。</p><p><code>GIT_WORKFLOW.md</code> 管提交规则，避免 AI 太热心，把不属于本轮的改动也一起提交。</p><p>这些文件拆开以后，AI 的工作路径会稳定很多。当前任务看 <code>TASK.md</code>，长期规划看 <code>ROADMAP.md</code>，本轮边界看 Sprint 文档，提交规则看 Git 文档。文档结构越清楚，沟通成本越低，AI 出问题的概率也越小。</p><h2 id="先让-AI-写文档，而不是直接写代码"><a href="#先让-AI-写文档，而不是直接写代码" class="headerlink" title="先让 AI 写文档，而不是直接写代码"></a>先让 AI 写文档，而不是直接写代码</h2><p>很多时候，我现在会先让 Codex 帮我写文档，而不是直接写代码。</p><p>比如我曾经提过一个 OCR 预览需求：用户在截图编辑页点击 OCR 后，不要立即复制并退出，而是在当前页面打开一个预览窗口。用户可以先看 OCR 内容，再选择复制或取消。预览窗口显示在选择框左右空间更大的一侧。</p><p>这段描述已经比“加个 OCR 预览”清楚不少，但里面仍然藏着很多细节：</p><ul><li>OCR 失败时是否关闭截图编辑？</li><li>OCR 空结果时按钮是否可点？</li><li>预览窗口是否能响应鼠标？</li><li>点击取消是取消 OCR，还是取消整个截图？</li><li>带标注后 OCR 识别的是原图还是标注后的图？</li></ul><p>这些问题不一定要自己对着空白文档硬想。我会让 AI 先整理一版 Sprint 文档：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs text">请基于这个需求整理：<br>1. 需求目标<br>2. Included<br>3. Out of Scope<br>4. 交互流程<br>5. 失败态和空态<br>6. 验收标准<br>7. 风险点<br></code></pre></td></tr></table></figure><p>AI 很适合做第一版结构化整理。人要做的不是照单全收，而是审它、删它、补它。这个过程其实就是需求澄清。很多模糊点，在写成文档时会自己浮出来。</p><h2 id="Out-of-Scope-很重要"><a href="#Out-of-Scope-很重要" class="headerlink" title="Out of Scope 很重要"></a><code>Out of Scope</code> 很重要</h2><p>在所有字段里，我越来越重视 <code>Out of Scope</code>。</p><p>项目失控很多时候不是因为做错了什么，而是因为“顺手多做了点什么”。尤其和 AI 协作时，这件事会被放大。</p><p>比如 <code>Sprint 30 - AI 结果增强</code>，目标只是让 AI 分析结果更稳定、更好复制。于是文档里明确写了不做：图片直传 AI、AI 对话、设置页 AI 配置、模型选择 UI、历史记录、自动修复。</p><p>这些“不做”并不是偷懒，而是给项目降温。</p><p>AI 很容易沿着一个方向继续扩展。你让它做 AI 分析，它可能建议做对话；做了对话，又可能建议历史记录；再往下，截图工具快变办公平台了。听起来很宏大，但用户只是想截个图。</p><p><code>Out of Scope</code> 的价值，就是在 AI 准备发散之前，先把边界钉住。AI 很会扩展，而项目经常更需要收敛。</p><h2 id="Plan-先行，Review-再动手"><a href="#Plan-先行，Review-再动手" class="headerlink" title="Plan 先行，Review 再动手"></a>Plan 先行，Review 再动手</h2><p>我现在不会直接对 Codex 说“实现吧”。更稳的做法是先让它写 Implementation Plan。</p><p>一份有用的 Plan 不只是 TODO。它至少应该说清楚：需求怎么理解、相关代码在哪里、有哪些方案、推荐哪一个、要改哪些文件、怎么验证、风险是什么。</p><p>还是 OCR 预览这个例子，当时有两个方案：一个是直接把 OCR 预览 UI 放进 <code>CaptureOverlayView</code>，另一个是新增 <code>OCRPreviewWindowService</code>。最后我选了后者，不是因为多拆一个文件显得高级，而是因为它能让截图流程、OCR 识别和预览窗口各归各位。</p><p>Plan 的意义在于把隐含决策变成显性文字。<strong>AI 真正写代码前，先暴露自己的理解。如果理解错了，在文档阶段改掉就行，别等代码写完再大动干戈</strong> 。</p><p>Plan 写完后，我还会让 Codex 做 Plan Review。这一步不是让它夸自己“方案完整、结构清晰”，而是专门挑刺。</p><p>OCR 预览的第一版 Plan 就被打回过：<code>ROADMAP</code> 状态没对齐、<code>NSPanel</code> 没明确可点击、closure 可能形成保留链、OCR 失败和空结果没说清楚。这些问题如果等代码写完再修，成本会高很多。</p><h2 id="脚本也是文档"><a href="#脚本也是文档" class="headerlink" title="脚本也是文档"></a>脚本也是文档</h2><p>除了 Markdown，脚本也很重要。</p><p>比如项目里有固定构建入口：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">./scripts/build.sh<br></code></pre></td></tr></table></figure><p>没有脚本时，Codex 可能会猜：用哪个 scheme？Debug 还是 Release？DerivedData 放哪里？要不要 clean？</p><p>一旦有了固定脚本，每次实现后就一句话：跑构建。AI 不需要把时间花在猜命令上，也不会因为猜错命令制造额外噪音。</p><p>构建、启动 Debug App、清理旧进程、运行测试、打包 Release，这些固定动作都适合脚本化。普通文档告诉 AI “应该怎么做”，脚本直接告诉 AI “照这个跑”。</p><h2 id="少一点“顺手”"><a href="#少一点“顺手”" class="headerlink" title="少一点“顺手”"></a>少一点“顺手”</h2><p>进入实现阶段后，我对 Codex 的要求反而变简单了：按计划改，少发挥。</p><p>“少发挥”不是不信任 AI，而是项目开发里最容易出事的两个字就是“顺手”。</p><ul><li>顺手重构一下</li><li>顺手抽个协议</li><li>顺手加个未来扩展点</li><li>顺手把 README 也更新一下</li></ul><p>每一项单独看都合理，合在一起就是一场小型迁移。</p><p>所以 Plan 里要写清楚文件变更范围。例如 AI 结果增强只动 <code>AIAnalysisService.swift</code>、<code>AIAnalysisPreviewWindowService.swift</code>、<code>CaptureSessionService.swift</code>。这样 AI 实施时就不容易把展示逻辑塞进 session 层，也不容易顺手抽一个当前根本用不上的通用框架。</p><p>让 AI 稳定产出代码的关键，不是一句“写得优雅点”，而是足够具体的上下文、边界、文件范围和验收标准。</p><h2 id="把结果写回去"><a href="#把结果写回去" class="headerlink" title="把结果写回去"></a>把结果写回去</h2><p>AI 很容易在构建通过后说“完成了”。但构建通过，只能说明代码能编译。</p><p>截图工具这类 App，很多问题只有真实运行才会出现：浮窗位置不对、按钮点不到、热键被旧实例抢走、OCR 空结果没反馈、AI 面板内容顺序反了、Auto Layout 运行时报约束冲突。</p><p>所以 Sprint 文档里要有<strong>验证清单</strong>。Codex 实现完后，也要按清单给出人工验证步骤。等人工验证通过，再进入 Sprint 收尾。</p><p>收尾时还有一件事很重要：把结果写回 <code>DEVLOG.md</code> 或 Sprint 文档。这个文档不是流水账，而是项目记忆。某个问题为什么出现、最后怎么解决、哪些边界要注意，都应该沉淀下来。</p><p><strong>AI 没有人类那种长期项目记忆，但它可以读文档。既然它会读，那我们就应该把重要经验写给它读</strong>。</p><h2 id="把-Codex-当成不同角色"><a href="#把-Codex-当成不同角色" class="headerlink" title="把 Codex 当成不同角色"></a>把 Codex 当成不同角色</h2><p>我现在更倾向于把 Codex 当成工程搭档，而不是许愿池。</p><p>许愿池式用法是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">帮我实现一个截图工具，最好带 OCR 和 AI。<br></code></pre></td></tr></table></figure><p>这当然也能生成东西，但项目会很难控。</p><p>更好的方式是给它明确阶段：规划 Sprint、输出 Implementation Plan、执行 Plan Review、实施、验证、收尾、提交。</p><p>规划时，它是产品助理；写 Plan 时，它是技术方案作者；Review 时，它是挑刺的人；实施时，它是工程师；验证时，它是测试助手；收尾时，它是文档维护者。</p><p>不要让它同一时间扮演所有角色。一个人同时当产品、架构、开发、测试、发布经理都容易乱，AI 也一样。</p><h2 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h2><p>AI 开发真正有价值的地方，不是让我们跳过工程流程，而是让工程流程跑得更快。</p><p>如果没有文档、没有边界、没有验证，AI 只会更快地制造混乱。它写得越快，你收拾得越忙。</p><p>但如果项目有清晰的文档结构、有明确的 Sprint、有可执行脚本、有 Plan Review、有验证清单、有 DEVLOG、有 Git 规则，AI 就会变成一个非常高效的工程搭档。</p><p><strong>AI 能把油门踩得很深，但方向盘还是要握在开发者手里。否则速度是上去了，项目去哪儿就不好说了</strong>。</p><p>最后，附上项目的 <a href="https://github.com/FlameTinary/TYScreenShotTool">Github</a> 和 <a href="https://apps.apple.com/cn/app/tshot/id6780356949?mt=12">app store</a> 地址，项目还缺了测试文档以及功能测试相关内容，其他功能也还没有开发完整，后续会继续迭代。</p>]]></content>
    
    
    <summary type="html">&lt;blockquote&gt;
&lt;p&gt;最近做了一个 macOS 截图工具，抛弃古法编程，从头到尾没查看 xcode 中的代码，AI 贯穿全程：立项时帮我梳理思路，开发时补代码、查问题，写文档时参与整理。做完之后我最明显的感受不是“AI 真会写代码”，而是：有 AI 以后，文档反而更重要了。&lt;/p&gt;
&lt;/blockquote&gt;</summary>
    
    
    
    <category term="AI" scheme="https://flametinary.github.io/categories/AI/"/>
    
    
    <category term="AI" scheme="https://flametinary.github.io/tags/AI/"/>
    
    <category term="Codex" scheme="https://flametinary.github.io/tags/Codex/"/>
    
    <category term="macOS" scheme="https://flametinary.github.io/tags/macOS/"/>
    
    <category term="App开发" scheme="https://flametinary.github.io/tags/App%E5%BC%80%E5%8F%91/"/>
    
    <category term="工程化" scheme="https://flametinary.github.io/tags/%E5%B7%A5%E7%A8%8B%E5%8C%96/"/>
    
  </entry>
  
  <entry>
    <title>Hexo博客自动化发布实践</title>
    <link href="https://flametinary.github.io/2026/06/17/Hexo%E5%8D%9A%E5%AE%A2%E8%87%AA%E5%8A%A8%E5%8C%96%E5%8F%91%E5%B8%83%E5%AE%9E%E8%B7%B5/"/>
    <id>https://flametinary.github.io/2026/06/17/Hexo%E5%8D%9A%E5%AE%A2%E8%87%AA%E5%8A%A8%E5%8C%96%E5%8F%91%E5%B8%83%E5%AE%9E%E8%B7%B5/</id>
    <published>2026-06-17T14:20:00.000Z</published>
    <updated>2026-09-12T03:37:07.563Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>记录一次把 Hexo 博客从本地手动发布，改造成 GitHub Actions 自动发布的完整过程。</p></blockquote><span id="more"></span><h2 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h2><p>这个博客原本采用的是 Hexo 比较经典的发布方式：</p><ul><li><code>hexo</code> 分支保存博客源码</li><li><code>master</code> 分支保存生成后的静态文件</li><li>本地执行 <code>hexo generate</code></li><li>本地执行 <code>hexo deploy</code></li></ul><p>这套方式本身没有问题，但长期维护下来会有几个明显痛点：</p><ul><li>发布依赖本机环境</li><li>需要本地 SSH、git deploy、<code>.deploy_git</code> 都正常</li><li>换一台电脑或者换一个维护者，接手成本比较高</li><li>一旦发布失败，排查信息主要留在本地</li></ul><p>所以这次的目标很明确：保留当前仓库结构不变，把发布动作迁移到 GitHub Actions。</p><h2 id="改造目标"><a href="#改造目标" class="headerlink" title="改造目标"></a>改造目标</h2><p>最终希望达到下面这个效果：</p><ol><li>日常只需要维护 <code>hexo</code> 分支的源码</li><li>push 到 <code>hexo</code> 分支后自动触发构建</li><li>自动把 <code>public</code> 目录内容同步到 <code>master</code></li><li>GitHub Pages 继续从 <code>master</code> 分支发布站点</li><li>本地 <code>hexo deploy</code> 继续保留，作为备用方案</li></ol><h2 id="改造前的清理"><a href="#改造前的清理" class="headerlink" title="改造前的清理"></a>改造前的清理</h2><p>在上自动化之前，我先做了一轮低风险整理，避免把历史包袱直接带进工作流：</p><ul><li>补充了 <code>package.json</code> 里的常用脚本</li><li>统一了搜索配置，去掉重复的索引生成方式</li><li>去掉未生效的 <code>sitemap</code> &#x2F; <code>baidusitemap</code> 配置</li><li>关闭了默认开启但站点并不使用的 <code>links</code> 页面</li><li>修正了一篇文章里触发构建 warning 的裸链接</li><li>删除了仓库中未使用的 <code>themes/next</code> 和 <code>themes/landscape</code></li><li>补充了根目录 <code>README.md</code></li></ul><p>这一步的好处是：后面如果自动化失败，我们更容易判断到底是工作流问题，还是历史配置问题。</p><h2 id="工作流设计"><a href="#工作流设计" class="headerlink" title="工作流设计"></a>工作流设计</h2><p>这次采用的是“最少改动现有习惯”的方案：</p><ul><li><code>hexo</code> 仍然作为源码分支</li><li><code>master</code> 仍然作为发布分支</li><li>GitHub Actions 从 <code>hexo</code> 分支构建</li><li>构建成功后，把生成结果推送到 <code>master</code></li></ul><p>对应的工作流文件放在：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">.github/workflows/deploy.yml<br></code></pre></td></tr></table></figure><p>核心步骤如下：</p><ol><li>checkout <code>hexo</code> 分支源码</li><li>安装 Node.js 和依赖</li><li>执行 <code>npm run clean</code></li><li>执行 <code>npm run build</code></li><li>使用 <code>git worktree</code> 检出 <code>master</code></li><li>将 <code>public</code> 同步到 <code>master</code></li><li>自动提交并推送</li></ol><h2 id="GitHub-仓库设置检查"><a href="#GitHub-仓库设置检查" class="headerlink" title="GitHub 仓库设置检查"></a>GitHub 仓库设置检查</h2><p>为了让自动发布真正跑通，还需要确认仓库设置没有拦截：</p><h3 id="1-Actions-权限"><a href="#1-Actions-权限" class="headerlink" title="1. Actions 权限"></a>1. Actions 权限</h3><p>仓库 <code>Settings -&gt; Actions -&gt; General</code> 中，需要满足：</p><ul><li>允许 GitHub Actions 运行</li><li><code>Workflow permissions</code> 为 <code>Read and write permissions</code></li></ul><p>因为工作流需要把构建产物推回 <code>master</code> 分支。</p><h3 id="2-Branch-规则"><a href="#2-Branch-规则" class="headerlink" title="2. Branch 规则"></a>2. Branch 规则</h3><p>如果 <code>master</code> 分支开启了保护规则，就要允许 Actions 推送，否则 workflow 会构建成功但 push 失败。</p><p>这次检查下来，仓库当前并没有开启 <code>master</code> 分支保护，所以不会拦截自动部署。</p><h3 id="3-Pages-发布来源"><a href="#3-Pages-发布来源" class="headerlink" title="3. Pages 发布来源"></a>3. Pages 发布来源</h3><p>仓库 <code>Settings -&gt; Pages</code> 中，继续保持：</p><ul><li>Source: <code>Deploy from a branch</code></li><li>Branch: <code>master</code></li><li>Folder: <code>/ (root)</code></li></ul><p>这样 GitHub Actions 只需要负责更新 <code>master</code>，Pages 会继续按原来的方式发布。</p><h2 id="第一次运行踩到的坑"><a href="#第一次运行踩到的坑" class="headerlink" title="第一次运行踩到的坑"></a>第一次运行踩到的坑</h2><p>第一版工作流推上去之后，第一次运行失败了。</p><p>失败现象是：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs text">fatal: not a git repository (or any of the parent directories): .git<br></code></pre></td></tr></table></figure><p>问题出在同步静态文件这一步。</p><p>最初我是这样写的：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">rsync -av --delete public/ ../deploy-branch/<br></code></pre></td></tr></table></figure><p>这里用了 <code>--delete</code>，本意是把 <code>master</code> 分支里的旧文件清掉，确保生成结果干净。但 <code>deploy-branch</code> 是通过 <code>git worktree</code> 检出的工作树，它的根目录里也有 <code>.git</code> 元数据入口。结果 <code>rsync --delete</code> 把这部分一起误删了，后面的 <code>git add</code> &#x2F; <code>git commit</code> 自然就报了 “not a git repository”。</p><p>修复方法很简单：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">rsync -av --delete --exclude <span class="hljs-string">&#x27;.git&#x27;</span> public/ ../deploy-branch/<br></code></pre></td></tr></table></figure><p>加上排除规则后，第二次运行就顺利通过了。</p><h2 id="最终结果"><a href="#最终结果" class="headerlink" title="最终结果"></a>最终结果</h2><p>修复后的发布链路已经验证通过：</p><ul><li>push 到 <code>hexo</code></li><li><code>Deploy Hexo Site</code> 自动执行</li><li>自动生成 <code>public</code></li><li>自动提交到 <code>master</code></li><li><code>pages-build-deployment</code> 接力发布</li><li>站点更新完成</li></ul><p>同时，仍然保留了本地手动发布能力：</p><ul><li>推荐方式：push <code>hexo</code> 分支，走 GitHub Actions</li><li>备用方式：本地执行 <code>npm run deploy</code></li></ul><h2 id="当前推荐用法"><a href="#当前推荐用法" class="headerlink" title="当前推荐用法"></a>当前推荐用法</h2><p>日常发布：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">git add .<br>git commit -m <span class="hljs-string">&quot;更新博客内容&quot;</span><br>git push origin hexo<br></code></pre></td></tr></table></figure><p>备用兜底：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">npm run deploy<br></code></pre></td></tr></table></figure><p>如果只是写文章、改页面、调配置，优先走自动化路径就够了。</p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>这次改造最大的价值，不是“少敲几个命令”，而是把发布这件事从“依赖某台电脑的本地操作”，变成了“仓库内可追踪、可复现、可交接的自动流程”。</p><p>对于个人博客这种长期维护项目，这类改造往往比继续堆主题效果或页面样式更值得优先做，因为它直接影响未来每一次发布的稳定性和接手成本。</p>]]></content>
    
    
    <summary type="html">&lt;blockquote&gt;
&lt;p&gt;记录一次把 Hexo 博客从本地手动发布，改造成 GitHub Actions 自动发布的完整过程。&lt;/p&gt;
&lt;/blockquote&gt;</summary>
    
    
    
    <category term="Hexo" scheme="https://flametinary.github.io/categories/Hexo/"/>
    
    
    <category term="Hexo" scheme="https://flametinary.github.io/tags/Hexo/"/>
    
    <category term="GitHub" scheme="https://flametinary.github.io/tags/GitHub/"/>
    
    <category term="GitHub Actions" scheme="https://flametinary.github.io/tags/GitHub-Actions/"/>
    
  </entry>
  
  <entry>
    <title>Agent Skill 详解</title>
    <link href="https://flametinary.github.io/2026/06/16/Agent-Skill-%E8%AF%A6%E8%A7%A3/"/>
    <id>https://flametinary.github.io/2026/06/16/Agent-Skill-%E8%AF%A6%E8%A7%A3/</id>
    <published>2026-06-16T02:00:00.000Z</published>
    <updated>2026-09-12T03:37:07.556Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>Agent Skill 是 AI Agent 的核心能力模块，用于定义和封装特定的技能。</p></blockquote><span id="more"></span><h2 id="什么是-Agent-Skill"><a href="#什么是-Agent-Skill" class="headerlink" title="什么是 Agent Skill"></a>什么是 Agent Skill</h2><p>Agent Skill 是 AI Agent 执行特定任务的能力单元。每个 Skill 封装了一组相关的功能，可以被 Agent 根据任务需求动态调用。</p><h2 id="Skill-的组成"><a href="#Skill-的组成" class="headerlink" title="Skill 的组成"></a>Skill 的组成</h2><p>一个完整的 Agent Skill 通常包含以下几个部分：</p><ul><li><strong>技能描述</strong>：定义 Skill 的用途和适用场景</li><li><strong>输入参数</strong>：定义 Skill 需要的输入数据</li><li><strong>执行逻辑</strong>：Skill 的核心处理逻辑</li><li><strong>输出结果</strong>：Skill 执行后返回的结果</li></ul><h2 id="Skill-的优势"><a href="#Skill-的优势" class="headerlink" title="Skill 的优势"></a>Skill 的优势</h2><ol><li><strong>模块化</strong>：每个 Skill 都是独立的模块，便于维护和扩展</li><li><strong>可复用</strong>：相同的 Skill 可以在不同场景下重复使用</li><li><strong>易扩展</strong>：新增功能只需添加新的 Skill，不影响现有系统</li><li><strong>智能化</strong>：Agent 可以根据上下文自动选择合适的 Skill</li></ol><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>Agent Skill 为 AI Agent 提供了灵活、可扩展的能力机制。通过合理设计和组合 Skill，可以构建出功能强大、适应各种场景的智能系统。</p>]]></content>
    
    
    <summary type="html">&lt;blockquote&gt;
&lt;p&gt;Agent Skill 是 AI Agent 的核心能力模块，用于定义和封装特定的技能。&lt;/p&gt;
&lt;/blockquote&gt;</summary>
    
    
    
    <category term="AI" scheme="https://flametinary.github.io/categories/AI/"/>
    
    
    <category term="AI" scheme="https://flametinary.github.io/tags/AI/"/>
    
  </entry>
  
  <entry>
    <title>cocoapods结构解析</title>
    <link href="https://flametinary.github.io/2020/09/20/cocoapods%E7%BB%93%E6%9E%84%E8%A7%A3%E6%9E%90/"/>
    <id>https://flametinary.github.io/2020/09/20/cocoapods%E7%BB%93%E6%9E%84%E8%A7%A3%E6%9E%90/</id>
    <published>2020-09-20T08:23:29.000Z</published>
    <updated>2026-09-12T03:37:07.565Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>CocoaPods是用 Ruby 写的，并由若干个 Ruby 包 (gems) 构成的。在解析整合过程中，最重要的几个 gems 分别是： CocoaPods&#x2F;CocoaPods, CocoaPods&#x2F;Core, 和 CocoaPods&#x2F;Xcodeproj (是的，CocoaPods 是一个依赖管理工具 – 利用依赖管理进行构建的！)。<br>podspecs存储路径：<del>&#x2F;.cocoapods<br>缓存文件存储路径：</del>&#x2F;Library&#x2F;Caches&#x2F;CocoaPods</p></blockquote><span id="more"></span><h2 id="核心组件"><a href="#核心组件" class="headerlink" title="核心组件"></a>核心组件</h2><h3 id="CocoaPods-CocoaPod"><a href="#CocoaPods-CocoaPod" class="headerlink" title="CocoaPods&#x2F;CocoaPod"></a>CocoaPods&#x2F;CocoaPod</h3><p>这是是一个面向用户的组件，每当执行一个 pod 命令时，这个组件都将被激活。该组件包括了所有使用 CocoaPods 涉及到的功能，并且还能通过调用所有其它的 gems 来执行任务。</p><h3 id="CocoaPods-Core"><a href="#CocoaPods-Core" class="headerlink" title="CocoaPods&#x2F;Core"></a>CocoaPods&#x2F;Core</h3><p>Core 组件提供支持与 CocoaPods 相关文件的处理，文件主要是 Podfile 和 podspecs。</p><ul><li>Podfile：Podfile 是一个文件，用于定义项目所需要使用的第三方库。该文件支持高度定制，你可以根据个人喜好对其做出定制。更多相关信息，请查阅 Podfile 指南。</li><li>Podspec：.podspec 也是一个文件，该文件描述了一个库是怎样被添加到工程中的。它支持的功能有：列出源文件、framework、编译选项和某个库所需要的依赖等。</li></ul><h3 id="CocoaPods-Xcodeproj"><a href="#CocoaPods-Xcodeproj" class="headerlink" title="CocoaPods&#x2F;Xcodeproj"></a>CocoaPods&#x2F;Xcodeproj</h3><p>这个 gem 组件负责所有工程文件的整合。它能够对创建并修改 .xcodeproj 和 .xcworkspace 文件。它也可以作为单独的一个 gem 包使用。如果你想要写一个脚本来方便的修改工程文件，那么可以使用这个 gem。</p><h2 id="运行-pod-install-命令"><a href="#运行-pod-install-命令" class="headerlink" title="运行 pod install 命令"></a>运行 pod install 命令</h2><p>当运行 pod install 命令时会引发许多操作。要想深入了解这个命令执行的详细内容，可以在这个命令后面加上 –verbose。运行指令后会有如下操作：</p><h3 id="读取Podfile文件"><a href="#读取Podfile文件" class="headerlink" title="读取Podfile文件"></a>读取Podfile文件</h3><p>在安装期间，第一步是要弄清楚显示或隐式的声明了哪些第三方库。在加载 podspecs 过程中，CocoaPods 就建立了包括版本信息在内的所有的第三方库的列表。Podspecs 被存储在本地路径 ~&#x2F;.cocoapods 中。</p><h3 id="版本控制和冲突"><a href="#版本控制和冲突" class="headerlink" title="版本控制和冲突"></a>版本控制和冲突</h3><p>如果依赖的某些库同时使用了一个第三方库，但是版本不同，那么默认是向后兼容，既高版本的兼容低版本的；<br>但是，总会有一些第三方库会存在不能向后兼容的问题，这时候就需要手动解决兼容问题。比如：同时依赖了一个库的1.2.5和2.1.3，那么需要用户通过明确指定使用的版本来解决冲突。</p><h3 id="加载源文件"><a href="#加载源文件" class="headerlink" title="加载源文件"></a>加载源文件</h3><p>CocoaPods 执行的下一步是加载源码。每个 .podspec 文件都包含一个源代码的索引，这些索引一般包裹一个 git 地址和 git tag。它们以 commit SHAs 的方式存储在 ~&#x2F;Library&#x2F;Caches&#x2F;CocoaPods 中。这个路径中文件的创建是由 Core gem 负责的。<br>CocoaPods 将依照 Podfile、.podspec 和缓存文件的信息将源文件下载到 Pods 目录中。</p><h3 id="生成Pod-xcodeproj"><a href="#生成Pod-xcodeproj" class="headerlink" title="生成Pod.xcodeproj"></a>生成Pod.xcodeproj</h3><p>每次 pod install 执行，如果检测到改动时，CocoaPods 会利用 Xcodeproj gem 组件对 Pods.xcodeproj 进行更新。如果该文件不存在，则用使用默认配置。否则，会将已有的配置项加载至内存中。</p><h3 id="安装第三方库"><a href="#安装第三方库" class="headerlink" title="安装第三方库"></a>安装第三方库</h3><p>当cocoapods往工程中添加一个第三方库时，不仅仅是添加了第三方库的源码，还会添加很多内容。由于每个第三方库会有一个独立的target，因此对于每个库，都会添加几个额外的文件：</p><ul><li>一个包含编译选项的.xcconfig文件</li><li>一个同时包含编译设置和CocoaPods默认配置的私有.xcconfig文件</li><li>一个编译所必须的prefix.pch文件</li><li>一个编译必须的dummy.m文件</li></ul><p>如果源码中包含资源bundle，那么在target的执行脚本Pods-Resources.sh中会添加bundle相关的指令。<br>还有一个Pods-environment.h文件，其中包含一些宏，这些宏是用来检测某个组件是否来自pod。<br>最后，将生成两个认可文件，一个是 plist，另一个是 markdown，这两个文件用于给最终用户查阅相关许可信息。</p><ul><li>Podfile.lock</li></ul><p>该文件记录了Pod中的依赖的每个组件，以及依赖组件的版本</p><ul><li>Manifest.lock</li></ul><p>这是每次运行 pod install 命令时创建的 Podfile.lock 文件的副本。如果你遇见过这样的错误 沙盒文件与 Podfile.lock 文件不同步 (The sandbox is not in sync with the Podfile.lock)。这是因为 Manifest.lock 文件和 Podfile.lock 文件不一致所引起。由于 Pods 所在的目录并不总在版本控制之下，这样可以保证开发者运行 app 之前都能更新他们的 pods，否则 app 可能会 crash，或者在一些不太明显的地方编译失败。</p><h2 id="xcproj"><a href="#xcproj" class="headerlink" title="xcproj"></a>xcproj</h2><p>如果你已经依照我们的建议在系统上安装了 xcproj，它会对 Pods.xcodeproj 文件执行一下 touch 以将其转换成为旧的 ASCII plist 格式的文件。为什么要这么做呢？虽然在很久以前就不被其它软件支持了，但是 Xcode 仍然依赖于这种格式。如果没有 xcproj，你的 Pods.xcodeproj 文件将会以 XML 格式的 plist 文件存储，当你用 Xcode 打开它时，它会被改写，并造成大量的文件改动。</p><h3 id="文件结构变化"><a href="#文件结构变化" class="headerlink" title="文件结构变化"></a>文件结构变化</h3><p>执行pod install 后，我们的工程结构发生了变化，在以前的基础上添加了如下文件：</p><ol><li>PodFile：依赖描述文件</li><li>Podfile.lock：当前安装的依赖库的版本</li><li>xxx.xcworkspace：xcworkspace文件，使用CocoaPod管理依赖的项目，XCode只能使用workspace编译项目，如果还只打开以前的xcodeproj文件进行开发，编译会失败 xcworkspace文件实际是一个文件夹，实际Workspace信息保存在contents.xcworkspacedata里，该文件的内容非常简单，实际上只指示它所使用的工程的文件目录</li></ol><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs xml"><span class="hljs-meta">&lt;?xml version=<span class="hljs-string">&quot;1.0&quot;</span> encoding=<span class="hljs-string">&quot;UTF-8&quot;</span>?&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">Workspace</span></span><br><span class="hljs-tag">   <span class="hljs-attr">version</span> = <span class="hljs-string">&quot;1.0&quot;</span>&gt;</span><br>   <span class="hljs-tag">&lt;<span class="hljs-name">FileRef</span></span><br><span class="hljs-tag">      <span class="hljs-attr">location</span> = <span class="hljs-string">&quot;group:CardPlayer/CardPlayer.xcodeproj&quot;</span>&gt;</span><br>   <span class="hljs-tag">&lt;/<span class="hljs-name">FileRef</span>&gt;</span><br>   <span class="hljs-tag">&lt;<span class="hljs-name">FileRef</span></span><br><span class="hljs-tag">      <span class="hljs-attr">location</span> = <span class="hljs-string">&quot;group:Pods/Pods.xcodeproj&quot;</span>&gt;</span><br>   <span class="hljs-tag">&lt;/<span class="hljs-name">FileRef</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">Workspace</span>&gt;</span><br></code></pre></td></tr></table></figure><ol start="4"><li>Pods&#x2F;</li></ol><ul><li>Pods.xcodeproj，所有的第三方库都由Pods工程构建，每个三方库对应一个Pods工程中的Target。</li><li>每个第三方库都会在Pods目录下对应一个目录。</li><li>Headers，在Headers文件中有两个目录，Private和Public，其中包含第三方库的私有和公有的头文件，这些文件都是快捷连接，其真正的位置在每个三方库的文件夹目录下。</li></ul><p><img src="https://s1.ax1x.com/2020/09/25/09o0VP.jpg" srcset="/img/loading.gif" lazyload alt="header结构"></p><ol start="5"><li>Target Support Files 支撑Target的文件</li></ol><p><img src="https://s1.ax1x.com/2020/09/25/09oYgH.jpg" srcset="/img/loading.gif" lazyload alt="Target Support Files"></p><h2 id="工程结构的变化"><a href="#工程结构的变化" class="headerlink" title="工程结构的变化"></a>工程结构的变化</h2><h3 id="Pods工程变化"><a href="#Pods工程变化" class="headerlink" title="Pods工程变化"></a>Pods工程变化</h3><p>Pods工程会为每个依赖的第三方库定义一个Target，还会定义一个Pods-xxx的Target，每个Target会生成一个静态库<br><img src="https://s1.ax1x.com/2020/09/25/09oUKA.png" srcset="/img/loading.gif" lazyload alt="Pods结构"></p><p>Pods工程会新建两个Configuration，每个Configuration会为不同的Target设置不同的xcconfig，xcconfig指出了头文件查找的目录，要链接的第三方，链接目录等。<br><img src="https://s1.ax1x.com/2020/09/25/09osPS.png" srcset="/img/loading.gif" lazyload alt="7"><br>AFNetworking.xcconfig文件的内容：</p><p>CONFIGURATION_BUILD_DIR &#x3D; $PODS_CONFIGURATION_BUILD_DIR&#x2F;AFNetworking<br>GCC_PREPROCESSOR_DEFINITIONS &#x3D; $(inherited) COCOAPODS&#x3D;1<br>HEADER_SEARCH_PATHS &#x3D; “${PODS_ROOT}&#x2F;Headers&#x2F;Private” “${PODS_ROOT}&#x2F;Headers&#x2F;Private&#x2F;AFNetworking” “${PODS_ROOT}&#x2F;Headers&#x2F;Public” “${PODS_ROOT}&#x2F;Headers&#x2F;Public&#x2F;AFNetworking”<br>OTHER_LDFLAGS &#x3D; -framework “CoreGraphics” -framework “MobileCoreServices” -framework “Security” -framework “SystemConfiguration”<br>PODS_BUILD_DIR &#x3D; $BUILD_DIR<br>PODS_CONFIGURATION_BUILD_DIR &#x3D; $PODS_BUILD_DIR&#x2F;$(CONFIGURATION)$(EFFECTIVE_PLATFORM_NAME)<br>PODS_ROOT &#x3D; ${SRCROOT}<br>PODS_TARGET_SRCROOT &#x3D; ${PODS_ROOT}&#x2F;AFNetworking<br>PRODUCT_BUNDLE_IDENTIFIER &#x3D; org.cocoapods.${PRODUCT_NAME:rfc1034identifier}<br>SKIP_INSTALL &#x3D; YES</p><p>Header_SERACH_PATHS：编译时查找头文件的目录<br>OTHER_LD_FLAGS：指明要链接的framework</p><p>Pods-CardPlayer.debug.xcconfig文件的内容：</p><p>GCC_PREPROCESSOR_DEFINITIONS &#x3D; $(inherited) COCOAPODS&#x3D;1<br>HEADER_SEARCH_PATHS &#x3D; $(inherited) “${PODS_ROOT}&#x2F;Headers&#x2F;Public” “${PODS_ROOT}&#x2F;Headers&#x2F;Public&#x2F;AFNetworking”<br>LIBRARY_SEARCH_PATHS &#x3D; $(inherited) “$PODS_CONFIGURATION_BUILD_DIR&#x2F;AFNetworking”<br>OTHER_CFLAGS &#x3D; $(inherited) -isystem “${PODS_ROOT}&#x2F;Headers&#x2F;Public” -isystem “${PODS_ROOT}&#x2F;Headers&#x2F;Public&#x2F;AFNetworking”<br>OTHER_LDFLAGS &#x3D; $(inherited) -ObjC -l”AFNetworking” -framework “CoreGraphics” -framework “MobileCoreServices” -framework “Security” -framework “SystemConfiguration”<br>PODS_BUILD_DIR &#x3D; $BUILD_DIR<br>PODS_CONFIGURATION_BUILD_DIR &#x3D; $PODS_BUILD_DIR&#x2F;$(CONFIGURATION)$(EFFECTIVE_PLATFORM_NAME)<br>PODS_PODFILE_DIR_PATH &#x3D; ${SRCROOT}&#x2F;..<br>PODS_ROOT &#x3D; ${SRCROOT}&#x2F;..&#x2F;Pods</p><p>OTHER_LDFLAGS：说明了编译Pods时需要链接的第三方库，还需要链接其它framework<br>所以我们在xcode里能看到AFNetworking依赖的framework:</p><p><img src="https://s1.ax1x.com/2020/09/25/09odbt.png" srcset="/img/loading.gif" lazyload alt="6"></p><h3 id="主工程变化"><a href="#主工程变化" class="headerlink" title="主工程变化"></a>主工程变化</h3><p>引入CocoaPods后，主工程的设置也会发生相应的变化，引入之前：<br><img src="https://s1.ax1x.com/2020/09/25/09oBUf.png" srcset="/img/loading.gif" lazyload alt="4"></p><p>Debug和Release中的Configuration没有设置任何配置文件，引入CocoaPods后的变化：<br><img src="https://s1.ax1x.com/2020/09/25/09oaDI.jpg" srcset="/img/loading.gif" lazyload alt="3"><br>可以看到，采用CocoaPods之后，Debug和Release设置了相应的配置文件，这些配置文件指明了头文件的查找目录和要链接的第三方库</p><h2 id="编译并链接第三方库"><a href="#编译并链接第三方库" class="headerlink" title="编译并链接第三方库"></a>编译并链接第三方库</h2><p><a href="http://www.srcmini.com/3965.html">Podfile语法中文参考文档</a></p>]]></content>
    
    
    <summary type="html">&lt;blockquote&gt;
&lt;p&gt;CocoaPods是用 Ruby 写的，并由若干个 Ruby 包 (gems) 构成的。在解析整合过程中，最重要的几个 gems 分别是： CocoaPods&amp;#x2F;CocoaPods, CocoaPods&amp;#x2F;Core, 和 CocoaPods&amp;#x2F;Xcodeproj (是的，CocoaPods 是一个依赖管理工具 – 利用依赖管理进行构建的！)。&lt;br&gt;podspecs存储路径：&lt;del&gt;&amp;#x2F;.cocoapods&lt;br&gt;缓存文件存储路径：&lt;/del&gt;&amp;#x2F;Library&amp;#x2F;Caches&amp;#x2F;CocoaPods&lt;/p&gt;
&lt;/blockquote&gt;</summary>
    
    
    
    <category term="iOS" scheme="https://flametinary.github.io/categories/iOS/"/>
    
    
    <category term="iOS" scheme="https://flametinary.github.io/tags/iOS/"/>
    
  </entry>
  
  <entry>
    <title>linux 配置远程登录服务器</title>
    <link href="https://flametinary.github.io/2020/01/21/linux%E9%85%8D%E7%BD%AE%E8%BF%9C%E7%A8%8B%E7%99%BB%E5%BD%95%E6%9C%8D%E5%8A%A1%E5%99%A8/"/>
    <id>https://flametinary.github.io/2020/01/21/linux%E9%85%8D%E7%BD%AE%E8%BF%9C%E7%A8%8B%E7%99%BB%E5%BD%95%E6%9C%8D%E5%8A%A1%E5%99%A8/</id>
    <published>2020-01-21T13:51:58.000Z</published>
    <updated>2026-09-12T03:37:07.567Z</updated>
    
    <content type="html"><![CDATA[<h2 id="创建服务器"><a href="#创建服务器" class="headerlink" title="创建服务器"></a>创建服务器</h2><p>创建谷歌GCP服务器</p><h2 id="在服务器配置sshd-config"><a href="#在服务器配置sshd-config" class="headerlink" title="在服务器配置sshd_config"></a>在服务器配置sshd_config</h2><h3 id="登录服务器"><a href="#登录服务器" class="headerlink" title="登录服务器"></a>登录服务器</h3><p>打开终端, 输入</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">ssh root@&lt;这里是你服务器的ip地址&gt;<br></code></pre></td></tr></table></figure><p>输入密码, 就完成了服务器的登录</p><h3 id="使用vim打开sshd-config文件"><a href="#使用vim打开sshd-config文件" class="headerlink" title="使用vim打开sshd_config文件"></a>使用vim打开<code>sshd_config</code>文件</h3><p>在服务器中打开sshd_config文件</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">vi /etc/ssh/sshd_config<br></code></pre></td></tr></table></figure><p>修改<code>sshd_config</code>文件中的几个地方</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">RSAAuthentication <span class="hljs-built_in">yes</span>     <span class="hljs-comment"># RSA认证</span><br>PubkeyAuthentication <span class="hljs-built_in">yes</span>  <span class="hljs-comment"># 公钥认证</span><br>AuthorizedKeysFile .ssh/authorized_keys  <span class="hljs-comment"># 公钥认证文件路径</span><br></code></pre></td></tr></table></figure><h2 id="本地生成ssh-keygen"><a href="#本地生成ssh-keygen" class="headerlink" title="本地生成ssh-keygen"></a>本地生成ssh-keygen</h2><p>回到本地, 使用<code>ssh-keygen</code>生成公钥和私钥</p><h2 id="将生成的public-key放到服务器"><a href="#将生成的public-key放到服务器" class="headerlink" title="将生成的public key放到服务器"></a>将生成的public key放到服务器</h2><p>进入<code>/.ssh</code>目录, 执行命令</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cat</span> -n ~/.ssh/id_rsa.pub&gt;&gt;root@&lt;这里是你服务器的ip地址&gt;:~/.ssh/authorized_keys<br></code></pre></td></tr></table></figure><p>期间会输入服务器的密码.</p><h2 id="本地创建ssh的config文件-并配置host"><a href="#本地创建ssh的config文件-并配置host" class="headerlink" title="本地创建ssh的config文件, 并配置host"></a>本地创建ssh的config文件, 并配置host</h2><p>在本机的目录<code>~/.ssh</code>下创建config文件</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">touch</span> config<br></code></pre></td></tr></table></figure><p>然后使用vim打开进行编辑</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment">#ssh模版</span><br>Host            hostname<br>HostName        xxx.xxx.xxx.xxx<br>Port            22<br>User            root <span class="hljs-comment"># 用户名</span><br>ServerAliveInterval 60 <span class="hljs-comment"># 每隔60秒 发送KeepAlive请求，保证不会因为超时空闲断开</span><br>IdentityFile    ~/.ssh/id_rsa <span class="hljs-comment">#私钥地址</span><br></code></pre></td></tr></table></figure><h2 id="使用配置好的Host登录远端服务器"><a href="#使用配置好的Host登录远端服务器" class="headerlink" title="使用配置好的Host登录远端服务器"></a>使用配置好的Host登录远端服务器</h2><p>使用命令</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">ssh &lt;刚才配置的HostName&gt;<br></code></pre></td></tr></table></figure><p>就完成了登录</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;创建服务器&quot;&gt;&lt;a href=&quot;#创建服务器&quot; class=&quot;headerlink&quot; title=&quot;创建服务器&quot;&gt;&lt;/a&gt;创建服务器&lt;/h2&gt;&lt;p&gt;创建谷歌GCP服务器&lt;/p&gt;
&lt;h2 id=&quot;在服务器配置sshd-config&quot;&gt;&lt;a href=&quot;#在服务器配置</summary>
      
    
    
    
    <category term="hide" scheme="https://flametinary.github.io/categories/hide/"/>
    
    
    <category term="hide" scheme="https://flametinary.github.io/tags/hide/"/>
    
  </entry>
  
  <entry>
    <title>Mac配置Vim</title>
    <link href="https://flametinary.github.io/2019/08/28/Mac%E9%85%8D%E7%BD%AEVim/"/>
    <id>https://flametinary.github.io/2019/08/28/Mac%E9%85%8D%E7%BD%AEVim/</id>
    <published>2019-08-28T10:51:29.000Z</published>
    <updated>2026-09-12T03:37:07.563Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>相信不少同学在Mac上使用过<code>vi</code>或者<code>vim</code>命令，这个命令是打开mac自带的vim编辑器的命令，如果只是简单的修改文件，使用vim无疑是很方便的；但是如果使用mac自带的vim来做大量文档编辑的工作，那就可能力不从心了。<br>这个时候，你就需要更复杂的vim编辑器来负担这样的工作了,Mac目前有两个Vim客户端可供我们使用<br>1、<strong>MacVim：</strong>使用Cocoa GUI，这是Mac上更新还很活跃的版本，也是Mac上最多人使用的版本。<a href="https://github.com/macvim-dev/macvim">下载地址</a><br>2、使用Carbon GUI的版本，但是这个版本目前基本上不再更新。<a href="http://sourceforge.net/projects/macosxvim/files/">下载地址</a></p></blockquote><span id="more"></span><p>我主要是使用的<strong>MacVim</strong>，所以就记录一下MacVim的安装以及使用过程。</p><h2 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h2><p>从上面提供的链接下载MacVim，并按照安装mac应用的步骤安装好MavVim。这个时候还没有结束。</p><p>想象一下，我们以前在terminal中打开一个文件是这样的：<code>vi &lt;文件名称&gt;</code>，这样很coooooooooool；</p><p>但是，我们现在使用MacVim，却得鼠标点击打开MacVim，然后再从MacVim打开文件。这样是不是比以前繁琐了很多。那么，能不能像从前一样，使用命令打开文件呢？</p><p>答案是可以的。</p><p>我们先把MacVim打开,第一次打开MacVim是这个样子：</p><p><img src="https://s2.ax1x.com/2019/08/28/m7xckQ.jpg" srcset="/img/loading.gif" lazyload alt="第一次打开MacVim"></p><p>在MacVim的界面中我们输入<code>:h mvim</code>，MacVim会提示我们将<code>mvim</code>命令的路径添加到我们的PATH</p><p><img src="https://s2.ax1x.com/2019/08/28/m7xgYj.jpg" srcset="/img/loading.gif" lazyload alt="提示"></p><p><strong>添加PATH步骤：</strong></p><ol><li><code>vi ~/.bash_podfile</code>打开PATH所在路径</li><li>将MacVim提供的命令路径写入PATH：<code>export PATH=/Applications/MacVim.app/Contents/bin:$PATH</code></li><li>执行<code>source ~/.bash_podfile</code>重启bash</li></ol><p>这个时候，我们就可以直接在Terminal中使用<code>mvim</code>命令来打开文件了。</p><p>是不是觉得输入mvim命令还是不如vim命令舒服？那么我们就使用vim来打开MacVim!</p><p>使用命令<code>mvim ~/.zshrc</code> 或者<code>mvim ~/.bashrc</code>，如果你使用的是zsh那么使用前者，如果使用的是bash，那么使用后者。</p><p>打开后，在文件末尾新增一段文字<code>alias vim=&#39;mvim&#39;</code>，意思就是给mvim命令起一个别名。</p><p>然后保存退出，如果你是zsh使用<code>source ~/.zshrc</code>重新载入，如果是bash使用<code>source ~/.bashrc</code>载入。</p><p>接下来就是见证奇迹的时刻了，输入命令<code>vim &lt;你要打开的文档&gt;</code>，看看是不是使用MacVim打开了!</p><h2 id="添加插件"><a href="#添加插件" class="headerlink" title="添加插件"></a>添加插件</h2><blockquote><p>经过上面的一系列骚操作，现在我们可以使用MacVim正常的进行编辑了，但是，我们还可以对MacVim进行更深层次的扩展，那就是给MacVim添加插件！</p></blockquote><h3 id="Vundle-命令"><a href="#Vundle-命令" class="headerlink" title="Vundle 命令"></a>Vundle 命令</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 安装插件</span><br>:BundleInstall<br><span class="hljs-comment"># 更新插件</span><br>:BundleUpdate<br><span class="hljs-comment"># 清除不需要的插件</span><br>:BundleClean<br><span class="hljs-comment"># 列出当前的插件</span><br>:BundleList<br><span class="hljs-comment"># 搜索插件</span><br>:BundleSearch<br><br></code></pre></td></tr></table></figure><h2 id="配置-vimrc文件"><a href="#配置-vimrc文件" class="headerlink" title="配置~&#x2F;.vimrc文件"></a>配置~&#x2F;.vimrc文件</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-string">&quot; 关闭vim的所有扩展功能</span><br><span class="hljs-string">set nocompatible</span><br><span class="hljs-string"></span><br><span class="hljs-string">&quot;</span> 配置插件路径<br><span class="hljs-built_in">set</span> rtp+=~/.vim/bundle/vundle/  <br>call vundle#begin()<br><br><span class="hljs-string">&quot; 在这里添加你想安装的Vim插件</span><br><span class="hljs-string">Bundle &#x27;gmarik/vundle&#x27;</span><br><span class="hljs-string">&quot;</span> Python补全强力插件<br>Bundle <span class="hljs-string">&#x27;davidhalter/jedi&#x27;</span><br><span class="hljs-string">&quot; 添加引号,括号配对补全</span><br><span class="hljs-string">Bundle &#x27;jiangmiao/auto-pairs&#x27;</span><br><span class="hljs-string">&quot;</span> 添加/解除注释<br>Bundle <span class="hljs-string">&#x27;scrooloose/nerdcommenter&#x27;</span><br><br>call vundle#end()       <span class="hljs-string">&quot; required</span><br><span class="hljs-string"></span><br><span class="hljs-string">&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><br><span class="hljs-string">&quot; 一般设定</span><br><span class="hljs-string">&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><span class="hljs-string">&quot;&quot;</span><br><span class="hljs-string">&quot; 设定默认解码</span><br><span class="hljs-string">set fenc=utf-8</span><br><span class="hljs-string">set fencs=utf-8,usc-bom,euc-jp,gb18030,gbk,gb2312,cp936</span><br><span class="hljs-string"></span><br><span class="hljs-string">&quot;</span>去掉讨厌的有关vi一致性模式，避免以前版本的一些bug和局限<br><span class="hljs-built_in">set</span> nocompatible<br><br><span class="hljs-string">&quot; 显示中文帮助</span><br><span class="hljs-string">if version &gt;= 603</span><br><span class="hljs-string">    set helplang=cn</span><br><span class="hljs-string">    set encoding=utf-8</span><br><span class="hljs-string">endif</span><br><span class="hljs-string"></span><br><span class="hljs-string">&quot;</span> 语法高亮<br>syntax on<br><br><span class="hljs-string">&quot; 设置配色</span><br><span class="hljs-string">colorscheme solarized  &quot;</span>可能需要提前下载<br><br><span class="hljs-string">&quot; 设置字体</span><br><span class="hljs-string">set guifont=Monaco:h14</span><br><span class="hljs-string"></span><br><span class="hljs-string">&quot;</span> 设置gvim启动窗口的位置，以及大小<br><span class="hljs-string">&quot; winpos 300 105</span><br><span class="hljs-string">&quot;</span> <span class="hljs-built_in">set</span> lines=30 columns=100<br><br><span class="hljs-string">&quot; 开启行号显示</span><br><span class="hljs-string">set number</span><br><span class="hljs-string"></span><br><span class="hljs-string">&quot;</span>下面两行在进行编写代码时，在格式对起上很有用；<br><span class="hljs-string">&quot;第一行，vim使用自动对起，也就是把当前行的对起格式应用到下一行；</span><br><span class="hljs-string">&quot;</span>第二行，依据上面的对起格式，智能的选择对起方式，对于类似C语言编<br><span class="hljs-string">&quot;写上很有用</span><br><span class="hljs-string">set autoindent</span><br><span class="hljs-string">set smartindent</span><br><span class="hljs-string"></span><br><span class="hljs-string">&quot;</span>查询时非常方便，如要查找book单词，当输入到/b时，会自动找到第一<br><span class="hljs-string">&quot;个b开头的单词，当输入到/bo时，会自动找到第一个bo开头的单词，依</span><br><span class="hljs-string">&quot;</span>次类推，进行查找时，使用此设置会快速找到答案，当你找要匹配的单词<br><span class="hljs-string">&quot;时，别忘记回车</span><br><span class="hljs-string">set incsearch</span><br><span class="hljs-string"></span><br><span class="hljs-string">&quot;</span> 高亮当前行<br><span class="hljs-built_in">set</span> cursorline<br><br><span class="hljs-string">&quot; 启动的时候不显示那个援助索马里儿童的提示</span><br><span class="hljs-string">set shortmess=atI</span><br><span class="hljs-string"></span><br><span class="hljs-string">&quot;</span> 我的状态行显示的内容（包括文件类型和解码）<br><span class="hljs-built_in">set</span> statusline=%F%m%r%h%w\[POS=%l,%v][%p%%]\%&#123;strftime(\&quot;%d/%m/%y\ -\ %H:%M\&quot;)&#125;<br><br><span class="hljs-string">&quot; 总是显示状态行</span><br><span class="hljs-string">set laststatus=2</span><br><span class="hljs-string"></span><br><span class="hljs-string">&quot;</span> 制表符为4<br><span class="hljs-built_in">set</span> tabstop=4<br><br><span class="hljs-string">&quot; 统一缩进为4</span><br><span class="hljs-string">set softtabstop=4</span><br><span class="hljs-string">set shiftwidth=4</span><br><span class="hljs-string"></span><br><span class="hljs-string">&quot;</span> 在c,c++,python文件中用空格代替制表符<br>autocmd FileType c,cpp,python <span class="hljs-built_in">set</span> shiftwidth=4 | <span class="hljs-built_in">set</span> expandtab<br><br><span class="hljs-string">&quot; 侦测文件类型</span><br><span class="hljs-string">filetype on</span><br><span class="hljs-string"></span><br><span class="hljs-string">&quot;</span> 载入文件类型插件<br>filetype plugin on<br><br><span class="hljs-string">&quot; 为特定文件类型载入相关缩进文件</span><br><span class="hljs-string">filetype indent on</span><br><span class="hljs-string">   &quot;</span> 如果文件类型为.sh文件<br>    <span class="hljs-keyword">if</span> &amp;filetype == <span class="hljs-string">&#x27;sh&#x27;</span><br>        call setline(1,<br>        <span class="hljs-string">&quot;\#########################################################################&quot;</span>)<br>        call append(line(<span class="hljs-string">&quot;.&quot;</span>), <span class="hljs-string">&quot;\# File Name: &quot;</span>.<span class="hljs-built_in">expand</span>(<span class="hljs-string">&quot;%&quot;</span>))<br>        call append(line(<span class="hljs-string">&quot;.&quot;</span>)+1, <span class="hljs-string">&quot;\# Author: Sheldon&quot;</span>)<br>        call append(line(<span class="hljs-string">&quot;.&quot;</span>)+2, <span class="hljs-string">&quot;\# mail: tinarychina@gmail.com&quot;</span>)<br>        call append(line(<span class="hljs-string">&quot;.&quot;</span>)+3, <span class="hljs-string">&quot;\# Created Time: &quot;</span>.strftime(<span class="hljs-string">&quot;%F %R&quot;</span>))<br>        call append(line(<span class="hljs-string">&quot;.&quot;</span>)+4, <span class="hljs-string">&quot;\#########################################################################&quot;</span>)<br>        call append(line(<span class="hljs-string">&quot;.&quot;</span>)+5, <span class="hljs-string">&quot;\#!/bin/bash&quot;</span>)<br>        call append(line(<span class="hljs-string">&quot;.&quot;</span>)+6, <span class="hljs-string">&quot;&quot;</span>)<br><br>    <span class="hljs-keyword">else</span><br>        call setline(1, <span class="hljs-string">&quot;/*************************************************************************&quot;</span>)<br>        call append(line(<span class="hljs-string">&quot;.&quot;</span>), <span class="hljs-string">&quot;    &gt; File Name: &quot;</span>.<span class="hljs-built_in">expand</span>(<span class="hljs-string">&quot;%&quot;</span>))<br>        call append(line(<span class="hljs-string">&quot;.&quot;</span>)+1, <span class="hljs-string">&quot;    &gt; Author: Sheldon&quot;</span>)<br>        call append(line(<span class="hljs-string">&quot;.&quot;</span>)+2, <span class="hljs-string">&quot;    &gt; Mail: tinarychina@gmail.com &quot;</span>)<br>        call append(line(<span class="hljs-string">&quot;.&quot;</span>)+3, <span class="hljs-string">&quot;    &gt; Created Time: &quot;</span>.strftime(<span class="hljs-string">&quot;%F %R&quot;</span>))<br>        call append(line(<span class="hljs-string">&quot;.&quot;</span>)+4, <span class="hljs-string">&quot; ************************************************************************/&quot;</span>)<br>        call append(line(<span class="hljs-string">&quot;.&quot;</span>)+5, <span class="hljs-string">&quot;&quot;</span>)<br>    endif<br>    <span class="hljs-keyword">if</span> &amp;filetype == <span class="hljs-string">&#x27;python&#x27;</span><br>        call append(line(<span class="hljs-string">&quot;.&quot;</span>)+5, <span class="hljs-string">&quot;\#!/usr/bin/env python&quot;</span>)<br>        call append(line(<span class="hljs-string">&quot;.&quot;</span>)+6, <span class="hljs-string">&quot;\#coding: utf-8&quot;</span>)<br>        call append(line(<span class="hljs-string">&quot;.&quot;</span>)+7, <span class="hljs-string">&quot;&quot;</span>)<br>    endif<br>    <span class="hljs-keyword">if</span> &amp;filetype == <span class="hljs-string">&#x27;cpp&#x27;</span><br>        call append(line(<span class="hljs-string">&quot;.&quot;</span>)+6, <span class="hljs-string">&quot;#include&lt;iostream&gt;&quot;</span>)<br>        call append(line(<span class="hljs-string">&quot;.&quot;</span>)+7, <span class="hljs-string">&quot;using namespace std;&quot;</span>)<br>        call append(line(<span class="hljs-string">&quot;.&quot;</span>)+8, <span class="hljs-string">&quot;&quot;</span>)<br>    endif<br>    <span class="hljs-keyword">if</span> &amp;filetype == <span class="hljs-string">&#x27;c&#x27;</span><br>        call append(line(<span class="hljs-string">&quot;.&quot;</span>)+6, <span class="hljs-string">&quot;#include&lt;stdio.h&gt;&quot;</span>)<br>        call append(line(<span class="hljs-string">&quot;.&quot;</span>)+7, <span class="hljs-string">&quot;&quot;</span>)<br>    endif<br>    <span class="hljs-string">&quot; 新建文件后，自动定位到文件末尾</span><br><span class="hljs-string">    autocmd BufNewFile * normal G</span><br><span class="hljs-string">endfunc</span><br></code></pre></td></tr></table></figure><h2 id="vim-安装主题"><a href="#vim-安装主题" class="headerlink" title="vim 安装主题"></a>vim 安装主题</h2><p>在<code>~/.vim/</code>路径下新建一个<strong>colors</strong>文件夹，然后下载主题，例如：<a href="https://github.com/tomasr/molokai%EF%BC%8C%E5%B0%86%E4%B8%8B%E8%BD%BD%E5%A5%BD%E7%9A%84molokai.vim%E6%94%BE%E5%85%A5colors%E6%96%87%E4%BB%B6%E5%A4%B9%E4%B8%AD%E3%80%82">https://github.com/tomasr/molokai，将下载好的molokai.vim放入colors文件夹中。</a></p><p>使用vim打开<code>~/.vimrc</code>，并添加：</p><figure class="highlight vim"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs vim"><span class="hljs-comment">&quot; 代码颜色主题</span><br><span class="hljs-keyword">syntax</span> <span class="hljs-keyword">on</span><br><span class="hljs-keyword">syntax</span> enable<br><span class="hljs-keyword">set</span> t_Co=<span class="hljs-number">256</span><br><span class="hljs-keyword">colorscheme</span> molokai<br></code></pre></td></tr></table></figure><p>然后重新打开vim编辑器，就发现主题已经更换了</p>]]></content>
    
    
    <summary type="html">&lt;blockquote&gt;
&lt;p&gt;相信不少同学在Mac上使用过&lt;code&gt;vi&lt;/code&gt;或者&lt;code&gt;vim&lt;/code&gt;命令，这个命令是打开mac自带的vim编辑器的命令，如果只是简单的修改文件，使用vim无疑是很方便的；但是如果使用mac自带的vim来做大量文档编辑的工作，那就可能力不从心了。&lt;br&gt;这个时候，你就需要更复杂的vim编辑器来负担这样的工作了,Mac目前有两个Vim客户端可供我们使用&lt;br&gt;1、&lt;strong&gt;MacVim：&lt;/strong&gt;使用Cocoa GUI，这是Mac上更新还很活跃的版本，也是Mac上最多人使用的版本。&lt;a href=&quot;https://github.com/macvim-dev/macvim&quot;&gt;下载地址&lt;/a&gt;&lt;br&gt;2、使用Carbon GUI的版本，但是这个版本目前基本上不再更新。&lt;a href=&quot;http://sourceforge.net/projects/macosxvim/files/&quot;&gt;下载地址&lt;/a&gt;&lt;/p&gt;
&lt;/blockquote&gt;</summary>
    
    
    
    <category term="Mac" scheme="https://flametinary.github.io/categories/Mac/"/>
    
    
    <category term="Mac" scheme="https://flametinary.github.io/tags/Mac/"/>
    
  </entry>
  
  <entry>
    <title>zsh命令大全</title>
    <link href="https://flametinary.github.io/2019/08/28/zsh%E5%91%BD%E4%BB%A4%E5%A4%A7%E5%85%A8/"/>
    <id>https://flametinary.github.io/2019/08/28/zsh%E5%91%BD%E4%BB%A4%E5%A4%A7%E5%85%A8/</id>
    <published>2019-08-28T10:00:09.000Z</published>
    <updated>2026-09-12T03:37:07.567Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>bash和zsh两种shell功能非常相似，只不过zsh的功能比bash更强大一些。这两种shell的快捷键命令也非常相似，对于常用shell的人来说，了解一些快捷键操作将会十分有益，因为使用快捷键将能大大节约操作时间。大部分快捷键是用来快速导航或编辑当前命令行。</p></blockquote><p>但是！！！</p><p>记不住呀，所以记录一下~</p><span id="more"></span><p>⌃ + u：清空当前行</p><p>⌃ + a：移动到行首</p><p>⌃ + e：移动到行尾</p><p>⌃ + f：向前移动</p><p>⌃ + b：向后移动</p><p>⌃ + p：上一条命令</p><p>⌃ + n：下一条命令</p><p>⌃ + r：搜索历史命令</p><p>⌃ + y：召回最近用命令删除的文字</p><p>⌃ + h：删除光标之前的字符</p><p>⌃ + d：删除光标所指的字符</p><p>⌃ + w：删除光标之前的单词</p><p>⌃ + k：删除从光标到行尾的内容</p><p>⌃ + t：交换光标和之前的字符</p><p>⌘ + Click：可以打开文件，文件夹和链接</p><p>⌘ + n：新建窗口</p><p>⌘ + t：新建标签页</p><p>⌘ + w：关闭当前页</p><p>⌘ + 数字&amp;⌘ + 方向键：切换标签页</p><p>⌥⌘ + 数字：切换窗口</p><p>⌘ + enter：切换全屏</p><p>⌘ + d：左右分屏</p><p>⇧⌘ + d：上下分屏</p><p>⌘ + ;：自动补全历史记录</p><p>⇧⌘ + h：自动补全剪贴板历史</p><p>⌥⌘ + e：查找所有来定位某个标签页</p><p>⌘ + r&amp;⌃ + l：清屏</p><p>⌘ + &#x2F;：显示光标位置</p><p>⌥⌘ + b：历史回放</p><p>⌘ + f：查找，然后用tab和⇧ + tab可以向右和向左补全，补全之后的内容会被自动复制， 还可以用⌥ + enter将查找结果输入终端</p>]]></content>
    
    
    <summary type="html">&lt;blockquote&gt;
&lt;p&gt;bash和zsh两种shell功能非常相似，只不过zsh的功能比bash更强大一些。这两种shell的快捷键命令也非常相似，对于常用shell的人来说，了解一些快捷键操作将会十分有益，因为使用快捷键将能大大节约操作时间。大部分快捷键是用来快速导航或编辑当前命令行。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;但是！！！&lt;/p&gt;
&lt;p&gt;记不住呀，所以记录一下~&lt;/p&gt;</summary>
    
    
    
    <category term="Mac" scheme="https://flametinary.github.io/categories/Mac/"/>
    
    
    <category term="Mac" scheme="https://flametinary.github.io/tags/Mac/"/>
    
  </entry>
  
  <entry>
    <title>Mac终端神器--iTerm2</title>
    <link href="https://flametinary.github.io/2019/08/28/Mac%E7%BB%88%E7%AB%AF%E7%A5%9E%E5%99%A8-iTerm2/"/>
    <id>https://flametinary.github.io/2019/08/28/Mac%E7%BB%88%E7%AB%AF%E7%A5%9E%E5%99%A8-iTerm2/</id>
    <published>2019-08-28T03:57:24.000Z</published>
    <updated>2026-09-12T03:37:07.563Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>打造人见人爱的终端</p></blockquote><span id="more"></span><h2 id="下载iTerm2"><a href="#下载iTerm2" class="headerlink" title="下载iTerm2"></a>下载iTerm2</h2><blockquote><p><a href="https://www.iterm2.com/">官网下载</a></p></blockquote><p>安装完成后，在<code>/bin</code>目录下会多出一个zsh的文件。</p><p>Mac系统默认使用dash作为终端，可以使用命令修改默认使用zsh：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">chsh -s /bin/zsh<br></code></pre></td></tr></table></figure>]]></content>
    
    
    <summary type="html">&lt;blockquote&gt;
&lt;p&gt;打造人见人爱的终端&lt;/p&gt;
&lt;/blockquote&gt;</summary>
    
    
    
    
  </entry>
  
  <entry>
    <title>Git贮藏（Stashing）</title>
    <link href="https://flametinary.github.io/2019/08/22/Git%E8%B4%AE%E8%97%8F%EF%BC%88Stashing%EF%BC%89/"/>
    <id>https://flametinary.github.io/2019/08/22/Git%E8%B4%AE%E8%97%8F%EF%BC%88Stashing%EF%BC%89/</id>
    <published>2019-08-22T06:53:30.000Z</published>
    <updated>2026-09-12T03:37:07.563Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>我们在工作中经常会遇到一种情况，当我们在项目的当前分支修改了一些代码，工作进行到一半，又需要去其他的分支处理一些事情；这个时候，你不想把修改了一半的代码提交到git，但不提交又无法切到其他分支；怎么办呢？</p></blockquote><span id="more"></span><p>我们可以使用Git的一个功能——Stashing（贮藏）。</p><h2 id="作用"><a href="#作用" class="headerlink" title="作用"></a>作用</h2><p>Stashing可以将当前工作区已修改状态下的文件储存起来，在栈中增加一条存储。</p><h2 id="用法"><a href="#用法" class="headerlink" title="用法"></a>用法</h2><h3 id="列出所有的存储列表"><a href="#列出所有的存储列表" class="headerlink" title="列出所有的存储列表"></a>列出所有的存储列表</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">git stash list<br></code></pre></td></tr></table></figure><h3 id="应用存储"><a href="#应用存储" class="headerlink" title="应用存储"></a>应用存储</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">git stash pop<br></code></pre></td></tr></table></figure><p>重新应用最后一次的存储，同时，将其从栈中移走。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">git stash apply stash@&#123;0&#125;<br></code></pre></td></tr></table></figure><p>通过存储名应用到当前工作区，但并不会从栈中移除。</p><h3 id="取消贮藏"><a href="#取消贮藏" class="headerlink" title="取消贮藏"></a>取消贮藏</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">git stash show -p stash@&#123;0&#125; | git apply -R<br></code></pre></td></tr></table></figure><p>取消之前所应用贮藏的修改。</p><h3 id="移除贮藏"><a href="#移除贮藏" class="headerlink" title="移除贮藏"></a>移除贮藏</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">git stash drop &lt;stash name&gt;<br></code></pre></td></tr></table></figure><p>从栈中移除贮藏</p><h3 id="通过贮藏创建新分支"><a href="#通过贮藏创建新分支" class="headerlink" title="通过贮藏创建新分支"></a>通过贮藏创建新分支</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">git stash branch &lt;branch name&gt;<br></code></pre></td></tr></table></figure><p>创建一个新的分支，并且检出你贮藏工作时所做的提交，重新应用于你的工作，如果成功，将会丢弃贮藏。</p>]]></content>
    
    
    <summary type="html">&lt;blockquote&gt;
&lt;p&gt;我们在工作中经常会遇到一种情况，当我们在项目的当前分支修改了一些代码，工作进行到一半，又需要去其他的分支处理一些事情；这个时候，你不想把修改了一半的代码提交到git，但不提交又无法切到其他分支；怎么办呢？&lt;/p&gt;
&lt;/blockquote&gt;</summary>
    
    
    
    <category term="Git" scheme="https://flametinary.github.io/categories/Git/"/>
    
    
    <category term="Git" scheme="https://flametinary.github.io/tags/Git/"/>
    
  </entry>
  
  <entry>
    <title>iOS搭建远程私有库</title>
    <link href="https://flametinary.github.io/2019/08/21/iOS%E6%90%AD%E5%BB%BA%E8%BF%9C%E7%A8%8B%E7%A7%81%E6%9C%89%E5%BA%93/"/>
    <id>https://flametinary.github.io/2019/08/21/iOS%E6%90%AD%E5%BB%BA%E8%BF%9C%E7%A8%8B%E7%A7%81%E6%9C%89%E5%BA%93/</id>
    <published>2019-08-21T02:51:29.000Z</published>
    <updated>2026-09-12T03:37:07.566Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>iOS搭建远程私有库</p></blockquote><span id="more"></span><h2 id="一、创建远程私有索引库"><a href="#一、创建远程私有索引库" class="headerlink" title="一、创建远程私有索引库"></a>一、创建远程私有索引库</h2><blockquote><p>这个库的作用是存放索引文件</p></blockquote><h2 id="二、将远程索引库链接（下载）到本地"><a href="#二、将远程索引库链接（下载）到本地" class="headerlink" title="二、将远程索引库链接（下载）到本地"></a>二、将远程索引库链接（下载）到本地</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">pod repo add &lt;仓库名&gt; &lt;仓库源地址（SSH地址）&gt;<br></code></pre></td></tr></table></figure><h2 id="三、创建远程代码私有仓库"><a href="#三、创建远程代码私有仓库" class="headerlink" title="三、创建远程代码私有仓库"></a>三、创建远程代码私有仓库</h2><h3 id="1-在本地创建-pod-项目"><a href="#1-在本地创建-pod-项目" class="headerlink" title="1. 在本地创建 pod 项目"></a>1. 在本地创建 pod 项目</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">pod lib create &lt;pod库名字&gt;<br></code></pre></td></tr></table></figure><p>执行结果</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br></pre></td><td class="code"><pre><code class="hljs bash">Cloning `https://github.com/CocoaPods/pod-template.git` into `&lt;我的pod库名称，手动马赛克&gt;`.<br>Configuring &lt;我的pod库名称，手动马赛克&gt; template.<br><br>------------------------------<br><br>To get you started we need to ask a few questions, this should only take a minute.<br><br>If this is your first <span class="hljs-keyword">time</span> we recommend running through with the guide:<br> - https://guides.cocoapods.org/making/using-pod-lib-create.html<br> ( hold cmd and click links to open <span class="hljs-keyword">in</span> a browser. )<br><br><br>What platform <span class="hljs-keyword">do</span> you want to use?? [ iOS / macOS ]<br> &gt; iOS<br><br>What language <span class="hljs-keyword">do</span> you want to use?? [ Swift / ObjC ]<br> &gt; ObjC<br><br>Would you like to include a demo application with your library? [ Yes / No ]<br> &gt; Yes<br><br>Which testing frameworks will you use? [ Specta / Kiwi / None ]<br> &gt; Kiwi<br><br>Would you like to <span class="hljs-keyword">do</span> view based testing? [ Yes / No ]<br> &gt; Yes<br><br>What is your class prefix?<br> &gt; TY<br><br>Running pod install on your new library.<br><br>Analyzing dependencies<br>Fetching podspec <span class="hljs-keyword">for</span> `&lt;我的pod库名称，手动马赛克&gt;` from `../`<br>Downloading dependencies<br>Installing &lt;我的pod库名称，手动马赛克&gt; (0.1.0)<br>Installing FBSnapshotTestCase (2.1.4)<br>Installing Kiwi (3.0.0)<br>Generating Pods project<br>Integrating client project<br><br>[!] Please close any current Xcode sessions and use `&lt;我的pod库名称，手动马赛克&gt;.xcworkspace` <span class="hljs-keyword">for</span> this project from now on.<br>Sending stats<br>Pod installation complete! There are 3 dependencies from the Podfile and 3 total pods installed.<br><br> Ace! you<span class="hljs-string">&#x27;re ready to go!</span><br><span class="hljs-string"> We will start you off by opening your project in Xcode</span><br><span class="hljs-string">  open &#x27;</span>&lt;我的pod库路径，手动马赛克&gt;<span class="hljs-string">&#x27;</span><br><span class="hljs-string"></span><br><span class="hljs-string">To learn more about the template see `https://github.com/CocoaPods/pod-template.git`.</span><br><span class="hljs-string">To learn more about creating a new pod, see `https://guides.cocoapods.org/making/making-a-cocoapod`.</span><br></code></pre></td></tr></table></figure><h3 id="2-修改内容"><a href="#2-修改内容" class="headerlink" title="2. 修改内容"></a>2. 修改内容</h3><p>把需要提交的代码放到路径(当前项目根目录&#x2F;项目名&#x2F;Classes&#x2F;)下面，删除ReplaceMe.m。</p><h3 id="3-修改-podspec文件"><a href="#3-修改-podspec文件" class="headerlink" title="3. 修改.podspec文件"></a>3. 修改.podspec文件</h3><h3 id="4-提交"><a href="#4-提交" class="headerlink" title="4. 提交"></a>4. 提交</h3><p>提交代码到本地pod库</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs bash">git add .<br>git commit -m <span class="hljs-string">&quot;add file&quot;</span><br>git remote add origin &lt;远端pod库地址:https://gitlab.com/xxx.git&gt;<br>git push -u origin master<br></code></pre></td></tr></table></figure><p>打tag并提交tag到origin</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">git tag 0.1.0<br>git push --tags<br></code></pre></td></tr></table></figure><p>验证本地spec文件是否有误</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">pod lib lint<br></code></pre></td></tr></table></figure><p>验证远程spec文件是否有误</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">pod spec lint<br></code></pre></td></tr></table></figure><p>上传.podspec索引文件到索引库</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">pod repo push &lt;索引库名称&gt; &lt;.podspec文件名称&gt;  --allow-warnings --use-libraries --verbose<br></code></pre></td></tr></table></figure><p>**tip: **消除pod库警告<code>inhibit_all_warnings!</code>。</p>]]></content>
    
    
    <summary type="html">&lt;blockquote&gt;
&lt;p&gt;iOS搭建远程私有库&lt;/p&gt;
&lt;/blockquote&gt;</summary>
    
    
    
    <category term="iOS" scheme="https://flametinary.github.io/categories/iOS/"/>
    
    
    <category term="iOS" scheme="https://flametinary.github.io/tags/iOS/"/>
    
  </entry>
  
  <entry>
    <title>iOS常用第三方库整理</title>
    <link href="https://flametinary.github.io/2019/08/19/iOS%E5%B8%B8%E7%94%A8%E7%AC%AC%E4%B8%89%E6%96%B9%E5%BA%93%E6%95%B4%E7%90%86/"/>
    <id>https://flametinary.github.io/2019/08/19/iOS%E5%B8%B8%E7%94%A8%E7%AC%AC%E4%B8%89%E6%96%B9%E5%BA%93%E6%95%B4%E7%90%86/</id>
    <published>2019-08-19T09:12:33.000Z</published>
    <updated>2026-09-12T03:37:07.565Z</updated>
    
    <content type="html"><![CDATA[<blockquote><p>整理我们在APP开发过程中经常或者必须使用的一些第三方的pod库。</p></blockquote><span id="more"></span><h2 id="主模块"><a href="#主模块" class="headerlink" title="主模块"></a>主模块</h2><h3 id="布局"><a href="#布局" class="headerlink" title="布局"></a>布局</h3><ul><li><a href="https://github.com/SnapKit/Masonry">Masonry</a> 代码布局</li><li><a href="https://github.com/forkingdog/UITableView-FDTemplateLayoutCell">UITableView-FDTemplateLayoutCell</a> tableview自适应高度</li></ul><h3 id="指示器"><a href="#指示器" class="headerlink" title="指示器"></a>指示器</h3><ul><li><a href="https://github.com/jdg/MBProgressHUD">MBProgressHUD</a></li><li><a href="https://github.com/SVProgressHUD/SVProgressHUD">SVProgressHUD</a></li><li><a href="https://github.com/scalessec/Toast">Toast</a></li><li><a href="https://github.com/CoderMJLee/MJRefresh">MJRefresh</a></li></ul><h3 id="网络"><a href="#网络" class="headerlink" title="网络"></a>网络</h3><ul><li><a href="https://github.com/AFNetworking/AFNetworking">AFNetworking</a></li><li><a href="https://github.com/yuantiku/YTKNetwork">YTKNetwork</a></li></ul><h3 id="数据处理"><a href="#数据处理" class="headerlink" title="数据处理"></a>数据处理</h3><ul><li><a href="https://github.com/CoderMJLee/MJExtension">MJExtension</a></li><li><a href="https://github.com/ibireme/YYModel">YYModel</a></li><li><a href="https://github.com/protocolbuffers/protobuf">Protobuf</a> 存储和读取结构化数据</li></ul><h3 id="数据库"><a href="#数据库" class="headerlink" title="数据库"></a>数据库</h3><ul><li><a href="https://github.com/ccgus/fmdb">FMDB</a></li><li><a href="https://github.com/Tencent/wcdb">WCDB</a></li></ul><h3 id="推送"><a href="#推送" class="headerlink" title="推送"></a>推送</h3><ul><li>JPush 极光</li></ul><h3 id="分享"><a href="#分享" class="headerlink" title="分享"></a>分享</h3><ul><li>UMengSocial 友盟</li></ul><h3 id="图片"><a href="#图片" class="headerlink" title="图片"></a>图片</h3><ul><li><a href="https://github.com/SDWebImage/SDWebImage">SDWebImage</a> 图片下载和缓存</li><li><a href="https://github.com/mwaterfall/MWPhotoBrowser">MWPhotoBrowser</a> 图片浏览</li><li><a href="https://github.com/Flipboard/FLAnimatedImage">FLAnimatedImage</a> gif动图加载</li><li><a href="https://github.com/gsdios/SDCycleScrollView">SDCycleScrollView</a> 轮播图</li></ul><h3 id="音视频"><a href="#音视频" class="headerlink" title="音视频"></a>音视频</h3><ul><li><a href="https://github.com/LaiFengiOS/LFLiveKit">LFLiveKit</a> RTMP streaming SDK for iOS.</li></ul><h3 id="字符串处理"><a href="#字符串处理" class="headerlink" title="字符串处理"></a>字符串处理</h3><ul><li><a href="https://github.com/TTTAttributedLabel/TTTAttributedLabel">TTTAttributedLabel</a> 富文本</li><li><a href="https://github.com/ibireme/YYText">YYText</a></li></ul><h3 id="动画"><a href="#动画" class="headerlink" title="动画"></a>动画</h3><ul><li><a href="https://link.jianshu.com/?t=https://github.com/facebook/pop">pop</a></li><li><a href="https://github.com/ChenYilong/CYLTabBarController">CYLTabBarController</a> tabbar 动画</li></ul><h3 id="js-OC交互"><a href="#js-OC交互" class="headerlink" title="js-OC交互"></a>js-OC交互</h3><ul><li><a href="https://github.com/marcuswestin/WebViewJavascriptBridge">WebViewJavascriptBridge</a></li></ul><h3 id="系统"><a href="#系统" class="headerlink" title="系统"></a>系统</h3><ul><li>Bugly</li><li><a href="https://github.com/kishikawakatsumi/UICKeyChainStore">UICKeyChainStore</a> 钥匙串API</li><li><a href="https://github.com/tonymillion/Reachability">Reachability</a> 网络情况监测</li><li><a href="https://github.com/CocoaLumberjack/CocoaLumberjack">CocoaLumberjack</a> Mac和iOS上一个简捷、强大、灵活的日志框架</li><li><a href="https://github.com/didi/DoraemonKit">DoraemonKit</a> 一款功能齐全的客户端（ iOS 、Android ）研发助手</li><li><a href="https://github.com/Tencent/MLeaksFinder">MLeaksFinder</a> 内存泄露</li><li><a href="https://github.com/soffes/SAMKeychain">SAMKeychain</a> 钥匙串API</li><li><a href="https://github.com/Flipboard/FLEX">FLEX</a> 可以在app中进行debug</li></ul><h2 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h2><ul><li><a href="https://github.com/ibireme/YYKit">YYKit</a></li><li><a href="https://github.com/hackiftekhar/IQKeyboardManager">IQKeyboardManager</a> 键盘管理</li><li><a href="https://github.com/ReactiveCocoa/ReactiveObjC">ReactiveObjC</a> RAC</li><li><a href="https://github.com/BlocksKit/BlocksKit">BlocksKit</a></li></ul>]]></content>
    
    
    <summary type="html">&lt;blockquote&gt;
&lt;p&gt;整理我们在APP开发过程中经常或者必须使用的一些第三方的pod库。&lt;/p&gt;
&lt;/blockquote&gt;</summary>
    
    
    
    <category term="iOS" scheme="https://flametinary.github.io/categories/iOS/"/>
    
    
    <category term="iOS" scheme="https://flametinary.github.io/tags/iOS/"/>
    
  </entry>
  
  <entry>
    <title>WCDB的使用和封装</title>
    <link href="https://flametinary.github.io/2019/08/13/WCDB%E7%9A%84%E4%BD%BF%E7%94%A8%E5%92%8C%E5%B0%81%E8%A3%85/"/>
    <id>https://flametinary.github.io/2019/08/13/WCDB%E7%9A%84%E4%BD%BF%E7%94%A8%E5%92%8C%E5%B0%81%E8%A3%85/</id>
    <published>2019-08-13T10:33:05.000Z</published>
    <updated>2026-09-12T03:37:07.565Z</updated>
    
    <content type="html"><![CDATA[<p>公司的项目所使用的数据库是FMDB，并对其进行了二次封装；由于年久失修，遂决定改为WCDB，因此，抽时间对WCDB进行了一番研究。<br><a href="https://github.com/Tencent/wcdb">WCDB</a>是腾讯开源的一款供客户端使用的数据库，基于<a href="https://github.com/sqlcipher/sqlcipher">SQLCipher</a>，支持iOS，macOS和Android。</p><span id="more"></span><h2 id="特点"><a href="#特点" class="headerlink" title="特点"></a>特点</h2><h3 id="易用"><a href="#易用" class="headerlink" title="易用"></a>易用</h3><p>WCDB通过深度封装数据库语言，使开发者可以通过OC或者Swift的代码方式使用数据库，无需为了拼接SQL而写一大坨代码；</p><h3 id="高效"><a href="#高效" class="headerlink" title="高效"></a>高效</h3><p>WCDB通过框架层和<a href="https://github.com/sqlcipher/sqlcipher">SQLCipher</a>源码优化，使其有更高效的表现；</p><h3 id="完整"><a href="#完整" class="headerlink" title="完整"></a>完整</h3><p>WCDB覆盖了数据库使用过程中大多数的使用场景和需求；</p><ul><li><strong>加密：</strong>WCDB提供基于<a href="https://github.com/sqlcipher/sqlcipher">SQLCipher</a>的数据库加密。</li><li><strong>损坏修复：</strong>WCDB内建了Repair Kit用于修复损坏的数据库。</li><li><strong>反注入：</strong>WCDB内建了对SQL注入的保护。</li></ul><h2 id="安装WCDB"><a href="#安装WCDB" class="headerlink" title="安装WCDB"></a>安装WCDB</h2><p>使用Cocoapods安装WCDB：</p><ol><li>更新pod repo：<code>pod repo update</code></li><li>在Podfile对应的target中，添加<code>pod &#39;WCDB&#39;</code></li><li>执行<code>pod install --verbose</code></li><li>打开workspace</li><li>引入头文件’#import &lt;WCDB&#x2F;WCDB.h&gt;’</li></ol><h2 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h2><p>使用WCD分为2部分：<strong>ORM</strong>和<strong>CRUD</strong>。</p><h3 id="ORM"><a href="#ORM" class="headerlink" title="ORM"></a>ORM</h3><p>ORM的作用就是映射数据库字段和我们的数据模型；WCDB使用内置的宏来连接类、属性与表、字段。共有三类宏，分别对应数据库的字段、索引和约束。</p><h4 id="字段宏"><a href="#字段宏" class="headerlink" title="字段宏"></a>字段宏</h4><p>字段宏以WCDB_SYNTHESIZE开头，定义了类属性与字段之间的联系。</p><ul><li><code>WCDB_SYNTHESIZE(className, propertyName)</code>是最简单的的用法，直接使用propertyName作为数据库中字段的名称。</li><li><code>WCDB_SYNTHESIZE_COLUMN(className, propertyName, columnName)</code>支持自定义字段名。</li><li><code>WCDB_SYNTHESIZE_DEFAULT(className, propertyName, defaultValue)</code>自定义字段默认值（可以是任意C类型或NSString, NSData, NSNumber, NSNull）。</li><li><code>WCDB_SYNTHESIZE_COLUMN_DEFAULT(className, propertyName, cloumnName, defaultValue)</code>。</li></ul><p><a href="https://github.com/Tencent/wcdb/blob/master/objc/sample/orm/WCTSampleORM.mm">字段宏参考</a></p><h4 id="索引宏"><a href="#索引宏" class="headerlink" title="索引宏"></a>索引宏</h4><p>索引宏以WCDB_INDEX开头，定义了数据库的索引属性。支持定义索引的排序方式。</p><ul><li><code>WCDB_INDEX(className, indexSubfixName, propertyName)</code>是基础用法，直接指定某个字段为索引。同时，WCDB会将tableName + indexSubfixName作为该索引的名字。</li><li><code>WCDB_INDEX_ASC(className, indexSubfixName, propertyName)</code>升序。</li><li><code>WCDB_INDEX_DESC(className, indexSubfixName, propertyName)</code>降序。</li><li><code>WCDB_UNIQUE_INDEX(className, indexSubfixName, propertyName)</code>唯一索引。</li><li><code>WCDB_UNIQUE_INDEX_ASC(className, indexSubfixName, propertyName)</code>唯一索引升序排列。</li><li><code>WCDB_UNIQUE_INDEX_DESC(className, indexSubfixName, propertyName)</code>唯一索引降序排列。</li></ul><p>WCDB通过<code>indexSubfixName</code>匹配多索引。相同的<code>indexSubfixName</code>会被组合为多字段索引。</p><p>例如：</p><figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs stylus"><span class="hljs-function"><span class="hljs-title">WCDB_INDEX</span><span class="hljs-params">(WCTSampleORMIndex, <span class="hljs-string">&quot;_multiIndexSubfix&quot;</span>, multiIndexPart1)</span></span><br><span class="hljs-function"><span class="hljs-title">WCDB_INDEX</span><span class="hljs-params">(WCTSampleORMIndex, <span class="hljs-string">&quot;_multiIndexSubfix&quot;</span>, multiIndexPart2)</span></span><br></code></pre></td></tr></table></figure><p><a href="https://github.com/Tencent/wcdb/blob/master/objc/sample/orm/WCTSampleORMIndex.mm">索引宏例子</a></p><h4 id="约束宏"><a href="#约束宏" class="headerlink" title="约束宏"></a>约束宏</h4><p>约束宏包含了<strong>字段约束</strong>和<strong>表约束</strong>。</p><h5 id="字段约束"><a href="#字段约束" class="headerlink" title="字段约束"></a>字段约束</h5><p>主键约束以WCDB_PRIMARY开头，定义了数据库的主键；支持自定义主键的排序方式、是否自增。</p><ul><li><code>WCDB_PRIMARY(className, propertyName)</code>是基础用法，直接使用propertyName作为数据库的主键。</li><li><code>WCDB_PRIMARY_ASC(className, propertyName)</code>主键升序。</li><li><code>WCDB_PRIMARY_DESC(className, propertyName)</code>主键降序。</li><li><code>WCDB_PRIMARY_AUTO_INCREMENT(className, propertyName)</code>主键自增。</li><li><code>WCDB_PRIMARY_ASC_AUTO_INCREMENT(className, propertyName)</code>主键自增及升序的组合。</li><li><code>WCDB_PRIMARY_DESC_AUTO_INCREMENT(className, propertyName)</code>主键自增及降序组合。</li></ul><p>非空约束：<code>WCDB_NOT_NULL(className, propertyName)</code>，当该字段插入数据为空时，数据库会返回错误。<br>唯一约束：<code>WCDB_UNIQUE(className, propertyName)</code>，当该字段插入数据与其他列冲突时，数据库会返回错误。</p><p><strong>注意：</strong>使用了主键自增后，创建的模型必须设置<code>isAutoIncrement</code>为YES。</p><h5 id="表约束"><a href="#表约束" class="headerlink" title="表约束"></a>表约束</h5><p>多主键约束以<code>WCDB_MULTI_PRIMARY</code>开头，定义了数据库的多主键，支持自定义每个主键的排序方式。</p><ul><li><code>WCDB_MULTI_PRIMARY(className, constraintName, propertyName)</code>为基础用法，多个主键通过constrainName匹配。</li><li><code>WCDB_MULTI_PRIMARY_ASC(className, constraintName, propertyName)</code>多主键升序。</li><li><code>WCDB_MULTI_PRIMARY_DESC(className, constraintName, propertyName)</code>多主键降序。</li></ul><p>多字段唯一约束以<code>WCDB_MULTI_UNIQUE</code>开头，定义了数据库的多字段组合唯一，支持自定义每个字段的排序方式。</p><ul><li><code>WCDB_MULTI_UNIQUE(className, constraintName, propertyName)</code>基础用法。</li><li><code>WCDB_MULTI_UNIQUE_ASC(className, constraintName, propertyName)</code>升序。</li><li><code>WCDB_MULTI_UNIQUE_DESC(className, constraintName, propertyName)</code>降序。</li></ul><p><a href="https://github.com/Tencent/wcdb/blob/master/objc/sample/orm/WCTSampleORMTableConstraint.mm">表约束示例</a></p><h4 id="修改字段"><a href="#修改字段" class="headerlink" title="修改字段"></a>修改字段</h4><p>由于SQLite只支持增加字段，并不支持删除和重命名字段。因此，WCDB在删除字段的时候只是将其定义删除；删除定义后，WCDB只是将该字段忽略，其旧数据依然在数据库中，但新增的数据基本<strong>不会因为该字段产生额外的性能和空间损耗</strong>。<br>由于SQLite不支持修改字段名称，所以WCDB采用重新映射的方式。</p><h3 id="CRUD"><a href="#CRUD" class="headerlink" title="CRUD"></a>CRUD</h3><p>WCDB提供了三个基础类进行数据库操作：<code>WCTDatabase</code>、<code>WCTTable</code>、<code>WCTTransaction</code>。它们的接口都是<strong>线程安全的</strong>。</p><h4 id="WCTDatabase"><a href="#WCTDatabase" class="headerlink" title="WCTDatabase"></a>WCTDatabase</h4><p>表示一个数据库，可以进行所有的数据库操作。</p><h5 id="创建数据库"><a href="#创建数据库" class="headerlink" title="创建数据库"></a>创建数据库</h5><p><code>WCTDatabase</code>通过<code>initWithPath:</code>接口进行创建。该接口会同时创建path中不存在的目录。</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs OBJC"><span class="hljs-built_in">NSString</span>* path = <span class="hljs-string">@&quot;&lt;这里是数据库路径&gt;&quot;</span>;<br>WCTDatabase *database = [[WCTDatabase alloc] initWithPath:path];<br></code></pre></td></tr></table></figure><h5 id="打开数据库"><a href="#打开数据库" class="headerlink" title="打开数据库"></a>打开数据库</h5><p>WCDB采用懒加载的方式管理对象，因此SQLite连接会在第一次被访问的时候打开。开发者<strong>不需要手动打开数据库</strong>。</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs OBJC"><span class="hljs-comment">// 判断数据库是否能够打开</span><br><span class="hljs-keyword">if</span> ([database canOpen]) &#123;<br>  <span class="hljs-comment">//...</span><br>&#125;<br><span class="hljs-comment">// 判断数据库是否已经打开</span><br><span class="hljs-keyword">if</span> ([database isOpened]) &#123;<br>  <span class="hljs-comment">//...</span><br>&#125;<br></code></pre></td></tr></table></figure><h5 id="关闭数据库"><a href="#关闭数据库" class="headerlink" title="关闭数据库"></a>关闭数据库</h5><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs OBJC">[database close];<br></code></pre></td></tr></table></figure><p><strong>注意：</strong>由于WCDB支持多线程访问数据库，因此，该接口会阻塞等待所有线程均已操作结束。</p><p>对于一个特定路径的数据库，WCDB会在所有对象对其的引用结束时，自动关闭数据库，并且回收内存和SQLite连接。因此，大多数的时候，开发者并<strong>不需要手动关闭数据库</strong>。</p><h4 id="WCTTable"><a href="#WCTTable" class="headerlink" title="WCTTable"></a>WCTTable</h4><p>表示一个表。等价于预设了<code>class</code>和<code>tableName</code>的<code>WCTDatabase</code>，仅可以进行数据库的CRUD。</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs OBJC">WCTTable* table = [database getTableOfName:tableName<br>                                 withClass:WCTSampleTable.class];<br></code></pre></td></tr></table></figure><h4 id="WCTTransaction"><a href="#WCTTransaction" class="headerlink" title="WCTTransaction"></a>WCTTransaction</h4><p>表示一个事务。</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs OBJC">WCTTransaction *transaction = [database getTransaction];<br></code></pre></td></tr></table></figure><p>与WCTDatabase的事务不同，WCTTransaction可以在函数和对象之间传递，实现跨线程的事务。</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs OBJC"><span class="hljs-comment">//You can do a transaction in different threads using WCTTransaction.</span><br><span class="hljs-comment">//But it&#x27;s better to run serially, or an inner thread mutex will guarantee this.</span><br><span class="hljs-type">BOOL</span> ret = [transaction begin];<br><span class="hljs-built_in">dispatch_async</span>(dispatch_queue_create(<span class="hljs-string">&quot;other thread&quot;</span>, DISPATCH_QUEUE_SERIAL), ^&#123;<br>  WCTSampleTransaction *object = [[WCTSampleTransaction alloc] init];<br>  <span class="hljs-type">BOOL</span> ret = [transaction insertObject:object<br>                                  into:tableName];<br>  <span class="hljs-keyword">if</span> (ret) &#123;<br>      [transaction commit];<br>  &#125; <span class="hljs-keyword">else</span> &#123;<br>      [transaction rollback];<br>  &#125;<br>&#125;);<br></code></pre></td></tr></table></figure><h4 id="CRUD操作"><a href="#CRUD操作" class="headerlink" title="CRUD操作"></a>CRUD操作</h4><h5 id="创建数据库-1"><a href="#创建数据库-1" class="headerlink" title="创建数据库"></a>创建数据库</h5><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs OBJC"><span class="hljs-type">BOOL</span> ret = [database createTableAndIndexesOfName:tableName<br>                                       withClass:WCTSampleTable.class];<br><br><span class="hljs-comment">// 或者</span><br><br><span class="hljs-type">BOOL</span> ret = [database createTableOfName:tableName<br>                     withColumnDefList:&#123;<br>                     WCTSampleTable.intValue.def(WCTColumnTypeInteger32),<br>                     WCTSampleTable.stringValue.def(WCTColumnTypeString)<br>           &#125;];<br></code></pre></td></tr></table></figure><h5 id="将数据插入表"><a href="#将数据插入表" class="headerlink" title="将数据插入表"></a>将数据插入表</h5><ul><li><code>insertObject:into:和insertObjects:into:</code>，插入单个或多个对象</li><li><code>insertOrReplaceObject:into</code>和<code>insertOrReplaceObjects:into</code>，插入单个或多个对象。当对象的主键在数据库内已经存在时，更新数据；否则插入对象。</li><li><code>insertObject:onProperties:into:</code>和<code>insertObjects:onProperties:into:</code>，插入单个或多个对象的部分属性</li><li><code>insertOrReplaceObject:onProperties:into</code>和<code>insertOrReplaceObjects:onProperties:into</code>，插入单个或多个对象的部分属性。当对象的主键在数据库内已经存在时，更新数据；否则插入对象。</li></ul><h5 id="删除数据"><a href="#删除数据" class="headerlink" title="删除数据"></a>删除数据</h5><ul><li><code>deleteAllObjectsFromTable:</code>删除表内的所有数据</li><li><code>deleteObjectsFromTable:</code>后可组合接 where、orderBy、limit、offset以删除部分数据</li></ul><h5 id="更新数据"><a href="#更新数据" class="headerlink" title="更新数据"></a>更新数据</h5><ul><li><code>updateAllRowsInTable:onProperties:withObject:</code>，通过object更新数据库中所有指定列的数据</li><li><code>updateRowsInTable:onProperties:withObject:</code>后可组合接 where、orderBy、limit、offset以通过object更新指定列的部分数据</li><li><code>updateAllRowsInTable:onProperty:withObject:</code>，通过object更新数据库某一列的数据</li><li><code>updateRowsInTable:onProperty:withObject:</code>后可组合接 where、orderBy、limit、offset以通过object更新某一列的部分数据</li><li><code>updateAllRowsInTable:onProperties:withRow:</code>，通过数组更新数据库中的所有指定列的数据</li><li><code>updateRowsInTable:onProperties:withRow:</code>后可组合接 where、orderBy、limit、offset以通过数组更新指定列的部分数据</li><li><code>updateAllRowsInTable:onProperty:withRow:</code>，通过数组更新数据库某一列的数据</li><li><code>updateRowsInTable:onProperty:withRow:</code>后可组合接 where、orderBy、limit、offset以通过数组更新某一列的部分数</li></ul><h5 id="查找数据"><a href="#查找数据" class="headerlink" title="查找数据"></a>查找数据</h5><ul><li><code>getOneObjectOfClass:fromTable:</code>后可接 where、orderBy、limit、offset以从数据库中取出一行数据并组合成object</li><li><code>getOneObjectOnResults:fromTable:</code>后可接 where、orderBy、limit、offset以从数据库中取出一行数据的部分列并组合成object</li><li><code>getOneRowOnResults:fromTable:</code>后可接 where、orderBy、limit、offset以从数据库中取出一行数据的部分列并组合成数组</li><li><code>getOneColumnOnResult:fromTable:</code>后可接 where、orderBy、limit、offset以从数据库中取出一列数据并组合成数组</li><li><code>getOneDistinctColumnOnResult:fromTable:</code>后可接 where、orderBy、limit、offset以从数据库中取出一列数据，并取distinct后组合成数组。</li><li><code>getOneValueOnResult:fromTable:</code>后可接 where、orderBy、limit、offset以从数据库中取出一行数据的某一列</li><li><code>getAllObjectsOfClass:fromTable:</code>，取出所有数据，并组合成object</li><li><code>getObjectsOfClass:fromTable:</code>后可接 where、orderBy、limit、offset以从数据库中取出一部分数据，并组合成object</li><li><code>getAllObjectsOnResults:fromTable:</code>，取出所有数据的指定列，并组合成object</li><li><code>getObjectsOnResults:fromTable:</code>后可接where、orderBy、limit、offset以从数据库中取出一部分数据的指定列，并组合成object</li><li><code>getAllRowsOnResults:fromTable:</code>，取出所有数据的指定列，并组合成数组</li><li><code>getRowsOnResults:fromTable:</code>后可接where、orderBy、limit、offset以从数据库中取出一部分数据的指定列，并组合成数组</li></ul><p><a href="https://github.com/Tencent/wcdb/blob/master/objc/sample/convenient/sample_convenient_main.mm">CRUD示例</a></p><h5 id="链式接口"><a href="#链式接口" class="headerlink" title="链式接口"></a>链式接口</h5><p>WCDB对于增删改查操作，都提供了对应的类以实现链式调用</p><ul><li>WCTInsert</li><li>WCTDelete</li><li>WCTUpdate</li><li>WCTSelect</li><li>WCTRowSelect</li><li>WCTMultiSelect</li></ul><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs OBJC">WCTSelect *select = [database prepareSelectObjectsOnResults:Message.localID.max()<br>                                                  fromTable:<span class="hljs-string">@&quot;message&quot;</span>];<br><span class="hljs-built_in">NSArray</span>&lt;Message *&gt; *objects = [[[[select where:Message.localID &gt; <span class="hljs-number">0</span>] <br>                               groupBy:&#123;Message.content&#125;]<br>                                    orderBy:Message.createTime.order()] <br>                                     limit:<span class="hljs-number">10</span>].allObjects;<br></code></pre></td></tr></table></figure><h3 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h3><h4 id="事务"><a href="#事务" class="headerlink" title="事务"></a>事务</h4><h4 id="调试SQL"><a href="#调试SQL" class="headerlink" title="调试SQL"></a>调试SQL</h4><p>[WCTStatistics SetGlobalSQLTrace:]会监控所有执行的SQL，该接口可用于调试，确定SQL是否执行正确。</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs OBJC"><span class="hljs-comment">//SQL Execution Monitor</span><br>[WCTStatistics SetGlobalSQLTrace:^(<span class="hljs-built_in">NSString</span> *sql) &#123;<br><span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@&quot;SQL: %@&quot;</span>, sql);<br>&#125;];<br></code></pre></td></tr></table></figure><h4 id="WINQ"><a href="#WINQ" class="headerlink" title="WINQ"></a>WINQ</h4><h4 id="自定义类型"><a href="#自定义类型" class="headerlink" title="自定义类型"></a>自定义类型</h4><h4 id="隔离C-代码"><a href="#隔离C-代码" class="headerlink" title="隔离C++代码"></a>隔离C++代码</h4><h4 id="关于数据库加密"><a href="#关于数据库加密" class="headerlink" title="关于数据库加密"></a>关于数据库加密</h4><h4 id="高级用法"><a href="#高级用法" class="headerlink" title="高级用法"></a>高级用法</h4><h5 id="主键自增-Auto-Increment"><a href="#主键自增-Auto-Increment" class="headerlink" title="主键自增(Auto Increment)"></a>主键自增(Auto Increment)</h5><h5 id="as重定向"><a href="#as重定向" class="headerlink" title="as重定向"></a>as重定向</h5><h5 id="多表查询"><a href="#多表查询" class="headerlink" title="多表查询"></a>多表查询</h5><h4 id="基础类共享"><a href="#基础类共享" class="headerlink" title="基础类共享"></a>基础类共享</h4><p>对于同一个路径的数据库，不同的<code>WCTDatabase</code>、<code>WCTTable</code>、<code>WCTTransaction</code>对象共享同一个WCDB核心。</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs OBJC">WCTDatabase* database1 = [[WCTDatabase alloc] initWithPath:path];<br>WCTDatabase* database2 = [[WCTDatabase alloc] initWithPath:path];<br>database1.tag = <span class="hljs-number">1</span>;<br><span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@&quot;%d&quot;</span>, database2.tag);<span class="hljs-comment">//print 1</span><br></code></pre></td></tr></table></figure><h4 id="文件操作"><a href="#文件操作" class="headerlink" title="文件操作"></a>文件操作</h4><p>WCDB提供了删除数据库、移动数据库、获取数据库占用空间和使用路径的文件操作接口。</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs OBJC">- (<span class="hljs-type">BOOL</span>)removeFilesWithError:(WCTError **)error;<br>- (<span class="hljs-type">BOOL</span>)moveFilesToDirectory:(<span class="hljs-built_in">NSString</span> *)directory withExtraFiles:(<span class="hljs-built_in">NSArray</span>&lt;<span class="hljs-built_in">NSString</span> *&gt; *)extraFiles andError:(WCTError **)error;<br>- (<span class="hljs-built_in">NSArray</span>&lt;<span class="hljs-built_in">NSString</span> *&gt; *)getPaths;<br>- (<span class="hljs-built_in">NSUInteger</span>)getFilesSizeWithError:(WCTError **)error;<br></code></pre></td></tr></table></figure><p>若是一个线程正在操作数据库，而另一个线程进行移动数据库的操作，可能会导致数据库的损坏；因此，文件操作通常放在关闭数据库后。</p><figure class="highlight objc"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs OBJC">[database close:^&#123;<br>  WCTError *error = <span class="hljs-literal">nil</span>;<br>  <span class="hljs-type">BOOL</span> ret = [database moveFilesToDirectory:otherDirectory withError:&amp;error];<br>  <span class="hljs-keyword">if</span> (!ret) &#123;<br>      <span class="hljs-built_in">NSLog</span>(<span class="hljs-string">@&quot;Move files Error %@&quot;</span>, error);<br>  &#125;<br>&#125;];<br></code></pre></td></tr></table></figure>]]></content>
    
    
    <summary type="html">&lt;p&gt;公司的项目所使用的数据库是FMDB，并对其进行了二次封装；由于年久失修，遂决定改为WCDB，因此，抽时间对WCDB进行了一番研究。&lt;br&gt;&lt;a href=&quot;https://github.com/Tencent/wcdb&quot;&gt;WCDB&lt;/a&gt;是腾讯开源的一款供客户端使用的数据库，基于&lt;a href=&quot;https://github.com/sqlcipher/sqlcipher&quot;&gt;SQLCipher&lt;/a&gt;，支持iOS，macOS和Android。&lt;/p&gt;</summary>
    
    
    
    <category term="iOS" scheme="https://flametinary.github.io/categories/iOS/"/>
    
    
    <category term="iOS" scheme="https://flametinary.github.io/tags/iOS/"/>
    
  </entry>
  
  <entry>
    <title>jenkins执行GitHub中的Python代码</title>
    <link href="https://flametinary.github.io/2019/08/13/jenkins%E6%89%A7%E8%A1%8CGitHub%E4%B8%AD%E7%9A%84Python%E4%BB%A3%E7%A0%81/"/>
    <id>https://flametinary.github.io/2019/08/13/jenkins%E6%89%A7%E8%A1%8CGitHub%E4%B8%AD%E7%9A%84Python%E4%BB%A3%E7%A0%81/</id>
    <published>2019-08-13T09:01:43.000Z</published>
    <updated>2026-09-12T03:37:07.567Z</updated>
    
    <content type="html"><![CDATA[<p>jenkins是很好用的一个自动化测试和执行代码的工具，<a href="https://sheldon.top/2019/08/12/jenkins%E6%89%93%E5%8C%85%E6%B5%81%E7%A8%8B/">查看如何安装jenkins</a>;</p><p>我们的代码是存储在代码仓库里面的，一般，我们使用GitHub来存放我们的代码；</p><p>那么，怎么使用jenkins来直接执行我们存放在GitHub中的代码呢？</p><span id="more"></span><h2 id="jenkins-创建一个任务"><a href="#jenkins-创建一个任务" class="headerlink" title="jenkins 创建一个任务"></a>jenkins 创建一个任务</h2><p><img src="https://s2.ax1x.com/2019/08/13/mCdKhR.jpg" srcset="/img/loading.gif" lazyload alt="jenkins创建任务"></p><h2 id="配置jenkins"><a href="#配置jenkins" class="headerlink" title="配置jenkins"></a>配置jenkins</h2><h3 id="选择GitHub项目"><a href="#选择GitHub项目" class="headerlink" title="选择GitHub项目"></a>选择GitHub项目</h3><p><img src="https://s2.ax1x.com/2019/08/13/mC0QOK.jpg" srcset="/img/loading.gif" lazyload alt="填写项目地址"></p><h3 id="填写源码管理"><a href="#填写源码管理" class="headerlink" title="填写源码管理"></a>填写源码管理</h3><p><img src="https://s2.ax1x.com/2019/08/13/mCBfCd.jpg" srcset="/img/loading.gif" lazyload alt="源码管理"></p><p>如果没有凭证，就点击Credentials右边的<strong>添加</strong><br><img src="https://s2.ax1x.com/2019/08/13/mCrrTO.jpg" srcset="/img/loading.gif" lazyload alt="添加凭证"><br><strong>注意：</strong>这里添加的是SSH的私钥</p><h4 id="如何查询自己的ssh密钥？"><a href="#如何查询自己的ssh密钥？" class="headerlink" title="如何查询自己的ssh密钥？"></a>如何查询自己的ssh密钥？</h4><ol><li>进入ssh文件夹<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cd</span> ~/.ssh<br></code></pre></td></tr></table></figure></li><li>查看私钥文件<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cat</span> id_rsa<br></code></pre></td></tr></table></figure></li></ol><h3 id="定时构建（可选）"><a href="#定时构建（可选）" class="headerlink" title="定时构建（可选）"></a>定时构建（可选）</h3><p><img src="https://s2.ax1x.com/2019/08/13/mCspAU.jpg" srcset="/img/loading.gif" lazyload alt="定时构建"></p><h3 id="每次构建之前清空一下Jenkins工作空间，避免拉取的代码有冲突"><a href="#每次构建之前清空一下Jenkins工作空间，避免拉取的代码有冲突" class="headerlink" title="每次构建之前清空一下Jenkins工作空间，避免拉取的代码有冲突"></a>每次构建之前清空一下Jenkins工作空间，避免拉取的代码有冲突</h3><p><img src="https://s2.ax1x.com/2019/08/13/mCswCQ.jpg" srcset="/img/loading.gif" lazyload alt="清空工作空间"></p><h3 id="构建"><a href="#构建" class="headerlink" title="构建"></a>构建</h3><p>在构建选项中选择<strong>增加构建步骤-&gt;Execute shell</strong>;</p><p>然后填写需要执行的脚本：<br><img src="https://s2.ax1x.com/2019/08/13/mCyGRJ.jpg" srcset="/img/loading.gif" lazyload alt="执行脚本"></p><h3 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h3><p>点击保存，然后就可以build我的项目啦！</p>]]></content>
    
    
    <summary type="html">&lt;p&gt;jenkins是很好用的一个自动化测试和执行代码的工具，&lt;a href=&quot;https://sheldon.top/2019/08/12/jenkins%E6%89%93%E5%8C%85%E6%B5%81%E7%A8%8B/&quot;&gt;查看如何安装jenkins&lt;/a&gt;;&lt;/p&gt;
&lt;p&gt;我们的代码是存储在代码仓库里面的，一般，我们使用GitHub来存放我们的代码；&lt;/p&gt;
&lt;p&gt;那么，怎么使用jenkins来直接执行我们存放在GitHub中的代码呢？&lt;/p&gt;</summary>
    
    
    
    <category term="jenkins" scheme="https://flametinary.github.io/categories/jenkins/"/>
    
    
    <category term="jenkins" scheme="https://flametinary.github.io/tags/jenkins/"/>
    
  </entry>
  
  <entry>
    <title>git本地库连接远端库</title>
    <link href="https://flametinary.github.io/2019/08/13/git%E6%9C%AC%E5%9C%B0%E5%BA%93%E8%BF%9E%E6%8E%A5%E8%BF%9C%E7%AB%AF%E5%BA%93/"/>
    <id>https://flametinary.github.io/2019/08/13/git%E6%9C%AC%E5%9C%B0%E5%BA%93%E8%BF%9E%E6%8E%A5%E8%BF%9C%E7%AB%AF%E5%BA%93/</id>
    <published>2019-08-13T06:55:27.000Z</published>
    <updated>2026-09-12T03:37:07.565Z</updated>
    
    <content type="html"><![CDATA[<p>我们使用git有时候会有这种情况：<br>我们只在本地创建了git仓库，并没有在GitHub上创建远端仓库；当我们在本的的git仓库提交了几次修改后，想要将此git仓库放在GitHub，那么我们需要怎么做呢？</p><span id="more"></span><h2 id="连接远程仓库"><a href="#连接远程仓库" class="headerlink" title="连接远程仓库"></a>连接远程仓库</h2><p>假设我们已经在GitHub上创建了一个空的仓库</p><p>先cd到我们本地git仓库所在的目录,然后执行git连接操作</p><figure class="highlight llvm"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs llvm">git remote <span class="hljs-keyword">add</span> origin git<span class="hljs-title">@github.com</span>:FlameTinary/TYPythonTools.git<br></code></pre></td></tr></table></figure><h2 id="错误步骤"><a href="#错误步骤" class="headerlink" title="错误步骤"></a>错误步骤</h2><p>然后执行<code>git pull</code>，会发现，报错了：</p><figure class="highlight vbnet"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><code class="hljs vbnet"><span class="hljs-symbol">warning:</span> no common commits<br><span class="hljs-symbol">remote:</span> Enumerating objects: <span class="hljs-number">4</span>, done.<br><span class="hljs-symbol">remote:</span> Counting objects: <span class="hljs-number">100%</span> (<span class="hljs-number">4</span>/<span class="hljs-number">4</span>), done.<br><span class="hljs-symbol">remote:</span> Compressing objects: <span class="hljs-number">100%</span> (<span class="hljs-number">4</span>/<span class="hljs-number">4</span>), done.<br>Unpacking objects: <span class="hljs-number">100%</span> (<span class="hljs-number">4</span>/<span class="hljs-number">4</span>), done.<br><span class="hljs-symbol">remote:</span> Total <span class="hljs-number">4</span> (delta <span class="hljs-number">0</span>), reused <span class="hljs-number">0</span> (delta <span class="hljs-number">0</span>), pack-reused <span class="hljs-number">0</span><br><span class="hljs-keyword">From</span> github.com:FlameTinary/TYPythonTools<br> * [<span class="hljs-built_in">new</span> branch]      master     -&gt; origin/master<br>There <span class="hljs-built_in">is</span> no tracking information <span class="hljs-keyword">for</span> the current branch.<br>Please specify which branch you want <span class="hljs-keyword">to</span> merge <span class="hljs-keyword">with</span>.<br>See git-pull(<span class="hljs-number">1</span>) <span class="hljs-keyword">for</span> details.<br><br>    git pull &lt;remote&gt; &lt;branch&gt;<br><br><span class="hljs-keyword">If</span> you wish <span class="hljs-keyword">to</span> <span class="hljs-keyword">set</span> tracking information <span class="hljs-keyword">for</span> this branch you can <span class="hljs-keyword">do</span> so <span class="hljs-keyword">with</span>:<br><br>    git branch --<span class="hljs-keyword">set</span>-upstream-<span class="hljs-keyword">to</span>=origin/&lt;branch&gt; master<br><br> ✘ sheldon@tianyudeMacBook-Pro  ~/Documents/TYPythonTools   master  git pull remote master<br><span class="hljs-symbol">fatal:</span> <span class="hljs-comment">&#x27;remote&#x27; does not appear to be a git repository</span><br><span class="hljs-symbol">fatal:</span> Could <span class="hljs-built_in">not</span> read <span class="hljs-keyword">from</span> remote repository.<br><br>Please make sure you have the correct access rights<br><span class="hljs-built_in">and</span> the repository exists.<br></code></pre></td></tr></table></figure><p>执行<code>git pull origin master --allow-unrelated-histories</code>也不行；</p><p>那么怎么办呢？</p><h2 id="正确步骤"><a href="#正确步骤" class="headerlink" title="正确步骤"></a>正确步骤</h2><p>原来是我们本的的branch和远端的branch无法对应起来；</p><p>可以执行<code>git branch --set-upstream-to=origin/master master</code>，会出现：</p><figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs routeros">Branch <span class="hljs-string">&#x27;master&#x27;</span> <span class="hljs-built_in">set</span> up <span class="hljs-keyword">to</span> track remote branch <span class="hljs-string">&#x27;master&#x27;</span> <span class="hljs-keyword">from</span> <span class="hljs-string">&#x27;origin&#x27;</span>.<br></code></pre></td></tr></table></figure><p>然后我们再执行<code>git pull origin master --allow-unrelated-histories</code>；</p><p>最后将本地内容推送到远端：</p><figure class="highlight maxima"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs maxima">git <span class="hljs-built_in">push</span> -u <span class="hljs-built_in">origin</span> master<br></code></pre></td></tr></table></figure><p>大功告成！！</p><h2 id="总结："><a href="#总结：" class="headerlink" title="总结："></a>总结：</h2><h3 id="1-连接远端仓库"><a href="#1-连接远端仓库" class="headerlink" title="1. 连接远端仓库"></a>1. 连接远端仓库</h3><figure class="highlight llvm"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs llvm">git remote <span class="hljs-keyword">add</span> origin git<span class="hljs-title">@github.com</span>:FlameTinary/TYPythonTools.git<br></code></pre></td></tr></table></figure><h3 id="2-连接分支"><a href="#2-连接分支" class="headerlink" title="2. 连接分支"></a>2. 连接分支</h3><figure class="highlight crmsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs crmsh">git branch --<span class="hljs-attr">set-upstream-to=</span>origin/<span class="hljs-keyword">master</span> <span class="hljs-title">master</span><br></code></pre></td></tr></table></figure><h3 id="3-拉取远端代码"><a href="#3-拉取远端代码" class="headerlink" title="3. 拉取远端代码"></a>3. 拉取远端代码</h3><figure class="highlight crmsh"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs crmsh">git pull origin <span class="hljs-keyword">master</span> <span class="hljs-title">--allow-unrelated-histories</span><br></code></pre></td></tr></table></figure><h3 id="4-推送本地代码到远端"><a href="#4-推送本地代码到远端" class="headerlink" title="4. 推送本地代码到远端"></a>4. 推送本地代码到远端</h3><figure class="highlight maxima"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs maxima">git <span class="hljs-built_in">push</span> -u <span class="hljs-built_in">origin</span> master<br></code></pre></td></tr></table></figure>]]></content>
    
    
    <summary type="html">&lt;p&gt;我们使用git有时候会有这种情况：&lt;br&gt;我们只在本地创建了git仓库，并没有在GitHub上创建远端仓库；当我们在本的的git仓库提交了几次修改后，想要将此git仓库放在GitHub，那么我们需要怎么做呢？&lt;/p&gt;</summary>
    
    
    
    <category term="Git" scheme="https://flametinary.github.io/categories/Git/"/>
    
    
    <category term="Git" scheme="https://flametinary.github.io/tags/Git/"/>
    
  </entry>
  
</feed>
